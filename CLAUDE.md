# Seis Destinos — Guía del proyecto

## Objetivo
Sistema de gestión de fábrica de Grupo Nuss sobre una única base de datos central en Supabase (nuss-central), con datos maestros compartidos y módulos separados. Nombre de la app: Seis Destinos. Las empresas del grupo son: Cucuruchos Nuss (Córdoba), Dolce Pasta (Rosario), Taller (Córdoba), Mengui/Heladitos Orly (Córdoba).

## Arquitectura
- PWA (Progressive Web App): instalable en celular y funciona en navegador de escritorio
- Multi-archivo: un HTML por módulo, CSS compartido (`css/main.css`), sin JS compartido entre módulos — cada archivo duplica localmente sus propios helpers (parseImporte, formatearImporte, colorAvatar, etc.), es el patrón establecido, no crear un JS común
- **Contraseñas — excepción consciente a la regla de arriba.** El mínimo del servidor es **10 caracteres** (configurado en el panel; NO está configurado "required characters"). Los helpers viven compartidos en `js/utils.js` porque la regla de duplicar aplica a los 6 módulos, y estas pantallas de auth ya comparten `auth.js` y `utils.js`: `LARGO_MINIMO_CONTRASENA` (10), `validarContrasena(texto)` → `{ largo, mayuscula, minuscula, numero, simbolo, todoOk }`, `renderizarRequisitos(contenedor, resultado)` y `renderizarCoincidencia(contenedor, coinciden)`.
  - Usa clases Unicode (`\p{Lu}`, `\p{Ll}`, `\p{N}`) y NO rangos ASCII: con `/[A-Z]/` una contraseña que arranca con "Ángel" no contaría la mayúscula. "Símbolo" se define como `[^\p{L}\p{N}]` — complementario exacto de las otras categorías, así ningún carácter queda sin clasificar ni cuenta dos veces. El espacio cuenta como símbolo (a propósito: habilita passphrases).
  - El cliente queda MÁS ESTRICTO que el servidor, que solo exige largo. Es el lado seguro para equivocarse: nadie pasa los cinco requisitos y aun así se come un rechazo en inglés. Si algún día se configura "required characters" en el panel, revisar que no pida algo que el cliente no exige.
  - Se usan en los CUATRO lugares donde se ELIGE una contraseña nueva: el modal de dashboard.html, los dos formularios de registro.html y restablecer-contrasena.html. En login.html NO — ahí se escribe una que ya existe. registro.html no tiene campo de confirmación, así que ahí van solo los cinco requisitos, sin el de coincidencia.
  - La validación no es solo visual: el botón queda `disabled` hasta que se cumpla todo. El `disabled` se aplica DESDE JS y no como atributo del HTML, para que si el script fallara los botones queden habilitados como antes en vez de dejar el formulario inutilizable. Y los `catch`/`finally` revalidan en lugar de habilitar a ciegas.
  - Accesibilidad: el estado de cada requisito NO se comunica solo con color — el ✓ / ○ va como TEXTO dentro del span, así lo lee un lector de pantalla. Sin `aria-live` a propósito: se dispararía en cada tecla.
  - CSS unificado en `css/main.css`: `.campo-contrasena`, `.btn-mostrar-contrasena` (el "ojito", con íconos Lucide) y `.requisitos-contrasena` / `.requisito` / `.requisito--ok`. Antes había TRES implementaciones distintas del ojito — copias locales en registro.html y restablecer-contrasena.html, más una versión con emoji y colores hardcodeados en login.html. Toda pantalla de contraseña necesita el `<script>` de Lucide cargado.
  - En `_traducirError` de `js/auth.js`, el mensaje de largo mínimo NO tiene el número hardcodeado: se lee del propio mensaje de Supabase con `/Password should be at least (\d+) characters/`. La versión anterior tenía una clave fija con el 6 adentro; al subir el mínimo a 10 esa clave dejó de matchear y el error caía en el genérico "Ocurrió un error", justo cuando más falta hacía saber el motivo.
- **Números — segunda excepción consciente a la regla de duplicar (21/09/2026).** Leer, formatear y escribir números vive COMPARTIDO en `js/utils.js`, porque dos módulos que entiendan distinto el mismo texto es exactamente como un cheque de 387 mil se guarda como 387 pesos sin ningún error. Formato argentino: **punto de miles, coma decimal**.
  - **Tres clases de número:** **IMPORTE** (plata, 2 decimales); **CANTIDAD** (bultos y unidades que se cuentan de a enteros → 0 decimales, kg y lt → 3, contenido por bulto → 3, tolerancia % → 2 con tope 100, kilometraje → 0); **IDENTIFICADOR** (número de cheque, cuenta, renglones de la banda CMC-7, CUIT, CBU, alias, lote, punto de venta y número de comprobante, DNI, CUIL, teléfono, legajo, códigos) → **NO se tocan nunca**: se comparan dígito por dígito contra un papel y un punto de miles los vuelve irreconocibles.
  - **`leerNumeroAr(texto, { decimales = 2, negativos = false })`** → número o `null`, nunca inventa. Sin coma y con UN solo punto seguido de 1 o 2 dígitos al final, ese punto es decimal (lo pegado: "387300.50" → 387300,5); con 3 dígitos es de miles ("1.500" → 1500). Un número JS se devuelve tal cual. Rechaza espacios internos, grupos mal armados, dos comas y más decimales que los permitidos.
  - **`formatearNumeroAr(n, { decimales, minimos })`** → "2.000.000,50"; null / '' / NaN → **"—"**. Un dato ausente nunca se muestra "$ 0,00" ni "$ NaN"; un cero de verdad sí es "0,00".
  - **`enlazarCampoNumero(input, { decimales, max, negativos })`**: el campo pasa a `type="text"` + `inputmode`, pone los puntos mientras se escribe, mantiene el cursor al escribir y al borrar en el medio, el punto tipeado es coma decimal, pegar lee con `leerNumeroAr` y un pegado ilegible no cambia el campo. `leerCampoNumero(input)` lee con esa misma configuración.
  - **`ponerNumero(input, n)` es la ÚNICA forma de escribir un número en un campo desde código** (prellenado del OCR, abrir para editar, restaurar un borrador). Recibe un número; nunca `input.value = …` con un número ni un `value="${…}"` en una plantilla.
  - **Cambio de significado que conviene saber:** en Ingreso y Stock, "1.500" kg antes era 1,5 y ahora es 1500. Tipeando no hay confusión (el punto tipeado se vuelve coma); al PEGAR, "0.300" **ya no da 300** (22/09/2026): un grupo de miles no puede empezar con 0, así que con parte entera "0" el punto es decimal —"0.300" da 0,3 donde entran 3 decimales y `null` donde no— y "00.300" / "012.345" / "0.300,5" dan `null`. "1.500" sigue siendo 1500.
  - Migrados la noche del 21/09/2026 los 6 módulos con números (Cobranzas, Gastos, Caja, CC, Ingreso, Stock; Empleados, Accesos y el dashboard no tienen). Se fueron los parsers distintos que había (`parseImporte`, que convertía "387300.50" en 38.730.050; `parseImporteCobranza`; `parsearCantidad` como parser propio) y los formateos en vivo de cada módulo. `numeroDesdeOcr` de Ingreso se mantiene para LEER el OCR (son dos contratos distintos). Suites: `test-numeros.js` + una `test-<modulo>-numeros.js` por módulo.
- **Cobranzas y Cheques — tercera excepción consciente a la regla de duplicar (22/09/2026).** Cuando la cartera de cheques se mudó de Cobranzas a su propio módulo (`modulos/cheques.html`), lo que usan los DOS pasó a vivir UNA sola vez en **`js/cobranzas-comun.js`** (módulo ES): `dvBcra`, `formatearImporte` (un importe ausente dice "—", nunca "$ 0,00"), `ZONA_AR`, `hoyArgentina`, `esFechaIso`, `diasEntre`, `formatearFechaCob`, `ETIQUETA_ESTADO_CHEQUE`, `nombreBancoDe(bancos, codigo)` y `textoSalidaCheque`. Pedido explícito de Facu: copiar esas reglas en los dos archivos sería tener dos versiones que divergen en silencio. **El escape NO va ahí**: cada módulo tiene el suyo (`escCob` en Cobranzas, `esc` en Cheques).
  - **Las suites lo leen con `pruebas/fuente-cobranzas.js`**, que pega detrás del `<script>` del módulo cada declaración del común (sin el `export`) que el HTML no declare: así el sandbox prueba el código REAL. `pruebas/mutar-cobranzas-comun.js` muta el archivo común.
  - **`pruebas/check-scripts.js` ahora también parsea `js/*.js` y verifica que cada nombre IMPORTADO exista como export** del archivo local: un `import { x }` de un nombre que no existe es un SyntaxError al enlazar —la pantalla entera no carga— y `node --check` no lo ve.
- Sin frameworks: HTML, CSS y JS vanilla
- Backend: Supabase (PostgreSQL administrado, región São Paulo)
- OCR de comprobantes: Edge Function `ocr-comprobante`, la de GASTOS. Extrae razon_social, cuit y demás campos del comprobante — ver detalle en el propio archivo de la función antes de asumir qué campos devuelve. **Ya no es intocable:** el 16/09/2026 (`4b677fa`, desplegada como versión 9) cambió cómo recibe el archivo y cómo elige el modelo. El prompt y los campos que devuelve NO cambiaron.
  - **RECIBE LA RUTA EN STORAGE (`storage_path`), igual que `ocr-cheques`.** Valida que la ruta empiece con la carpeta de quien llama y que no tenga segmentos vacíos, `.` ni `..`, y baja el archivo del bucket `comprobantes` **con el token del usuario**, así que la policy del bucket sigue aplicando. El tipo sale del blob o, si falta, de la extensión; un PDF sigue yendo como bloque `document`. Antes recibía la imagen entera en base64 y el mismo archivo viajaba dos veces desde el celular.
  - **Modelo por el secret `OCR_COMPROBANTE_MODELO`, con default `claude-sonnet-4-6` — el mismo de antes, a propósito.** Cambiarlo se decide evaluando con comprobantes reales, y eso lo prueba Facu. La respuesta incluye `modelo`, para saber cuál leyó cada uno. **Ojo al evaluar:** `gastos.html` comprime a 2576 px, pero los modelos de nivel estándar (el default entre ellos) miran a ~1568 px, así que con el default esa resolución extra no se aprovecha.
  - **RAMA DE TRANSICIÓN — HAY QUE SACARLA.** La función todavía acepta el formato viejo (`imagen_base64` + `mime_type`) porque una pestaña abierta con el `gastos.html` anterior seguiría mandando eso, y sin la rama se le rompería el OCR. **Existe solo para esa ventana:** después es código muerto, y una rama que acepta base64 sin validar la ruta invita a que alguien la vuelva a usar. Para sacarla: borrar el `else` de "Rama de transición" en `index.ts`, desplegar, y actualizar esta entrada.
    - **SE PUEDE MEDIR, y esta entrada decía lo contrario.** Afirmaba *"no hay forma de medir si todavía llegan pedidos viejos: la función no registra por qué rama entró, así que la fecha es una estimación y no una medición"*. **Era falso**, y es la peor clase de frase: no solo miente, **apaga la revisión** — quien la lee no va a ir a mirar los logs, que es exactamente donde estaba la respuesta. Misma familia que el comentario que describe una seguridad inexistente.
    - **NO HACE FALTA AGREGAR UN `console.log` NI DESPLEGAR NADA, y esa es la parte que se decidió midiendo:** un log nuevo mediría **solo hacia adelante**, mientras que los logs que ya existen miden **hacia atrás**. La opción cómoda era la peor.
    - **EL DISCRIMINADOR ES `request.headers.content_length` en `function_edge_logs`:** un cuerpo con ruta pesa **73 bytes** (`{"storage_path":"<uid>/<archivo>"}`) y uno con base64 pesa **millones** (lo medido: entre 3 y 6 MB). No hay zona gris. Y **`version`** dice qué versión sirvió cada llamada, que es lo que separa *"entró por la rama vieja de la v9"* de *"es anterior al deploy"* — sin ese segundo campo, una llamada con base64 de antes del deploy se lee como si la rama todavía se usara.

      ```sql
      select timestamp,
             log_attributes['version'] as version,
             toUInt64OrZero(log_attributes['request.headers.content_length']) as bytes,
             log_attributes['request.sb.jwt.authorization.payload.subject'] as quien
      from logs
      where source = 'function_edge_logs'
        and log_attributes['request.pathname'] like '%ocr-comprobante%'
        and log_attributes['request.method'] = 'POST'
      order by timestamp desc
      ```
    - **Medición del 17/09/2026: desde que la v9 está arriba —deploy entre las 22:05 y las 23:07 UTC del 16/09— CERO llamadas con base64.** Las dos que aparecen con base64 las sirvió la **versión 8**, o sea son anteriores al deploy. **Son tres las personas que usan este OCR** en la ventana de retención (Facu, Emanuel y Franco): las dos primeras ya corrieron por la ruta nueva; falta confirmar la tercera con una carga real, y recién ahí se saca la rama.
    - **NO SE SACÓ CON ESE DATO, a propósito:** eran **22 horas de vida de la v9 y 5 llamadas**. *"Nadie entró por la rama vieja"* y *"la rama vieja no hace falta"* no son lo mismo con esa muestra, y la diferencia la paga un celular en la fábrica que se queda sin OCR.
    - **LA VENTANA DE RIESGO ES MÁS CORTA DE LO QUE DECÍA ESTA ENTRADA.** No es *"la PWA queda abierta días"*: **GitHub Pages sirve el HTML con `Cache-Control: max-age=600`** —10 minutos— así que quien reabre la app tiene el archivo nuevo enseguida, y lo único expuesto es una pestaña abierta **sin recargar** desde antes del deploy. Medido con `curl -sSI` contra `https://cucuruchosnuss-gastos.github.io/seis-destinos/modulos/gastos.html` el 17/09/2026; **queda anotado el origen porque no es consultable desde el MCP** — se re-verifica con ese mismo comando, no de memoria.
    - **La retención de logs es de 7 días** (verificado el 17/09/2026: hay datos del 10/09). La medición hay que hacerla **antes de que pasen 7 días del deploy**, o la ventana deja de cubrirlo entero y el cero pasa a significar "no veo" en vez de "no pasó".
- OCR de remitos/facturas de materia prima: Edge Function `ocr-materia-prima`, SEPARADA de `ocr-comprobante`. **Desde agosto 2026 lee el comprobante COMPLETO.** Por renglón: descripcion, **descripcion_literal**, marca, unidad_medida, cantidad_bultos, contenido_por_bulto, cantidad_total, `codigo_articulo`, `precio_unitario`, `precio_total`, `alicuota_iva` y `tipo_sugerido`.
  - **`descripcion` y `descripcion_literal` son DOS campos de texto con propósitos incompatibles, y por eso son dos.** `descripcion` es el nombre limpio —sin marca ni envase— que prellena el producto y con el que se matchea contra el catálogo. `descripcion_literal` es la **transcripción EXACTA del renglón del papel**, sin limpiar ni normalizar ni resumir, y es la que se guarda como `texto_proveedor` del alias **y con la que se BUSCA en la libretita**.
    - El caso que lo motivó (factura A 0069-00109678 de Carlos Boero Romano, agosto 2026): el papel decía `HARINA 000 JUPITER x 25 Kg. (NARANJA)` y el OCR devolvía `Harina 000`, porque **el prompt le pedía resumir**. Ese texto quedaba guardado como alias, y la factura siguiente —que vuelve a traer el texto largo— no matcheaba nunca. La libretita se llenaba de entradas que no aciertan, **dando la impresión de que el sistema aprende cuando no aprendía nada**.
    - Y no alcanzaba con volver `descripcion` literal: se usa para el nombre del producto, así que el catálogo se habría llenado de insumos llamados "HARINA 000 JUPITER x 25 Kg. (NARANJA)". Un problema por otro.
    - El prompt exige que `descripcion_literal` sea **ESTABLE entre lecturas del mismo papel**: es la clave de un índice único, y un texto que dependa de cómo el modelo decida resumir ese día no sirve. Solo se le hace `trim()` de los bordes — cualquier retoque de adentro haría que el mismo papel diera un texto distinto según por dónde pasó.
    - Si no vino utilizable va a `null` y el wizard **cae a `descripcion`**, sin romperse. Ese fallback es lo que hace seguro pushear el frontend antes de desplegar la función.
  - `unidad_medida` es `"kg" | "lt" | "un"`. **`lt`, no `l`** — ver la nota de unidades en el módulo.
  - **La función NO descarta el desglose cuando `cantidad_bultos * contenido_por_bulto` no cierra contra `cantidad_total`.** Lo hacía y estaba al revés: bultos y contenido son transcripción directa del renglón, mientras que el total es una **multiplicación que hizo el modelo**. Los tres viajan como vinieron y es el wizard el que recalcula y avisa. Lo único que se normaliza es el tipo: lo que no sea un número finito y positivo va a `null`. Totales: `importe_neto`, `importe_iva`, `importe_total`. Cabecera: `condicion_venta` y `punto_venta`.
  - **Devuelve DOS objetos: `datos` (post-validación, lo que usa el wizard) y `crudo` (PRE-validación, lo que se archiva en `ocr_crudo`). Están separados a propósito y NO hay que unificarlos "para simplificar":** el filtro de `datos` descarta los renglones con `cantidad <= 0`, y las facturas argentinas traen líneas de BONIFICACIÓN, DESCUENTO y FLETE con cantidad cero o negativa que importan para reconstruir precios en el futuro.
  - **NINGÚN importe se muestra en pantalla.** El jsonb es archivo, no un dato del sistema. La decisión de "nada de precios" en el módulo sigue firme.
  - `tipo_sugerido`: `'materia_prima'` | `'insumo'` | `null`, clasificando si el producto forma parte del producto final. Lo que no es producto físico —fletes, servicios, bonificaciones— va en `null`: *un renglón que dice "Fletes Harinas" es un servicio de transporte, no harina*. La función valida contra la lista y fuerza `null` ante cualquier otro valor: **sugiere, nunca decide**.
  - `max_tokens` 4096, con guarda de `stop_reason === 'max_tokens'` ANTES del `JSON.parse`: si la respuesta se trunca, el mensaje dice que el comprobante tiene demasiados renglones, en vez del genérico "no se pudo leer".
  - **EL LOTE SIGUE PROHIBIDO en el prompt**, con la razón escrita al lado (los lotes no vienen en el remito, están en la etiqueta del envase) y una nota para no confundir esa regla con la de precios: una ampliación futura no tiene que llevársela puesta.
- OCR de cheques: Edge Function `ocr-cheques`, la del módulo Cobranzas. Desplegada el 16/09/2026 (`verify_jwt: true`, como las otras cinco).
  - **RECIBE LA RUTA EN STORAGE, NO LA IMAGEN EN BASE64, y esa es su decisión de diseño principal:** el cuerpo trae `storage_path`, la función **valida que empiece con el uid de quien llama** y baja la foto con el **token del usuario** (cliente creado con la `SUPABASE_ANON_KEY` más el `Authorization` que llegó), así que el RLS del bucket sigue aplicando y la función no puede leer la foto de otro. La foto se sube **una sola vez** desde el celular, en vez de viajar dos veces por una red de fábrica.
  - Modelo por secret **`OCR_CHEQUES_MODELO`**, con default **`claude-opus-5`**. Verificado el 16/09/2026: **el secret no está configurado y la función cae al default**, que es un modelo válido — no es un error, es el camino previsto.
  - Devuelve **`cheques` normalizados + `controles` + `crudo`**. `crudo` es lo que respondió el modelo sin tocar y se archiva en `cobranza_fotos.ocr_crudo`, mismo criterio que `materia_prima_ingresos.ocr_crudo`.
  - **Los `controles` son nueve y cada uno puede dar `true` / `false` / `null`** —`null` = "no se pudo saber", que no es lo mismo que "está mal"—: `dv_ruta_ok`, `dv_numero_ok`, `dv_cuenta_ok` (los tres dígitos verificadores), `completado_desde_cmc7` y `cmc7_coincide` (**el CMC-7 de 29 dígitos del pie contra lo que dice el recuadro** — dos transcripciones del mismo dato que tienen que cerrar entre sí, y si el recuadro no se leyó, el CMC-7 lo completa), `letras_coinciden` (el importe en letras contra el número, con tolerancia de medio centavo), `fechas_ok`, `moneda_ok` y `cuits_ok`.
  - **Ninguno de los controles bloquea por su cuenta**: la función informa y la pantalla decide. Los que sí son barrera dura son los tres CHECK de dígito verificador de la tabla.
- Hosting: GitHub Pages (repo: https://github.com/cucuruchosnuss-gastos/seis-destinos)

## Base de datos central (nuss-central)
- URL: https://xtorxouhzuizdvawqakb.supabase.co
- Publishable key: sb_publishable_G8GZe2uAvb6VdJ1S4DD8nA_CC7iugYw
- **RLS activado en TODAS las tablas de `public`: 59 de 59 al 23/09/2026** (consulta de tablas sin RLS: cero filas; con Producción y Pedidos ya no son 36). Lo de abajo es la verificación anterior: **RLS en las 36 tablas de `public`, las 36, sin ninguna excepción** (re-verificado contra `pg_class.relrowsecurity` el 17/09/2026 — la consulta de tablas sin RLS devolvió CERO filas; la 36 es el respaldo `_respaldo_urls_comprobantes_20260916`, ver Seguridad). Eran 20 en la auditoría del 18/07/2026, y durante un tiempo este documento decía "son 22+, y el RLS de las nuevas no se re-verificó": **ese pendiente queda cerrado.** Ver sección Seguridad por lo que todavía falta afinar, que no es el RLS de las tablas sino el `EXECUTE` de las funciones y las URLs firmadas de Storage.

### Tablas (verificado contra information_schema el 18/07/2026 — no confiar en versiones anteriores de este documento si contradicen esto)

**Maestros**
- `unidades_negocio`: id, nombre, ciudad, activo, created_at
- `categorias`: id, nombre, icon, activo, created_at
- `vehiculos`: id, nombre, marca, patente, unidad_negocio_id, activo, created_at
- `proyectos`: id, nombre, activo, created_at
- `proveedores`: id, razon_social, nombre_fantasia, cuit, activo, created_at, estado_alta, creado_por, direccion, cuenta_corriente
  - `cuenta_corriente` (boolean, NOT NULL, **default `false`**): **LA regla del circuito Ingreso ↔ Gastos ↔ Cuentas Corrientes** (21/09/2026). Con cuenta corriente, toda factura suya va a su cuenta; ocasional, **nunca pasa por la cuenta corriente** y su compra vive solo en Gastos. Por el default, **un proveedor creado al vuelo (desde Ingreso o Gastos) nace ocasional**. Ver la sección del circuito, más abajo.
  - `estado_alta`: `'activo'` / `'pendiente_aceptacion'` / `'rechazado'`. El booleano `activo` se mantiene sincronizado automáticamente por las RPCs (`aprobar_proveedor`/`rechazar_proveedor`/`crear_proveedor_pendiente`) — nunca tocarlo a mano por separado.
  - Índice único parcial en `cuit` (solo cuando no es null) — es la clave de matching real entre módulos, no `razon_social` (que es texto libre y varía).
- `productos`: id, nombre, unidad_negocio_id, activo, created_at
  - **Sin vincular a ningún módulo actual conocido.** Verificar con Facu si es resto de la app vieja o tiene un uso futuro no documentado antes de asumir cualquier cosa sobre esta tabla.

**Empleados / Accesos**
- `empleados`: id, nombre, unidad_negocio_id, rol, cuil, activo, created_at, tipo, auth_user_id, rol_app, origen, fecha_nacimiento, telefono, email, domicilio, legajo, fecha_alta, contacto_emergencia_nombre, contacto_emergencia_telefono, caja_raiz, oculto_como_contraparte
  - `rol_app`: permiso dentro de la app — solo `super_admin` / `usuario` (el valor `admin` ya no existe, ver sección Sistema de permisos). No confundir con `rol`.
  - `rol`: puesto de RRHH (viene de Naaloo — Categoría + Subcategoría concatenadas, ej "Fuera de convenio · Categoría I").
  - `tipo`: `naaloo` / `admin` / `empresa` — bucket usado por el selector de empleados de Gastos, no tocar sin revisar ese uso. `'empresa'` identifica al empleado ficticio de la Cuenta de Empresa (activo=false, sin auth_user_id, unidad_negocio_id null — ver sección Sistema de permisos).
  - `origen`: `naaloo` (importado desde Excel) / `app_registro` (creado por auto-registro sin match de CUIL).
  - `caja_raiz` (boolean): LEGADO — YA NO CONTROLA NADA desde la migración de permisos de julio 2026; el control de ingresos externos pasó a las tareas `caja:ingreso_externo_propio` y `caja:ingreso_externo_empresa`. La columna todavía existe y caja.html la sigue leyendo (~líneas 1709, 1727, 2174, 2284, 4177: comentarios y selects sin efecto) — código muerto, pendiente conocido de limpieza (no urgente). Pendiente de drop en la limpieza final.
  - `contacto_emergencia_*`: carga manual únicamente, Naaloo no lo trae.
  - `oculto_como_contraparte` (boolean, not null, default false): quien la tiene en `true` **no aparece como opción elegible** en el selector de contraparte de Caja. Hoy la tiene una sola persona.
    - **Por qué existe:** al elegirla como contraparte, el selector ofrecía sus cuentas PERSONALES, pero en los movimientos de empresa la plata sale siempre de la Cuenta de Empresa. El movimiento quedaba mal atribuido. Redirigir a las cuentas de Empresa no era opción: `crear_solicitud_movimiento_caja` valida `v_cuenta_contraparte.empleado_id <> p_contraparte_empleado_id` y lo rechaza — y además dejaría un registro falso.
    - **Por qué una columna y no el nombre hardcodeado:** si mañana pasa lo mismo con otra persona, es un UPDATE de un renglón en vez de otro commit.
    - **Dónde se aplica el filtro:** en `poblarSelectorContraparte()` (caja.html ~4041), sobre la lista de candidatos ya armada, DESPUÉS de las tres ramas (desde Empresa / desde un super_admin / desde un usuario común) y ANTES de agregar a Empresa. Una sola línea cubre las tres. **NO se filtra en `cargarSuperAdmins()` ni en `cargarPersonasConAcceso()`**: `estado.empleados` lo comparten el listado general, el Directorio y los filtros de "Retiros socios" y "Todos los movimientos", así que filtrar al cargar haría desaparecer a esa persona de todos esos lugares.
    - **Controla a quién se puede ELEGIR, no quién puede elegir.** Quien está oculto sigue viendo a todos los demás como contraparte desde su propia ficha — el filtro nunca toca `estado.miEmpleado`.
    - **Las DOS consultas que alimentan los candidatos tienen que traer la columna**, y una lee `v_empleados_publico`: ver el aprendizaje sobre la lista fija de columnas de una vista, que es de donde salió este caso.
    - **Nota de diseño — la RPC NO valida esto, a propósito** (verificado contra `pg_get_functiondef`). No es un permiso ni una barrera de seguridad, es una preferencia operativa, y ponerlo duro bloquearía también una corrección manual legítima. La red que queda: una solicitud entre personas nace `'pendiente'` y no se aplica hasta que la otra parte la confirma, así que un pedido armado a mano llegaría a esa persona y lo rechazaría. **OJO con el alcance real de esa red:** `v_auto` se activa cuando quien llama es super_admin **y hay Empresa de uno de los dos lados**, y ahí el movimiento se aplica solo, sin confirmación de nadie. O sea que para el circuito Empresa↔persona —justo el que motivó la columna— la única barrera es la UI.
- `empleado_modulos`: id, empleado_id, modulo, habilitado, otorgado_por, otorgado_en. El dashboard SÍ la consulta (dashboard.html ~línea 419) y filtra los tiles con `esAdmin || misModulos.includes(modulo.clave)` — la variable del código se sigue llamando `esAdmin`, pero con el rol `admin` muerto equivale a super_admin; los super_admin ven todos los módulos sin pasar por esta tabla.
- `empleado_tareas`: id, empleado_id, modulo, tarea, habilitado, alcance (jsonb), más columnas de auditoría. Sistema de permisos granulares: el toggle de `empleado_modulos` decide si la persona VE el módulo; `empleado_tareas` decide qué puede HACER adentro. Las tareas son independientes entre sí, con UNA excepción forzada por servidor y UI: `cuentas_corrientes:ver_todo` requiere `gastos:ver_exportar` (ver la regla de otorgamiento en Sistema de permisos).
  - `alcance` (jsonb): limita una tarea a unidades de negocio concretas — `{"todas": true}` o `{"unidades": [uuid, ...]}`.
  - Hoy son NUEVE las tareas con alcance: `empleados:ver_editar`, las tres de `materia_prima` (`cargar`, `ver_todo`, `editar_anular`) y cinco de `stock` (`ver`, `dar_baja`, `enviar_transferencia`, `recibir_transferencia`, `ajustar_inventario`). **El mecanismo es data-driven desde el flag `conAlcance` de cada entrada de `CATALOGO_TAREAS`** (mismo precedente que `requiere` para las dependencias): una tarea nueva con alcance es UNA LÍNEA del catálogo, sin tocar lógica. La constante `CLAVE_VER_EDITAR` ya no existe — se eliminó en el commit `1730511`, que generalizó estado, prefill, validación y payload (el render ya era data-driven).
    - **Marcar `conAlcance` NO alcanza para que el alcance tenga efecto: son dos mitades del mismo sistema.** El catálogo hace que la pantalla dibuje el sub-bloque de unidades y guarde el jsonb; que ese jsonb **haga algo** depende de que las policies y RPCs de ese módulo lo lean con `tiene_tarea_alcance()`. El catálogo no valida esa otra mitad. Caso vivo: la policy de `empleados` ("leer propia o con tarea de alcance") resuelve el acceso con `tiene_tarea_alcance('empleados','ver_editar', unidad_negocio_id)`, así que esa tarea gobierna quién lee la tabla. Al sumar `conAlcance` a una tarea nueva, revisar **también** el lado del servidor: si nadie lo lee, la pantalla va a ofrecer un alcance que no restringe nada.
  - El catálogo válido lo define el CHECK constraint `chk_tarea_valida` (**43 tareas**, verificado contra `pg_get_constraintdef` el 24/09/2026 — eran 32, pasaron a 36 con las cuatro de `cobranzas`, a 37 con `gastos:gestionar_proyectos`, a 40 con las tres de `produccion` y a 43 con las tres de `pedidos`; lista completa con labels en la sección Sistema de permisos); en el frontend está hardcodeado en `CATALOGO_TAREAS` dentro de accesos.html (decisión explícita: no se lee de la base). Los dos tienen que mantenerse en sincronía a mano, y **hoy lo están: las 43 claves del CHECK son las 43 del catálogo** (`test-accesos-produccion.js` las compara). Estuvieron desincronizados en la dirección peligrosa del 23 al 24/09/2026: el catálogo tenía las tres de Pedidos (`41fe94c`) antes que el CHECK, y tildarlas y guardar lo rechazaba la base — el caso inverso al de siempre, que el passthrough no cubre.
    - **Estuvieron desincronizados unos días**, desde `3cd5ef7`: `caja:transferir_entre_personas` entró al CHECK con el commit del módulo Caja y al catálogo recién después, porque `CATALOGO_TAREAS` es territorio del chat de permisos. En esa ventana la tarea se otorgó por SQL y funcionó igual —el servidor la valida y caja.html la lee—, pero no se podía tildar desde la pantalla de Accesos.
    - **El passthrough cubrió la ventana, que es exactamente para lo que fue diseñado:** las dos personas que ya la tenían no la perdieron al editarles los permisos, aunque la pantalla no supiera dibujarla. **Es la segunda vez que pasa** —la primera fue con las 4 de `materia_prima`—, así que no es un accidente sino la consecuencia normal de que el CHECK y el catálogo tengan dueños distintos. Por eso no se filtra el payload contra el catálogo.
  - **Estructura de `CATALOGO_TAREAS`** — por GRUPO: `grupo` (título), `modulos: [...]` (claves de `empleado_modulos`, guión MEDIO, que gobiernan el despliegue condicional — ver Sistema de permisos), `nota` opcional. Por TAREA: `modulo` + `tarea` (guión BAJO en el módulo), `label`, **`descripcion`** (texto largo que se muestra en un globo al tocar el botón `?` al lado del label; las 40 la tienen), y los flags opcionales `conAlcance` y `requiere`. El botón `?` va FUERA del `<label>` a propósito: adentro, tocarlo tildaría la tarea. Y la descripción va como contenido de un `<div>` y no en un `title=""`, porque varias llevan comillas dobles.
  - El campo `modulos` va a mano contra claves que vienen de la tabla `modulos`. `construirListasModulos()` avisa por consola si una no existe — sin eso, cambiar una clave en la base dejaría el grupo invisible para siempre sin ningún error.
- Convención de nombres (ya existente, explícita porque confunde): `modulos.clave` / `empleado_modulos.modulo` / `MODULOS[].clave` de dashboard.html usan GUIÓN MEDIO (`'cuentas-corrientes'`, `'materia-prima'`); `empleado_tareas.modulo` usa GUIÓN BAJO (`'cuentas_corrientes'`). Son namespaces distintos, no mezclar.
  - Caso vivo de la doble convención: la fila de `modulos` es `clave='materia-prima'` (guión MEDIO), nombre visible "Insumos - Materia Prima", orden 6; sus cuatro tareas en `empleado_tareas` usan `modulo='materia_prima'` (guión BAJO). El `CATALOGO_TAREAS` de accesos.html lleva un comentario al respecto en ese grupo.
- `solicitudes_acceso`: id, nombre, apellido, email, cuil, estado, fecha_solicitud, usuario_id, fecha_nacimiento, telefono, tuvo_match
  - CHECK **`chk_solicitud_texto_sin_html`** (16/09/2026, chat de permisos; validado, cero filas existentes lo violaban): `nombre`, `apellido` y `email` no pueden contener `<` ni `>`. Es **defensa en profundidad** del XSS de `accesos.html`, no el arreglo: el arreglo es escapar al mostrar (ver la regla en Aprendizajes clave). `crear-solicitud-acceso` valida lo mismo ANTES de crear la cuenta — ver Login y roles.

**Gastos**
- `gastos`: id, fecha, periodo, empleado_id, unidad_negocio_id, vehiculo_id, proveedor_id, categoria_id, proyecto_id, tipo_doc, numero_doc, razon_social, importe, moneda, kilometraje, lugar_servicio, foto_url, descripcion, observaciones, estado, created_at, receptor, descripcion_item, medio_pago, fecha_pago, cuenta_id, anulado_por, anulado_en, motivo_anulacion, sin_ingreso_motivo
  - `sin_ingreso_motivo` (text, CHECK de 3 a 300 caracteres o null): un gasto de "Insumos - Materia Prima" que **no lleva ingreso de mercadería** (un servicio, un flete). Lo escribe `descartar_ingreso_de_gasto` y lo saca de "Pagado sin ingresar".
  - `estado`: `'registrado'` / `'anulado'`. **NO tiene CHECK constraint** — la restricción vive solo en el código, así que un INSERT a mano puede meter cualquier string.
  - `anulado_por` (uuid), `anulado_en` (timestamptz), `motivo_anulacion` (text): los completa `anular_gasto`. `anulado_por` está SIN FK a `empleados` a propósito (ver Aprendizajes: rompería los embeds de PostgREST por ambigüedad).
- `facturas_pendientes`: id, unidad_negocio_id, categoria_id, razon_social, tipo_documento, numero_comprobante, importe, moneda, empleado_id, fecha_factura, lugar, observaciones, comprobante_url, estado, gasto_id, created_at, updated_at, proyecto_id, vehiculo_id, kilometraje, modulo_origen, proveedor_id, saldo_pendiente
  - `estado`: `'pendiente'` / `'parcial'` / `'pagada'` / `'anulada'` / **`'sin_importe'`**. El CHECK constraint de esta columna tuvo que ampliarse a mano para admitir `'parcial'` — si en el futuro se agrega otro estado nuevo, recordar revisar el constraint (`facturas_pendientes_estado_check`), no asumir que Postgres lo acepta solo.
  - **`'sin_importe'` (21/09/2026): una descarga SIN COMPROBANTE que Ingreso cargó en la cuenta de un proveedor con cuenta corriente.** `importe` y `saldo_pendiente` van en **null** —las dos columnas son nullables desde ese día—, y **ninguna vista de saldo la suma** (`v_saldo_proveedor` mira `pendiente`/`parcial`; en `v_cuenta_corriente_movimientos` llega con `monto` null y el `sum() over` la saltea). O sea que todo saldo de un proveedor con descargas sin importe está **incompleto**, y Cuentas Corrientes lo dice. Pasa a `pendiente` con `completar_importe_factura`, que completa `importe_cargado_por` / `importe_cargado_en`.
  - **Índice único `uq_facturas_pendientes_proveedor_numero`** sobre `(proveedor_id, normalizar_numero_doc(numero_comprobante))`, parcial: `estado <> 'anulada'` y los dos no nulos. **La misma factura no puede estar dos veces en la cuenta de un proveedor**, entre por Ingreso, por Gastos o a mano.
  - `modulo_origen`: `'gastos'` / `'materia_prima'` — para cuándo Materia Prima también inserte acá.
  - `saldo_pendiente`: arranca igual a `importe` (trigger `fn_inicializar_saldo_pendiente`), baja con cada pago aplicado o crédito aplicado. Fuente de verdad real del estado de deuda de esa factura puntual.
  - `gasto_id`: legado del diseño atómico original (una factura = un pago completo). Desde que existe `aplicaciones_pago`, un pago puede repartirse entre varias facturas — este campo ya no es la fuente de verdad para saber qué pago cubrió una factura, usar `aplicaciones_pago` en su lugar.

**Cuentas Corrientes** (proveedores — evolución de facturas_pendientes que soporta pagos parciales/mixtos)
- `aplicaciones_pago`: id, gasto_id, factura_pendiente_id, monto_aplicado, created_at — un pago (una fila de `gastos`) puede repartirse entre varias facturas.
- `creditos_proveedor`: id, proveedor_id, unidad_negocio_id, moneda, monto_original, monto_disponible, origen_gasto_id, estado (`'disponible'`/`'agotado'`), created_at — saldo a favor por sobrepago.
- `aplicaciones_credito`: id, credito_id, factura_pendiente_id, monto_aplicado, created_at — un crédito puede aplicarse a más de una factura futura.
- `intereses_factura`: id, factura_pendiente_id, monto, moneda, fecha, tasa_pct, tasa_periodo, dias_transcurridos, observaciones, creado_por, created_at, base_calculo — intereses por mora sobre una factura pendiente. RLS: solo SELECT, con la validación `rol_app IN ('admin','super_admin')` — ver la nota de permisos en las RPCs de Cuentas Corrientes.
  - `base_calculo`: `'importe'` / `'saldo_pendiente'` — sobre qué monto se calculó. Lo elige la persona al cargarlo.
  - `tasa_pct` / `tasa_periodo` / `dias_transcurridos` quedan en null cuando el interés se cargó como monto exacto en vez de calculado por tasa.
  - Interés SIMPLE, nunca compuesto. LIMITACIÓN CONOCIDA: cargar interés dos veces sobre la misma factura hace que la segunda base incluya el primer interés (porque `agregar_interes_factura` suma el interés a `importe` y a `saldo_pendiente`).
- Todas con RLS, sin políticas de escritura — toda escritura pasa por las RPCs `SECURITY DEFINER` de abajo. (La descripción vieja "SELECT solo para admin/super_admin" es pre-migración de permisos; hoy los gates van por tareas — re-verificar el detalle de las policies en la próxima auditoría.)

**Caja**
- `cuentas_caja`: id, empleado_id, nombre, medio, moneda, favorita, activa, created_at, unidad_negocio_id, cbu, numero_cuenta, alias
  - `unidad_negocio_id` (uuid, nullable, FK a `unidades_negocio`): agregada en la sesión de Cuenta de Empresa.
  - `cbu` / `numero_cuenta` / `alias` (text): datos bancarios públicos, editables vía `editar_datos_publicos_cuenta_caja`.
- `caja_movimientos`: id, empleado_id, tipo, monto, moneda, medio_pago, gasto_id, fecha, descripcion, creado_por, created_at, contraparte_empleado_id, cuenta_id
  - `tipo`: a los 7 valores que ya existían se sumó `'ingreso_reversion_gasto'` — lo inserta `anular_gasto` para devolver a Caja la plata de un gasto anulado.
- `caja_solicitudes_movimiento`: id, origen_empleado_id, destino_empleado_id, monto, moneda, medio_pago, fecha, descripcion, estado, creado_por, motivo_rechazo, respondido_por, respondido_en, created_at, cuenta_origen_id, cuenta_destino_id
  - Regla de negocio: transferencias entre personas requieren que al menos una de las dos partes sea `super_admin`. DOS EXCEPCIONES: las que involucran a la Cuenta de Empresa no exigen super_admin de contraparte, y cualquier super_admin puede aceptarlas en su nombre (auto-aceptación permitida); y quien tenga `caja:transferir_entre_personas` puede armar un movimiento entre dos personas comunes. Ver sección Sistema de permisos.

**Materia Prima / Insumos** (esquema completo con RLS. El módulo de INGRESO tiene el listado y el **wizard de carga completo** — ver el detalle funcional en la sección Módulos. El stock y la administración del catálogo se mudan a un módulo aparte, todavía no construido.)
- `insumos`: id, nombre, marca, unidad_medida, tipo, categoria, aclaracion, activo, estado_alta, created_at. CATÁLOGO ÚNICO COMPARTIDO por las 4 unidades de negocio — NO tiene unidad_negocio_id (se eliminó a propósito).
  - `categoria` (text, nullable, CHECK `chk_categoria` con **11 valores**, re-verificado el 10/09/2026): `Harinas`, `Grasas`, `Azúcares y secos`, `Aditivos`, `Cajas`, `Bolsas`, `Separadores y tiras`, `Paletizado`, `Cintas y adhesivos`, `Limpieza`, `Proceso y mantenimiento`.
    - **LA CAPTURAN LAS DOS PANTALLAS, con criterios distintos y a propósito** (septiembre 2026): `crear_insumo` y `editar_insumo` reciben `p_categoria`, y el wizard de Ingreso la manda al crear al vuelo. **En Ingreso es OBLIGATORIA; en el catálogo de Stock es opcional.** No es una inconsistencia: Stock es la pantalla de **curación** y tiene que poder **DESclasificar** algo mal puesto —obligatoria lo haría imposible—, mientras que en Ingreso el producto se crea al vuelo y lo que no se clasifica ahí no lo mira nadie nunca más. Cada lado es estricto donde le corresponde, y está escrito así en el código de los dos archivos.
    - **LISTA CERRADA Y NO TEXTO LIBRE, a propósito.** Con texto libre terminaríamos con "Cajas", "cajas" y "Caja" como **tres grupos distintos en pantalla** — que es exactamente lo que ya pasó con `unidad_medida` ("kg" / "Kg" / "kilos"). Agregar una categoría es raro y merece ser deliberado: un `DROP` + `ADD` del CHECK, no un tipeo distraído.
    - Los 53 activos están categorizados y **ninguno en null**; un insumo creado al vuelo desde Ingreso va a nacer así.
  - `aclaracion` (text, nullable, CHECK `length(btrim(...))` entre **1 y 100**, verificado el 02/09/2026): una nota corta para que el nombre alcance. **Se agrega solo donde hace falta, no en todos.**
    - **PARA QUÉ:** "Separador N°1" y "Separador N°2" son indistinguibles para alguien que recién entra al depósito, y "Pegamento hotmelt" no dice que es el de los conitos. **Confundirlos en un recuento mete el conteo en la fila equivocada y genera un ajuste falso en LAS DOS.**
    - **POR QUÉ EL TOPE DE 100:** se muestra entre paréntesis al lado del nombre, en listas de 53 filas. Sin tope rompe la tarjeta en mobile.
    - El CHECK exige **al menos un carácter cuando no es null**, así que una cadena vacía lo viola igual que una de 200: el cliente manda `null`, no `''`.
  - `vista_preferida` (text, **NOT NULL, default `'base'`**, CHECK `('base','bulto')`, verificado el 10/09/2026): cómo se muestra ese insumo en el listado de Stock — en su **unidad base** (kg/un/lt) o **en bultos**. La harina en bultos, la lecitina en kilos. Hoy **13 de los 55 activos** están en `'bulto'`.
    - Se elige desde el catálogo de Stock y **SOLO EN EDICIÓN**: `crear_insumo` **NO** recibe `p_vista_preferida` (solo `editar_insumo` lo hace), así que ofrecerlo en el alta prometería algo que no se guarda. Un insumo nace en `'base'` por el default de la columna.
    - **LA CAÍDA AUTOMÁTICA ES EL CASO MAYORITARIO, NO EL BORDE:** un insumo marcado `'bulto'` **sin presentación cargada** se muestra en su unidad igual, sin romperse. La mayor parte del stock entró por el **recuento inicial**, donde nadie declaró presentación, así que cae por ese camino (al 17/09/2026, **32 de los 40** movimientos del libro no tienen `contenido_por_bulto`; ya no es "ninguno", pero sigue siendo la mayoría). La preferencia dice *"cuando PUEDA, mostrame bultos"*, no *"mostrame bultos o fallá"*.
      - **Y no sale de un `if`, sale de `textoBultos()`**, que ya devuelve `null` cuando no hay un `contenido_unico` que sirva: la decisión vive en UN solo lugar. Si alguien "optimiza" ese camino no se rompe un caso raro — se rompe casi todo el listado, y **SOLO para los marcados `'bulto'`**, así que pasaría desapercibido. Tiene mutaciones por los dos extremos.
    - Con **varias presentaciones** tampoco aplica: no existe "un" bulto que represente al total.
  - `tipo`: `'materia_prima'` (exige lote) | `'insumo'` (no lleva lote). Materia prima es lo que entra a la receta (harina, azúcar, colorante, grasa, lecitina, fécula, cacao, esencias, bicarbonato); insumo es lo auxiliar (cajas, bolsas, limpieza, repuestos).
  - `estado_alta`: `'activo'` | `'pendiente_revision'`. Los productos creados al vuelo durante una carga entran como `'pendiente_revision'`.
    - **La policy de INSERT acepta CUALQUIER `tipo` mientras `estado_alta` sea `'pendiente_revision'`.** (Una versión anterior de este documento decía que forzaba `tipo='materia_prima'` — ya no es así.) El control real no es imponer el tipo, sino que **todo producto creado al vuelo quede marcado para revisar**: quien carga elige si es materia prima o insumo en la tarjeta del ítem, y alguien lo confirma después. Forzar el tipo obligaba a marcar "lote ilegible" en cosas que no llevan lote, como una caja de cartón.
  - Índice único: `(lower(nombre), lower(coalesce(marca,'')))`.
  - Los insumos NO se borran nunca (romperían la trazabilidad histórica): se desactivan con `activo=false`.
- `materia_prima_ingresos`: id, fecha, tipo_doc, numero_doc, razon_social, nombre_fantasia, proveedor_id, unidad_negocio_id, foto_url, remito_vinculado_id, empleado_id, editado_por, editado_en, created_at, ocr_crudo, gasto_id, factura_pendiente_id, sin_stock_motivo.
  - `gasto_id` / `factura_pendiente_id` (uuid, FK): **el destino contable** de una factura o descarga, uno o el otro (CHECK `chk_destino_contable_unico`). Los escriben las RPCs del circuito **y el trigger `trg_gasto_autovincular_ingreso`** de `gastos` (ver el circuito). Los dos en null = todavía sin vincular.
  - `sin_stock_motivo` (text, CHECK de 3 a 300 o null): **factura de mercadería que ya estaba** (se contó antes, por ejemplo en el recuento inicial): el ingreso **no suma stock** —`fn_espejar_stock_ingreso` suma solo si es null—. **Se fija al crear el ingreso y el trigger `trg_ingreso_sin_stock_inmutable` impide cambiarla después**: si alguien se equivoca, anula y vuelve a cargar. La casilla del wizard está solo en facturas. **Desde el 22/09/2026 el trigger de lote (`fn_validar_item_materia_prima`) SÍ la mira** —lee `sin_stock_motivo` de la cabecera con el mismo criterio que `fn_espejar_stock_ingreso`—, así que un renglón de materia prima de un ingreso que no suma **no pide lote**. Lo que lo hace seguro: la cabecera se inserta ANTES que los ítems (`confirmarIngreso()`), y `trg_ingreso_sin_stock_inmutable` impide cambiar `sin_stock_motivo` después, así que un ítem validado con ese valor no puede quedar inválido más tarde.
  - `tipo_doc`: `'remito'` | `'factura_a'` | `'factura_x'` | `'sin_comprobante'`
  - `remito_vinculado_id`: autorreferencia. **LEGADO desde agosto 2026: los ingresos NUEVOS ya no la escriben** — el vínculo pasó a la tabla puente `materia_prima_factura_remitos`, que admite varios remitos, y qué suma stock se decide por RENGLÓN con `ya_recibido_con_remito`. **La dependencia con Stock QUEDÓ CUMPLIDA** (fase 2 terminada, agosto 2026): ninguna de las tres vistas de stock —`v_stock_por_lote`, `v_stock_insumos`, `v_stock_negativo`— la filtra ya. El stock sale del libro `stock_movimientos`, y qué entra al libro lo decide el trigger según `ya_recibido_con_remito`, no esta columna. Borrarla ya no rompe el stock.
    - **NO SE PUEDE DROPEAR TODAVÍA: tiene DOS consumidores vivos** (verificado el 31/08/2026). Del lado del servidor, la función **`remitos_vinculables`** la referencia (`pg_proc.prosrc`), y es la que alimenta el paso del vínculo del wizard. Del lado del cliente, **`modulos/materia-prima.html` la trae en un `.select()`** (~línea 2411) y la usa para deduplicar entre las dos formas del vínculo (~2457). Dropearla rompería las dos cosas, y la del frontend con un 400 de PostgREST — el modo de falla de "no codificar contra una columna que alguien dio por creada", entrando por la puerta de salida.
    - Lo que sí quedó cumplido es **la dependencia con las vistas de Stock**: ninguna de las tres la filtra ya. Eso era correcto; lo falso era la conclusión de que por eso no le quedaban consumidores.
    - **Y ESTO ES LO MEJOR DE LA ENTRADA:** la afirmación *"no tiene consumidores conocidos"* la traía este mismo documento, dada por buena, y lo que la atajó fue **la condición de re-verificar antes del DROP que el propio documento exigía**. Sin esa condición se habría dropeado una columna en uso. Es "verificar antes de asumir" pagando dividendos sobre una afirmación propia — que es justo el caso donde menos se sospecha.
    - **CONDICIÓN ANTES DEL DROP, ahora concreta:** migrar `remitos_vinculables` a la tabla puente y sacar la columna del `.select()` de materia-prima.html. Recién ahí re-verificar de nuevo, porque el DROP puede pasar mucho después.
    - Dato al **17/09/2026**: **`remito_vinculado_id` sigue en CERO filas no nulas**, así que la condición para dropearla **no empeoró**. Lo que sí cambió es la tabla puente: **ya tiene filas**, o sea que la nota de más abajo sobre "la fila histórica migrada está en las DOS formas" **vuelve a describir datos vivos** y la lógica de deduplicación tiene otra vez qué deduplicar.
      - **Una versión anterior de esta entrada decía que no había nada que deduplicar.** Era cierto el día que se escribió y dejó de serlo en dos semanas, sin que nadie tocara una línea de código. Por eso el dato va con fecha: **un conteo de filas no es una propiedad del sistema**, es una foto.
  - `ocr_crudo` (jsonb, nullable): la lectura del comprobante TAL CUAL la devolvió el modelo, PRE-validación. Es el registro del papel —que después no está más—, no un dato del sistema: **no se muestra en ninguna pantalla**. Null cuando no hubo OCR (`sin_comprobante` o lectura fallida), que es lo esperado y no un error. Ver el detalle de qué trae en la sección de la Edge Function.
  - `proveedor_id` (uuid, nullable, FK a `proveedores`): vínculo con el catálogo de proveedores. **`razon_social` se sigue guardando además**, como texto histórico de lo que decía el comprobante — puede no coincidir exactamente con el nombre del proveedor en la tabla, y esa diferencia es dato, no ruido.
  - `editado_por` (uuid) y `editado_en` (timestamptz): auditoría de edición, las completa el trigger `trg_marcar_edicion_ingreso`. **`editado_por` va SIN FK a `empleados` a propósito**: la tabla ya tiene `empleado_id` con FK y una segunda rompería los embeds de PostgREST por ambigüedad (mismo criterio que `gastos.anulado_por`). El nombre se resuelve contra `v_empleados_publico`, NUNCA con un embed.
- `materia_prima_items`: id, ingreso_id, insumo_id, cantidad, lote, lote_ilegible, ficha_tecnica_url, foto_lote_url, cantidad_bultos, contenido_por_bulto, cantidad_documento, motivo_diferencia, ya_recibido_con_remito, created_at. NO tiene unidad_medida (se eliminó: la unidad vive solo en el catálogo, para no sumar kg con bolsas).
  - `cantidad` siempre está en la unidad base del catálogo. **INVARIANTE que vale en todo el archivo: `cantidad` es y sigue siendo lo REALMENTE RECIBIDO, que es lo que suma stock.** Lo único que cambia con el control de recepción parcial activo es qué input la escribe: el de arriba pasa a `cantidad_documento` y el de abajo a `cantidad`. CHECK `materia_prima_items_cantidad_check`: siempre `> 0`.
  - `cantidad_documento` (numeric, nullable): lo que dice el comprobante. **NULL cuando coincide con lo recibido —el caso normal—**: coincidir no es una diferencia y no hay nada que registrar. CHECK `chk_cantidad_documento_positiva`: null o `> 0`.
  - `motivo_diferencia` (text, nullable): OBLIGATORIO cuando `cantidad_documento` no es null y difiere de `cantidad`. Lo fuerza el CHECK `chk_motivo_diferencia` (`cantidad_documento is null OR cantidad_documento = cantidad OR motivo <> ''`). Es el dato que después se le reclama al proveedor.
  - **En modo desglose, `cantidad_bultos` son los bultos RECIBIDOS**, no los del documento — si no, `chk_presentacion_coherente` (que exige `cantidad = bultos * contenido`) no cerraría.
  - `ya_recibido_con_remito` (boolean, NOT NULL, default false): `false` = el ítem SUMA stock. `true` = esa mercadería ya entró con un remito anterior y NO vuelve a sumar. **Es el CONTRATO CON EL MÓDULO STOCK: su trigger de espejado suma exactamente los ítems en `false`.**
  - `cantidad_bultos` / `contenido_por_bulto`: la presentación ("200 bolsas × 25 kg"). CHECK `chk_presentacion_coherente`: si están cargados, `cantidad` debe ser igual a `cantidad_bultos * contenido_por_bulto`.
    - **NULLABLES, y es a propósito** (verificado el 01/09/2026: `is_nullable = YES` en las dos). El `NOT NULL` estuvo puesto unos días y **se aflojó porque se había pasado de alcance**: hacía imposible representar "vino a granel", que es un caso real —33 kg de film en 6 bobinas que pesan distinto cada vez— y obligaba a inventar una presentación falsa ("33 bultos de 1").
      - La evidencia de que sobraba estaba del lado de Stock: **`stock_movimientos.contenido_por_bulto` es nullable a propósito** y el documento ya lo describía como *"entró sin presentación conocida"*, y **`v_stock_insumos.kilos_sueltos` existe exactamente para mostrar ese saldo**. Con el `NOT NULL` puesto, esa columna daba 0 para siempre: código muerto por construcción.
      - Además exigía el dato a los renglones con `ya_recibido_con_remito = true`, que **nunca llegan al libro** — el trigger suma solo los que están en `false`. Era pedir un dato para nada, y dejaba un error rojo flotando sobre una tarjeta ya atenuada.
    - **La obligatoriedad vive en la UI, no en la base**, y ahí es donde puede distinguir "vino a granel" de "se lo olvidaron" — cosa que un CHECK no puede.
  - `contenido_documento` (numeric, nullable, CHECK `chk_contenido_documento_positivo`: null o `> 0`; verificado el 02/09/2026): **unidades por bulto SEGÚN EL COMPROBANTE.** Se guarda siempre que se conozca, **incluso cuando el par `cantidad_bultos` / `contenido_por_bulto` va en null** porque lo recibido no se reparte en bultos enteros.
    - **EL PAR `cantidad_bultos` / `contenido_por_bulto` LO DERIVA EL PAYLOAD CON UNA SOLA REGLA** (`73d0ebd`): `contenido_por_bulto: bultos == null ? null : item.contenido`. El CHECK acepta los dos en null o los dos con valor, nada intermedio, y hasta ahí eso **dependía de que cada rama de `derivarCantidades` se acordara de anular los dos** — que es la mitad del constraint que falló las dos veces anteriores.
      - Lo obligó el modo `'bultos'`: ahí el contenido es **el que se tipea**, así que no se puede anular en el estado sin vaciarle el campo a la persona. Resolverlo al armar la fila vale en los tres modos y **hace el par a medias imposible por construcción**, en vez de depender de disciplina.
    - **Va SUELTA, fuera de `chk_presentacion_coherente`, y esa es la razón de que sea una columna aparte:** ese CHECK valida la presentación de lo RECIBIDO (`cantidad = bultos × contenido`), y esta no describe lo recibido — describe el papel. Meterla adentro la ataría a una coherencia que no le corresponde.
  - `interpretacion` (text, nullable, CHECK `chk_interpretacion`: `'bultos'` | `'unidades'` | `'granel'`): **qué significaba el número del comprobante en ese renglón.** Se guarda porque NO se puede derivar de lo guardado: "250 bultos de 25" y "250 unidades en 25 bultos" no se distinguen mirando el resultado. Sin esta columna no hay nada que aprender y la "más frecuente" saldría de adivinar. Ver el rediseño del paso 3 del wizard en la sección del módulo.
  - `lote`: se tipea SIEMPRE a mano, nunca sale del OCR (los lotes no vienen en el remito, están en la etiqueta del envase).
  - `lote_ilegible`: para etiquetas rotas o borrosas. CHECK `chk_lote_ilegible_sin_lote`: no puede estar en true y tener lote a la vez.
  - `ficha_tecnica_url` y `foto_lote_url` son adjuntos SEPARADOS y opcionales.
- `materia_prima_factura_remitos`: factura_id, remito_id, created_at. PK compuesta, RLS activa. **Una factura puede vincularse a VARIOS remitos.** Es trazabilidad DOCUMENTAL, separada de la lógica de stock, que vive en `materia_prima_items.ya_recibido_con_remito`.
  - Policies: leer con `materia_prima:ver_todo` o el ingreso propio con `cargar`; crear con `cargar`; borrar con `editar_anular` o el propio con `cargar`. Todas por la unidad **de la factura**, no la del remito.
  - **NO se puede traer con un embed de PostgREST**: tiene DOS FKs a `materia_prima_ingresos` (`factura_id` y `remito_id`) y falla por ambigüedad, el mismo caso que `gastos.anulado_por`. Va en consulta aparte.
  - La fila histórica migrada está en las DOS formas a la vez (columna vieja Y tabla puente): todo lo que lea ambas fuentes tiene que deduplicar.
- `proveedor_insumo_alias`: id, proveedor_id, texto_proveedor, insumo_id, creado_por, created_at. La **libretita de traducción**: cada proveedor le pone su propio nombre a la misma cosa, y esta tabla mapea el texto del papel al insumo del catálogo.
  - Índice único `(proveedor_id, normalizar_texto(texto_proveedor))`.
  - **SOLO POR PROVEEDOR, nunca global**: el mismo texto significa cosas distintas según quién lo escriba.
  - **BORRAR UN PROVEEDOR LE BORRA TODOS SUS ALIAS, EN SILENCIO Y SIN NINGUNA BARRERA** (verificado contra `pg_constraint` el 17/09/2026). De las **CINCO** claves foráneas que apuntan a `proveedores`, **cuatro están en `NO ACTION`** —`gastos`, `facturas_pendientes`, `materia_prima_ingresos` y `creditos_proveedor`—, así que la base **FRENA** el borrado de un proveedor que tenga movimientos y uno se acostumbra a que lo frene. Pero **`proveedor_insumo_alias_proveedor_id_fkey` está en `ON DELETE CASCADE`**, así que esa fila se va sin que nada avise.
    - **EL DAÑO NO ES UN ERROR VISIBLE, y por eso es peligroso:** la libretita simplemente **deja de acertar**. El wizard de Ingreso vuelve a crear el alias en la próxima carga, y queda **la impresión de que el sistema aprende cuando en realidad perdió lo que sabía** — que es EXACTAMENTE el modo de falla que motivó que la libretita exista (ver el caso de la harina de Carlos Boero Romano en `ocr-materia-prima`). El mismo síntoma, por la puerta de atrás.
    - **REGLA: antes de borrar un proveedor, mirar si tiene alias y REAPUNTARLOS al proveedor que va a quedar.** La FK no avisa, y las otras cuatro FK dan una falsa sensación de que la base te cuida. Al 17/09/2026 hay **20 alias cargados y ninguno huérfano**.
  - Policies (verificado contra `pg_policy` el 02/09/2026): **leer con `materia_prima:cargar` OR `stock:gestionar_catalogo`**; crear con `cargar` (el INSERT exige además `creado_por = mi_empleado_id()`); **editar y borrar con `stock:gestionar_catalogo`**, y el UPDATE lo lleva tanto en `USING` como en `WITH CHECK`. O sea que quien carga mercadería puede crear alias pero no pisarlos.
    - **El `OR gestionar_catalogo` del SELECT es una corrección, y hace falta para que la pantalla de gestión exista.** Antes leer era solo `cargar`, así que alguien con `gestionar_catalogo` y sin `cargar` **podía editar y borrar alias que no podía ver**: la lista le salía vacía, sin ningún error, y el permiso de escritura quedaba inalcanzable. Es la trampa de "las filas no llegan" con el agravante de que las dos mitades del permiso estaban en tablas de decisión distintas.
  - `texto_proveedor` guarda la descripción CRUDA del OCR, sin normalizar: la normalización la hace el índice.
- `normalizar_texto(p_texto)` — IMMUTABLE, la función del índice de arriba: `lower(btrim(translate(...)))` sobre una lista fija de acentos. **Minúsculas, acentos y espacios de los BORDES. NO colapsa espacios internos, no toca la puntuación ni los símbolos** (`Nº1` y `N1` son distintos; `btrim` saca espacios pero no tabs).
  - **`modulos/materia-prima.html` la replica EXACTA en `normalizarTextoAlias()` y NO reusa `claveInsumo()`**, que sí colapsa espacios internos. Si divergieran, el front creería que no hay alias, intentaría insertarlo y chocaría contra el índice único con un `23505`. Ahí la réplica es inevitable: el wizard BUSCA en la libretita antes de insertar.
  - **`modulos/stock.html` NO la replica, y es deliberado.** La pantalla de gestión edita únicamente `insumo_id`; el texto no se toca, y el índice es sobre `(proveedor_id, texto)`, así que **un choque no es alcanzable editando**. Un chequeo para algo que no puede pasar tiene dos costos: obliga a mantener una segunda réplica de esta función sincronizada para siempre —una divergencia silenciosa esperando— y, si alguna vez saltara, mostraría un mensaje amable sobre un estado que en realidad está corrupto y habría que investigar. **Si aparece un `23505`, que se vea el error**: es la señal de que pasó algo que no debería.
- Trigger `trg_validar_item_materia_prima` → `fn_validar_item_materia_prima()` (SECURITY DEFINER): si el insumo es `tipo='materia_prima'` **y `ya_recibido_con_remito = false`** **y la cabecera tiene `sin_stock_motivo is null`** (desde el 22/09/2026, verificado contra `pg_get_functiondef`), exige lote cargado o `lote_ilegible=true`. Un renglón que no suma no pide lote: ese lote ya se tipeó al cargar el remito, y pedirlo de nuevo obliga a inventarlo o a copiarlo distinto. Se valida solo al insertar/actualizar el ítem: si después alguien cambia el tipo de un insumo del catálogo, los ítems viejos no se re-validan.
- Trigger `trg_marcar_edicion_ingreso` → `fn_marcar_edicion_ingreso()`: BEFORE UPDATE sobre `materia_prima_ingresos`, completa `editado_por` y `editado_en`. No lo escribe el frontend.
  - **Desde el 21/09/2026 NO marca edición si lo único que cambió es `gasto_id` y/o `factura_pendiente_id`** (compara `to_jsonb(new)` contra `to_jsonb(old)` sin esas dos columnas ni las de auditoría). Vincular un ingreso a su destino contable —lo hacen las RPCs del circuito y el trigger de `gastos`, muchas veces sin que nadie toque el ingreso— no es editarlo; sin esto, el detalle diría "editado por" quien cargó un gasto en otro módulo.
- **Visibilidad de los ingresos (cambió — la versión anterior de este documento decía que se veía todo lo de la unidad):** con `materia_prima:cargar` la persona ve SOLO los ingresos que cargó ella; con `materia_prima:ver_todo` ve los de todas las personas, en las unidades de su alcance.
- Funciones propias del módulo:
  - `mi_empleado_id()` — SECURITY DEFINER. Devuelve el `empleado_id` del usuario actual sin chocar con el RLS de `empleados`, que solo deja leer la fila propia. La usan las policies del módulo.
  - `remitos_vinculables(p_unidad_negocio_id, p_proveedor_id default null)` — SECURITY DEFINER. Devuelve los **10** últimos remitos SIN vincular de esa unidad, **filtrados por proveedor cuando se le pasa uno** (hay proveedores que emiten varios remitos por semana y los facturan juntos al cierre; sin el filtro se mezclan). Excluye los ya vinculados **por la columna vieja Y por la tabla puente**, con sus ítems en un `jsonb` (`[{nombre, marca, cantidad, unidad}]`), a quien tenga `materia_prima:cargar` ahí. **Existe porque el listado muestra solo lo propio pero el vínculo remito↔factura necesita ver los remitos de los compañeros**: el de depósito carga el remito y el de administración la factura. Devuelve a propósito filas que la persona NO ve en el listado general — por eso su **uso está acotado al paso del vínculo del wizard y no debe usarse para nada más**. Todo el filtrado (que sean remitos, de esa unidad, sin vincular, el filtro por proveedor, el orden y el tope de 10) vive del lado del servidor: el frontend no lo replica.
  - `presentaciones_sugeridas(p_proveedor_id uuid, p_insumo_ids uuid[])` — SECURITY DEFINER, STABLE, con `search_path` fijo. Devuelve `TABLE(insumo_id uuid, interpretacion text, presentaciones jsonb)`, con `presentaciones` = `[{contenido, veces}]` ordenado por frecuencia. **Una fila por insumo pedido, SIEMPRE**: uno sin historial vuelve con `interpretacion` null y lista vacía, que es el caso "preguntá siempre" y **no un error**. Con `p_proveedor_id` null mira todos los proveedores.
    - **Existe por la misma razón que `remitos_vinculables`:** el RLS de `materia_prima_items` deja ver SOLO los propios a quien tiene `cargar`, así que contar los ingresos anteriores en el navegador daría una cuenta distinta según quién cargue y vacía para alguien que carga por primera vez. Es la trampa de "las filas no llegan", sin ningún error.
    - Solo devuelve estadística: no expone cantidades, fechas ni de qué ingreso salió cada dato. Y gatea por `tiene_tarea_alcance('materia_prima','cargar', …)`, así que no cruza unidades donde la persona no puede cargar.
- Vistas (ambas con `security_invoker=true`):
  - `v_stock_insumos` (insumo_id, insumo_nombre, marca, unidad_medida, unidad_negocio_id, cantidad_total) — filtra por las DOS condiciones mientras convivan las dos formas del vínculo: `remito_vinculado_id is null AND ya_recibido_con_remito = false`.
  - `v_stock_insumos_presentacion` — lo mismo + contenido_por_bulto, bultos.
- Decisión de diseño explícita: el módulo de Materia Prima NO captura precios, importes, IVA ni moneda, y NO toca gastos ni caja. Solo cantidades. Está excluido deliberadamente en esta etapa.
  - **UNA EXCEPCIÓN ACOTADA desde el 21/09/2026: el "Total de la factura".** El wizard lo pide —prellenado con el `importe_total` del OCR— **solo en facturas de un proveedor CON cuenta corriente**, y **no lo guarda en ninguna tabla de materia prima**: se lo pasa a `registrar_factura_de_ingreso`, que lo escribe en `facturas_pendientes`. Sin cuenta corriente no se pide. Y **Ingreso sigue sin crear gastos ni tocar caja**: ver el circuito.
- Pendientes conocidos del módulo:
  - **Fotos huérfanas en Storage**: si alguien reemplaza la foto a mitad del wizard, la anterior ya se subió y queda en el bucket. Limpieza pendiente, no urgente — borrar de Storage es destructivo y se decidió no hacerlo automático.
  - **No existe pantalla de edición de un ingreso**, así que `editado_en` está siempre en null por ahora. La columna y el trigger están listos para cuando se agregue.
  - **NADA del módulo se verificó en un navegador con datos reales más allá de cargas sueltas.** En particular el circuito completo de una factura MIXTA —renglones que suman y renglones ya recibidos con remito— está sin probar de punta a punta contra lo que después muestre el stock. Todo lo que hay son pruebas de lógica sobre el código extraído del archivo.
  - **Corregir un alias NO corrige el pasado**: los ingresos ya cargados siguen apuntando al insumo viejo, y eso se arregla con un ajuste de inventario. Cuando se construya la pantalla de gestión de alias (en Stock) tiene que decirlo.
  - **CORREGIDO** (septiembre 2026, ex "PENDIENTE ABIERTO"): un comentario de `modulos/materia-prima.html` decía que *"el RLS acota qué unidades ve cada usuario (hoy: la propia, salvo super_admin)"*, y era **falso**. La policy de SELECT de `unidades_negocio` es **UNA sola y `USING (true)`** para cualquier autenticado —verificado contra `pg_policy` el 10/09/2026, y antes por el chat de Stock—, así que esa consulta trae **las 4 unidades**, no la propia. Era de la familia del comentario que promete una garantía que el código no da, y acá invitaba a las dos cosas: a agregar un filtro "que falta" —que no falta— y a desconfiar de una consulta que funciona bien. El comentario nuevo dice qué trae la consulta y aclara que **el alcance por unidad no vive ahí**: lo resuelven las policies de las tablas del módulo con `tiene_tarea_alcance()`.
  - **La pantalla de recepción de transferencias no se probó en un navegador**, y es la primera del módulo que escribe stock a través de una RPC de otro módulo. Tampoco se probó el listado con transferencias (`2f6afd1`) — y para eso ya hay un dato real en la base: la transferencia a Mengui debería aparecer al abrir el listado de esa unidad.

**Cobranzas** (esquema completo con RLS, creado el 16/09/2026 — todo lo de abajo verificado contra `information_schema`, `pg_constraint`, `pg_indexes` y `pg_policies` ese día)
- `bancos_bcra`: codigo (PK, `^[0-9]{3}$`), denominacion, activo. El catálogo de entidades del BCRA, **59 filas**, solo para mostrar el nombre del banco.
  - **Los cheques NO tienen FK a esta tabla, a propósito:** un banco que falte en el catálogo no puede frenar un cheque real que está en la mano. El código se guarda igual y la pantalla muestra el número cuando no encuentra el nombre.
- `cobranzas`: id, empleado_id, cliente, cliente_normalizado, fecha, efectivo, moneda, comprobante_referencia, observaciones, estado, procesada_por/_en, anulada_por/_en, motivo_anulacion, created_at, **unidad_negocio_id**. **17 columnas.**
  - **`unidad_negocio_id` (uuid, nullable, 22/09/2026): la unidad de negocio de la plata, que se decide AL ASENTAR** —el repartidor no la sabe—. La pone `marcar_cobranza_asentada` y se cambia con `asignar_unidad_cobranza` (ver RPCs). Una cobranza por controlar no tiene; las asentadas antes del 22/09/2026 quedaron sin unidad (11, medido ese día) hasta que alguien se la asigne. Existe porque en Administración se va a pagar una deuda de una unidad con un cheque de otra, y para eso primero hay que saber de quién es cada cheque.
  - `id` es un **uuid generado en el celular**, no en la base: es la clave de idempotencia del alta (ver `guardar_cobranza`).
  - `cliente` es **texto libre** (1 a 120), sin lista de clientes a propósito: la conciliación contra un padrón de clientes es otra pantalla, todavía no construida. `cliente_normalizado` es **GENERATED ALWAYS … STORED** sobre `normalizar_razon_social(cliente)`, con índice propio — es la clave con la que busca el listado.
  - `estado` ∈ `registrada` / `procesada` / `anulada` (CHECK), más tres CHECK que atan el estado a sus datos: `chk_cobranza_procesada` (procesada exige quién y cuándo), `chk_cobranza_anulada` (`(estado='anulada') = (anulada_en is not null)`, una equivalencia y no una implicación) y `chk_cobranza_anulada_datos` (anulada exige quién **y motivo no vacío**).
  - `moneda` tiene CHECK `= 'ARS'`: **solo pesos**, y que sea un CHECK y no un default es lo que impide que entre un dólar sin que nadie lo decida.
  - **`efectivo` admite 0** y el total NO se guarda en ningún lado: se calcula. Ver `v_cobranzas`.
- `cobranza_fotos`: id, cobranza_id, storage_path (UNIQUE), subida_por, ocr_crudo (jsonb), ocr_modelo, created_at. Además de la PK, `uq_foto_cobranza_id` sobre `(cobranza_id, id)` — existe solo para que los cheques puedan apuntar a la foto con una FK compuesta.
  - **Una foto puede tener VARIOS cheques y se comparte entre ellos**: es normal fotografiar tres cheques juntos sobre el mostrador. El vínculo vive en `cobranza_cheques.foto_id`, no al revés.
  - `ocr_crudo` es lo que devolvió el modelo **sin tocar**, igual que `materia_prima_ingresos.ocr_crudo`: archivo del papel, no un dato del sistema.
- `cobranza_cheques`: id, cobranza_id, foto_id, banco_codigo, sucursal_codigo, codigo_postal, dv_ruta, numero, dv_numero, cuenta, dv_cuenta, tipo, fecha_emision, fecha_pago, importe, importe_letras, titulares (jsonb), beneficiario, origen_datos, ocr_propuesto (jsonb), estado, created_at. **22 columnas.**
  - **LOS CÓDIGOS VAN COMO TEXTO CON LARGO FIJO Y NO COMO NÚMERO, y es la decisión más importante de la tabla:** banco `^[0-9]{3}$`, sucursal `^[0-9]{3}$`, código postal `^[0-9]{4}$`, número `^[0-9]{8}$`, cuenta `^[0-9]{11}$`. **Llevan ceros a la izquierda** —el Galicia es `007`— y un `integer` se los come en silencio, dejando un código que ya no es el del cheque y que además no matchea contra nada.
  - **LOS TRES DÍGITOS VERIFICADORES SE GUARDAN Y LOS VALIDA UN CHECK** con `dv_bcra()`: `chk_dv_ruta` sobre `banco||sucursal||codigo_postal`, `chk_dv_numero` sobre el número y `chk_dv_cuenta` sobre la cuenta. Norma BCRA *"Características de los instrumentos de pago"*, punto 1.3.1, ponderador **9713**. Es imposible guardar un código mal leído, venga del OCR o de un dedazo.
    - **LA MISMA FÓRMULA ESTÁ EN TRES LUGARES: `public.dv_bcra()`, `dvBcra()` de la Edge Function y `dvBcra()` de `js/cobranzas-comun.js`** (hasta el 22/09/2026 vivía dentro de `cobranzas.html`; ahora la usan Cobranzas y Cheques desde el archivo común). **Si se cambia una, se cambian las tres** — los tres lo dicen al lado de la función. Verificado el 16/09/2026: los tres cuerpos son idénticos (el del cliente además tolera `null`). **La versión DESPLEGADA de `ocr-cheques` conserva el comentario viejo** que nombra a `cobranzas.html`: el cambio del 22/09 fue solo de comentario y no se desplegó.
  - **`uq_cheque_en_cartera`: índice único PARCIAL sobre `(banco_codigo, sucursal_codigo, cuenta, numero) WHERE estado <> 'anulado'`.** Es el bloqueo duro del cheque repetido, y **que sea parcial es lo que hace que anular libere el número**: un cheque cargado por error se anula y el mismo papel se puede volver a cargar bien.
  - **El aviso BLANDO va por `(banco_codigo, numero)`** —su propio índice `ix_cheques_banco_numero`— y es más amplio que el bloqueo a propósito: **el BCRA avisa que el número de cheque se repite entre cuentas distintas de un mismo banco**, así que ahí un choque no prueba nada y bloquear sería frenar un cheque legítimo. Avisa y deja seguir; ver `buscar_cheque_cargado`.
  - **`fk_cheque_foto_misma_cobranza`: FK COMPUESTA `(cobranza_id, foto_id)` → `cobranza_fotos(cobranza_id, id)`.** Hace **estructuralmente imposible** que un cheque apunte a la foto de OTRA cobranza. Con dos FKs sueltas eso sería un bug posible y silencioso: la foto existe, la cobranza existe, y el cheque queda mostrando el papel de otro cliente.
  - `tipo` ∈ `comun` / `diferido`, y `chk_fechas_cheque` los distingue: un común **no lleva** fecha de pago, un diferido la exige, no anterior a la emisión y **a no más de 360 días** (el máximo legal).
  - `origen_datos` ∈ `ocr` / `ocr_corregido` / `manual`, más `ocr_propuesto` (jsonb) con lo que el modelo había propuesto para ESE cheque. **Están para medir el OCR en producción**: cuántos cheques salen sin tocar, cuáles se corrigen y qué campo se corrige. Sin eso, "el OCR anda bien" es una impresión.
  - `estado` ∈ **`en_cartera` / `depositado` / `endosado` / `anulado`** (CHECK `cobranza_cheques_estado_check`, verificado el 21/09/2026). Un cheque pasa a `anulado` **solo cuando se anula la cobranza entera** (`anular_cobranza`), nunca por su cuenta.
  - **SALIDA DE CARTERA** (21/09/2026): `depositado` y `endosado` significan que el papel ya no está en la mano. Sale **solo** con `marcar_salida_cheque` —exige `cobranzas:procesar` y la cobranza `procesada`— y vuelve **solo** con `volver_cheque_a_cartera`, con motivo obligatorio.
    - Columnas `salida_fecha` (date), `salida_destino` (text), `salida_por` (uuid → `empleados`) y `salida_registrada_en` (timestamptz). `chk_salida_coherente` exige fecha, quién y cuándo en los dos estados de salida y **destino en el endoso**, y los cuatro en null en cualquier otro estado. `chk_salida_destino` pide de 1 a 150 caracteres.
    - **EL DESTINO ES OBLIGATORIO EN EL ENDOSO Y OPCIONAL EN EL DEPÓSITO, y la asimetría es a propósito:** cuando se conecte Cuentas Corrientes, **solo el endosado va a descontar la cuenta corriente** —endosarle un cheque a un proveedor es pagarle—, y para eso hay que saber a quién. Un depósito queda en una cuenta propia y no le debe nada a nadie.
    - `salida_destino` **lo tipea una persona: es sink de XSS** y va siempre con `escCob`.
    - `marcar_salida_cheque` rechaza además una fecha de salida **anterior a la fecha de la cobranza**, y la pantalla replica esa regla.
  - **`trg_cobranza_cheque_proteger_salida`** (BEFORE UPDATE OR DELETE, función `_cobranza_cheque_proteger_salida()`): **impide borrar o anular un cheque que ya salió.** Como la edición de una cobranza es un reemplazo total de los cheques, **editar o anular una cobranza con un cheque afuera falla** con un mensaje redactado para la persona (*"El cheque 007 Nº … ya salió de cartera (depositado). Para editar o anular esta cobranza, primero volvelo a cartera."*). **La pantalla lo muestra TAL CUAL, sin taparlo con un genérico**, y tiene mutaciones que lo exigen. Sin el trigger, reeditar una cobranza haría reaparecer "en cartera" un papel que ya se depositó o se entregó.
- `cobranza_historial`: id (identity), cobranza_id, accion, empleado_id, antes (jsonb), despues (jsonb), motivo, created_at.
  - `accion` ∈ `alta` / `edicion` / `procesada` / `reabierta` / `anulada` / **`cheque_salida`** / **`cheque_vuelve_cartera`**, y `chk_historial_motivo` exige motivo en `reabierta`, `anulada` y `cheque_vuelve_cartera` (verificado el 21/09/2026). El snapshot incluye las cuatro columnas de salida.
  - **`antes` y `despues` guardan el SNAPSHOT COMPLETO de la cobranza** (cabecera + fotos + cheques), no el campo que cambió: la edición es un reemplazo total, así que un diff por campo no reconstruiría nada. Lo arma `_cobranza_snapshot()`.
  - **Lo escriben SOLO las RPCs.** La tabla no tiene policy de escritura, así que el historial no se puede fabricar ni borrar desde el cliente.
- **HAY DOS FKs A `empleados` EN EL MÓDULO, UNA POR TABLA: `cobranzas.empleado_id` y `cobranza_cheques.salida_por`** (esta última desde el 21/09/2026; hasta ese día este documento decía "la única FK es `cobranzas.empleado_id`"). Como están en tablas distintas no generan ambigüedad dentro de una misma tabla, pero **el nombre de `salida_por` igual se resuelve contra `v_empleados_publico` y NUNCA con un embed**: el RLS de `empleados` solo deja leer la fila propia. `procesada_por`, `anulada_por`, `cobranza_fotos.subida_por` y `cobranza_historial.empleado_id` van **SIN FK a propósito**: una segunda FK a `empleados` desde la misma tabla rompe los embeds de PostgREST por ambigüedad, el mismo criterio ya escrito para `gastos.anulado_por` y `materia_prima_ingresos.editado_por`. Los nombres se resuelven contra `v_empleados_publico` —la vista lo hace con tres `left join`— y **el frontend no usa ningún embed entre estas tablas.**
- **RLS: las cinco tablas tienen SOLO policy de SELECT** (verificado contra `pg_policies`: cinco policies, las cinco `SELECT`, las cinco a `authenticated`). Cero escritura desde el cliente: todo entra por las RPCs. `cobranzas` se lee con `empleado_id = mi_empleado_id() OR tiene_tarea('cobranzas','ver_todo')`, y las otras tres cuelgan de un `exists` sobre `cobranzas`, así que **heredan ese criterio en un solo lugar** en vez de repetirlo cuatro veces.

**Producción** (esquema completo con RLS, creado el 22/09/2026 por el chat de arquitectura; todo verificado contra `information_schema`, `pg_constraint`, `pg_policy` y `pg_get_functiondef` ese día). **Las 16 tablas tienen SOLO policy de SELECT** (`puede_ver_produccion()` = cualquiera de las tres tareas; `productos_terminados`, `producto_presentaciones`, `marcas_personalizadas` y `stock_terminado_movimientos` también con `stock:ver`): toda escritura entra por las RPCs.
- `maquinas` (unidad_negocio_id, nombre, activa, orden; nombre único por unidad) — cada unidad tiene las suyas (Cucuruchos Nuss: 2).
- `ingredientes` (nombre único, descuenta_stock, orden, activo) — **comunes a todas las unidades**. `ingrediente_insumos` (ingrediente_id, insumo_id): qué insumos del catálogo cumplen cada ingrediente. **Un ingrediente que descuenta stock pero no tiene insumo NO descuenta** (al 22/09/2026: grasa, colorante y fécula).
- `recetas` (maquina_id, tipo_masa texto libre 1–40, **version**, nota, creada_por) + `receta_items` (ingrediente_id, cantidad_kg de UNA masa simple, insumo_preferido_id). **La vigente es la de versión más alta** (`_receta_vigente`); guardar crea una versión nueva. Las cargadas hoy vinieron del prototipo y su nota dice "revisar".
- `turnos_produccion` (**lote** entero único, unidad, maquina_id, fecha, turno ∈ Mañana/Tarde/Noche, encargado_id, estado abierto/cerrado, abierto_en, cerrado_en, hora_inicio, hora_apagado, scrap_kg, observaciones) — **un turno es UNA máquina**: abrir tres máquinas son tres lotes. `turno_operarios` (turno_id, empleado_id).
- `masas` (turno_id, nro por turno, hora, tipo_masa, doble, **origen** ∈ original/anterior/modificada —lo calcula la base—, receta_id, masero_id, anulada + motivo, **client_uuid único**) + `masa_items` (ingrediente_id, insumo_id, lote, **lote_fuera_de_stock** —lo calcula la base—, cantidad_simple_kg, cantidad_kg = simple × 2 si es doble).
- `paradas_produccion` (turno_id, inicio, fin null = en curso, motivo 2–300).
- `puestos_produccion` (empleado_id, unidad_negocio_id, puesto ∈ encargado/masero/operario).
- `productos_terminados` (unidad, nombre, tipo_masa, activo, orden) + `producto_presentaciones` (nombre, con_cono, media_caja, empaque, **unidades_por_caja**, activa, orden) + `marcas_personalizadas` (nombre único en mayúsculas, activa).
- `produccion_items` (turno_id, **orden**, **sublote** único = `lote-orden`, presentacion_id, marca_id, cajas, unidades_por_caja guardadas, unidades) y `stock_terminado_movimientos` (unidad, presentacion_id, marca_id, lote = el sublote, cajas, unidades, tipo ∈ produccion/inventario_inicial/ajuste/despacho, …). **Cambiar las unidades por caja de una presentación no toca lo ya producido.**
- `stock_movimientos.masa_item_id`: cada renglón de masa con insumo descuenta su lote con un movimiento `consumo_sala_masa` (trigger `_masa_item_descontar_stock` sobre `masa_items`); `anular_masa` borra esos movimientos. **El libro de Stock es compartido** con el módulo Stock.
- **Las RPCs** (todas SECURITY DEFINER con `search_path=public`, `authenticated` sí y `anon` no): `abrir_turnos(p_fecha, p_turno, p_encargado_id, p_maquinas [{maquina_id, operario_id}])` → [{maquina_id, maquina, turno_id, lote}] (llama a `abrir_turno` por máquina; exige `produccion:cargar` en la unidad, fecha no futura, máquina sin turno abierto); `datos_para_masa(p_turno_id, p_tipo_masa)` → {turno, original, anterior, insumos con sus lotes con stock}; `registrar_masa(p_turno_id, p_tipo_masa, p_doble, p_masero_id, p_items, p_client_uuid)` (ver la regla del uuid en el módulo); `anular_masa(p_masa_id, p_motivo)` (de un turno abierto con `cargar`, o de cualquiera con `configurar`); `iniciar_parada(p_turno_id, p_motivo)` / `terminar_parada(p_parada_id)`; `cerrar_turno(p_turno_id, p_hora_apagado, p_scrap_kg, p_observaciones, p_productos [{presentacion_id, cajas, marca_id?}])` → {lote, sublotes [{sublote, cajas, unidades}]} (rechaza con una parada en curso); `personal_produccion(p_unidad_negocio_id)` → (id, nombre, misma_unidad, puestos[]) de todo el personal activo; y las de configuración `guardar_maquina`, `guardar_receta_original`, `guardar_ingrediente`, `guardar_ingrediente_insumos`, `guardar_producto`, `guardar_presentacion`, `guardar_marca` y `guardar_puestos` (con `configurar` en la unidad; ingredientes y marcas con `tiene_tarea` sin unidad, porque son comunes).

**Pedidos** (creado por el chat de arquitectura; todo verificado contra `information_schema`, `pg_constraint`, `pg_indexes`, `pg_policy` y `pg_get_functiondef` el 23/09/2026). **Las tres tablas tienen SOLO policy de SELECT** con `tiene_tarea_alcance('pedidos','ver', unidad)` (`pedido_items` por un `exists` sobre `pedidos`): toda escritura entra por las RPCs. Ningún trigger.
- `clientes`: id, unidad_negocio_id (NOT NULL), nombre (CHECK 2 a 120), **apodos text[] NOT NULL default '{}'**, localidad, telefono, observaciones, activo, creado_por (FK a `empleados`), creado_en. Índice único `uq_cliente_nombre` sobre `(unidad_negocio_id, lower(btrim(nombre)))`: **cada unidad tiene sus clientes**.
  - **Los apodos son cómo lo nombran en los mensajes** ("el Turco"), para reconocerlo después: hoy los usa el buscador de la pantalla, y mañana el programa que lea los WhatsApp.
- `pedidos`: id, unidad_negocio_id, cliente_id (FK a `clientes`, la ÚNICA FK entre las dos, así que el embed `clientes(...)` es inequívoco), **numero** (bigint, `pedido_numero_seq`), fecha, fecha_entrega, estado (CHECK `pendiente` / `en_produccion` / `listo` / `entregado` / `anulado`), observaciones, **texto_original** (el mensaje de WhatsApp tal cual, para la precarga futura), anulado_motivo, creado_por, creado_en, actualizado_en.
- `pedido_items`: id, pedido_id (ON DELETE CASCADE), orden (único por pedido), presentacion_id, marca_id, cajas (CHECK `> 0`), cajas_cumplidas (default 0, CHECK `>= 0`), texto_libre, observacion.
  - **UN RENGLÓN ES UNA DE DOS COSAS, NUNCA LAS DOS: un producto (`presentacion_id`) o un texto que no se pudo interpretar (`texto_libre`).** Lo garantiza el CHECK `pedido_items_check`: `(presentacion_id IS NOT NULL) <> (texto_libre IS NOT NULL)`.
- **Las RPCs** (todas SECURITY DEFINER con `search_path=public`, `authenticated` sí y `anon` no):
  - `guardar_cliente(p_id, p_unidad_negocio_id, p_nombre, p_apodos text[], p_localidad, p_telefono, p_observaciones, p_activo)` → uuid. Exige `pedidos:configurar` en la unidad. Limpia y deduplica los apodos. Un nombre repetido en la unidad da "Ya hay un cliente con ese nombre en esta unidad.".
  - `guardar_pedido(p_id, p_unidad_negocio_id, p_cliente_id, p_fecha, p_fecha_entrega, p_observaciones, p_items jsonb, p_texto_original default null)` → `{pedido_id, numero}`. Exige `pedidos:cargar`. **Una sola llamada con todos los renglones: todo o nada.** El cliente tiene que estar ACTIVO en la unidad; la entrega no puede ser anterior al pedido. En `p_items`, un renglón sin `presentacion_id` es de texto libre (mínimo 2 letras, **y sin cajas la base pone 1**); uno con `presentacion_id` exige presentación activa de la unidad, cajas `> 0` y un cono (`marca_id`) no rechazado. Al EDITAR, **si algún renglón ya tiene `cajas_cumplidas > 0` rechaza**: "Este pedido ya tiene renglones cumplidos: corregilos de a uno, no reemplaces la lista." Un pedido entregado o anulado no se edita. **Al editar NO toca `texto_original`.**
  - `marcar_avance_pedido(p_item_id, p_cajas_cumplidas)` → `{estado}`. Exige `pedidos:cargar`. **No se cumplen más cajas que las pedidas.** Y **el estado del pedido lo decide el AVANCE**: suma cumplida 0 = `pendiente`, algo = `en_produccion`, todo = `listo`.
  - `cambiar_estado_pedido(p_pedido_id, p_estado, p_motivo default null)` → void. Exige `pedidos:cargar`. **A mano solo `entregado` o `anulado`**; anular pide motivo de 3 letras o más. Deja anular también un entregado.
  - `pedidos_de(p_unidad_negocio_id, p_desde, p_hasta, p_estado)` (los tres últimos con DEFAULT null) → `(id, numero, fecha, fecha_entrega, cliente, estado, cajas_pedidas, cajas_cumplidas, renglones, sin_interpretar, observaciones)`, ordenado por fecha y número descendentes. **LANGUAGE sql SECURITY DEFINER con `tiene_tarea_alcance('pedidos','ver', …)` adentro.**
- **LOS PRODUCTOS SON DE PRODUCCIÓN, Y ESO DEJA UN HUECO:** los renglones apuntan a `producto_presentaciones` y `marcas_personalizadas`, cuyas policies de SELECT piden `puede_ver_produccion()` o `stock:ver`. **Alguien con solo tareas de Pedidos recibe CERO productos, sin ningún error.** La pantalla lo dice y ofrece "No sé qué es"; **CERRADO el 24/09/2026:** las policies de SELECT de `productos_terminados`, `producto_presentaciones`, `marcas_personalizadas` y `clientes` ya aceptan `pedidos:ver` y `pedidos:cargar` (verificado contra `pg_policy`), así que tampoco queda el caso de `pedidos:cargar` sin `ver` que no leía los clientes.

### Circuito Ingreso ↔ Gastos ↔ Cuentas Corrientes (21/09/2026)

La misma compra de mercadería entra por hasta tres puertas —Ingreso (las cantidades), Gastos (la plata que salió de una caja) y Cuentas Corrientes (la deuda con el proveedor)— y hasta este día cada una la registraba por su lado. El circuito las conecta. **Toda la lógica vive en la base**; las pantallas llaman y cuentan el resultado en palabras.

- **LA REGLA ES DEL PROVEEDOR, `proveedores.cuenta_corriente`:**
  - **Con cuenta corriente:** toda factura va a su cuenta. La carga Ingreso (`registrar_factura_de_ingreso`) o Gastos, la que llegue primero, y la segunda la **encuentra** por proveedor + número normalizado en vez de duplicarla. Un gasto con ese número **PAGA** esa factura.
  - **Ocasional:** **nunca pasa por la cuenta corriente.** La compra vive en Gastos, y la mercadería de Ingreso se vincula a ese gasto (`materia_prima_ingresos.gasto_id`).
- **EL GASTO DE UN OCASIONAL SE VINCULA SOLO: trigger `trg_gasto_autovincular_ingreso`** (21/09/2026, chat de arquitectura; verificado contra `pg_trigger` y `pg_get_functiondef` ese día). AFTER INSERT OR UPDATE OF `numero_doc`, `proveedor_id`, `estado` sobre `gastos`, función `_gasto_autovincular_ingreso()` (`SECURITY DEFINER`, `search_path = public`, sin EXECUTE para `anon` ni `authenticated`: solo corre como trigger).
  - **Qué hace:** si el gasto tiene proveedor **ocasional** y un número que normaliza a algo, busca un ingreso **`factura_a` / `factura_x`** del mismo proveedor, con el mismo `normalizar_numero_doc()`, **sin `gasto_id` ni `factura_pendiente_id`** y con `fecha >= fecha_inicio_circuito_stock()`, y le pone `gasto_id`. Si hay más de uno, el de fecha más vieja. **Se cargue el gasto por donde se cargue**: desde "Cargar gasto", desde cero en el wizard o a mano.
  - **POR QUÉ EXISTE: cierra el riesgo de STOCK DOBLE** que dejó abierto el traspaso del circuito. Se ingresaba la factura de un ocasional, después alguien cargaba el gasto desde cero, nada los vinculaba, el gasto aparecía en "Pagado sin ingresar" y el depósito podía ingresar la misma mercadería otra vez.
  - **Con cuenta corriente NO actúa**: ahí el vínculo pasa por la factura en CC. Tampoco si el gasto ya tiene un ingreso, está anulado, no tiene proveedor o el número no tiene dígitos.
  - **Anular el gasto desvincula**: el ingreso vuelve a quedar sin gasto (y reaparece en "Facturas ingresadas sin gasto").
  - **No mira descargas `sin_comprobante`**: sin número no hay con qué matchear.
  - **Límites, del cuerpo tal cual está:** si un gasto ya vinculado cambia de número, **el vínculo viejo queda** (la función sale temprano cuando el gasto ya tiene ingreso); y si hubiera dos ingresos del mismo proveedor con el mismo número, se lleva el más viejo aunque la persona estuviera cargando el gasto del otro desde "Cargar gasto" — en ese caso `vincular_ingreso_a_gasto` va a rechazar con "Ese gasto ya tiene su ingreso." y Gastos lo muestra tal cual.
- **INGRESO NUNCA CREA GASTOS.** Un gasto mueve una caja, y el depósito no sabe quién pagó ni de qué caja. Lo que Ingreso no puede resolver queda en listas de pendientes para quien sí puede.
- **`fecha_inicio_circuito_stock()` → `2026-10-01`** (IMMUTABLE). **Todo lo "pendiente" cuenta desde ahí**: `gastos_sin_ingreso`, `ingresos_sin_gasto` y `remitos_sin_facturar` filtran por `fecha >= fecha_inicio_circuito_stock()`, y el detalle de un ingreso solo ofrece "Pasar a cuenta corriente" desde esa fecha. **Registrar la factura al guardar un ingreso NO está atado a la fecha.** Vive en la base y las pantallas la leen con `rpc()`: no hardcodearla. **Consecuencia para probar antes del 01/10: los datos de prueba tienen que llevar fecha de octubre**, o no aparecen en ninguna lista.
- **`normalizar_numero_doc()` CAMBIÓ:** compara **punto de venta y número sin ceros a la izquierda**. Con dos o más grupos de dígitos toma el primero y el último (`0001-00012345` → `1-12345`); un solo grupo de 9 o más dígitos se parte en los 8 de la derecha como número (`000100012345` → `1-12345`); uno más corto es solo número (`12345` → `-12345`); sin dígitos, **NULL**. Es la clave del índice único de `facturas_pendientes` y de todos los "¿ya está?" del circuito, y la usa también `buscar_comprobante_cargado`.
- **Descarga sin comprobante** de un proveedor con cuenta corriente → una factura `sin_importe` en su cuenta (ver la tabla). Un administrador le pone precio desde Cuentas Corrientes con `completar_importe_factura`. De un ocasional → queda esperando el gasto, como cualquier factura suya.
- **EL ESPEJO EN CUENTA CORRIENTE YA NO ES PARA TODO GASTO CON PROVEEDOR.** Gastos sigue llamando a `registrar_pago_directo_proveedor` para todo gasto con `proveedor_id`, y **la base decide**: con un ocasional **no hace nada**; si la factura ya está en la cuenta, el gasto **la paga** (con errores nuevos, escritos para una persona: la factura está sin importe, ya figura pagada, la moneda no coincide, el gasto supera lo que se debe); si no está, crea el espejo `pagada` como antes. `sincronizar_pago_directo_proveedor` también cambió. Ver la entrada de esas dos RPCs.

**Las RPCs del circuito** (todas `SECURITY DEFINER` con `search_path = public`; `authenticated` sí, `anon` no — verificado el 21/09/2026):
- `registrar_factura_de_ingreso(p_ingreso_id, p_importe, p_moneda default 'ARS')` → jsonb con `accion` ∈ `creada` / `creada_sin_importe` / `vinculado_cc` (con `importe_distinto` e `importe_cc`) / `vinculado_gasto` / `falta_gasto` / `ya_vinculado` / `remito`. **Idempotente**: si el ingreso ya tiene destino, devuelve `ya_vinculado` sin tocar nada. Exige `materia_prima:cargar` o `editar_anular` con alcance en la unidad del ingreso. Con `vinculado_cc` e `importe_distinto`, `importe_cc` puede venir **null** (la factura de la cuenta está sin importe): no es un "$0".
- `vincular_ingreso_a_gasto(p_ingreso_id, p_gasto_id)` — lo usan los dos lados ("Ingresar" desde un gasto pagado, "Cargar gasto" desde una factura ingresada). Rechaza si el ingreso ya tiene destino o el gasto ya tiene su ingreso. **Idempotente desde el 21/09/2026: si el ingreso ya está vinculado a ESE MISMO gasto, vuelve sin error** (después de chequear permisos). Hace falta porque el trigger de `gastos` puede vincular antes de que la pantalla llame: sin eso, "Cargar gasto" terminaría con un error falso sobre un vínculo que salió bien.
- `descartar_ingreso_de_gasto(p_gasto_id, p_motivo)` — "este gasto no lleva ingreso". Exige `materia_prima:editar_anular` o `gastos:editar_anular`.
- `completar_importe_factura(p_factura_id, p_importe)` — de `sin_importe` a `pendiente`. Exige `cuentas_corrientes:registrar_pago`.
- `gastos_sin_ingreso()` — "Pagado sin ingresar": gastos de "Insumos - Materia Prima" desde la fecha de inicio, sin ingreso, sin descarte, y que **no pagaron una factura de CC ya vinculada a un ingreso**. Visible con `materia_prima:cargar` / `editar_anular` o `gastos:ver_exportar`.
- `ingresos_sin_gasto()` — "Facturas ingresadas sin gasto": facturas y descargas de proveedores **ocasionales** sin destino. Trae `importe_ocr` (texto, de `ocr_crudo`). Visible con `gastos:ver_exportar` o `materia_prima:editar_anular`.
- `remitos_sin_facturar(p_proveedor_id)` — remitos sin factura vinculada. Con `cuentas_corrientes:ver_todo` o `registrar_pago`.

**Dónde se ve, por módulo** (el detalle, en cada módulo):
- **Ingreso**: al guardar una factura o una descarga se llama a `registrar_factura_de_ingreso` —**DESPUÉS** de guardar el ingreso— y el resultado se dice en el detalle. Si la RPC falla, el ingreso queda guardado sin vincular y el detalle ofrece reintentar. Lista "Pagado sin ingresar" y deep link `?desde_gasto=<id>`.
- **Gastos**: aviso de después de guardar (el error del espejo en CC, entero; "Ingresar la mercadería ahora" en un gasto de materia prima) y lista "Facturas ingresadas sin gasto".
- **Cuentas Corrientes**: la descarga sin importe en rojo con "Cargar importe", "+ N descargas sin importe" al lado de todo saldo, y "Remitos sin facturar" en la ficha.

### Vistas
- `v_cobranzas` (`security_invoker=true`) — **25 columnas**: las 16 originales de `cobranzas` más `total_cheques`, `cantidad_cheques`, `total`, `cargada_por_nombre`, `procesada_por_nombre`, `anulada_por_nombre`, `editada` y, al final (22/09/2026), `unidad_negocio_id` y `unidad_negocio_nombre`.
  - **`total` = `efectivo + total_cheques`, y se CALCULA en la vista porque la TABLA no lo guarda.** Un total guardado sería un campo que alguien tiene que mantener sincronizado con sus cheques, y la edición los borra y reinserta: la primera desincronización no daría ningún error, solo un número mal en una pantalla de plata.
  - **`total_cheques` suma TODOS los cheques de la cobranza, incluidos los `anulado`,** y es deliberado: los cheques solo se anulan cuando se anula la cobranza entera, y ahí se quiere seguir viendo de cuánto era. Una cobranza anulada con el total en cero no dejaría rastro de lo que se había cargado.
  - `editada` es un `exists` sobre `cobranza_historial` con `accion='edicion'`: la pantalla marca las cobranzas que se tocaron después del alta.
- `v_caja_saldos` (empleado_id, moneda, saldo), `v_caja_saldos_cuenta` (cuenta_id, saldo), `v_caja_saldos_medio` (empleado_id, moneda, medio_pago, saldo)
- `v_saldo_proveedor` (proveedor_id, unidad_negocio_id, moneda, deuda_pendiente, credito_disponible) — hace `FULL OUTER JOIN` entre facturas pendientes y créditos disponibles, para que un proveedor con solo crédito (sin deuda) también aparezca.
- `v_cuenta_corriente_movimientos` — 13 columnas, en este orden: proveedor_id, unidad_negocio_id, moneda, fecha, tipo, monto, factura_pendiente_id, gasto_id, credito_id, referencia, saldo_acumulado, monto_informativo, orden_desempate. `saldo_acumulado` es un `SUM() OVER (PARTITION BY ... ORDER BY fecha)`, saldo corrido cronológico, no depende de qué filtro de fecha esté aplicado en pantalla.
  - `tipo`: `'factura'` / `'pago'` / `'credito_aplicado'` / `'interes'`. `'credito_generado'` existió un tiempo y **se eliminó**: un pago ahora es UNA sola fila con el monto total real del gasto, no fragmentado por factura ni con el sobrante aparte.
  - `orden_desempate`: para ordenar los movimientos del mismo día. `fecha` es `date` (sin hora), así que sin esta columna el orden entre movimientos de la misma fecha queda arbitrario.
  - Excluye los gastos con `estado = 'anulado'`.
- `v_stock_insumos` y `v_stock_insumos_presentacion` — ver detalle en la sección **Materia Prima / Insumos** de arriba (ambas con `security_invoker=true`, excluyen ingresos con `remito_vinculado_id` no nulo).

### RPCs (todas SECURITY DEFINER; desde la migración de permisos de julio 2026 re-verifican permisos server-side con `tiene_tarea()`/`tiene_tarea_alcance()` — NUNCA gatear por rol_app, salvo el bypass de super_admin que ya viene dentro de esos helpers. El conteo "31" era de la auditoría del 18/07/2026: después se agregaron al menos `tiene_tarea` y `tiene_tarea_alcance` y cambiaron firmas — re-verificar contra information_schema en la próxima auditoría. De esa auditoría vieja: se eliminó `rechazar_solicitud_acceso`, versión vieja que no borraba la cuenta de Auth huérfana al rechazar, reemplazada por la Edge Function `rechazar-solicitud-acceso`)

**MFA**: `listar_factores_mfa(p_auth_user_id uuid)` → devuelve `id`, `friendly_name`, `status` de `auth.mfa_factors` para ese usuario. Existe porque NO hay forma de leer los factores de otra persona desde el SDK:
- `admin.mfa.listFactors()` está documentada de forma contradictoria (el ejemplo JS no lleva argumentos pero la descripción dice "for a user") y su parámetro no está documentado en ningún lado. No adivinarlo.
- El objeto `user` documentado NO tiene campo `factors`, así que `admin.getUserById()` tampoco sirve.
- Un `.from('mfa_factors')` con service_role NO funciona: verificado contra `pg_class.relacl`, la ACL de `auth.mfa_factors` es solo `supabase_auth_admin`, `postgres` y `dashboard_user` — `service_role` tiene CERO privilegios. Y PostgREST no expone el esquema `auth`.
- OJO con la trampa: `service_role` tiene `rolbypassrls = true`, pero BYPASSRLS saltea las POLICIES, no otorga PRIVILEGIOS. Sin GRANT no puede leer igual.
Es `SECURITY DEFINER` con `search_path = ''` y su EXECUTE está revocado de `public`/`anon`/`authenticated` y otorgado SOLO a `service_role`: la llama únicamente la Edge Function, nunca el navegador.

**Permisos**: `tiene_tarea(modulo, tarea)` y `tiene_tarea_alcance(modulo, tarea, unidad_negocio_id)` — los helpers estándar que usan todas las RPCs y policies RLS, con bypass de super_admin incorporado. `tiene_tarea_explicita(modulo, tarea)` — misma idea pero SIN bypass, para operaciones que no deben heredarse por rol (hoy las dos de ingreso externo de Caja). Ver sección Sistema de permisos.

**Accesos / Registro**: `aprobar_solicitud_acceso(p_solicitud_id uuid, p_modulos text[], p_tareas jsonb, p_unidad_negocio_id uuid)` y `actualizar_permisos_empleado(p_empleado_id uuid, p_modulos text[], p_tareas jsonb)` — firmas nuevas SIN parámetro de rol; `p_tareas` con semántica de REEMPLAZO TOTAL (ver sección Sistema de permisos) —, `obtener_mi_solicitud_acceso`, `buscar_empleado_por_cuil` (**solo `service_role`** desde el 16/09/2026: la llama la Edge Function `buscar-empleado-cuil`, nunca el navegador — ver Login y roles y Seguridad) (el rechazo de una solicitud NO es una RPC — es la Edge Function `rechazar-solicitud-acceso`, ver sección Login y roles)

**Empleados**: `completar_datos_empleado`, `importar_empleados_naaloo` (regla: `unidad_negocio_id` se asigna solo en el alta/INSERT, nunca se pisa en una reimportación — para no revertir correcciones manuales, ej. Taller viene agrupado con Cucuruchos Nuss en el Excel de Naaloo), `actualizar_contacto_emergencia`

**Gastos / Facturas pendientes**: `anular_factura_pendiente` (bloquea si `estado` ya es `'parcial'`/`'pagada'`), `editar_factura_pendiente` (bloquea si `estado` no es `'pendiente'`), `fn_inicializar_saldo_pendiente` (trigger, no se llama directo), `fn_fecha_a_periodo` (helper de formato, ej "jul-26")

**`pagar_factura_pendiente` YA NO EXISTE** (verificado contra `pg_proc` el 31/08/2026: cero filas). Era el pago atómico de una factura completa, del diseño original; la reemplazó `registrar_pago_proveedor`, que reparte un pago entre varias facturas. Se dropeó en algún momento y **este documento la siguió listando como viva** — el caso inverso al habitual: no es el doc atrasado respecto de algo nuevo, es el doc describiendo algo que ya no está.
- **Nadie la invoca**: verificado con grep sobre todo el repo, no hay ningún `.rpc('pagar_factura_pendiente')`. No hay botón roto en producción.
- Lo que sí quedó son **dos comentarios de `modulos/gastos.html` que la nombran** ("ver `pagar_factura_pendiente()`", ~líneas 3743 y 4803) como si se pudiera ir a leerla. Son punteros a una función inexistente — la misma familia que los comentarios mentirosos, en su forma de puntero. **Territorio del chat de Gastos**, anotado acá para que lo levante.

**Detector de comprobantes duplicados** (verificado contra `pg_proc` el 07/09/2026):
- `buscar_comprobante_cargado(p_proveedor_id uuid, p_numero_doc text, p_razon_social text default null, p_excluir_modulo text default null, p_excluir_id uuid default null)` → `TABLE(modulo, registro_id, numero_doc, razon_social, fecha, unidad_negocio_id, importe)`. Busca el mismo papel en las TRES tablas —`materia_prima_ingresos`, `gastos`, `facturas_pendientes`— por **proveedor O razón social**, más el número normalizado. Excluye `gastos.estado='anulado'` y `facturas_pendientes.estado='anulada'`.
  - **`SECURITY DEFINER` a propósito y no por descuido:** tiene que encontrar el duplicado **aunque quien pregunta no tenga permiso de ver ese registro**. Si respetara el RLS, alguien de Dolce Pasta no vería que la factura ya está cargada en Cucuruchos y la duplicaría igual. Devuelve solo lo mínimo para reconocerlo, no el detalle.
  - `EXECUTE` a `authenticated` y `service_role`, **no a `anon`**.
  - La rama de `materia_prima` devuelve **`importe` en NULL hardcodeado** (ese módulo no captura precios). Es el caso NORMAL y no el borde: cualquier cliente que formatee sin rechazar el null antes de convertir muestra **"$0"**, porque `Number(null)` es 0.
  - **ES DE GASTOS/INGRESO, NO DE CUENTAS CORRIENTES.** Estuvo asignada a ese chat por un traspaso hasta que se verificó: **no la consume** —no tiene ninguna pantalla donde se tipee un número de comprobante, las facturas entran siempre por el wizard de Gastos—.
  - **`p_excluir_modulo` / `p_excluir_id` — RESUELTO el 17/09/2026** (lo desplegó el chat de Ingreso; ver el registro de escrituras). Excluyen el registro que se está EDITANDO, que si no se detectaría a sí mismo. `p_excluir_modulo` usa los mismos valores que la columna `modulo` que devuelve la función (`'materia_prima'`, `'gastos'`, `'cuentas_corrientes'`). Verificado el 17/09/2026: **una sola firma**, sin sobrecargas.
    - **Los dos van con `DEFAULT null`**, así que las llamadas de tres parámetros de los wizards siguen andando sin tocarlas.
    - **La exclusión está escrita como `(p_excluir_modulo is distinct from '<modulo>' or <id> is distinct from p_excluir_id)`** en cada rama, y NO como `not (p_excluir_modulo = '<modulo>' and <id> = p_excluir_id)`: esa segunda forma da NULL con los parámetros en null y **filtra TODO**. Ver el aprendizaje del NULL en el WHERE.
    - Consumidores con exclusión: los dos formularios de edición de `gastos.html` (`ce135b0`).
- `normalizar_razon_social(p_texto)` — IMMUTABLE. Minúsculas, sin acentos y **sin nada que no sea alfanumérico**: `'ALOVERO GARCÍA - LO MAS SRL'` y `'ALOVERO GARCÍA LO MAS SRL'` dan las dos `aloverogarcialomassrl`, y `'BADIC S.A.S'` = `'BADIC S.A.S.'` = `badicsas`.
  - **Devuelve NULL —no cadena vacía— cuando no queda nada**: con `''`, con `null` y también con un texto de puros símbolos como `'!!! ---'`. **Es lo que impide que matchee contra cualquier registro sin razón social.** Si devolviera `''`, la comparación `= ''` haría match masivo.
  - **ES UNA FUNCIÓN NUEVA Y NO UN CAMBIO A `normalizar_texto()`, y esa es la decisión de fondo:** `normalizar_texto` es la del **índice único de `proveedor_insumo_alias`** de Materia Prima. Tocarla habría cambiado la semántica de ese índice y **los alias habrían dejado de acertar sin que nadie se enterara**. Verificado después del cambio: sigue devolviendo `alovero garcia - lo mas srl`, con el guion.
  - **LA MEDICIÓN QUE LO JUSTIFICA, y está acá para que nadie la afloje de vuelta por miedo a las colisiones:** antes de cambiarla se contaron los pares que HOY NO matchean y **pasarían a matchear**. Dieron **5 sobre las CUATRO tablas juntas** (`gastos`, `facturas_pendientes`, `materia_prima_ingresos` y `proveedores`), y **los 5 son la misma empresa** — diferencias de un punto, una coma o un guion. **CERO falsos positivos.**
    - **OJO AL REPRODUCIRLA:** acotada solo a `gastos` da **1**, no 5. No es una contradicción, son universos distintos — pero quien corra la consulta chica y espere 5 va a creer que algo cambió.
  - **La firma NO cambió**, así que los tres consumidores detectan mejor **sin tocar una línea de su código**.

**Cuentas Corrientes**: `crear_proveedor_pendiente`, `aprobar_proveedor`, `rechazar_proveedor`, `asignar_proveedor_factura_pendiente` (para facturas legado sin proveedor), `sugerir_facturas_fifo` (devuelve `factura_pendiente_id, fecha_factura, numero_comprobante, saldo_pendiente, monto_a_aplicar` — nombres exactos, no adivinar variantes), `registrar_pago_proveedor` (recibe `p_aplicaciones jsonb` como array de `{factura_pendiente_id, monto_aplicado}` — la clave es literalmente `monto_aplicado`, no `monto`; si la suma aplicada es menor al monto pagado, el resto se registra como crédito automáticamente; la categoría del gasto resultante es la categoría real si todas las facturas del pago comparten la misma, o la categoría genérica "Pago a Cta. Cte. Proveedor" si son mixtas), `aplicar_credito_a_factura` (siempre manual, nunca automático — decisión de diseño explícita)

Cuentas Corrientes, agregadas después (verificadas contra information_schema el 10/08/2026):
- `crear_proveedor_activo(p_razon_social, p_cuit, p_nombre_fantasia, p_direccion)` — alta manual desde Cuentas Corrientes; el proveedor queda activo directo, sin pasar por aprobación. **Razón social y CUIT OBLIGATORIOS.**
- `importar_proveedores_excel(p_filas jsonb)` — importación masiva. Acá el **CUIT es OPCIONAL** — es una diferencia real con el alta manual, no un descuido. Cada fila corre en su propio bloque de excepción, así que una fila mala no voltea el resto. Devuelve `{creados, errores: [{fila, razon_social, motivo}]}`. **Ese `fila` es el índice 1-based dentro de `p_filas`, NO el número de fila del Excel**: el frontend lo ignora a propósito y ubica la fila real por `razon_social`, porque en cuanto hay filas ignoradas al parsear el offset se corre y el mapeo empieza a apuntar al renglón equivocado en silencio.
- `agregar_interes_factura(p_factura_id, p_monto, p_observaciones, p_base_calculo, p_tasa_pct, p_tasa_periodo, p_dias_transcurridos, p_fecha)` — suma el interés a `importe` Y a `saldo_pendiente` de la factura. Bloquea si la factura no está en `'pendiente'`/`'parcial'`.
- `tiene_cuenta_corriente_activa(p_proveedor_id)` → boolean. Abierta a cualquier usuario autenticado: no expone montos, solo sí/no.
- `editar_proveedor(p_proveedor_id, p_razon_social, p_cuit, p_nombre_fantasia, p_direccion)` — edita un proveedor existente, desde el padrón completo. Razón social obligatoria; el **CUIT es OPCIONAL a propósito**, porque hay proveedores importados por Excel sin CUIT que tienen que poder editarse igual. Valida que el CUIT no quede duplicado contra OTRO proveedor. Es la **única RPC de edición** de proveedores: todas las demás son de alta (`crear_proveedor_activo`, `crear_proveedor_pendiente`, `importar_proveedores_excel`) o de aprobación (`aprobar_proveedor`, `rechazar_proveedor`).

Cruce Gastos ↔ Cuentas Corrientes (diseñadas en el chat de Cuentas Corrientes):
- `registrar_pago_directo_proveedor(...)` — para el gasto que se paga en el momento pero igual tiene que quedar asentado en la cuenta corriente. Crea una `facturas_pendientes` ya en `'pagada'` más su fila de `aplicaciones_pago`. **NO toca Caja** (de eso ya se encarga el trigger del gasto). Abierta a cualquier usuario. Valida que el gasto sea propio O que quien llama tenga la tarea `gastos:ver_exportar` — que es la misma que ya habilita cargar un gasto a nombre de otra persona en el wizard: si puede cargar el gasto para otro, puede reflejarlo en cuenta corriente, es la consecuencia directa y no una atribución nueva. La factura espejo se inserta con el `empleado_id` DEL GASTO, no con el de quien llama: la factura espejo no es un registro propio, es el reflejo de un gasto que ya existe. Además, la política RLS de lectura de `facturas_pendientes` es "propia O `cuentas_corrientes:ver_todo`", así que atribuirla a quien la carga dejaría la factura invisible para la persona dueña del gasto.
  - **No hay checkbox: el frontend la llama para TODO gasto con `proveedor_id`**, y el checkbox "Registrar también en la cuenta corriente" se eliminó del wizard de Gastos (un historial incompleto es peor que no tenerlo). **Pero desde el 21/09/2026 NO todo gasto con proveedor termina en la cuenta corriente: decide la base.** Con un proveedor **ocasional** (`cuenta_corriente = false`) la función **vuelve sin hacer nada**; si la factura con ese número **ya está** en la cuenta (la cargó Ingreso o alguien a mano), el gasto **la paga** —fila en `aplicaciones_pago` y baja de `saldo_pendiente`— en vez de crear otra; y si no está, crea el espejo `pagada` como siempre. Los errores nuevos (factura sin importe, ya pagada, otra moneda, gasto mayor a lo que se debe) están escritos para una persona. Ver el circuito, en la sección de tablas.
  - **`sincronizar_pago_directo_proveedor` también cambió:** si el gasto pagó **varias** facturas rechaza (se corrige desde Cuentas Corrientes); si pagó una factura que **no nació** con él, recalcula el saldo de esa factura en vez de pisarla.
- `sincronizar_pago_directo_proveedor(...)` — mantiene ese espejo al día cuando se edita el gasto. Si el gasto nunca se registró en cuenta corriente no hace nada — eso NO es un error, es el caso normal.
- `anular_gasto(p_gasto_id, p_motivo)` — anula un gasto y devuelve la plata a Caja con un movimiento `'ingreso_reversion_gasto'`, solo si el medio de pago fue efectivo o transferencia (con cheque no toca Caja, coherente con que tampoco la descuenta al cargarlo). Revierte el `saldo_pendiente` y el `estado` de cada factura que ese pago había saldado, y marca `'anulado'` cualquier crédito que ese pago hubiera generado. **BLOQUEA la anulación si ese crédito ya fue consumido por otra factura** — deshacerlo implicaría desarmar en cascada movimientos posteriores. Permisos: el dueño del gasto O `tiene_tarea('gastos','editar_anular')`.
  - **La devolución se atribuye al DUEÑO REAL de la cuenta** (`cuentas_caja.empleado_id` de `v_gasto.cuenta_id`), NO a `v_gasto.empleado_id` — mismo criterio que `fn_sincronizar_caja_gasto`. Antes usaba el empleado del gasto y ese era el bug: anular un gasto pagado con una cuenta de Empresa dejaba la plata descontada de Empresa y acreditada a la persona equivocada. `creado_por` guarda quién anuló.
  - Condición exacta para devolver: `medio_pago in ('efectivo','transferencia')` **Y `cuenta_id is not null`**. OJO con la asimetría contra el trigger, que cuando `cuenta_id` es null cae a la cuenta favorita y SÍ genera el egreso — ver el pendiente al final de esta sección.

**Nota de permisos (actualizada — verificado contra `pg_get_functiondef` y `pg_policy` el 12/08/2026):** el pendiente de unificar estas validaciones con tareas granulares **ya está hecho**. Ninguna de `anular_gasto`, `registrar_pago_directo_proveedor`, `agregar_interes_factura` ni el RLS de `intereses_factura` chequea `rol_app`: todas usan `tiene_tarea()`. La policy de `intereses_factura` es `tiene_tarea('cuentas_corrientes','ver_todo') OR tiene_tarea('facturas_pendientes','editar_interes')`. `sincronizar_pago_directo_proveedor` no valida permisos por su cuenta (solo actualiza un espejo que ya existe). **No queda ningún `rol_app IN ('admin', ...)` en estas RPCs** — ese patrón está muerto en todo el proyecto.

**Caja**: `crear_cuenta_caja`, `desactivar_cuenta_caja`, `renombrar_cuenta_caja`, `marcar_cuenta_favorita_caja`, `registrar_ingreso_propio_caja`, `registrar_ingreso_externo_caja` (exige con `tiene_tarea_explicita` —SIN bypass de super_admin— la tarea que corresponda según el DUEÑO de la cuenta elegida: `caja:ingreso_externo_propio` si es de quien llama, `caja:ingreso_externo_empresa` si es de la Cuenta de Empresa. Ya NO usa `caja_raiz` ni la tarea vieja `ingreso_externo`. Params: `p_monto`, `p_cuenta_id`, `p_descripcion` + `p_fecha` opcional), `registrar_retiro_caja` (mismos params que la anterior), `registrar_traspaso_cuenta_caja` (params `p_cuenta_origen_id`, `p_cuenta_destino_id`, `p_monto_origen`, `p_monto_destino` + `p_fecha` opcional, MÁS `p_empleado_id` opcional para traspasar entre cuentas de OTRA caja —hoy solo Empresa, con `caja:traspaso_empresa`—; si se omite opera sobre quien llama), `crear_solicitud_movimiento_caja` (**DEVUELVE TEXT** con el estado resultante: `'aceptada'` si el movimiento se aplicó atómicamente, `'pendiente'` si quedó como solicitud a confirmar. Quien decide es el servidor —se auto-acepta cuando la crea un super_admin y hay Empresa de por medio—; el frontend solo informa y refresca según ese valor, nunca lo asume. Antes devolvía el uuid, que nadie usaba. Acepta además `p_empleado_propio_id` opcional, para que "mi lado" del movimiento sea Empresa y no quien llama. **La regla de "al menos un super_admin o la Empresa entre las dos partes" lleva desde `3cd5ef7` un `and not tiene_tarea_explicita('caja','transferir_entre_personas')` al final**: quien tenga esa tarea saltea la regla. Se exige SOLO a quien LLAMA — la contraparte no necesita nada para aceptar), `responder_solicitud_movimiento_caja`, `cancelar_solicitud_movimiento_caja`, `fn_sincronizar_caja_gasto` (trigger — descuenta Caja automáticamente al insertar un `gasto`; con `medio_pago='cheque'` y `cuenta_id=null` no descuenta nada, es comportamiento esperado, no hay circuito de cheques todavía)

**`fn_sincronizar_caja_gasto` en detalle** (verificado contra `pg_get_functiondef` el 12/08/2026) — es el trigger que sostiene toda la Cuenta de Empresa, así que conviene tenerlo escrito:
- **El movimiento espejo se atribuye al DUEÑO REAL de la cuenta elegida**, no a quien cargó el gasto: `select empleado_id into v_dueno_cuenta from cuentas_caja where id = v_cuenta_id` y después `empleado_id = coalesce(v_dueno_cuenta, new.empleado_id)`. Es lo que hace que un gasto contra una cuenta de Empresa descuente de **Empresa** y no de la caja personal de quien lo carga. **Antes usaba `new.empleado_id` y ese era el bug.** `creado_por` sí guarda quién lo cargó (`new.empleado_id`), para auditoría — o sea que las dos cosas quedan registradas, en columnas distintas.
  - CONSECUENCIA PARA EL FRONTEND: `gastos.empleado_id` define a quién se ATRIBUYE el gasto, y NO de qué caja sale la plata. No asumir que son lo mismo (ver la nota del selector de cuenta en el módulo Cuentas Corrientes).
- **Guarda de `estado='anulado'`**: si el gasto YA estaba anulado y se edita (`old.estado = 'anulado' and new.estado = 'anulado'`), el trigger corta al principio y no toca nada. Sin esa guarda, editar un gasto ya anulado borraba la devolución que insertó `anular_gasto` y volvía a descontar la plata.
  - **La transición a anulado SÍ pasa a propósito** (`old <> 'anulado'`, `new = 'anulado'`): ahí el trigger tiene que regenerar el egreso, porque `anular_gasto` inserta después la devolución que lo compensa. La sutileza es que la guarda distingue "ya estaba anulado" de "se está anulando ahora" — no es un simple `if new.estado = 'anulado' then return`.
- Cuando `cuenta_id` viene null y el medio es efectivo/transferencia, resuelve la cuenta **favorita** del empleado del gasto y genera el egreso igual.

**Nota de diseño — asimetría entre el trigger y `anular_gasto` con `cuenta_id` null:** el trigger, cuando `cuenta_id` es null, cae a la cuenta favorita y **sí** descuenta; pero `anular_gasto` exige `cuenta_id is not null` para insertar la devolución. Un gasto guardado sin `cuenta_id` explícito quedaría descontado y sin devolver al anularlo.

**Hoy NO afecta a nadie**: verificado contra la base el 12/08/2026, no hay ningún gasto de efectivo/transferencia sin anular con `cuenta_id` en null. Es un chequeo de datos de ese día, no una garantía estructural — no hay constraint que lo impida.

**Si alguna vez se permite guardar un gasto de efectivo/transferencia sin `cuenta_id`, hay que arreglar `anular_gasto` primero**: que resuelva la cuenta con el mismo fallback a favorita que ya usa el trigger, en vez de saltear la devolución. La regla de fondo es que las dos funciones tienen que resolver la cuenta IGUAL — si una usa fallback y la otra no, la plata sale por un camino y no vuelve por el otro.

**Cobranzas** (las **diez** originales creadas el 16/09/2026; verificado contra `pg_proc` ese día: **ninguna de esas diez tiene parámetros con DEFAULT** —`pronargdefaults = 0`—, **todas llevan `SET search_path = public`** y **a ninguna la puede ejecutar `anon`**. La **excepción es `resumen_cobranzas`**, la undécima, que SÍ tiene los cinco parámetros con DEFAULT null — ver abajo.)
- `guardar_cobranza(p_id, p_cliente, p_fecha, p_efectivo, p_comprobante_referencia, p_observaciones, p_fotos jsonb, p_cheques jsonb)` → jsonb `{id, ya_existia}`. Exige `cobranzas:cargar`.
  - **ES IDEMPOTENTE, y de eso depende que el módulo funcione sin señal:** el `p_id` lo genera el celular, así que si la respuesta se pierde y el sincronizador reintenta, la segunda llamada encuentra la cobranza ya cargada y devuelve `ya_existia: true` **en vez de duplicarla**. Si ese id existe pero es de otra persona, corta con error: un uuid ajeno no se pisa.
- `editar_cobranza(…mismos 8 parámetros…)` → jsonb `{id, cambio}`. **Solo sobre una cobranza `registrada`** (una procesada se reabre primero). Puede el dueño con `cargar`, o cualquiera con `editar_anular`.
  - **Es un REEMPLAZO TOTAL de los cheques**: los borra y los reinserta. Por eso el historial guarda el snapshot completo y no un diff.
  - **Solo escribe en el historial si el snapshot cambió** (`v_despues is distinct from v_antes`): abrir una cobranza, no tocar nada y guardar no la marca como editada.
- `marcar_cobranza_procesada(p_id)` → void. Exige `cobranzas:procesar`, solo desde `registrada`.
- `reabrir_cobranza(p_id, p_motivo)` → void. Exige `cobranzas:procesar` y **motivo obligatorio**, solo desde `procesada`. Limpia `procesada_por`/`procesada_en`.
- `anular_cobranza(p_id, p_motivo)` → void. Motivo obligatorio. Puede el dueño si está `registrada` y tiene `cargar`, o cualquiera con `editar_anular` **en cualquier estado** (también una ya procesada).
  - **Anula TAMBIÉN sus cheques** (`estado = 'anulado'`), y eso es lo que **libera el índice único parcial**: el mismo cheque se puede volver a cargar en la cobranza correcta. Es la única vía por la que un cheque cambia de estado.
- `buscar_cheque_cargado(p_banco_codigo, p_numero, p_sucursal_codigo, p_cuenta, p_excluir_cobranza_id)` → `TABLE(cobranza_id, fecha, cargada_por, importe, coincide_completo)`. Exige `cargar` **o** `ver_todo`.
  - **`SECURITY DEFINER` a propósito y no por descuido**, igual que `buscar_comprobante_cargado`: tiene que encontrar el cheque **aunque lo haya cargado otra persona**, que es justo lo que el RLS esconde. Devuelve lo mínimo para reconocerlo.
  - **Busca por banco + número —el criterio AMPLIO— y `coincide_completo` dice si además coinciden sucursal y cuenta.** Esa columna es la que permite distinguir el duplicado real del choque esperable de numeración entre cuentas de un mismo banco, y por eso la decisión de bloquear o solo avisar la toma la pantalla y no la consulta.
  - **NACE CON `p_excluir_cobranza_id`**: sin él, editar una cobranza la haría detectarse a sí misma y el aviso aparecería siempre — que es exactamente lo que dejó al detector de Gastos desconectado de sus formularios de edición hasta que `buscar_comprobante_cargado` sumó su propia exclusión (17/09/2026). Acá el problema se evitó antes de existir.
- `resumen_cobranzas(p_clave text, p_desde date, p_hasta date, p_estado text, p_repartidor uuid)` → `TABLE(por_controlar bigint, cantidad bigint, total numeric)`. Alimenta las cifras de cabecera del listado (ver el módulo). **`LANGUAGE sql STABLE`, SECURITY INVOKER sobre `v_cobranzas`** (respeta el RLS, a diferencia de las dos `buscar_*`), `search_path = public`, EXECUTE para `authenticated` y `service_role`, no `anon`. **Los CINCO parámetros tienen DEFAULT null** — es la única de Cobranzas con defaults. `por_controlar` cuenta las `registrada` **ignorando `p_estado`**; `cantidad` y `total` **excluyen siempre las anuladas** y aplican `p_estado` cuando viene. Aplicada por la migración `20260922000832` (chat del rediseño de Cobranzas).
- **`marcar_cobranza_asentada(p_id, p_unidad_negocio_id)`** → void (22/09/2026, SECURITY DEFINER, `search_path=public`, `authenticated` sí, `anon` no): si la unidad es null o no existe, "Elegí a qué unidad de negocio pertenece esta cobranza."; después llama a `marcar_cobranza_procesada(p_id)` —de ahí hereda el permiso `cobranzas:procesar`: no lo chequea por su cuenta— y pone la unidad. **No escribe `unidad_asignada`** (el historial queda `procesada`). **La pantalla ya no llama a `marcar_cobranza_procesada`**, que sigue existiendo y la usa esta por dentro.
- **`asignar_unidad_cobranza(p_id, p_unidad_negocio_id)`** → void (mismo perfil): exige `cobranzas:procesar`; unidad inexistente → "La unidad de negocio no existe."; solo sobre una `procesada`; si la unidad no cambia, no hace nada; si cambia, la pone y escribe `cobranza_historial` con **`accion = 'unidad_asignada'`** (`antes`/`despues` = `{unidad_negocio_id}`, sin motivo; el CHECK de `accion` la admite).
- Internas, **sin `EXECUTE` para ningún rol de usuario** (ni `authenticated` ni `anon`): `_cobranza_hoy_ar()` (la fecha de hoy en Argentina — Supabase corre en UTC, así que sin esto "hoy" se adelanta nueve horas antes de medianoche), `_cobranza_snapshot(p_id)`, `_cobranza_validar_cabecera(…)` y `_cobranza_escribir_detalle(p_cobranza_id, p_fotos, p_cheques)`.
  - **`_cobranza_escribir_detalle` es la única que escribe fotos y cheques, y la comparten el alta y la edición**: una sola copia de las validaciones, en vez de dos que se desincronizan. Inserta los cheques **de uno en uno dentro de su propio bloque de excepción** para poder decir CUÁL falló y por qué —traduce cada constraint a una frase en castellano, nombrando el cheque por banco y número— en vez de mostrar `chk_dv_ruta` en pantalla.
  - Valida además que la ruta de cada foto empiece con el uid de quien llama y el id de la cobranza, y que el archivo **exista de verdad en Storage** (`storage.objects`, con `owner_id`): sin eso se podría guardar un cheque cuya foto nunca terminó de subir.
  - **Borra las fotos que quedaron sin ningún cheque** y **rechaza la cobranza vacía** (sin cheques y con efectivo 0).

## Base vieja (solo lectura, referencia)
- Proyecto: oxcypiztfoxxxhtuqmrd
- NO se modifica nunca. Conectada por MCP en solo lectura.
- Los módulos (gastos, ingresos_mp) NO se migran. Solo se migraron los maestros.

## Dashboard — burbujas de pendientes (21/09/2026)
- `dashboard.html` llama a **`mis_pendientes()`** (SECURITY DEFINER, `search_path=public`, `authenticated` sí, `anon` no) una vez al abrir y otra al volver a la pestaña (`visibilitychange`), con un turno para que una respuesta vieja no pise la nueva. La RPC devuelve `(modulo, clave, cantidad, texto)` **solo con lo que la persona que mira puede resolver, según sus permisos**.
- Un círculo rojo arriba a la derecha de cada tarjeta (`.tarjeta-modulo__burbuja`) con la suma de las cantidades de su módulo ("99+" arriba de 99); no aparece si es 0. El detalle va en `title` y `aria-label`, escapado: "5 cobranzas por controlar".
- **Si la llamada falla, no se muestra ninguna burbuja: nunca un número viejo o inventado.**
- Los nombres de la RPC (guión BAJO, como `empleado_tareas`) se mapean EXPLÍCITAMENTE a las claves de `MODULOS` (guión MEDIO) en `MODULO_DE_PENDIENTE`; un módulo que no esté en el mapa se ignora y se avisa por consola. Hoy la RPC pone "Insumos nuevos por revisar" en `materia_prima` aunque lo resuelve `stock:gestionar_catalogo`: se respeta la RPC.
- **Tarjeta "Cheques" (22/09/2026).** `mis_pendientes()` devuelve `('cheques', 'por_vencer', N, 'Cheques que vencen esta semana')` —los en cartera con `cheque_plazo_presentacion(...) <= hoy + 7`, vencidos incluidos, a quien tiene `cobranzas:procesar`— y `MODULO_DE_PENDIENTE` la lleva a la tarjeta `cheques`. **Esa tarjeta no tiene fila propia en `empleado_modulos`:** `MODULOS` admite dos campos nuevos, `requiereModulo` (Cheques cuelga de `cobranzas`) y `requiereTareas` (`['cobranzas:ver_todo', 'cobranzas:procesar']`, alcanza con una), y los decide la función pura **`moduloVisible()`**. Para eso el dashboard lee las `empleado_tareas` de la persona (salvo super_admin, que las tiene todas); si esa lectura falla, esas tarjetas no se muestran.
- **Tarjeta "Producción"** (22/09/2026): clave `produccion` (fila propia en `modulos`, orden 9, creada por el chat de arquitectura), ícono `factory`, color `azul`. **Desde el 23/09/2026 SÍ tiene burbuja** (`c6f035d`): `mis_pendientes()` devuelve `('produccion', 'conos_por_revisar', N, 'Conos nuevos por revisar')` —las `marcas_personalizadas` en `pendiente_revision`, a quien tenga `produccion:configurar`— y ahora `MODULO_DE_PENDIENTE` tiene esa clave. **La RPC ya devolvía esa fila desde que existe la tarjeta**: sin la clave en el mapa caía en el aviso de "módulo sin tarjeta" y no se contaba en ningún lado. Se resuelven en Configuración → Marcas / Conos, que es donde se aceptan o se rechazan.
- **ENTRAR DERECHO A LA TABLET** (23/09/2026, `c6f035d`). Una cuenta que **SOLO tiene Producción habilitada** no pasa por un dashboard de una sola tarjeta: entra directo a `modulos/produccion.html`, con `location.replace()` para no dejar el dashboard en el historial. Lo decide la función pura **`entraDerechoAProduccion()`**, y tiene **dos guardas que no son de más**:
  - **EXIGE ADEMÁS UNA TAREA DE PRODUCCIÓN** (`produccion:cargar` / `ver` / `configurar`). `produccion.html` **vuelve al dashboard cuando no hay ninguna** (su `sinAcceso()`), así que redirigir sin mirarlas sería **el bucle de redirecciones ya documentado en Aprendizajes**, del que no se sale por la UI. Si la consulta de tareas falló, `misTareas` queda vacío y **no se redirige**: se cae del lado de mostrar el dashboard, que siempre tiene salida.
  - **UNA VEZ POR SESIÓN**, marcado en `sessionStorage` (`dashboard.directoProduccion`), **y solo si se pudo dejar constancia**. Sin eso, el "Volver al inicio de la app" de la tablet rebotaría para siempre contra esta redirección; y si el storage no se puede escribir, **no se redirige**, porque un dashboard de una tarjeta de más es mucho menos grave que una vuelta atrapada. Una tablet que se enciende abre una sesión nueva, así que el kiosco sigue entrando derecho.
  - Después de redirigir se corta el dibujado con `await new Promise(() => {})`, para que el dashboard no aparezca un instante antes de irse (es un `<script type="module">`, así que el `return` de nivel superior no vale).
- **Tarjeta "Pedidos"** (23/09/2026, `41fe94c`): clave `pedidos` (fila propia en `modulos`, orden 10), ícono `clipboard-list`, color `naranja` (el acento comercial, el mismo de Cobranzas), abre `modulos/pedidos.html`. **Sin burbuja**: `mis_pendientes()` no devuelve nada de pedidos (verificado el 23/09/2026). Al lado de Producción no cambia la entrada derecha a la tablet: esa exige que Producción sea la ÚNICA tarjeta visible. Suite `test-dashboard-pedidos.js` (+ mut).
- Reemplazó a la burbuja vieja de Accesos (una consulta directa a `solicitudes_acceso`). `.tarjeta-modulo__badge` de main.css quedó sin uso.
- Suites `pruebas/test-dashboard-pendientes.js` y `pruebas/test-dashboard-produccion.js`, las dos con su `mut-*`.

## Login y roles
- Supabase Auth (email + contraseña)
- Auto-registro (`registro.html`): busca el CUIL; si matchea, pre-completa datos; si no, completa a mano. Crea la cuenta de Auth y una fila en `solicitudes_acceso` pendiente — todo vía Edge Function `crear-solicitud-acceso` (service role, atómico con rollback), nunca insert directo desde el cliente.
  - **El paso del CUIL YA NO llama a la RPC directa** (`f49bdd9`, 16/09/2026). `buscarEmpleadoPorCuil(cuil, captchaToken)` de `js/auth.js` invoca la **Edge Function `buscar-empleado-cuil`** (v1, `verify_jwt: true`, igual que `crear-solicitud-acceso`: el cliente le manda la key pública como Bearer, sin sesión). La función exige un token de Turnstile **antes** de consultar, y recién ahí llama a `buscar_empleado_por_cuil` con `service_role`. Devuelve `{ ok, empleado: { nombre, rol } | null }`; los rechazos del CAPTCHA traen `esErrorCaptcha: true` y el cliente les pone un mensaje propio, distinto del genérico. Por qué existe: ver el pendiente cerrado en Seguridad.
  - **`registro.html` tiene TRES widgets de Turnstile, no dos**, con render explícito y un `widgetId` cada uno: el del CUIL (`#turnstile-cuil`) y uno por formulario de solicitud. **El del CUIL se renderiza AL CARGAR la página y los otros dos al mostrarse su paso**, y no es una inconsistencia: `#paso-cuil` está visible desde el arranque, mientras que un widget dentro de un contenedor `[hidden]` puede no ejecutar el desafío. "Unificarlo" renderizándolo al mostrar su paso lo dejaría sin dibujar nunca, porque ese paso nunca se "muestra". Se lanza sin `await` —con su `.catch`— para no frenar el resto de la página mientras carga Cloudflare; hasta entonces `tokenDe()` devuelve `''` y "Buscar en la base" pide *"Esperá a que termine la verificación de seguridad."*. El botón valida primero el CUIL y después exige el token, y en el `catch` hace `reset()` **de ese widget**: el token es de un solo uso.
  - **`crear-solicitud-acceso` (v5, `f49bdd9`) valida `<` y `>` en `nombre`, `apellido` y `email` ANTES de Turnstile y de crear la cuenta**, con un 400 cuyo mensaje nombra el campo. Antes se creaba la cuenta, fallaba el insert por `chk_solicitud_texto_sin_html`, se borraba la cuenta y la persona leía *"Probá de nuevo en unos minutos"* — falso: reintentar fallaba siempre.
    - **Red de último recurso:** si el insert igual falla, se devuelve el mensaje específico **solo si el error es `23514` Y nombra `chk_solicitud_texto_sin_html`** (en `message` o `details`); la cuenta se revierte igual. **Exige las dos cosas a propósito:** con solo el código, cualquier otro CHECK que se agregue mañana y falle con `23514` daría un mensaje sobre `<` y `>` que no tiene nada que ver. Cualquier otro error sigue con el genérico, que ahí sí es honesto.
    - **Mínimo de contraseña: 10** (era 6). La función lo tiene en una constante local, `LARGO_MINIMO_CONTRASENA`, **copia de la de `js/utils.js`** —una Edge Function no puede importarla—: si se cambia una, se cambia la otra, y las dos siguen al mínimo configurado en el panel de Auth, que es el que manda.
    - **En el camino con match el nombre sale de `empleados` y la persona no lo puede editar**, así que un empleado con `<` o `>` en el nombre no podría registrarse. Verificado el 16/09/2026: ninguno los tiene (está anotado en el código con la consulta).
- Rechazo de una solicitud: Edge Function `rechazar-solicitud-acceso` (no es una RPC de Postgres) — además de marcar la solicitud como rechazada, borra la cuenta de Auth huérfana asociada para que la persona pueda volver a registrarse con el mismo email. Reemplazó a una RPC vieja del mismo nombre (con guión bajo) que no hacía ese borrado — esa RPC ya no existe, fue eliminada con `DROP FUNCTION`.
- Aprobación de solicitudes y edición de permisos/roles de otros usuarios: **exclusivo de `super_admin`**. `modulos/accesos.html` es soloSuperAdmin.
- Roles (`empleados.rol_app`): solo DOS valores — `super_admin` (control total, bypass automático de todos los chequeos; se asigna solo por SQL directo, nunca desde ninguna UI) y `usuario` (lo que puede hacer lo definen sus tareas en `empleado_tareas`). El rol `admin` YA NO EXISTE — ver sección Sistema de permisos. **Desde el 31/08/2026 lo garantiza la BASE y no solo el código**: el CHECK `empleados_rol_app_check` es `rol_app = any(array['super_admin','usuario'])`, verificado con `pg_get_constraintdef`. Era el último pendiente estructural de la migración de permisos.
- Rescate de MFA: Edge Function `quitar-mfa-empleado`. Un super_admin le da de baja TODOS los factores TOTP a otra persona que perdió su dispositivo. Sin esto la única salida sería tocar la base a mano, porque Supabase no tiene códigos de recuperación. Recibe `empleado_id` (NO el auth_user_id). Valida, en orden: sesión válida → `rol_app = 'super_admin'` → **sesión en aal2** → body con `empleado_id` → esa persona tiene `auth_user_id` → no es uno mismo. Borra todos los factores (un factor vivo la sigue trabando); un borrado parcial devuelve error, no éxito.
  - Rescatar a OTRO super_admin está permitido a propósito: es el caso de uso principal, los super_admin se rescatan entre ellos.
  - **PRECONDICIÓN OPERATIVA, no es un bug**: exigir aal2 significa que un super_admin SIN MFA propio no puede rescatar a nadie (su sesión es aal1 y la función lo rechaza). Sumado a que no se permite el auto-rescate, hacen falta **DOS super_admin con MFA activo** para que este circuito sirva. Si solo una persona tiene MFA y pierde el dispositivo, no hay rescate posible desde la app. Es la decisión de seguridad correcta: sin el requisito de aal2, una sesión robada podría desactivarle el MFA a todo el equipo.
  - El claim `aal` se lee decodificando el payload del token DESPUÉS de que `getUser()` lo validó contra el servidor de Auth. Eso es seguro y el archivo lleva un comentario largo explicando por qué, para que nadie lo "arregle" mal: la verificación fuerte ya ocurrió en `getUser()`; decodificar después un token ya probado no es confiar en un token sin verificar. Quitar el `getUser()` y dejar solo el decode SÍ sería un agujero grave. Si el claim no viene, se asume aal1 y se rechaza — la doc dice "JWTs without an `aal` claim are at the `aal1` level".
  - El botón está en `modulos/accesos.html`, en la tarjeta de cada persona de "Usuarios y roles". Su condición es INDEPENDIENTE de `puedeEditar` (que sigue gateando solo a "Editar", porque un super_admin no edita permisos de otro). Si colgara de `puedeEditar` sería imposible rescatar a un super_admin, que es justamente el caso principal. Lo único que se excluye es la tarjeta propia.
- Tablets de fábrica: cuenta genérica + PIN de turno por encargado (a implementar)

## Verificación en dos pasos (MFA / TOTP) — agosto 2026

MFA es OPCIONAL por usuario: quien no lo activa entra como siempre. TOTP es gratis y viene habilitado por defecto en todos los proyectos de Supabase.

**NO EXISTEN CÓDIGOS DE RECUPERACIÓN.** La referencia de Auth MFA lo dice textual: "Recovery codes are not supported". El único respaldo posible es dar de alta un segundo dispositivo (máximo 10 factores por usuario). Todo el diseño de la UI y la existencia de la Edge Function de rescate salen de acá — no buscar una API de recovery codes, no la hay.

Otras dos consecuencias del diseño de Supabase, ambas visibles para el usuario:
- Verificar un factor CIERRA todas las demás sesiones de esa persona.
- Dar de baja un factor NO degrada el JWT de aal2 a aal1 hasta el próximo refresco automático; hace falta `refreshSession()` explícito.

### Chequeo de nivel (AAL) — `js/auth.js`, dentro de `verificarSesion()`

Va después del `getSession()` y ANTES de los dos returns de redirección. Vive ahí, y no en cada pantalla, para que proteja automáticamente a **las 16 páginas** que llaman a `verificarSesion()` — **incluidos los 10 módulos**, Cheques y Producción entre ellos — sin que ninguna tenga que acordarse. (Re-contado el 22/09/2026 con grep; eran 11 y 6 cuando se escribió esto, y 14 y 8 el 16/09. Que un módulo nuevo quede protegido **sin escribir una línea** es exactamente para lo que el chequeo vive en `verificarSesion()` y no en cada pantalla.)

- Condición para exigir el segundo factor: `nextLevel === 'aal2' && nextLevel !== currentLevel`
- **Falla CERRADA**: si `getAuthenticatorAssuranceLevel()` devuelve error, se exige el factor igual. No saber el nivel no es "seguí de largo".
- Corre SOLO si hay sesión. index.html, login.html, registro.html y recuperar-contrasena.html llaman con `redirigirSiNoHay:false`; sin este guard un visitante anónimo entrando a la home terminaría rebotando contra mfa.html.
- No redirige si ya estamos en mfa.html (se compara por `pathname`, no por string suelto, para que funcione igual en GitHub Pages que en local).
- Va ANTES de los returns a propósito: si fuera después, una sesión con el factor pendiente que abre login.html saltaría primero al dashboard por `redirigirSiHay` y recién de ahí a mfa.html.

`restablecer-contrasena.html` NO llama a `verificarSesion()` (maneja su propia sesión de recuperación), así que el reseteo de contraseña no queda bloqueado. Termina redirigiendo a dashboard.html, donde el chequeo sí corre.

### Funciones de MFA en `js/auth.js`

- `listarFactoresMfa()` → solo los factores TOTP con `status === 'verified'`.
- `iniciarAltaMfa(nombreAmigable)` → `{ factorId, qrCode, secret }`. El `qrCode` viene en SVG, va directo al `src` de un `<img>`. Antes de crear el factor nuevo da de baja los `unverified` colgados: `enroll()` crea la fila apenas se lo llama, y si la persona abandona el modal esa fila queda para siempre haciendo chocar el reintento con `mfa_factor_name_conflict`. NUNCA toca un factor `verified`.
- `verificarCodigoMfa(factorId, codigo)` → `challenge()` + `verify()`. Sirve tanto para completar el alta como para el desafío al entrar. El challenge se crea al verificar y no al abrir la pantalla: tiene expiración propia.
- `darDeBajaMfa(factorId)` → `unenroll()` + `refreshSession()`.

**Los errores de MFA se traducen por `error.code`, no por el texto del mensaje** (`_traducirErrorMfa`). Los códigos son contrato público documentado de Supabase; el texto no. Si el código no viene, cae al genérico y loguea a consola. Dos códigos que importan por UX y no solo por traducción:
- `mfa_ip_address_mismatch`: el alta tiene que empezar y terminar en la misma IP. En celular, pasar de WiFi a datos móviles en el medio la rompe. El mensaje traducido lo explica con esas palabras.
- `mfa_factor_name_conflict`: dos factores del mismo usuario no pueden tener el mismo nombre. Por eso el alta pide un nombre de dispositivo editable.

### `mfa.html` — pantalla del desafío al entrar

Misma estructura visual que login (`.pagina-auth` + `.tarjeta-auth`). Sin CAPTCHA: el usuario ya pasó el login. Tres salidas posibles:
- Sesión ya en `aal2` → al dashboard (alguien entró a la URL a mano).
- Hay factores verificados → pide el código de 6 dígitos.
- NO hay factores verificados → mensaje sin salida + botón de cerrar sesión. **NO redirige.** Ver el aprendizaje sobre bucles de redirección.

Un código mal tipeado NO cierra la sesión: se limpia el campo y se reintenta.

### Alta desde `dashboard.html` (menú de perfil → "Verificación en dos pasos")

Modal con dos estados: sin factor (aviso + nombre de dispositivo + Activar → QR + secreto + código) y con factor (lista + "Agregar otro dispositivo"). NO hay botón de desactivar.

El aviso previo al alta es obligatorio y explícito porque no hay recovery codes: "Guardá el código de texto en un lugar seguro y escaneá el QR en un segundo dispositivo. Si perdés el único dispositivo, no vas a poder entrar."

PENDIENTE DE CONFIRMAR: no está documentado si `listFactors()` devuelve `created_at`. La columna existe en `auth.mfa_factors`, pero la API puede no devolverla. El código muestra la fecha si viene y solo el nombre si no.

### Pendiente operativo

- **Configurarle MFA al segundo super_admin. SIGUE ABIERTO, y ahora está medido:** al **17/09/2026** hay **UN SOLO factor TOTP verificado en todo el sistema** y **DOS super_admin**. Como el circuito exige aal2 y no permite el auto-rescate, `quitar-mfa-empleado` está desplegada e **inutilizable**: si esa única persona pierde el dispositivo, no hay rescate posible desde la app (ver la precondición en Login y roles).

## Sistema de permisos (migrado — julio 2026)

REGLA CENTRAL: el rol `admin` YA NO EXISTE. Solo hay dos valores de `empleados.rol_app`: `super_admin` (control total, bypass automático de todos los chequeos) y `usuario`. NUNCA escribir código nuevo que chequee `rol_app IN ('admin', ...)` — ese patrón está muerto. **Y desde el 31/08/2026 la base lo impide**: el CHECK `empleados_rol_app_check` solo acepta esos dos valores, así que un `UPDATE` a `'admin'` ahora es un error y no un dato raro que aparece meses después. Con eso **la migración de permisos queda cerrada también a nivel estructural**, no solo de código.

Los permisos finos son TAREAS granulares en la tabla `empleado_tareas` (empleado_id + modulo + tarea + habilitado + alcance jsonb). El catálogo válido lo define el CHECK constraint `chk_tarea_valida` — 43 tareas hoy, listadas abajo, las mismas 43 que el `CATALOGO_TAREAS` de accesos.html. Las tareas son independientes entre sí y no hay jerarquías, **con UNA excepción forzada por servidor y UI: `cuentas_corrientes:ver_todo` requiere `gastos:ver_exportar`** (ver "Regla de otorgamiento" más abajo).

**Vínculo tarea ↔ módulo en la UI (decisión de Facu del 13/08/2026, que REVIERTE la anterior de "las tareas son independientes de los toggles de módulos"):** cada grupo de `CATALOGO_TAREAS` declara un campo **`modulos: [...]`** con claves de `empleado_modulos` (guión MEDIO), y el modal solo muestra sus tareas si **ALGUNO** de esos módulos está tildado — semántica "any", porque `facturas_pendientes:editar_interes` se opera desde Gastos o desde Cuentas Corrientes y con uno solo ya es útil. Es data-driven, mismo precedente que `conAlcance` y `requiere`.
- **El servidor sigue permisivo a propósito: NO valida el vínculo tarea-módulo.** Si lo validara rompería el passthrough de claves fuera de catálogo y los otorgamientos por SQL directo — justo los casos que la pantalla no sabe dibujar y no tiene que revocar.
- **Memoria de sesión al destildar:** las tareas de los grupos que se ocultan **por ese evento** salen del Set activo y quedan guardadas en `estado.modal.tareasRecordadas`; al volver a tildar el módulo se restauran, con su alcance intacto (`alcances` no se toca nunca). La restauración es silenciosa a propósito: el grupo reaparece en pantalla con sus checkboxes tildados, así que un aviso anunciaría algo que la persona ya está viendo.
- **SEMÁNTICA DE NO-PÉRDIDA — la regla que no hay que romper:** una tarea otorgada por SQL cuyo módulo NO está tildado **se preserva al guardar**. Al abrir el modal no se mueve nada: esas tareas quedan en el Set, ocultas pero vivas, y abrir y guardar sin tocar produce el payload idéntico. **Solo la ACCIÓN de destildar un módulo durante la sesión revoca; no tocar preserva.** Es el mismo principio del fix `0e70708`.
- Las tres invariantes están escritas en `moverTareasPorCambioDeModulos()` dentro de accesos.html, que es la única función que mueve tareas — y se llama SOLO desde el handler del toggle, nunca al abrir ni desde un render.

Catálogo completo de tareas (verificado contra `pg_get_constraintdef` del CHECK real, no reconstruido — claves exactas `modulo:tarea`, no inventar claves nuevas ni duplicar una existente con otro nombre; labels según `CATALOGO_TAREAS` de accesos.html):
- `gastos:ver_exportar` — Ver todos los gastos de la empresa + exportar Excel (incluye cargar gastos a nombre de otro)
- `gastos:editar_anular` — Editar y anular cualquier gasto
- `gastos:gastos_empresa` — Cargar gastos contra las cuentas de la Empresa (CON bypass)
- `gastos:gestionar_proyectos` — Dar de alta y baja proyectos del Taller (SIN alcance, CON bypass). Entró al CHECK el 16/09/2026 (chat de permisos) y al catálogo en `8361046`.
  - **SIN ALCANCE porque la tabla no lo permite:** `proyectos` tiene solo `id, nombre, activo, created_at` (verificado el 16/09/2026) — **no tiene `unidad_negocio_id`**, así que un proyecto es visible desde cualquier unidad y no hay contra qué acotar. Marcar `conAlcance` dibujaría un selector de unidades que nada puede aplicar. **Si algún día se le agrega la columna, recién ahí tiene sentido el alcance** — y en las dos mitades, catálogo y servidor.
  - **YA HABILITA ALGO (desde el 16/09/2026):** la exigen `crear_proyecto`, `dar_de_baja_proyecto` y `reactivar_proyecto` (verificado contra `pg_proc`), y `modulos/gastos.html` (`892297d`) muestra con ella la pantalla "Proyectos del Taller" y el "+ Proyecto nuevo" del wizard. Hasta ese día era la mitad "catálogo" esperando a la mitad "servidor". `proyectos` sigue sin policy de escritura: todo entra por esas tres RPCs.
- `caja:ver_listado` — Ver el listado completo de cajas de la empresa
- `caja:retiros_todos` — Ver los retiros personales de todos
- `caja:movimientos_todos` — Ver todos los movimientos de todas las cuentas
- `caja:ingreso_externo_propio` — Cargar ingresos externos en su propia caja (SIN bypass de super_admin — ver la nota de abajo)
- `caja:ingreso_externo_empresa` — Cargar ingresos en las cuentas de la Empresa (SIN bypass de super_admin — ver la nota de abajo)
- `caja:ver_empresa` — Ver la ficha y las cuentas de la Empresa (SIN bypass — es la puerta de entrada a todo lo de Empresa: sin esta tarea no aparece el atajo a la ficha)
- `caja:egreso_empresa` — Transferir plata de la Empresa a una persona (SIN bypass)
- `caja:traspaso_empresa` — Mover plata entre cuentas de la Empresa (SIN bypass)
- `caja:transferir_entre_personas` — Transferir plata entre dos personas comunes, sin super_admin ni Empresa de por medio (SIN bypass). Ya está en `CATALOGO_TAREAS`.
  - La exige **solo quien CREA** el movimiento; la contraparte no necesita nada para aceptar. Y sigue siendo una **solicitud**: la plata no se mueve hasta que la otra persona la confirma, así que la tarea no habilita a sacarle plata a nadie — habilita a pedírsela.
- `empleados:ver_editar` — Ver y editar empleados (CON ALCANCE por unidad)
- `empleados:importar_naaloo` — Importar/actualizar empleados desde Naaloo
- `empleados:reasignar_unidad` — Reasignar empleados entre unidades de negocio
- `cuentas_corrientes:ver_todo` — Ver proveedores, saldos y facturas de toda la empresa
- `cuentas_corrientes:alta_proveedor` — Dar de alta proveedores directamente
- `cuentas_corrientes:aprobar_rechazar_proveedor` — Aprobar/rechazar proveedores pendientes
- `cuentas_corrientes:registrar_pago` — Registrar pagos a proveedores
- `cuentas_corrientes:aplicar_credito` — Aplicar créditos a favor a facturas
- `cuentas_corrientes:anular_factura` — Anular facturas pendientes
- `cuentas_corrientes:asignar_proveedor_legado` — Asignar proveedor a facturas sin vincular
- `facturas_pendientes:editar_interes` — Editar facturas pendientes y agregar intereses (válida desde Gastos y desde Cuentas Corrientes)
- `materia_prima:cargar` — Registrar ingresos de materia prima (y ver los propios) (CON ALCANCE por unidad)
- `materia_prima:ver_todo` — Ver los ingresos de todas las personas (CON ALCANCE por unidad)
- `stock:ver` — Ver el stock, los lotes y los movimientos (CON ALCANCE por unidad)
- `stock:gestionar_catalogo` — Dar de alta y editar insumos del catálogo (SIN alcance — el catálogo de insumos es único y compartido por las 4 unidades, así que acotarlo por unidad no tendría sentido)
  - **Esta es la tarea que decide si un insumo EXIGE LOTE o no**, así que quien la tiene puede apagar la trazabilidad de un insumo. Otorgar con criterio restrictivo, a poca gente.
- `stock:dar_baja` — Dar de baja mercadería: rotura, vencimiento, merma (CON ALCANCE por unidad)
- `stock:enviar_transferencia` — Enviar mercadería a otra unidad de negocio (CON ALCANCE por unidad — el alcance controla desde dónde se SACA; el destino puede ser cualquier unidad, porque recibirla la controla el otro lado con su propia tarea)
- `stock:recibir_transferencia` — Recibir y confirmar mercadería de otra unidad (CON ALCANCE por unidad)
- `stock:ajustar_inventario` — Ajustar el stock del sistema al conteo real (CON ALCANCE por unidad)
  - **CONSTANCIA DEJADA A PEDIDO EXPLÍCITO (traspaso de Stock, 13/08/2026):** un ajuste positivo **crea stock de la nada**. Es el análogo exacto de las tareas de Caja-Empresa —donde meter plata "de la nada" motivó elegir `tiene_tarea_explicita` SIN bypass— y es además el mecanismo por el que se puede **tapar un faltante**. Acá se implementó **CON bypass de super_admin**, por decisión explícita de Facu tomada conociendo esa tensión. Si algún día aparece un descuadre raro de stock, este es el primer lugar a revisar.
- `materia_prima:editar_anular` — Corregir o anular ingresos ya cargados (CON ALCANCE por unidad)
- `cobranzas:cargar` — Cargar cobranzas y ver las propias (SIN alcance). Incluye editar y anular **lo propio mientras siga `registrada`**: quien cobra en la calle puede corregir su propio error antes de que administración lo procese, sin que haya que darle permiso sobre las cobranzas de todos.
- `cobranzas:ver_todo` — Ver las cobranzas de todas las personas (SIN alcance)
- `cobranzas:procesar` — Marcar una cobranza como procesada y reabrirla con motivo (SIN alcance)
- `cobranzas:editar_anular` — Editar cobranzas ajenas sin procesar y anular cualquiera (SIN alcance)
  - **Los dos límites son distintos y los pone el servidor para TODOS**, tenga quien tenga la tarea (verificado en `pg_get_functiondef` el 16/09/2026): `editar_cobranza` corta si `estado <> 'registrada'` —una procesada hay que reabrirla primero, con `procesar`—, y `anular_cobranza` solo corta si ya está `anulada`, así que anula también una procesada.
  - **El límite va en el LABEL y no solo en la descripción** (`8361046`): el label es lo que se lee al tildar y la descripción está detrás del `?`. El anterior, *"Editar y anular cobranzas de cualquier persona"*, hacía creer que habilitaba a corregir lo ya procesado.
  - **LAS CUATRO VAN SIN ALCANCE POR UNIDAD, y es una decisión y no un olvido:** las RPCs gatean con `tiene_tarea()` y **ninguna** con `tiene_tarea_alcance()`, porque **al cargarla una cobranza no pertenece a ninguna unidad de negocio** — el repartidor le cobra a un cliente, no a una empresa del grupo. Desde el 22/09/2026 `cobranzas.unidad_negocio_id` existe, pero la pone el ASENTAR, no la carga, y las RPCs siguen gateando sin alcance. Marcar `conAlcance` en el catálogo dibujaría un selector de unidades que el servidor ignora: la pantalla prometería un recorte que no existe.
  - Las cuatro **CON bypass de super_admin** (`tiene_tarea`, no `tiene_tarea_explicita`): ninguna mueve plata de la empresa ni crea valor de la nada — registran cheques de papel que ya existen.

- `produccion:cargar` — Abrir y cerrar turnos, registrar masas, paradas y lo producido desde la tablet (CON ALCANCE por unidad, CON bypass). Es la tarea de la cuenta de cada tablet.
- `produccion:ver` — Ver el historial de producción y el stock terminado (CON ALCANCE por unidad, CON bypass).
- `produccion:configurar` — Máquinas, recetas, productos, marcas, personal e insumos de cada ingrediente (CON ALCANCE por unidad, CON bypass). Ingredientes y marcas son comunes: `guardar_ingrediente`, `guardar_ingrediente_insumos` y `guardar_marca` la piden con `tiene_tarea()`, sin unidad. También deja anular una masa de un turno ya cerrado.
  - Las tres entraron al CHECK y al catálogo el 22/09/2026 (grupo "Producción", `modulos: ['produccion']`); verificado: el catálogo tiene las 40 claves del CHECK (`pruebas/test-accesos-produccion.js`).

- `pedidos:ver` — Ver los pedidos de los clientes y su avance (CON ALCANCE por unidad, CON bypass). Es la que lee las tres tablas del módulo (sus policies) y la que filtra `pedidos_de`.
- `pedidos:cargar` — Cargar pedidos, marcar su avance y entregarlos o anularlos (CON ALCANCE por unidad, CON bypass). La exigen `guardar_pedido`, `marcar_avance_pedido` y `cambiar_estado_pedido`.
- `pedidos:configurar` — Dar de alta y editar los clientes y sus apodos (CON ALCANCE por unidad, CON bypass). La exige `guardar_cliente`.
  - Están en el catálogo (grupo "Pedidos", `modulos: ['pedidos']`, `41fe94c`) y, desde el 23-24/09/2026, también en el CHECK (verificado con `pg_get_constraintdef` el 24/09/2026). El aviso de que faltaba se sacó de accesos.html, y `test-accesos-pedidos.js` exige que no vuelva.

PASSTHROUGH del modal de Accesos (sigue vigente, y desde `0e70708` es completo): el modal precarga TODAS las filas de `empleado_tareas` de la persona y el payload reenvía el Set completo, así que **una tarea otorgada por SQL que todavía no está en el catálogo conserva el permiso Y SU ALCANCE** al guardar, aunque la pantalla no sepa dibujarla. Hasta `0e70708` preservaba el permiso pero pisaba el alcance con null en cada guardado — una revocación parcial silenciosa (ver el aprendizaje sobre passthroughs a medias). **NO agregar un filtro del payload contra el catálogo**: revocaría en silencio esas tareas cada vez que se editen los permisos de esa persona. Es la red de seguridad para toda tarea que entre al CHECK antes que a `CATALOGO_TAREAS` — situación que ya ocurrió con las 4 de `materia_prima`.

Chequeo server-side (SIEMPRE en RPCs SECURITY DEFINER y policies RLS):
- `tiene_tarea(modulo, tarea)` — el estándar, CON bypass de super_admin
- `tiene_tarea_alcance(modulo, tarea, unidad_negocio_id)` — para tareas con alcance por unidad (hoy solo `empleados:ver_editar`; alcance = `{"todas": true}` o `{"unidades": ["uuid", ...]}`). También con bypass.
- `tiene_tarea_explicita(modulo, tarea)` — variante SIN bypass: exige la fila otorgada aunque quien llame sea super_admin. Se usa hoy en las SEIS tareas de Caja que tocan plata de Empresa, meten plata "de la nada" o saltean una regla de control: `ingreso_externo_propio`, `ingreso_externo_empresa`, `ver_empresa`, `egreso_empresa`, `traspaso_empresa` y `transferir_entre_personas`. El criterio: esas operaciones no se heredan por rol — ser super_admin no alcanza, hace falta la fila otorgada. (La sexta no toca plata de Empresa: lo que hace es saltear el requisito de que haya un super_admin en la operación, así que heredarla por rol sería circular — el super_admin ya cumple la regla por su cuenta.) El resto de las tareas usa `tiene_tarea()`. El frontend replica esta distinción: caja.html tiene `tieneTarea()` y `tieneTareaExplicita()` — ojo que ahí la firma es de UN solo argumento, el módulo va implícito.

Chequeo frontend (patrón en los módulos migrados —hoy los 10, Cobranzas, Cheques y Producción incluidos; Cheques usa las tareas de `cobranzas`—, copiar de gastos.html): al init se consulta `empleado_tareas` filtrando por módulo(s), se guarda un Set con claves `'modulo:tarea'`, y un helper `tieneTarea(modulo, tarea)` devuelve true si `rol_app === 'super_admin'` o el Set contiene la clave. El frontend solo oculta UI — la barrera real es siempre server-side.

### Regla de otorgamiento — `cuentas_corrientes:ver_todo` requiere `gastos:ver_exportar`

**Es una REGLA FORZADA, no una recomendación.** El servidor la valida en `actualizar_permisos_empleado` (y `aprobar_solicitud_acceso` la hereda porque la invoca internamente), y la UI del modal de Accesos la refleja desde el commit `e9accc2`.

Motivo: la vista `v_cuenta_corriente_movimientos` hace un UNION de cinco tablas y una de ellas es `gastos` — los pagos a proveedor son filas de `gastos`. Sin `gastos:ver_exportar`, el RLS filtra esas filas y la persona ve el listado SIN los pagos, con el `saldo_acumulado` calculado sobre datos incompletos.

Esto NO es "queda a medias": son números que parecen correctos y están mal, sin ningún error a la vista y sin nada que le avise a la persona que le falta información. Es el peor modo de falla posible para un módulo de plata.

En la UI la dependencia se declara con el campo `requiere` en `CATALOGO_TAREAS` (mismo precedente que `conAlcance`), así que una dependencia nueva es una línea del catálogo y no otro `if`. Se BLOQUEA el guardado y NO se auto-tilda la tarea faltante: `gastos:ver_exportar` incluye "cargar gastos a nombre de otro", y auto-otorgarla sería una escalada silenciosa de privilegios en la única pantalla donde se reparten permisos.

Gestión: pantalla de Accesos (solo super_admin) con checkboxes por tarea. RPCs con firma nueva (SIN parámetro de rol):
- `actualizar_permisos_empleado(p_empleado_id uuid, p_modulos text[], p_tareas jsonb)`
- `aprobar_solicitud_acceso(p_solicitud_id uuid, p_modulos text[], p_tareas jsonb, p_unidad_negocio_id uuid)`

`p_tareas`: `[{"modulo":"...","tarea":"...","alcance":null|{...}}, ...]` con semántica de REEMPLAZO TOTAL (lo no incluido se apaga).

Cuenta de Empresa (Caja): empleado ficticio identificado por `tipo='empresa'` (activo=false, sin auth_user_id, unidad_negocio_id null).

Toda la ficha de Empresa —incluido el atajo de navegación que lleva a ella— exige `caja:ver_empresa` explícita. Adentro, cada operación tiene su propia tarea, también explícita: cargar ingresos exige `caja:ingreso_externo_empresa`, sacar plata hacia una persona exige `caja:egreso_empresa`, y mover plata entre cuentas de Empresa exige `caja:traspaso_empresa`. **Ninguna tiene bypass: un super_admin sin la fila otorgada tampoco puede.** (Versiones anteriores de este documento decían que el atajo usaba `tieneTarea` CON bypass como "excepción deliberada" — eso ya no es así, verificado contra caja.html ~líneas 3034 y 4533; la excepción y su consecuencia desaparecieron.)

Las transferencias que involucran a Empresa no exigen super_admin de contraparte, y cualquier super_admin puede responderlas en su nombre, incluso si él mismo las creó (auto-aceptación permitida: si no, una solicitud creada por el único super_admin disponible quedaría pendiente para siempre). El frontend replica esa regla en dos lugares que tienen que decir lo mismo — los botones Aceptar/Rechazar de cada solicitud y el contador del badge de pendientes — vía los helpers `involucraAEmpresa()` / `puedoResponderSolicitudesDeEmpresa()` de caja.html.

Formato del CHECK `chk_tarea_valida`: hoy valida con `(modulo || ':' || tarea) = any(array[...])` — ya NO es la cadena de OR anidados de la versión original. Para cambiarlo hay que hacer DROP + ADD con el array completo de las 43 (no se puede alterar in-place) — leyendo antes el constraint real, nunca regenerándolo de memoria (ver la regla del territorio compartido en "Cómo trabajar").

**Migración `materia_prima:gestionar_catalogo` → `stock:gestionar_catalogo` (12-13/08/2026).** La pantalla del catálogo de insumos se mudó al módulo Stock, así que la tarea se mudó con ella. La clave vieja YA NO EXISTE en el CHECK ni en `CATALOGO_TAREAS`.

**Lección de orden de ejecución, que es lo que hay que recordar de esto:** al migrar una clave del CHECK, el `UPDATE` de las filas existentes de `empleado_tareas` va **ENTRE el DROP y el ADD**. El `ADD` valida las filas que ya están en la tabla contra el array nuevo, así que si el UPDATE va después, el ADD falla por las filas con la clave vieja; y si va antes del DROP, falla contra el constraint viejo, que todavía no conoce la clave nueva. El orden correcto es DROP → UPDATE → ADD.

(Si alguna fila con la clave vieja sobreviviera igual, el modal de Accesos la deja pasar por el passthrough sin romperse — pero no cuenta como plan, es la red.)

Para módulos NUEVOS: definir sus tareas con Facu, agregarlas al CHECK constraint `chk_tarea_valida` (DROP + ADD con el array completo), al catálogo hardcodeado `CATALOGO_TAREAS` de accesos.html —con su `descripcion`, el `modulos` del grupo, y `conAlcance: true` si la tarea se acota por unidad—, y gatear RPCs/RLS con `tiene_tarea()` / `tiene_tarea_alcance()` — o con `tiene_tarea_explicita()` si la operación no debe heredarse por rol — nunca con `rol_app`.

PENDIENTE de Materia Prima: las 4 tareas ya se pueden otorgar desde Accesos, pero **el frontend del módulo todavía NO las chequea** — `modulos/materia-prima.html` solo verifica que exista la fila de `empleados`, sin `tieneTarea()`. Las barreras server-side (RLS y RPCs) sí están, así que no hay agujero real: lo que falta es ocultar lo que la persona no puede hacer. Queda para el chat de Materia Prima.

## Módulos

### Existentes
1. **Gastos** (`modulos/gastos.html`): wizard de carga con OCR (que extrae CUIT además de razón social), vínculo a vehículo y/o unidad de negocio, envío a "pendiente de pago" (ver Cuentas Corrientes), y anulación de gasto con motivo obligatorio. El selector de proveedor con matching automático (CUIT normalizado, con fallback a razón social) ahora funciona también en el flujo NORMAL y no solo en "pendiente de pago" — antes `armarGasto()` mandaba `proveedor_id` en null siempre.
   - **Buscador de proveedor en el campo "Razón social / Proveedor"** (paso Datos). **Es el ÚNICO: el del paso Detalles se sacó** (`fdd06ca`) porque era información redundante — el matching ya ocurre antes, por OCR o por este buscador. Existe porque ahí es donde la gente escribe el nombre del proveedor —el label lo promete— y porque el OCR no siempre puede leerlo: las facturas tipo "X" no traen el nombre impreso. Sin sugerencias ahí, la persona no encontraba al proveedor, asumía que no existía y creaba un duplicado. **No hay umbral mínimo de caracteres**: busca desde la primera letra, contra `razon_social` y `nombre_fantasia`, hasta 8 resultados. La comparación **ignora acentos** (`normalize('NFD')`), porque los comprobantes vienen en mayúsculas sin tildes y el padrón no — "LACTEOS DEL SUR" tiene que encontrar a "Lácteos del Sur". Esa misma normalización se aplica al match automático (`normalizarRazonSocial`).
     - Elegir una sugerencia **pisa el texto** con el nombre del padrón, y el campo queda editable por si el papel decía otra cosa.
     - El panel de sugerencias **se abre hacia arriba cuando abajo no entra**, medido contra `visualViewport` y NO contra `innerHeight`: en el celular `innerHeight` no se achica al abrirse el teclado, así que un panel tapado seguiría midiendo "entra perfecto". Aplica a los dos buscadores que quedan —éste y el del formulario de edición de FACTURA, que es propio y paralelo—, que comparten la función de render.
     - **`proveedorSeleccionado` es la ÚNICA FUENTE DE VERDAD del vínculo** (`fdd06ca`). Antes había además un input hidden (`#campo-proveedor-id`) que la espejaba y que leían los dos payloads: un espejo que podía desincronizarse. **Y sacar ese hidden dejando la referencia colgada NO habría roto nada de forma visible**: `toStr()` usa optional chaining, así que habría devuelto `null` **en silencio** y el gasto se habría guardado sin proveedor. La única que rompía con `null.value` era la validación del flujo pendiente, que lee sin `?.`.

   **EL VÍNCULO DE PROVEEDOR SE SUELTA AL VACIAR EL CAMPO** (`dbc728b`). El caso que lo motivó, encontrado en producción: se eligió "FERRETERIA KM 711" de las sugerencias, se borró TODO el texto y se escribió otro proveedor — y el vínculo quedaba pegado, así que el gasto se guardaba en la cuenta corriente equivocada, sin ningún error.
   - **Seguir escribiendo NO lo suelta, y esa decisión sigue en pie**: tener dos campos existe justamente para que el texto del comprobante pueda diferir del nombre del padrón. Pero **un campo vacío no puede ser una corrección menor**, así que ahí no hay falso positivo posible.
   - La ✕ y el caso "campo vacío" comparten `soltarVinculoProveedor()`: estaba escrito solo en el handler de la ✕ y los dos caminos podían divergir.
   - **EL CARTEL DICE LA CONSECUENCIA Y SE PONE EN ÁMBAR AL DIVERGIR.** Pasó de "Proveedor: X" —que se lee como contexto— a **"Se va a guardar en la cuenta de X"**, y cuando el texto ya no coincide con el padrón se pinta en ámbar y el botón se nombra: **"✕ No es este"**. Eso cubre el gesto que lo anterior no puede: **seleccionar todo y escribir encima**, donde el campo nunca pasa por vacío.
   - **La medida de divergencia es la MÁS ESTRICTA posible** —cualquier diferencia que no sea acento, mayúscula o espacio— y se puede ser así de estricto **justamente porque no descarta nada**: el costo de un falso positivo es un cartel más llamativo, no un vínculo perdido.
   - **POR QUÉ NO SE SUELTA EL VÍNCULO POR FALTA DE PARECIDO, que es lo que hay que no "mejorar":** una regla capaz de soltar `FERRETERIA KM 711` → `FERRETERIA don pepe` **soltaría también** `Distribuidora del Centro SRL` → `DIST. CENTRO S.R.L.`, que es EXACTAMENTE el caso legítimo para el que existe este campo. Y ese falso positivo **pierde un vínculo correcto en silencio** — el mismo error que esto vino a arreglar, en la otra dirección. Medido al implementarlo: 14 gastos vinculados, los 14 con el texto idéntico al padrón, así que el ámbar no es ruido.
   - **El checkbox "Registrar también en la cuenta corriente" YA NO EXISTE.** El frontend llama a `registrar_pago_directo_proveedor` para todo gasto con `proveedor_id`, y **la base decide** si hay algo que registrar: con un ocasional no hace nada (ver esa RPC y el circuito). Fundamento de no dejarlo a criterio: un historial de proveedor incompleto es peor que no tenerlo.

   **CIRCUITO CON INGRESO Y CUENTAS CORRIENTES** (`f5cb513`, 21/09/2026).
   - **"¡Gasto guardado y registrado en cuenta corriente!" se dice SOLO si el proveedor tiene cuenta corriente.** Antes se decía siempre que la RPC no diera error, y con un ocasional —que ahora vuelve sin hacer nada— era falso. Por eso los dos `.select()` de `proveedores` traen `cuenta_corriente`.
   - **El aviso de después de guardar vive en la lista, no en un toast** (`#aviso-post-gasto`, hasta "Entendido"): si el espejo en CC falla dice que **el gasto quedó guardado**, el mensaje de la base **entero** y qué hacer ("no lo cargues de nuevo"). En un gasto de "Insumos - Materia Prima" que todavía no tiene su mercadería: con `materia_prima:cargar`, **"Ingresar la mercadería ahora"** (`materia-prima.html?desde_gasto=<id>`, con `encodeURIComponent` y comillas dobles); sin ese permiso, "Quedó pendiente de ingresar para el depósito".
     - **Si falta o no, lo dice `gastos_sin_ingreso()`**, que sabe de fechas, descartes y facturas de CC ya ingresadas. **Sin permiso para consultarla** se supone que falta si la fecha entra al circuito: puede avisar de más en un caso (el gasto pagó una factura de CC ya ingresada), nunca de menos.
   - **"Facturas ingresadas sin gasto"** (`ingresos_sin_gasto()`, con `gastos:ver_exportar`): "Cargar gasto" abre el wizard con proveedor, número, fecha, el importe que leyó el OCR de Ingreso, la categoría de materia prima, la unidad y **una copia** de la foto (se baja de la URL del ingreso y se sube al guardar, en la raíz `{uid}/` de Gastos: la del ingreso está bajo `mp/` y la policy no dejaría verla a quien mira gastos). Al guardar llama a `vincular_ingreso_a_gasto`.
     - **`estado.wizard.desdeIngreso` se limpia al ABRIR el wizard (`iniciarWizard`) y NO en `resetearWizard()`**: volver al paso 1 cambia la foto y los datos, pero la persona sigue cargando el gasto de esa mercadería.
     - **El flujo "enviar a pendiente de pago" no vincula**: no crea un gasto con el que vincular.

   **"SIN COMPROBANTE" NO PIDE NÚMERO NI TIPO DE DOCUMENTO** (`18d2765`). Sin papel no hay número, y la gente ponía ficticios ("1", "0") para llenar el campo — que además rompe el detector de duplicados: había 3 gastos con `numero_doc = "1"` del mismo proveedor. Un número VACÍO es seguro, porque `normalizar_numero_doc()` devuelve NULL y la RPC no matchea nada.
   - **El flujo no estaba marcado de NINGUNA manera**: el handler era una línea y llevaba al mismo lugar que el botón normal. Primero hubo que crear el estado: **`estado.wizard.sinComprobante`, un flag EXPLÍCITO**, que NO se deriva de `fotoArchivo === null` porque se puede volver al paso Foto y cargar una, y ahí los dos estados se contradicen. Lo escriben los dos botones del paso Foto y el `change` del input, más el reset.
   - `irASubpaso` **no tenía bloque de ENTRADA para `'datos'`** (el que existía es de `validarSubpaso` y valida la SALIDA). Se agregó, y oculta los dos grupos con `style.display`, el patrón que esa misma función ya usa. **Ocultar no alcanza**: lo tipeado o elegido ANTES de volver atrás se guardaría igual, así que se limpia el número y se resetea el tipo.
   - **Tipo de documento también se oculta**: "Factura A" / "Factura X" describen un comprobante que por definición no existe. `tipo_doc` es nullable y sin CHECK, y 24 gastos ya lo tenían en null.
   - **BUG LATENTE QUE DESTAPÓ:** `seleccionarTipoDoc` tenía el `querySelectorAll` **sin acotar**, así que tocaba los OCHO `.btn-tipo-doc` del archivo —los 3 de medio de pago incluidos—. Elegir un tipo de documento **deseleccionaba VISUALMENTE el medio de pago** mientras `medioPagoSeleccionado` seguía con su valor: la pantalla decía una cosa y la variable otra, y la validación pasaba igual. Va acotado a `#grilla-tipo-doc`, como `seleccionarMedioPago`, que ya lo hacía bien.

   **VOLVER AL PASO 1 RESETEA EL WIZARD ENTERO, Y ES LO ÚNICO QUE RESETEA** (`b1575cd`). El paso 1 es donde se define de dónde salen los datos —una foto, o "sin comprobante"—, así que cambiar esa respuesta invalida todo lo cargado. **Volver del 4 al 3 o del 3 al 2 NO borra nada**: ahí la persona corrige un campo puntual y perder el resto sería peor que el bug.
   - Los dos casos que lo motivaron: volver al paso Foto y tocar "Sin comprobante" dejaba los campos con los datos del comprobante anterior; y cargar una foto NUEVA reemplazaba lo que el OCR llena pero **dejaba pegado el medio de pago**, porque el OCR no lo toca.
   - `resetearWizard()` se extrajo de `iniciarWizard()`, que la reusa: un solo cuerpo, dos usos. **NO incluye la carga de maestros ni el poblado de selects** — eso es apertura, no reseteo.
   - **Tres cosas que se arrastraban y no estaban en el pedido:** `esPendiente` (si queda pegado en true, **el gasto se guarda como FACTURA PENDIENTE en vez de gasto**); `camposOcr` y las clases `.campo-ocr`, que dejaban campos marcados como "leído de la foto" sobre datos que ya no salen de ninguna foto; y `campo-proyecto`, que **se puebla UNA sola vez al abrir el wizard**, así que su valor elegido sobrevivía a un reseteo intermedio (`campo-empleado` y `campo-cuenta` no hacen falta: se repueblan al entrar a Detalles).
   - **"← Editar" del resumen va a `'detalles'`, NO al paso 1.** Era el tercer camino al paso 1, y con el reseteo ahí ese botón **habría borrado el gasto entero** justo cuando la persona quiere corregir un campo — lo contrario de lo que pide. Editar desde un resumen es volver a la última pantalla editable, no cargar otro comprobante. El breadcrumb NO es un camino al paso 1: sus ítems son solo `destino`, `vehiculo-destino` y `categoria`.
   - El reseteo PARCIAL del handler de `btn-cambiar-foto` **no se toca**: "cambiar la foto" y "volver al paso 1" son cosas distintas, y ahí limpiar solo lo que llenó el OCR es deliberado.

   **EL PASO DETALLES PERDIÓ "DESCRIPCIÓN"** (`79ffddf`). Se superponía con **"Descripción del ítem"**, no con Observaciones: `descripcion_item` se usa **94 de 100** veces y `descripcion` 29, y lo que se escribe en las dos es lo mismo — *"2 Oring bomba M1"*, *"Diésel viaje"*: QUÉ se compró.
   - **Lo histórico no se toca.** La columna `gastos.descripcion` queda, y los 29 registros que la tienen **se siguen viendo en el export a Excel y en el detalle**. `fila()` ya oculta los vacíos, así que un gasto nuevo no dibuja una fila muda.
   - **En el formulario de edición se sacaron el campo Y la clave del payload, JUNTOS.** Sacar solo el campo hace explotar la edición (`null.value`); y **cualquier variante que MANDE la clave —aunque sea null— borra los 29 históricos al primer guardado, sin ningún error**. Sacando los dos, el UPDATE no menciona la columna. Consecuencia aceptada: un gasto viejo con Descripción se sigue viendo pero ya no se puede corregir.
   - **"LUGAR / SERVICIO" SE QUEDA, y el pedido original decía lo contrario.** Los datos lo desmintieron: **41 de 100** usados, y **52% en septiembre contra 38% en agosto — la tasa SUBIÓ**. Se usa como **el nombre del comercio cuando no hay proveedor en el padrón** (*"Axion rosario"*, *"Ferrolux Rancagua y Diag. Ica"*), concentrado en Combustible (16/20) y Repuestos (6/6).

   **"PEDIR ALTA DE PROVEEDOR EN CUENTAS CORRIENTES"** (`1792b7d`) — el botón que abre el modal de alta, que **se mudó al paso Datos** cuando se sacó el buscador de Detalles. No podía irse sin reemplazo: es la única forma de dar de alta un proveedor desde el wizard, y en el flujo "pendiente" —donde el proveedor es OBLIGATORIO— sin él el flujo **no se puede completar** para un proveedor nuevo. Y ese es su lugar correcto y no un parche: **el modal ya se prellenaba con `campo-razon-social` y el CUIT del OCR**, o sea que el contexto que necesita siempre vivió en ese paso.
   - **APARECE SOLO CUANDO LA BÚSQUEDA NO ENCONTRÓ NADA**: campo vacío → oculto; con texto y coincidencias → oculto (que elija de la lista); **con texto y CERO → visible**; con proveedor ya vinculado → oculto. El vínculo se chequea PRIMERO: con proveedor elegido el texto puede diferir del padrón —para eso existen los dos campos— y la búsqueda daría cero sin que falte nada.
   - **No se duplica la búsqueda**: la función recibe la lista que ya calculó `filtrarProveedores()` en el listener.
   - **Y ESO ES LO QUE PERMITE QUE SEA UN BOTÓN LINDO Y VISIBLE.** Estuvo dos iteraciones como link apagado porque, estando SIEMPRE presente, invitaba al toque accidental — y el texto viejo, "+ Cargar proveedor nuevo", se leía como "guardar este dato del formulario" cuando lo que dispara es una **solicitud de alta que va a aprobación**. Ahora que aparece como respuesta a *"no lo encuentro"*, el riesgo desapareció.
   - **Turquesa**, el mismo tratamiento de `.btn-cuentas-corrientes`: en este proyecto ese color significa *"esto te lleva a Cuentas Corrientes"*, y el botón manda una solicitud justamente ahí. **NO ámbar**, que es el color propio de Caja.
   - **VA DEBAJO DEL CAMPO Y NO AL COSTADO, y eso se midió**: en un navegador a 375px con la fuente y los paddings reales del módulo, el ancho útil es **335px** y el botón mide **298** — al lado dejaría **29px de input**, que no es apretado, es inutilizable. Ningún texto más corto conserva las dos cosas que tiene que decir (que es un PEDIDO, y que va a CUENTAS CORRIENTES). La visibilidad condicional resuelve mejor el motivo por el que se había pedido al costado: ahí ocuparía lugar siempre, acá no existe hasta que hace falta.
   - Un detalle de orden: en `resetearWizard`, `limpiarMatchProveedor()` corre ANTES de vaciar los campos, así que el botón se calculaba contra el texto del gasto anterior y quedaba visible sobre un campo que un renglón más abajo se vacía. Se recalcula después del vaciado.

   **DETECTOR DE COMPROBANTES YA CARGADOS** (`6f40317`, `19b36b2`, `babb1fe`). Llama a `buscar_comprobante_cargado()` —la RPC que construyó el chat de Ingreso— y avisa si el mismo papel ya está en Ingreso, en Gastos o en Cuentas Corrientes. **NO BLOQUEA**: un bloqueo duro empuja a inventar un número para poder cargar, y eso es peor — el duplicado se ve, el número inventado no.
   - **CORRE AL TERMINAR EL OCR**, que es el momento natural: ahí ya hay número y razón social, y la persona todavía no invirtió tiempo en completar el wizard. También en las **dos salidas del paso Datos** —"Siguiente" y "Enviar a pendiente"—, porque corregir el número a mano tiene que volver a consultar. Las tres llamadas comparten la MISMA función.
   - **EL MATCH DE PROVEEDOR CORRE ANTES DE CONSULTAR, y eso es lo que hace que el detector sirva.** `intentarMatchAutomaticoProveedor()` corría recién al entrar a Detalles, o sea DESPUÉS de la consulta, así que el detector preguntaba con `proveedorSeleccionado` en null **incluso para los gastos que sí iban a matchear un proveedor**: se perdía la vía FUERTE y quedaba dependiendo de que el OCR hubiera leído la razón social igual que la vez anterior. Sigue corriendo también al entrar a Detalles: se AGREGÓ, no se movió. Efecto visible y buscado: el cartel del vínculo aparece apenas termina el OCR.
   - **TAMBIÉN EN LOS DOS FORMULARIOS DE EDICIÓN, desde `ce135b0` (17/09/2026).** Editar no crea un registro, pero sí puede cambiarle el número a uno que choca con OTRO. Hasta que la RPC sumó `p_excluir_modulo` / `p_excluir_id`, cada edición se habría detectado a sí misma, y por eso el detector vivía solo en el wizard. El formulario del gasto excluye `('gastos', id)` y el de la factura pendiente `('cuentas_corrientes', id)`; la llamada del wizard no cambió.
     - **CONSULTA SOLO CUANDO NÚMERO, RAZÓN SOCIAL O PROVEEDOR CAMBIAN respecto de lo guardado, NUNCA AL ABRIR** (decisión de Facu). Al abrir no hay nada nuevo que avisar —nadie tocó el número— y el aviso aparecería casi siempre: al 17/09/2026, **15 de los 20 gastos con proveedor tienen su factura espejo con el MISMO número**. Un aviso que aparece siempre deja de leerse. Si el valor vuelve al original, el aviso se limpia. En la factura, elegir un proveedor también dispara la consulta: `seleccionarProveedorEdicion()` la llama, porque elegir no dispara ningún evento del campo.
     - **ABIERTO A PROPÓSITO — el espejo propio puede aparecer, y NO es un bug.** Al editar un gasto con factura espejo, cambiarle la razón social sin tocar el número hace que la RPC encuentre esa factura (mismo proveedor y número). Taparlo exigiría que la RPC entienda la relación entre las dos filas. Si se cambia el número, el espejo no aparece: todavía tiene el viejo, porque se sincroniza al guardar.
     - **La lista de gastos no traía `proveedor_id` en su `.select()`**, y el formulario del gasto no tiene campo de proveedor: sin sumarlo, el detector habría consultado **siempre con `p_proveedor_id` en null**, perdiendo la vía fuerte **sin ninguna señal**. Es la trampa de "la columna no llega".
     - Mismas reglas que el wizard (parámetros por nombre, número crudo, sin caché, `try/catch` que no traba el formulario, importe null rechazado antes de convertir), más un **contador de turno** que descarta la respuesta que llega tarde o la de un formulario que ya se cerró. El aviso se arma en un solo lugar, `htmlAvisoDuplicado()`, para el wizard y los dos formularios.
   - **"Sin comprobante" no dispara**, con dos guardas: el flag explícito (intención) y un número sin dígitos (mecánica, el mismo criterio de `normalizar_numero_doc`).
   - **Tres cosas del traspaso de Ingreso, respetadas:** el `try/catch` alrededor de la llamada —sin él una caída de red deja el botón deshabilitado para siempre—; el importe rechaza `null`/`undefined`/`''` **antes** de convertir, porque la rama de `materia_prima` lo devuelve en NULL hardcodeado y `Number(null)` es 0, así que sin el guard todo duplicado de Ingreso mostraría **"$0"**; y **sin caché**, porque el resultado depende de lo que la persona acaba de tipear.
   - **EL MATCH SIN PROVEEDOR YA ESTÁ RESUELTO — acá decía "LIMITACIÓN CONOCIDA" y quedó viejo.** Sin proveedor vinculado —**91 de 105 gastos, el 87%**— la única vía es la razón social, y con `normalizar_texto()` la factura `0005-00022151` de ALOVERO GARCÍA se cargó dos veces sin aviso: el OCR leyó `ALOVERO GARCÍA - LO MAS SRL` y después `ALOVERO GARCÍA LO MAS SRL`, y **un guion tiraba abajo la vía de match**. Se arregló dentro de la RPC con `normalizar_razon_social()` — ver la sección de RPCs. Sigue descartado matchear **solo por número** sin proveedor: `0001-00000123` es comunísimo y convertiría el aviso en ruido permanente.
   - **Y `buscar_comprobante_cargado` ES DE ESTE MÓDULO, no de Cuentas Corrientes.** Se la trató como territorio ajeno un tiempo porque un traspaso lo dijo, y al verificarlo resultó que **ese chat ni la consume**: no tiene ninguna pantalla donde se tipee un número de comprobante, las facturas entran siempre por el wizard de Gastos. Ver el aprendizaje sobre la propiedad de un objeto compartido.

   **TODO EL TEXTO LIBRE SE ESCAPA** (`9a0e9a8`, 16/09/2026): razón social, lugar, observaciones y el resto de lo que tipea un usuario común entraban crudos a `innerHTML`, y los mira el super_admin que revisa gastos ajenos —en el listado, con solo entrar—. Las cuatro `fila()` y `poblarSelect()` escapan por dentro; `filaHtml()` es la variante para HTML crudo. Ver la regla en Aprendizajes clave.

   **LA FOTO DEL COMPROBANTE SE COMPRIME Y SE SUBE AL ELEGIRLA** (`b0d1375`, `4b677fa`, 16/09/2026).
   - **Compresión en el celular**, mismo patrón que Cobranzas: `createImageBitmap` con `imageOrientation: 'from-image'` (sin eso las fotos verticales llegan acostadas), lado largo de 2576 px sin agrandar, JPEG al 0,85. Los JPEG del bucket promediaban 3,4 MB. **Un PDF pasa sin tocar**, y si la compresión falla —un HEIC que el navegador no decodifica— se usa el original. El tope de 10 MB se mide sobre el archivo final.
   - **SE SUBE AL ELEGIRLA, NO AL GUARDAR, y el OCR recibe la RUTA** (ver `ocr-comprobante` en Arquitectura). Antes la foto viajaba entera en base64 al OCR y se volvía a subir al guardar: dos transferencias pesadas desde un celular de fábrica.
     - **Si la subida falla** (sin señal), no hay OCR: la pantalla lo dice, la foto sigue elegida y `fotoUrl` queda en null, así que `guardarGasto()` / `guardarFacturaPendiente()` **reintentan la subida** —la condición `fotoArchivo && !fotoUrl` ya existía—.
     - **`turnoFoto`** sube cada vez que se elige o se descarta una foto, y todo lo que termina después de un `await` lo compara. Sin eso, una subida o una lectura lenta de la foto anterior pisaría el formulario —o peor, **la ruta**— de la foto que está en pantalla, y el gasto se guardaría con el comprobante equivocado. El guard va **antes** de guardar la ruta.
   - **COSTO ACEPTADO — FOTOS HUÉRFANAS EN EL BUCKET. No es un bug.** Una foto elegida queda subida aunque la persona **abandone el wizard** o **la cambie por otra**: ningún gasto la referencia. Se decidió así (Facu, 16/09/2026) porque es lo que permite una sola transferencia pesada, y es el mismo costo que ya tienen Cobranzas e Ingreso. Son **invisibles para todos salvo para quien las subió** y **no se borran solas**: limpiarlas es el pendiente de fotos huérfanas de la sección Seguridad, que pide `service_role` y aprobación de Facu porque borrar archivos es destructivo.

   **CATEGORÍAS** (`bd7219c`): `ICONOS_CATEGORIA` mapea por NOMBRE, así que **se desincroniza en silencio** — una categoría sin entrada no rompe nada, solo cae al genérico `tag` y pierde su ícono. Hoy son **19 activas y 19 entradas, sin huérfanas**. `Insumos` se renombró a **`Insumos - Materia Prima`** (conserva `package`) y se sumó **`Préstamos`** con **`hand-coins`** — una mano entregando monedas, plata que SALE; se descartó `piggy-bank` porque una alcancía es guardar plata, lo opuesto a pagar una cuota, y `banknote` porque es "plata" a secas y todas las categorías son plata.

   - **Anulación de un gasto**: botón "Anular" en el detalle, con motivo obligatorio, que llama a `anular_gasto()` y devuelve la plata a Caja con un movimiento visible de tipo `'ingreso_reversion_gasto'` (salvo cheques, que tampoco la descuentan al cargarlos).
     - **Anular y Editar NO tienen el mismo gate, y esa diferencia es el punto.** Editar exige `gastos:editar_anular`; anular lo permite **el dueño del gasto O** quien tenga esa tarea. Así, alguien puede corregir un error propio sin que haya que darle permiso de edición libre sobre los gastos de todos — la anulación con motivo obligatorio deja rastro, la edición no.
     - Los gastos anulados **quedan visibles en la lista**, no se ocultan: atenuados, con el importe tachado y badge "Anulado". El detalle muestra motivo, quién anuló y cuándo. **No cuentan en el total de "Gastos de hoy/mes"** —la plata se devolvió— **pero sí en el contador de "Registros"**, porque el registro existe igual.
     - **TRES ARREGLOS DE BASE que la anulación necesitó** (verificados contra la base el 07/09/2026):
       - **`caja_movimientos_tipo_check` admite `'ingreso_reversion_gasto'`** — son OCHO tipos. Sin eso la devolución no entra.
       - **El índice único de `caja_movimientos` es por `(gasto_id, tipo)` y no por `gasto_id` solo** (`caja_movimientos_gasto_id_tipo_key`, parcial: `WHERE gasto_id IS NOT NULL`). Con la clave vieja, un gasto no podía tener a la vez su `egreso_gasto` y su `ingreso_reversion_gasto` — o sea que **la devolución chocaba contra el egreso original** y la anulación no se podía completar.
       - **`facturas_pendientes.origen_gasto_id`** distingue la factura que el gasto **CREÓ** —el espejo de `registrar_pago_directo_proveedor`— de las que ese gasto vino a **PAGAR**. Sin esa distinción, anular un pago no sabía cuál revertir y cuál borrar. Hoy 15 de 32 facturas la tienen.
2. **Caja** (`modulos/caja.html`): cuentas de efectivo/banco por persona, ingresos/egresos/traspasos/retiros, transferencias entre personas (requieren al menos un `super_admin` en la operación, salvo que quien la crea tenga `caja:transferir_entre_personas` — ver esa tarea y la excepción de la Cuenta de Empresa en la sección Sistema de permisos), vista "Retiros socios" (gateada por la tarea `caja:retiros_todos`) y "Todos los movimientos" (gateada por `caja:movimientos_todos`)
   - El selector de contraparte excluye a quien tenga `empleados.oculto_como_contraparte = true` — ver el detalle de esa columna en la sección de tablas.
   - **TODO EL TEXTO LIBRE SE ESCAPA** (`a3fd993`, 17/09/2026). Descripciones de movimientos y solicitudes, nombres de cuenta y de personas, la razón social y la categoría de los gastos y los nombres de unidad entraban crudos a `innerHTML`; los escribe cualquier usuario y los lee la contraparte, muchas veces un super_admin que tiene que aceptar. Ver la regla en Aprendizajes clave.
     - `esc()` es el de `gastos.html`. **`opcionesCuenta()` y las opciones del multiselect escapan por dentro**: los call sites les pasan los datos crudos.
     - **`etiquetaMovimiento()` queda en TEXTO PLANO** porque también alimenta el export a Excel; se escapa su RESULTADO donde entra a HTML (trae el nombre de la contraparte en "Entregado a …").
     - **`formatearImporte()` queda en TEXTO PLANO y todo importe en HTML va por `importeHtml()`**, igual que en Cuentas Corrientes. `formatearImporteCentavosSuaves()` escapa la moneda por dentro. La moneda ya tiene CHECK en la base, así que esto es consistencia y no un vector: hay un solo lugar que decide cómo entra un importe a la página.
     - **El `href` al gasto es seguro por `encodeURIComponent` + comillas DOBLES** en el atributo, con el mismo aviso que en Cuentas Corrientes.
3. **Cuentas Corrientes** (`modulos/cuentas-corrientes.html`) — **circuito con Ingreso** (`acae755`, 21/09/2026): las descargas sin importe se ven en rojo como "Falta importe", con las cantidades del ingreso vinculado y, con `cuentas_corrientes:registrar_pago`, **"Cargar importe"** (el total, o el precio por unidad que la pantalla multiplica —**solo con un producto**; con varios, el total—; **el precio por unidad admite hasta 4 decimales** (0,3333) y el total que se guarda se redondea a 2: 0,333 × 3.000 = 999,00) → `completar_importe_factura` con el TOTAL. Como ninguna vista de saldo las suma, **la lista, el resumen, el padrón y el banner de la ficha dicen "+ N descargas sin importe" en rojo**, un proveedor con **solo** descargas aparece igual en la lista de trabajo, y **sin saldo conocido se muestra "—" y no "$ 0,00"**. La ficha suma **"Remitos sin facturar"** (`remitos_sin_facturar`, informativo, no suma). El historial y su Excel ya no convierten un monto null en 0.
   - **LAS CANTIDADES NECESITAN PERMISO DE INGRESO:** salen de `materia_prima_ingresos` + `materia_prima_items`, cuyo RLS pide `materia_prima:ver_todo` (o ser quien lo cargó). Un administrador de CC sin esa tarea ve la fila con el aviso de que no puede ver las cantidades, y **solo puede cargar el total**. Resolverlo es una RPC `SECURITY DEFINER` nueva, que no existe.
   - Lo que ya estaba: saldo por proveedor (deuda o crédito, nunca ambos mostrados a la vez — prioridad a la deuda), historial de facturas, registro de pagos con sugerencia FIFO editable, aplicación manual de créditos a favor, aprobación de proveedores nuevos, ver/editar/eliminar una factura (eliminar = `anular_factura_pendiente`, nunca un DELETE real). Además: intereses por mora (monto exacto o calculado por tasa mensual/anual), desglose de cada pago (a qué facturas se aplicó y qué pasó con el sobrante hasta hoy), badge de estado por factura (Pendiente/Parcial/Pagada) en las dos pantallas, filtros y orden en Movimientos, alta manual de proveedor e importación masiva por Excel.
   - **Tiene 3 vistas, no 2** (drill-down por `hidden`, no pestañas):
     - **Pestaña "Proveedores"** — la lista de TRABAJO: solo los que tienen saldo distinto de cero, deuda o crédito. Es a propósito: responde "a quién le debo o quién me debe", no es el padrón.
     - **Padrón completo** (botón "Ver todos los proveedores") — TODOS los proveedores activos y aprobados (`activo=true` Y `estado_alta='activo'`), incluidos los que están en $0 y los que nunca tuvieron un movimiento. Los **pendientes de aprobación NO aparecen acá**: se ven en la pestaña "Pendientes de aceptación", que es donde se aprueban. Muestra razón social, CUIT, dirección y el saldo **neto por moneda, agregado entre todas las unidades de negocio** (la ficha sigue mostrando el detalle por unidad). El buscador compara el CUIT **por dígitos de los dos lados**, así "30-123…" encuentra al guardado como "30123…". Desde acá se edita cada proveedor (`editar_proveedor`) y se entra a su ficha.
     - **Ficha de un proveedor** — la cuenta corriente se lleva SEPARADA por unidad de negocio (decisión de negocio, no técnica: la plata de un pago sale de una empresa concreta, y consolidar haría que una empresa termine pagando deuda de otra). Pero la ficha se puede MIRAR consolidada: se entra siempre en "Todas las unidades" (ya no se pregunta al entrar) y hay un filtro de unidad adentro.
       - En modo **"Todas las unidades"**: el banner muestra el saldo desglosado por unidad (con el nombre de la empresa más destacado que la moneda), cada movimiento muestra a qué unidad pertenece, y se **OCULTA la línea de saldo corrido por fila** — `saldo_acumulado` viene de la vista particionado por proveedor + unidad + moneda, así que con las unidades mezcladas cada fila trae el saldo de SU unidad y verlas intercaladas daría una secuencia que no cierra. "Registrar pago" y "Aplicar crédito" quedan **DESHABILITADOS (no escondidos)** con el motivo escrito al lado, más un `return` temprano en sus funciones como guarda de fondo: `unidadId` viaja hasta el `p_unidad_negocio_id` de las RPCs, y un pago con unidad nula sería plata mal atribuida.
       - Con **una unidad elegida**: todo habilitado, saldo corrido visible, igual que siempre.
       - Si el proveedor no tiene ningún movimiento, la ficha **no se abre** y avisa. El deep link `?proveedor=X` sin `&unidad=` abre en "Todas".
   - Gates de permiso de estas pantallas: "Ver todos los proveedores" con `cuentas_corrientes:ver_todo` (muestra saldos de toda la empresa); editar un proveedor con `cuentas_corrientes:alta_proveedor`, la misma tarea que crear uno.
   - **TODO EL TEXTO LIBRE SE ESCAPA** (`1664384` y `6f09e8e`, 16/09/2026). Razón social, número de comprobante, CUIT, dirección, nombre de fantasía, nombres de cuenta y de unidad, y la moneda entraban crudos a `innerHTML`; los escribe cualquier usuario (proveedores pendientes, el wizard de Gastos, sus cuentas de caja) y los mira quien aprueba o tiene `ver_todo`, varios en listados que se dibujan con solo entrar. Ver la regla en Aprendizajes clave.
     - `esc()` es el de `gastos.html`. `poblarSelect()` escapa por dentro `placeholder`, `value` y `label`, y `formatearImporteCentavosSuaves()` escapa la moneda por dentro (devuelve HTML).
     - **`formatearImporte()` queda en TEXTO PLANO a propósito, y no hay que "corregirlo":** también se usa con `textContent` y en el label que va a `poblarSelect()`, así que escapar ahí mostraría `&amp;` literal. Para HTML está **`importeHtml()`** = `esc(formatearImporte())`, y **todo importe que entra a HTML va por ella**. Cuando se escribió, la moneda era texto libre en la base; desde el 17/09/2026 tiene CHECK (ver la regla), así que hoy la razón es tener un solo lugar que decide cómo entra un importe a HTML.
     - **Los `href` a `gastos.html` son seguros por `encodeURIComponent` + comillas DOBLES en el atributo.** `encodeURIComponent` NO escapa la comilla simple: cambiar el delimitador a comillas simples —algo que parece cosmético— tira la defensa. Lo dice un comentario en el código y lo verifica la prueba.
   - **De qué cuenta sale la plata** — el selector del modal "Registrar pago" muestra dos grupos: "Mis cuentas" (las de quien está registrando el pago, siempre) y "Cuenta de Empresa" (las 9 cuentas del empleado ficticio con `tipo='empresa'`), este último SOLO si tiene la tarea `gastos:gastos_empresa`. Antes estaba hardcodeado a las cuentas de Pablo, que era la regla cuando había una sola caja general; con la Cuenta de Empresa eso quedó viejo y provocaba que, por ejemplo, al elegir "Transferencia" no apareciera ninguna cuenta (Pablo solo tiene una de efectivo). El id de Empresa NO se hardcodea: sale de `v_empleados_publico` filtrando por `tipo='empresa'` — tiene que ser esa vista y no `empleados`, porque el RLS de `empleados` no deja a un usuario común leer esa fila y el grupo no aparecería nunca. Cuando ningún grupo tiene cuentas para el medio de pago y la moneda elegidos, se muestra un aviso explicando por qué, en vez de un selector vacío y mudo.
     - El `p_empleado_id` que va a la RPC es el del usuario que registra el pago (antes iba Pablo fijo, así que todos los pagos quedaban a su nombre sin importar quién los hacía). **Ojo: eso define solo a quién se ATRIBUYE el gasto** — de qué caja sale la plata lo decide el trigger `fn_sincronizar_caja_gasto`, que descuenta al DUEÑO DE LA CUENTA elegida, no a `gastos.empleado_id`.
4. **Accesos** (`modulos/accesos.html`, soloSuperAdmin): aprobación de solicitudes de registro y gestión de módulos + tareas granulares por usuario
5. **Empleados** (`modulos/empleados.html`): directorio agrupado por unidad de negocio, ficha con datos de Naaloo, importación de Excel de Naaloo
   - **TODO EL TEXTO LIBRE SE ESCAPA** (`3ee44c5`, 17/09/2026). Nombre, puesto, domicilio, teléfono, legajo, CUIL y contacto de emergencia los carga la propia persona o vienen del Excel de Naaloo, y los mira quien tiene `empleados:ver_editar`; el listado se dibuja con solo entrar. Ver la regla en Aprendizajes clave.
     - **Los formularios de edición precargan con `value="${esc(...)}"`**: en un atributo alcanza una comilla para cerrarlo, así que escapar solo el contenido de los `<span>` no alcanzaba.
     - **`iniciales()` y `formatearCuil()` devuelven texto crudo** —`formatearCuil` devuelve el CUIL tal cual si no tiene 11 dígitos— y se escapa su RESULTADO.
     - **El chip de módulo escapa también el fallback**: `empleado_modulos.modulo` es texto sin CHECK ni FK (verificado el 17/09/2026), así que un módulo desconocido se muestra crudo si no se escapa.
   - **PIN DE PRODUCCIÓN EN LA FICHA** (`071bf64`, 23/09/2026, parte 6 del rediseño de Producción). En la ficha de cada persona, una sección **"PIN de producción"** con su estado —**"Sin PIN"** / **"PIN pendiente de cambiar"** / **"PIN propio"**— y, si corresponde, **"Asignar PIN"** o **"Resetear PIN"**.
     - Sale de **`estado_pin_produccion(p_empleado_id)`**, que devuelve `{tiene_pin, debe_cambiar, puede_asignar}` **o `null`**. **`null` = quien mira no tiene ni `produccion:configurar` ni `empleados:ver_editar`, y entonces la sección NO se dibuja.** Y **`puede_asignar` es `produccion:configurar`**, más restrictivo que ver la sección: alguien con solo `empleados:ver_editar` la ve pero no puede tocar nada. **El gate lo pone la RPC, no la pantalla.**
     - Asignar y resetear **son la misma llamada**, `asignar_pin_produccion(p_empleado_id, p_pin)`: hace upsert con `debe_cambiar = true` y `expira_en = null`, y resetea intentos y bloqueo. Por eso el botón solo cambia de rótulo según `tiene_pin`.
     - **El PIN es un IDENTIFICADOR y va como texto**, sin `enlazarCampoNumero` ni separador de miles. **El cliente valida solo el largo (4 dígitos) y NO replica la lista de PINes obvios** (`0000`…`9999`, `1234`, `4321`, `0123`, `9876`) que ya tiene `_pin_valido` en la base: una segunda copia se desincroniza en silencio, y el mensaje de la base ya está escrito para una persona. Hay un test que manda `1234` a propósito y verifica que llegue a la base.
     - **Si la RPC falla se dice "No se pudo leer el estado del PIN", nunca un "Sin PIN" inventado** — que sería falso y llevaría a asignar un PIN que ya existe. El caso "no tenés permiso" llega como `null`, no como error, así que los dos no se confunden. Mientras carga no se dibuja nada, y hay un **turno por `empleadoId`** para que una respuesta que llega tarde no pise la ficha abierta.
     - **Esto crea una dependencia cruzada que antes no existía: `empleados.html` consume dos RPCs de Producción.** Si `estado_pin_produccion` o `asignar_pin_produccion` cambian de firma, esta pantalla se entera. **No hace falta ninguna tarea nueva**: usa `produccion:configurar` y `empleados:ver_editar`, que ya están en el CHECK.
     - Suites `pruebas/test-empleados-pin.js` y `pruebas/mut-empleados-pin.js` (69/69 y 23/23); `test-empleados-xss.js` pasó de 110 a 115.
6. **Ingreso — Insumos / Materia Prima** (`modulos/materia-prima.html`, tile "Ingreso", clave `materia-prima`): listado de ingresos con detalle en modal, y **wizard de carga completo**. El catálogo y el stock se mudan a un módulo aparte todavía no construido — este módulo no los muestra. Sin precios ni importes por decisión de diseño (ver sección de tablas), **con la excepción acotada del "Total de la factura" del circuito**.

   **CIRCUITO CON GASTOS Y CUENTAS CORRIENTES** (`6edcdaf`, 21/09/2026). Ver la sección del circuito en las tablas.
   - **El ingreso se guarda PRIMERO y la RPC va después.** `pasarAlCircuito()` **no tira nunca** —devuelve el error como resultado—, porque el `catch` de `confirmarIngreso()` **borra el ingreso** si algo tira adentro: un vínculo fallido se reintenta; una carga de mercadería perdida, no. Tiene assertion y mutación.
   - Factura o sin comprobante → `registrar_factura_de_ingreso`; desde un gasto → **`vincular_ingreso_a_gasto`, y no la otra**; remito → nada, como siempre. El resultado se dice en el detalle que se abre al guardar, en palabras, y el error de la RPC **tal cual**.
   - **"Total de la factura"** solo si el proveedor identificado **tiene cuenta corriente** (`cargarProveedores()` trae `cuenta_corriente`) y el ingreso no viene de un gasto: con un ocasional o uno nuevo la RPC lo ignora. Prellenado con el `importe_total` del OCR, editable, obligatorio cuando se pide. **La moneda va siempre `ARS`**: el OCR de Ingreso no lee moneda.
   - **"Mercadería que ya estaba (se contó antes): no suma stock"** — solo en facturas, con motivo obligatorio y el aviso de que no se puede cambiar. Va en `sin_stock_motivo` al insertar.
     - **Desde el 22/09/2026 la casilla vive en el paso DATOS, no en Confirmar** (opción A del traspaso de la parte 10c): decide si los renglones piden lote, así que hay que saberlo ANTES de cargarlos. La dibuja `renderizarBloqueSinStock()` al entrar a Datos y al cambiar el tipo de documento; el motivo se pide al salir de Datos y se revalida al confirmar.
     - **Con la casilla marcada el lote es OPCIONAL**: la tarjeta abierta dice OPCIONAL en vez de OBLIGATORIO (el campo y "Lote ilegible" siguen, nada se marca solo), la plegada y el detalle del ingreso **no muestran el chip "Sin lote"**, y el payload manda el lote como se tipeó (`null` si no hay, `lote_ilegible: false`). Un solo helper, `ingresoNoSumaStock(w)` (factura + casilla), lo usan la validación, las tarjetas y el payload de la cabecera. Al cambiar la casilla las tarjetas se redibujan, y al desmarcarla `validarItems()` vuelve a exigir lote. Suite `pruebas/test-materia-prima-sin-stock.js` (+ mut).
   - **El detalle dice el estado del circuito de cada comprobante** (en CC, vinculado a un gasto, falta el gasto de un ocasional, sin proveedor) y, si quedó sin vincular, ofrece **"Pasar a cuenta corriente"** —o **"Buscar el gasto de nuevo"** con un ocasional—, que reintenta la misma RPC (idempotente). **Solo desde `fecha_inicio_circuito_stock()`**: los ingresos históricos no se llenan de botones.
   - **"Pagado sin ingresar"** (`gastos_sin_ingreso()`): **"Ingresar"** abre el wizard con la foto del gasto (la baja de Storage; si la policy no deja —la raíz de otra persona pide `gastos:ver_exportar` o `cuentas_corrientes:ver_todo`— se pide una foto del papel), su proveedor, su número y su unidad. **"No lleva ingreso"** pide motivo y llama a `descartar_ingreso_de_gasto` (con `materia_prima:editar_anular` o `gastos:editar_anular`). **`?desde_gasto=<id>`** abre el mismo flujo. `estado.wizard.desdeGasto` **sobrevive a `reiniciarLecturaWizard()`**: de qué gasto viene es una decisión de la persona, no un derivado de la foto.
   - **Se escaparon, de paso, textos que entraban crudos** en la fila del listado (título, iniciales, meta) y en el detalle (número, fantasía, lote, nombre y marca del insumo, el `href` de la foto). **El barrido completo del archivo sigue pendiente**: esto cubre solo lo que se tocó.
   - **Wizard, 4 pasos**: comprobante (foto → OCR vía `ocr-materia-prima`, o "sin comprobante") → encabezado → vínculo con remito, solo si es factura → ítems → confirmación. El paso del vínculo **se saltea solo** cuando no hay remitos vinculables, para no preguntar algo que no tiene respuesta posible.
     - **El paso del vínculo es de selección MÚLTIPLE y YA NO SALTEA el paso de ítems.** Antes, con remito vinculado la factura no cargaba ítems y se iba derecho a confirmar; con el vínculo por renglón eso dejó de tener sentido, porque una factura vinculada puede traer mercadería nueva además de la que ya entró.
   - **Ítems**: los que devuelve el OCR se matchean contra el catálogo por nombre + marca normalizados (minúsculas, espacios colapsados). Los que no matchean se crean al vuelo con badge NUEVO y selector de tipo, siempre en `pendiente_revision`. El lote se tipea a mano y es obligatorio solo para `tipo='materia_prima'` que además sume stock.
     - **NO hay default de tipo.** Si el OCR sugiere uno, queda preseleccionado y marcado como sugerencia; si no, **no hay ninguna opción marcada y no se puede avanzar sin elegir**. El default silencioso anterior (`materia_prima`) hacía que un toque accidental quedara guardado EN EL CATÁLOGO y se propagara: ese producto no volvía a pedir lote nunca más, y el trigger solo valida al insertar, así que corregir el tipo después no re-valida lo ya cargado.
     - **Marca de "sin revisar"**: cuando un ítem matchea contra un insumo con `estado_alta='pendiente_revision'`, un aviso ámbar dice cómo quedó clasificado, qué consecuencia tiene ahí (pide o no pide lote) y dónde se corrige. Esa clasificación **no la confirmó nadie** —varias vienen del default viejo— y el wizard la trataba igual que una aprobada. Es informativo: `insumos` no tiene policy de UPDATE para el cliente.
   - **VÍNCULO FACTURA↔REMITO POR RENGLÓN — el cambio conceptual más grande del módulo.** El caso que lo motivó: llega una factura por 100 cajas + 5.000 bolsas; las cajas ya entraron con un remito y las bolsas llegan recién ahora. A nivel documento no había opción correcta —si la factura no sumaba, las bolsas nunca entraban; si sumaba, las cajas entraban dos veces—, así que la decisión bajó al renglón.
     - Cada tarjeta de ítem lleva la casilla **"Esta mercadería ya entró con un remito"**, SOLO en facturas. Marcada: la fila va atenuada, con chip "No suma stock", no pide lote, y un aviso arriba cuenta cuántos renglones no suman.
     - Los ítems de los remitos vinculados **premarcan** las casillas que coinciden, con marca de sugerencia y sin pisar lo que la persona ya decidió. Sin match, sin marcar: **marcar de más es mercadería que nunca entra al stock**.
   - **LA REGLA DE FUSIÓN DEL LISTADO CAMBIÓ, y ahora es SEMÁNTICA y no estructural: se fusiona la factura con su remito SOLO si la factura no suma nada (todos sus renglones marcados) Y apunta a un único remito visible.** Una factura con mercadería nueva **es una llegada** y lleva fila propia, con chip "Vinculada a N remitos". El comentario viejo de `fusionarEntregas` —"la factura vinculada tiene 0 ítems a propósito"— **ya no vale**: era la premisa de la regla anterior y el vínculo por renglón la mató. Con los datos existentes la regla nueva da exactamente el mismo resultado que la vieja.
   - **ALIAS DE PROVEEDOR — la libretita de traducción.** Corrugadora escribe "CAJA 4 PISOS C/IMPRESION" y otro proveedor escribe "CAJA N°1 NUSS C/IMP": las dos son el mismo insumo, y el matcheo por nombre+marca exactos nunca acierta. La primera vez la persona lo mapea y el sistema se lo guarda en `proveedor_insumo_alias`; la segunda ya viene resuelto.
     - **Orden de resolución de un renglón: alias del proveedor → nombre+marca contra el catálogo → producto nuevo.** Sin proveedor identificado (`sin_comprobante`, o el proveedor no matcheó) no hay alias posible: ni se busca ni se guarda. Los alias se cargan **también en remitos** — un remito también tiene proveedor.
     - **NUNCA se aplica callado**: el renglón resuelto por alias lo dice, muestra el texto del papel y ofrece Corregir. Y **muestra el nombre DEL CATÁLOGO y no el del papel**: sin ver a qué producto se mapeó, la marca no serviría para detectar un alias mal cargado, que es justo para lo que existe. Un alias mal mapeado se propaga en silencio a todas las cargas futuras de ese proveedor.
     - **INSERT-ONLY desde el wizard, y es decisión de diseño, no una limitación técnica:** si el texto ya está mapeado a otro insumo, el ingreso se guarda como eligió la persona pero el alias NO se pisa, y se avisa **a qué producto está mapeado hoy** (sin eso no hay forma de saber si el viejo está bien). Que alguien que carga apurado pueda re-apuntar en silencio un mapeo existente es peor que no poder hacerlo. Corregirlo es territorio de Stock, con `stock:gestionar_catalogo` — la policy de UPDATE dice lo mismo.
     - El texto que se guarda es **`descripcion_literal`**, la transcripción del papel — nunca el nombre editado ni el resumen del OCR. **Y se BUSCA con el mismo campo**: si una punta usara `descripcion` y la otra `descripcion_literal`, la libretita guardaría con una clave y buscaría con otra, no acertaría jamás, y encima parecería que aprende.
     - **En el ítem hay TRES piezas de texto, y ninguna puede colapsar con otra:** `nombre` (editable, prellena y **va al catálogo**), `nombreOcr` (el limpio, que repone el botón "Corregir") y `textoOriginal` (el literal, para el alias). Reponer el literal en `nombre` llenaría el catálogo de entradas como "HARINA 000 JUPITER x 25 Kg. (NARANJA)" — el mismo problema entrando por la otra puerta.
     - Al desplegar `descripcion_literal` **los alias ya guardados dejaron de acertar**: se habían guardado con el texto limpio. No se migraron porque los datos de `materia_prima_*` son de prueba.
   - **UNIDADES: `lt` y NO `l`** (`7733709`). `UNIDADES_MEDIDA` del wizard y el prompt del OCR decían `'l'`, pero el catálogo real usa `'lt'` (verificado: 14 en kg, 5 en lt, 32 en un, **cero en `l`**). Cada líquido creado desde el wizard nacía con una unidad distinta de los que ya existían y **el stock de ese insumo se partía en dos**. Todavía no había mordido: ningún insumo tenía el mismo nombre en dos unidades.
     - Además hay **`normalizarUnidad()` al consumir el OCR**: si llega algo fuera de la lista cae a `'kg'`. Sin eso, el estado quedaba en `'l'` pero el `<select>` ya no tenía esa opción y mostraba la **primera** — la persona veía "kg" y el insumo se creaba en "l". Cubre la ventana entre el push del frontend y el deploy de la función, que **van por separado**, y también el hecho de que la Edge Function NO valida `unidad_medida` contra ninguna lista (a diferencia de `tipo_sugerido`).
   - **Buscador de catálogo en la tarjeta del ítem** (agregado con los alias). Son las DOS salidas de un renglón sin resolver —elegir del catálogo o crear un producto nuevo— y hasta ahora solo existía la segunda. Sin buscador, mapear a mano era imposible en la práctica: nadie adivina "Caja N°1 NUSS" partiendo de "CAJA 4 PISOS C/IMPRESION". Sirve para cualquier renglón, no solo para el flujo de alias.
   - **SELECTOR DE CATEGORÍA AL CREAR UN PRODUCTO NUEVO** (`f63b552`, septiembre 2026). El caso: se cargó un remito con "Harina Wali 000" y quedó con `categoria` en null, mientras las otras tres harinas del catálogo tienen `'Harinas'` — en Stock aparecía bajo "sin categoría", separada de las suyas. La causa: `insumos.categoria` la agregó el módulo de Stock y el wizard no la conocía, así que **TODO producto creado al vuelo desde acá quedaba sin categoría**. No era un error de quien cargó: la pantalla nunca se lo preguntó.
     - **ES OBLIGATORIA EN EL WIZARD, aunque la columna sea OPCIONAL por diseño** —`chk_categoria` acepta null, y ese null cae en "Sin categoría" al final del listado de Stock—. **Es una decisión tomada, no un descuido**, y está **discutida con el chat de Stock, que recomendaba lo contrario**.
       - El motivo: **quien recibe el camión tiene el producto en la mano y sabe qué es**. Un producto sin categoría queda al final del listado esperando una revisión que puede no llegar.
       - Y **el costo de equivocarse es asimétrico**: elegir mal se corrige en un toque desde el catálogo de Stock; no elegir deja el producto fuera de su grupo **en la pantalla que se mira todos los días**, y a "sin categoría" nadie va a buscar.
     - **ASÍ QUE EL WIZARD ES MÁS ESTRICTO QUE LA PANTALLA DE ADMINISTRACIÓN, A PROPÓSITO. NO lo "corrijas" aflojando este lado por simetría.** En Stock la categoría es opcional porque esa es la pantalla de **curación** y tiene que poder **DESclasificar** algo mal puesto —obligatoria lo haría imposible, el mismo argumento por el que `editar_insumo` la asigna directo y no con `coalesce`—. Acá el producto se crea **al vuelo**, y lo que no se clasifica en ese momento no lo mira nadie nunca más. Cada lado es estricto donde le corresponde: lo que parece una inconsistencia es la respuesta a dos preguntas distintas, y está escrito así en el código de los dos archivos.
     - **LISTA CERRADA Y NO TEXTO LIBRE:** el CHECK admite NULL o uno de 11 valores exactos, así que un texto libre **violaría el constraint y el insert se caería AL CONFIRMAR, con todos los renglones ya cargados**. Agregar una categoría es un `DROP` + `ADD` del CHECK, deliberado a propósito y territorio de Stock — la pantalla lo dice.
     - **LAS OPCIONES SALEN DE LA BASE Y NO DE UNA LISTA HARDCODEADA, y con lista cerrada ese argumento es MÁS fuerte, no más débil:** todo valor que exista en `insumos.categoria` **pasó el CHECK por definición**, así que es imposible ofrecer uno que el insert rechace. Hardcodear tiene el riesgo inverso y peor — si Stock saca un valor del CHECK, el wizard lo seguiría ofreciendo. Se derivan del catálogo **que ya está en memoria** (cero consultas nuevas), que se carga sin filtrar por `activo` ni `estado_alta`. El orden lo pone un array local con el criterio del negocio, y **lo que no esté en él cae al FINAL sin romper nada**: el CHECK puede ganar un valor antes que esta pantalla.
     - **EL GUARD DE LISTA VACÍA — degrada, NO bloquea.** Si las categorías no llegan a cargarse no se dibuja un selector mudo: **se avisa y el producto entra sin categoría**. Una lista vacía solo puede venir de un fallo al cargar el catálogo, y trabar la carga de mercadería por un problema de infraestructura es el mismo error ya escrito para el detector de duplicados — *un detector que rompe la carga es peor que no tener detector*.
       - **La validación mira que HAYA categorías para elegir, NO que el campo esté en null.** Si mirara el null, ese mismo fallo de carga **trabaría el wizard entero**. Tiene mutación propia.
     - Un ítem que **matchea contra un insumo existente no pregunta nada**: la categoría sale del catálogo, igual que el tipo. Y la policy de INSERT no se toca — su `WITH CHECK` es `tiene_tarea('materia_prima','cargar') AND estado_alta = 'pendiente_revision'` y no menciona la columna.
   - **REDISEÑO DEL PASO 3 (`082a27a` → `7a03965`, septiembre 2026).** Salió de cargar dos facturas reales en fábrica. Lo que estaba mal de fondo: **el número del comprobante a veces son BULTOS y a veces son UNIDADES** —"250" de harina son 250 bolsas de 25 kg, "2.000" bolsitas vinieron en 8 rollos— y la pantalla no lo preguntaba: ponía dos campos vacíos y quien cargaba elegía mal la mitad de las veces.
     - **El total en unidad base salía bien igual, así que nada se quejaba**: la multiplicación cierra con los factores dados vuelta. Lo que quedaba falso era el DESGLOSE, y de ahí sale el "N bultos" que muestra Stock. Verificado en la base: la bolsa 100x80 quedó "250 bultos de 8" cuando son 8 de 250, la cinta "36 bultos de 2" cuando son 2 cajas de 36. **No hay riesgo de descuadre —los kilos siempre estuvieron bien— es un problema de calidad del dato.**
     - **LA PREGUNTA.** Cada renglón declara qué es el número del papel, y de ahí se deriva el resto. `interpretacion` se guarda en la fila.
       - **son bultos** → se pide el contenido; total = número × contenido
       - **son unidades** → se pide en cuántos bultos vino; contenido = número ÷ bultos
       - **a granel / un solo envase** → no se pide nada y el par queda en **NULL**
     - **UN CAMPO, UN SIGNIFICADO — `bultosPapel` vs `bultos`** (`341937d`, septiembre 2026). **`bultosPapel`** es el que se tipea en modo `'unidades'` y describe el **COMPROBANTE**; **`bultos`** es SIEMPRE derivado, describe lo **RECIBIDO**, y es el que viaja al payload. Sin recepción parcial valen lo mismo.
       - **Antes `item.bultos` significaba las dos cosas según el modo:** en `'bultos'` `derivarCantidades` lo pisa con lo recibido, y en `'unidades'` era lo tipeado, o sea el papel. El payload mandaba siempre el mismo campo, así que con recepción parcial la fila decía "recibí 298" junto a "25 bultos de 12" —que da 300— y violaba `chk_presentacion_coherente` **al confirmar, con los cinco renglones ya cargados**.
       - **CUALQUIER recepción parcial en modo `'unidades'` era imposible de guardar**, incluso una que cerrara redondo: no dependía de los decimales ni del arrastre de punto flotante. Verificado contra el código anterior.
     - **LO RECIBIDO PUEDE NO TENER PRESENTACIÓN.** Si no entra en un número entero de bultos —298 unidades de a 12 son 24 bultos y 10 sueltas— entonces ninguna presentación lo describe, y el par `cantidad_bultos` / `contenido_por_bulto` va en **NULL, los DOS juntos**, que es lo que el CHECK acepta y lo mismo que ya se hace a granel. La cantidad y `cantidad_documento` quedan igual, y **la fila lo dice**: no es una pérdida silenciosa.
       - **Lo garantiza `bultosEnteros()` y NO `dividirExacto()`** (`4589159`). Esta entrada describió durante un rato un comportamiento que el código no tenía: `dividirExacto` verifica que la división sea **EXACTA, no que dé un ENTERO**, así que 1998 / 250 = 7,992 pasaba —es exacto— y la fila cerraba contra el CHECK (1998 = 7,992 × 250) y se guardaba. Stock iba a mostrar **"7,992 bultos"**. No rompía nada: era dato basura, que es justo el problema que este rediseño vino a sacar.
       - Adentro de `bultosEnteros` va `dividirExacto` y **no una división pelada**, y eso importa con contenido decimal: `0,3 / 0,1` da `2.9999999999999996` en float y `Number.isInteger` lo rechazaría, perdiendo la presentación de un caso válido.
     - **PERO LO QUE SE MUESTRA NO ES "SIN PRESENTACIÓN": ES EL BULTO INCOMPLETO** (`4943248`). Si de 8 bultos de 250 llegaron 1.985, lo que pasó en el depósito es que **llegaron 7 bultos completos y uno abierto con 235** — se rompió o se mojó parte de uno. Decir "sin presentación" era una renuncia; el desglose es información que sirve para ir a mirar.
       - Se deriva de `contenido_documento` con `Math.floor` y el resto: `desgloseIncompleto()`. Casos: sin ningún completo dice **"1 bulto incompleto de N"**; sin sobrante, el desglose común; y singular/plural según corresponda, que con dos bultos en el papel es un caso normal.
       - **Se muestra en el detalle Y EN EL WIZARD, en vivo al tipear la cantidad recibida.** El wizard es el que más rinde: ahí la persona puede **levantar la vista y contar contra el camión** —"¿te llegaron 7 cajas enteras y una abierta?"— y después ya no. El detalle sirve para reconstruir; el wizard, para verificar.
       - **Los ítems anteriores a la columna no muestran nada**: no se inventa el desglose ni se avisa que falta. `contenido_documento` en null es el caso normal de lo viejo, no un error.
       - En el estado del wizard el campo es **`contenidoPapel`, separado de `contenido`** justamente porque `contenido` se anula cuando lo recibido no entra en bultos enteros, que es **cuando más se lo necesita**. Se limpia al cambiar de respuesta —a granel, o al pasar a mixto—: arrastrarlo mostraría bultos que no existen.
       - **Al `.select()` del detalle hubo que sumarle `contenido_documento` e `interpretacion`.** Sin eso el desglose no aparecía nunca y en silencio: la trampa de "la columna no llega", ya documentada.
     - **`dividirExacto()` rechaza lo que no se reparte en partes iguales**, con tolerancia `1e-9` y NO la de comparación: `chk_presentacion_coherente` exige `cantidad = bultos * contenido` como igualdad estricta, así que 100 en 3 bultos daría 33,333333 y el insert se caería **al confirmar el wizard, con todo ya cargado**. Se dice en la fila, en el momento.
     - **EL TOTAL DEL COMPROBANTE NO LO ESCRIBE NADIE.** Es una línea propia que sale de `cantidadOcrOriginal`. Antes compartía renglón con el total calculado, así que al escribir en cualquier campo se lo pisaba — justo la referencia contra la que se controla lo cargado.
       - **Y su etiqueta depende de la respuesta:** si el número son bultos, no es un total en la unidad del catálogo. Decir "250 kg" de 250 bolsas es el mismo error del rediseño entrando por la etiqueta.
       - Por lo mismo, **el aviso de discrepancia solo compara cuando hay un total real** —cuando el OCR trajo su propio desglose—. Si no, estaría comparando el número del papel contra el total recién calculado con la respuesta de la persona: saltaría en TODA carga de harina diciendo una obviedad.
     - **SE APRENDE, POR INSUMO Y POR PROVEEDOR** (`presentaciones_sugeridas`, ver la RPC arriba). La primera vez se pregunta; después se propone y es un toque de confirmar. Por proveedor igual que el alias: si un molino factura la harina por bultos y otro en kilos, cada uno tiene su regla.
       - **Se PROPONE, nunca se aplica callado** — mismo criterio que el alias y por lo mismo, porque una propuesta equivocada se propaga sola. La respuesta propuesta va marcada y **el total calculado está siempre a la vista**: es la protección real contra el caso absurdo (harina aprendida en bultos + una factura que dice 6.250 da 156.250 kg, y eso se ve).
       - **NO hay un detector automático de "esto no tiene sentido", y es deliberado:** toda regla construible con los datos disponibles se dispara también en el caso normal —250 divide exacto por 25 igual que 6.250— y una alarma que suena en cada carga entrena a la gente a ignorarla.
       - **Una favorita que no reparte exacto NO se impone:** no se aplica, se explica por qué, y se ofrecen las presentaciones ya usadas que sí dividen. Caso real: 200 kg de lecitina con favorita 215 no da entero, y con bultos de 50 son 4.
       - Responder, o tocar un chip, **apaga la marca de propuesta**; y una respuesta ya dada nunca se pisa, ni siquiera si el historial llega después por la red.
     - **PRESENTACIONES MIXTAS** (`item.lineas`): "4 bultos de 250 y 2 de 500". Cada línea emite **su propia fila de `materia_prima_items`**, que es lo que Stock ya espera —el libro guarda una fila por presentación y `v_stock_por_lote` agrupa por `(lote, contenido_por_bulto)`—, así que no hizo falta tocar la base. El insert pasó de `map` a `flatMap` y **sigue siendo UNA sola sentencia**.
       - No hace falta una regla nueva: **la suma de las líneas reconstruye el número del papel**, leído en la unidad que la respuesta dice que es. En `'bultos'` la suma de los bultos; en `'unidades'` la suma de bultos × contenido. Es la misma regla que ya rige con una sola presentación.
       - **Cada fila cierra sola** contra `chk_presentacion_coherente`, además de que el conjunto cierre contra el comprobante.
       - `lineas` es **null en el caso normal —el 95%—** y ahí mandan los campos de siempre: no conviven dos representaciones del mismo dato. Activar el modo **convierte lo ya tipeado en la línea 1** en vez de vaciar, y bajar a una sola línea vuelve al modo normal.
       - El lote y los adjuntos son **del renglón** y se copian idénticos a cada fila. Si dos presentaciones traen lotes distintos, van como dos renglones.
       - **Mixto y "recibí menos" son excluyentes.** Combinarlos pide una cantidad recibida por línea y abre una segunda dimensión —¿el motivo es por línea o del renglón?— que nadie pidió. **La pantalla lo dice en los dos sentidos**, porque a cualquiera de los dos se puede llegar primero: un botón deshabilitado sin explicación manda a adivinar.
       - Los chips de presentaciones aprendidas **se ocultan en modo mixto**: un chip pone UN contenido y con varias líneas no hay a cuál aplicarlo.
     - **SUGERENCIA POR PARECIDO, y crear como SALIDA y no como entrada.** La tarjeta venía con el nombre del OCR ya escrito en el bloque de crear y el badge NUEVO puesto, así que nadie buscaba. Verificado: los 5 renglones de una factura salieron como NUEVO y **4 YA EXISTÍAN** — el catálogo habría pasado de 53 a 57 con 4 duplicados, y con el catálogo partido ningún saldo sirve.
       - **Se corta la cola de envase** ("X UNID", "X CAJA", "X KG", "X 2 ROLLOS") antes de comparar. Es el hallazgo que hizo funcionar el caso difícil: ese "X CAJA" final metía un token `caja` en "CINTA D 48X100 X CAJA" que la empujaba hacia los productos "Caja N°1", y **ningún umbral la salvaba**.
       - Los tokens **pesan por rareza**: "caja" está en diez insumos y no distingue nada, "cinta" está en dos y sí. El prefijo cubre singular/plural con **mínimo 4 letras**, que es lo que impide que "mt" matchee "mts" y "Esquineros 1 mt" empate con el de 2.
       - El score mide **cuánto del NOMBRE DEL CATÁLOGO está en el papel**, no al revés: el papel trae adornos que el catálogo no tiene, y penalizarlos dejaría afuera justo los nombres cortos, que son los que mejor identifican.
       - **El umbral (0,4) NO separa los casos dudosos, y no puede.** "Cinta embalar" (0,45, la que se quiere) y "Rollo corrugado" (0,50, la que no) son el mismo caso estructural: comparten el sustantivo y les falta el calificativo. La protección no es el número — **las sugerencias son botones que hay que tocar, nunca se aplican solas**, el texto crudo del papel está a la vista para juzgarlas, y crear queda a un toque. Para una línea sin relación no sugiere nada.
       - El badge NUEVO aparece **recién cuando la persona eligió crear**: antes afirmaba algo que nadie había decidido. Y `validarItems()` corta si el renglón no tiene producto elegido ni creado.
     - **El aviso de los renglones sin clasificar** sigue redactado como **pregunta abierta con las dos salidas** ("si no es mercadería, borralo; si es un producto, elegí el tipo"): `tipo_sugerido = null` NO sirve para eximir de nada, porque el prompt dice "ANTE LA DUDA, null" y ese estado también aparece con productos reales.
     - **EL ERROR GANA SOBRE LA TARJETA ATENUADA.** `.item-mp__error` es hijo DIRECTO de `.item-mp__cuerpo` y la regla del atenuado le bajaba la opacidad al 50%: el cartel rojo flotaba sobre una tarjeta deshabilitada y se leía como adorno. Son dos estados que dicen cosas opuestas —"esto no cuenta" y "esto te está esperando"— así que **con error el atenuado no va** (`:not(.item-mp--error)` en las dos reglas).
     - **EL CONTADOR DEL LISTADO CUENTA RENGLONES DEL PAPEL, NO FILAS.** Ese número existe para controlar contra el comprobante —"5 ítems, 4 suman" contra una factura de 5 renglones—, así que tiene que contar lo mismo que el comprobante; si un producto en dos presentaciones contara 2, el número dejaría de coincidir **meses después y sin nada que lo explicara**. La clave es `(insumo, lote)`: un renglón partido comparte las dos, y dos renglones del mismo producto van con lotes distintos, que es por qué se cargan separados.
       - **Es un PROXY declarado, no un dato guardado:** dos renglones del mismo insumo con el MISMO lote se cuentan como uno. Decisión tomada con el caso adelante: para que falle hace falta que el proveedor emita dos renglones del mismo producto y mismo lote, el costo es un contador que dice 4 donde el papel dice 5 —molesto, no peligroso, los kilos y el stock quedan bien—, y **una columna de agrupación hay que mantenerla consistente en el insert, la edición y el borrado; si se desincroniza da un contador igual de mal pero sin que nadie sospeche que es un proxy.** Un dato guardado que puede estar mal es peor que un cálculo declarado como aproximación.
   - **Recepción parcial**: botón "Recibí menos de lo que dice el remito" en cada ítem, en bordó para distinguirlo de los que solo cambian CÓMO se carga el número. Arriba va la cantidad del comprobante y abajo la recibida — ver la invariante de `cantidad` en la sección de tablas. Se pide **en la misma unidad que el número de arriba**, que es la que la persona tiene en la cabeza mirando el papel: bultos si el papel cuenta bultos —si llegaron 98 de 100 cajas, `cantidad_bultos` es 98, que es lo que exige el CHECK contra `cantidad`— y el total en los modos `'unidades'` y `'granel'`. Antes era siempre bultos, que dejó de ser cierto en cuanto el número del papel pudo significar otra cosa. **En `'unidades'` la presentación de lo recibido se DERIVA** (ver `bultosPapel` más arriba), y si no da un número entero de bultos el par va en null.
     - **EL BLOQUE ES UNA ZONA VISUALMENTE SEPARADA, no una continuación de lo de arriba** (`c3b95ca`). Con la cantidad del comprobante en gris, los bultos y la cantidad recibida había **tres números parecidos** en pantalla, y la barra bordó fina a la izquierda no alcanzaba: no se entendía si había que modificar el de arriba o el de abajo.
       - Recuadro con **borde en los CUATRO lados** y **fondo `--bordo-suave`**, para que la separación se note sin leer. Título en forma de **pregunta directa** —"¿Cuánto recibiste en realidad?"— y las dos zonas nombradas: arriba pasa a decir **"Lo que dice el papel"** mientras el bloque está abierto.
       - **Los campos de adentro van sobre fondo blanco**: sobre el teñido se pierde el borde y dejan de leerse como editables.
       - **`--bordo` y NUNCA el rojo de error.** Recibir menos no es un error de la persona, es un dato normal: si el bloque se ve alarmante, la gente lo evita **justo cuando corresponde usarlo**.
     - **LA ROTURA PASA A NIVEL UNIDAD, NO A NIVEL BULTO** (`73d0ebd`), así que en modo `'bultos'` se elige en qué se expresa lo recibido: **`En bultos`** (default, el caso común de que falte una caja entera) o **`En <unidad>`** (la rotura, que pasa adentro del bulto). La etiqueta del campo cambia con la elección.
       - El caso que lo motivó: Rollo Manos, la factura dice **6 bultos de 2 unidades** y se rompió un rollo, así que llegaron **11**. El campo pedía bultos, y 11 unidades son **5,5 bultos**: no había número que se pudiera escribir. No existen físicamente, y además contradice la regla de bultos enteros.
       - **No hizo falta lógica nueva:** 11 con contenido 2 da null en `bultosEnteros`, el par queda en null y `desgloseIncompleto` ya muestra "5 bultos de 2 un y 1 incompleto de 1 un" — exactamente lo que pasó.
       - **NUNCA se guardan bultos fraccionarios**, por ningún camino: no existen físicamente. Es la misma regla de `bultosEnteros`, y este modo es el que más la tensiona porque es donde el número tipeado y el guardado dejan de ser el mismo.
       - **Cambiar de unidad VACÍA el campo.** Un 5 escrito pensando en bultos no puede pasar a significar 5 unidades sin que nadie lo toque: es la misma familia del bug de `item.bultos` significando dos cosas según el modo, y acá el número ya está a la vista, así que reinterpretarlo en silencio es peor que perderlo.
       - **A granel NO aplica**, verificado: ahí `bultos` y `contenido` van siempre en null y lo recibido ya se expresa en unidad base. Es el único modo donde nunca se contó en bultos, así que no hay nada que elegir y el selector no se dibuja.
       - Dos detalles del estado inicial del campo, del mismo commit: **se fue el `placeholder="0"`** —un cero se lee como un valor ya puesto, y en un campo de cantidad eso es peligroso— y el mensaje pasa a decir **"Escribí cuánto recibiste"** cuando la del comprobante ya está cargada, en vez de "Cargá las dos cantidades", que se leía como si ninguna contara.
     - Dos simplificaciones de jerarquía del mismo commit: la línea **"Total" solo se muestra en modo `'bultos'`** —en `'unidades'` y `'granel'` el total del comprobante ya está dicho dos veces arriba, y en bultos es el único lugar que lo dice en unidad base—, y **"Total recibido X" y "Faltan Y" se fusionaron en "Recibiste X · faltan Y"**: eran dos líneas para una sola idea, y juntas la relación entre los dos números queda explícita.
   - **LOS CAMPOS NUMÉRICOS VAN `type="text"` + `inputmode="decimal"`, NUNCA `type="number"`** (`6862597`) — ver la trampa en Aprendizajes clave. Eran cinco y los cinco se comían la coma.
     - **DOS helpers separados a propósito, que NO hay que unificar.** `numeroDesdeOcr()` para los valores que llegan de la Edge Function (números JSON, ya canónicos) y `parsearCantidad()` para el texto que tipea una persona, con la regla estricta. Meterle la regla estricta al camino del OCR haría que descartara datos en silencio el día que la función devuelva un string con separadores: cambiar un bug callado por otro. El nombre viejo, `numeroDesdeInput`, **era parte de por qué el bug sobrevivió** — se lee como "esto ya viene validado de un campo" y cuatro de sus cinco usos eran del OCR.
     - `parsearCantidad` acepta coma o punto como decimal, **un solo separador** —"1.500,25" se rechaza, no se adivina—, y **rechaza los espacios internos**: `trim()` saca los de los bordes, pero "8 5" es un dedazo donde iba la coma y daría 85.
     - **Los campos de BULTOS rechazan cualquier separador**, sin mirar la unidad: un bulto es un paquete discreto y "1.500" cajas daría 1,5 bultos. Es más estricto que la base, y está bien que lo sea.
     - Lo que no se pudo leer **se dice en la fila, en el momento**, y `validarItems()` bloquea el avance: antes un `bultos` ilegible se propagaba a `cantidad` y disparaba "la cantidad tiene que ser mayor a cero", que apunta al campo equivocado.
   - **Proveedores**: se matchean por CUIT y, si no, por razón social normalizada — mismo criterio que gastos.html. Si no existe, se crea con `crear_proveedor_pendiente` y queda para aprobar en Cuentas Corrientes. **OJO: NO confundir con `crear_proveedor_activo`**, que es la de cuentas-corrientes.html y da de alta directo. Si el alta falla, el ingreso se guarda igual con `proveedor_id` en null y se avisa: perder la carga de mercadería por no poder dar de alta un proveedor sería peor.
   - **El listado FUSIONA en una sola fila el remito y la factura** cuando corresponde (ver la regla semántica más arriba): son dos comprobantes de la misma entrega, y la base son los datos del remito. Los dos chips de tipo van juntos y el modal lista ambos comprobantes, cada uno con su foto. **El modal pide los ítems por TODOS los comprobantes de la entrega y no solo por la base** — con `.eq()` sobre la base, los ítems propios de una factura vinculada quedaban invisibles. Casos borde cubiertos: la factura queda sola y avisada cuando el RLS oculta su remito, y si más de una factura apuntara al mismo remito aparece un chip de alerta — esa mercadería podría estar facturada dos veces.
   - **Filtro de vínculo** (Todos / Remitos sin factura / Con factura vinculada), en una segunda fila de chips junto a la de unidades. Se encadena con el filtro de unidad y el buscador.
     - **Mira el vínculo desde el lado del REMITO** (`estado.remitosFacturados`) y no la cantidad de comprobantes de la entrega: desde el vínculo por renglón, una factura puede estar vinculada a un remito SIN fusionarse con él, y ahí el remito queda como una entrega de un solo comprobante que sin embargo ya está facturada. Sin eso, "Remitos sin factura" —que es la herramienta de control— pasaría a listar remitos que sí tienen factura.
     - Con **"Remitos sin factura"** activo cada fila muestra la antigüedad, y **el orden se invierte: más viejos primero**. El listado va por defecto de más reciente a más antiguo, que con este filtro dejaría abajo justo lo que interesa.
     - La antigüedad se calcula sobre **`fecha`** (la del documento) y NO sobre `created_at`: lo que hace viejo a un remito sin facturar es cuánto hace que llegó la mercadería, no cuándo alguien lo tipeó. Si se carga hoy un remito del mes pasado, `created_at` diría "hoy" y taparía el caso que este filtro existe para encontrar.
     - Se resalta en ámbar a partir de `DIAS_REMITO_VIEJO = 45`, constante arriba del archivo. **Es un número del negocio**: 45 días es el plazo máximo real en que puede llegar una factura después de la mercadería, así que hasta ahí no hay nada que reclamar y pasado ese plazo sí.
     - **LIMITACIÓN, y es la que importa si se lo usa para controlar:** el filtro trabaja sobre lo que la persona PUEDE VER. Con solo `materia_prima:cargar` ve únicamente sus propios ingresos, así que su lista de remitos sin factura está incompleta y no lo dice. Para usarlo como herramienta de control hace falta `materia_prima:ver_todo`.

   **RECEPCIÓN DE TRANSFERENCIAS — "Ingresos internos"** (`96932de`, septiembre 2026). Cucuruchos le presta mercadería a Dolce Pasta: el ENVÍO y el TRÁNSITO viven en Stock (`52e6b6f`) y acá se RECIBE, que es donde se recibe mercadería venga de un proveedor o de otra empresa. Botón propio en la pantalla principal, separado del wizard de comprobantes, y tercera pantalla por `hidden` (`pantalla-internos`) con lista → detalle → paso de rechazo.
   - **VIVE EN INGRESO PERO NO CREA UN INGRESO.** La única escritura es `responder_transferencia_stock`. Los TRES motivos, y conviene tenerlos escritos porque cada uno solo alcanza para descartar una parte:
     1. **`materia_prima_ingresos.tipo_doc` no contempla transferencias** — el CHECK admite `remito` / `factura_a` / `factura_x` / `sin_comprobante`, y una transferencia interna no es ninguno.
     2. **El DOBLE espejado.** `materia_prima_items` se espeja al libro por `trg_espejar_stock_ingreso`, así que si la recepción además creara ítems, la mercadería entraría DOS VECES: una por el espejo y otra por el `transferencia_entrada` que inserta la RPC. **Este es el motivo que no se ve leyendo el esquema** y el que hace daño en silencio — el stock quedaría al doble sin ningún error.
     3. **Se perdería el vínculo con la transferencia de origen**: el ingreso paralelo no sabría de qué envío salió, y el circuito deja de ser auditable de punta a punta.
     - Hay assertions y mutaciones que verifican que el bloque de recepción no nombre ninguna de las tres tablas ni tenga un `.insert(`.
   - **Gateada por `stock:recibir_transferencia`** —la tarea que ya existe en el CHECK—, **no por una tarea de `materia_prima`**. Y el botón **solo aparece si hay alguna unidad en `v_mis_unidades_recepcion`**: con la tarea otorgada pero sin alcance en ninguna unidad, la pantalla abriría vacía y el botón prometería algo que no hay. El alcance sale de esa vista y no se reimplementa en el cliente, mismo criterio que `v_mis_unidades_envio`.
     - **Este commit le agregó al módulo la carga de `rol_app` y `empleado_tareas`, que no tenía**, más un `tieneTarea()` con bypass de super_admin. **Gatea SOLO el botón nuevo**: el gateo del resto del módulo sigue siendo el pendiente de más abajo, y hay una assertion que cuenta las llamadas a `tieneTarea` para que no se resuelva de contrabando.
   - **DOS CAMINOS DE CANTIDAD, según `contenido_por_bulto` del ítem.** Con presentación se cuentan **bultos + fracción** (`¼ ⅓ ½ ⅔ ¾`) y el helper traduce a unidad base: un bidón que salió medio usado se recibe como medio bidón, no pesando los litros. Sin presentación se carga la cantidad directo.
     - **Y el "sin presentación" NO es el borde: es el caso mayoritario.** Verificado el **17/09/2026**: de los **40** movimientos de `stock_movimientos`, **32 no tienen `contenido_por_bulto` y 8 sí**. Si se hubiera construido solo el camino de bultos, la pantalla no habría servido para casi nada de lo cargado.
       - **El argumento cambió de forma aunque la decisión no:** esta entrada decía *"es el ÚNICO caso que existe hoy"* y se apoyaba en un **cero que ya no existe**. Construir los dos caminos sigue estando bien, pero por "la mayoría no tiene" y no por "no hay ninguno". **Un argumento apoyado en un cero se cae entero con la primera fila que aparece.**
   - **EL HELPER DE FRACCIONES ES EL INVERSO del de `stock.html`, y va con nombre propio a propósito.** `baseDesdeBultos()` va de bultos a unidad base para GUARDAR; `equivalenteEnBultos()` de stock.html va de unidad base a bultos para MOSTRAR. **Llamarlos igual invitaría a "unificarlos" y romper los dos** — es el mismo género que los dos helpers de cantidad de Insumos (`numeroDesdeOcr` vs `parsearCantidad`), donde el nombre confuso fue parte de por qué el bug sobrevivió.
     - Lleva redondeo a 6 decimales y **snap a lo despachado dentro de `1e-9`**: un tercio no termina en decimal, así que sin eso "llegó todo" daría una diferencia fantasma —o algo mayor a lo enviado, que la RPC rechaza—. La tolerancia es solo para el ruido de punto flotante, **no** para tapar diferencias reales.
   - **EL GUION COMO DEFAULT, nunca la cantidad enviada.** Cada renglón arranca sin confirmar. **Prellenar con lo despachado haría que confirmar sin mirar diera "llegó todo", que es la peor falla posible en una recepción**: el faltante del traslado desaparecería sin que nadie lo note. Un **0 es una acción explícita** ("conté y no llegó nada") y la RPC lo acepta —solo rechaza los negativos— sin insertar movimiento.
   - **RECHAZAR Y RECIBIR MENOS SON COSAS DISTINTAS, y la pantalla lo dice.** Rechazar devuelve **todo** el envío al origen; recibir menos se confirma con la diferencia y su motivo. Sin esa aclaración, alguien rechaza un envío completo porque faltaban tres bolsas — y ahí la mercadería que sí llegó vuelve a figurar en el stock del que la mandó, que es peor que el faltante.
   - Lo despachado se muestra **en el mismo vocabulario en que se pide lo recibido** (en bultos cuando hay presentación), para que la comparación no quede en la cabeza de la persona.
   - **Las tres capas que evitan que el aviso venga del servidor.** `responder_transferencia_stock` exige que TODOS los ítems vengan respondidos y corta con "Faltan ítems por confirmar": (1) el botón queda `disabled` —desde JS, no como atributo—, (2) un contador vivo **nombra** los renglones que faltan, y (3) la función de confirmar revalida antes de llamar. Los otros dos cortes de la RPC —recibir más de lo despachado, y diferencia sin motivo— se atajan acá con **la MISMA tolerancia cero que ella**: una validación más permisiva que la base deja pasar justo las filas que el servidor va a rechazar.
   - **LOS ERRORES: diccionario con FALLBACK AL GENÉRICO.** Los mensajes que lanza la RPC ya están escritos para una persona, así que se reconocen y se muestran. Lo importante es lo otro: **todo lo irreconocible cae a un genérico y el crudo va a consola**, porque un mensaje que no está en el diccionario es casi seguro un error crudo de Postgres —un nombre de constraint, un `violates`, una negativa de RLS— y **pasar `error.message` a ciegas es exactamente lo que puso `chk_presentacion_coherente` en pantalla**. Mismo criterio que `_traducirError()` de `js/auth.js`. El genérico además dice que **no quedó nada a medias**, que es cierto: la RPC es una sola transacción y cualquier `raise` la revierte entera.
   - El motivo del rechazo va en **un paso que REEMPLAZA al detalle**, no en un `prompt()`: no se puede estilar, se bloquea en algunos webviews y **no lo usa ninguna otra pantalla del proyecto**.

   **LAS TRANSFERENCIAS RECIBIDAS APARECEN EN EL LISTADO PRINCIPAL** (`2f6afd1`, septiembre 2026). El caso que lo motivó: 500 bolsas de Cucuruchos a Mengui, 450 recibidas, el movimiento bien registrado en `stock_movimientos` y en el listado de Ingreso de Mengui **no aparecía NADA** — alguien que abriera el módulo al día siguiente no tenía forma de saber que habían llegado 450 bolsas. La pantalla se llama "Ingreso" y eso ES un ingreso; que la fuente sea otra tabla es un detalle de implementación que al usuario no le importa. Sigue sin haber fila en `materia_prima_ingresos`: esa regla evita el doble conteo y no se toca — estas filas se leen de `stock_transferencias` + sus ítems, **solo lectura**.
   - Van en la MISMA lista, ordenadas junto al resto por fecha, con chip **`--verde`** y etiqueta **"INTERNO"**. **SOLO las RECIBIDAS** (`estado='aceptada'` y la unidad como DESTINO): las que salieron son egresos y se ven en Stock.
   - **El verde estaba libre en este módulo** —los cuatro tipos de comprobante ocupan marrón, cian, violeta y gris-azul, y el ámbar es el de "sin revisar"— **y es el acento del módulo Stock, de donde vienen los datos**: además de no chocar, significa algo. Y dice "INTERNO" y no "TRANSFERENCIA" porque *interno* es lo que la distingue de un proveedor, que es la pregunta de quien mira la lista.
   - **NO PASAN POR `fusionarEntregas`.** Esa función tiene cubiertos los casos borde de factura huérfana y de anomalía por varios comprobantes, y meterle un tipo que no tiene comprobantes ni vínculos es pedirle que decida sobre algo que no existe. Se **concatenan después**, ya con forma de entrega y con el mismo comparador de orden para que la lista quede mezclada y no en bloques. **Hay una assertion que exige que `fusionarEntregas` quede BYTE-IDÉNTICA**, que es lo que hace verificable la decisión.
   - **Excluidas de los dos filtros de vínculo con remito.** Una transferencia no tiene ni puede tener factura, así que listarla en "Remitos sin factura" la marcaría como pendiente de algo que nunca va a llegar — lo contrario de lo que ese filtro busca. En "Todos" aparecen: el guard va **DESPUÉS** del `if (!filtro) return true`, con mutación que verifica el orden.
   - **Incluidas en el banner**, en las dos cifras: un contador que no cuadra con la lista que está justo debajo es la peor forma de tener un contador. Sale gratis porque `estado.entregas` ya las trae; lo que la suite verifica es que **no se las excluya**.
   - La fila muestra la unidad destino, el origen y la fecha en la línea chica, y cuántos renglones (ver el bloque del título más abajo). El detalle tiene **camino propio con salida temprana** —no hay comprobantes que listar ni ítems que pedir, ya vinieron en el embed— y muestra por renglón despachado, recibido y **LA MERMA DEL TRASLADO destacada en `--bordo` con su motivo**, que es el dato que más importa de una transferencia. Si hay diferencia sin motivo, lo dice. Una `cantidad_recibida` en null **no inventa una merma**.

   **EL TÍTULO DE LA FILA ES LA UNIDAD DESTINO** (`045d828`, septiembre 2026). Parece cosmético y no lo es: **define de qué se trata la pantalla.** La fila titulaba con el origen ("Desde Cucuruchos Nuss") y dejaba el destino chico abajo. **En Ingreso el protagonista es DÓNDE ENTRÓ la mercadería**; de dónde salió se mira en Stock. Así que el título es la unidad destino y el origen baja a la línea secundaria, junto a la fecha ("desde Cucuruchos Nuss · 03/09/2026"). El detalle sigue el mismo criterio, y ahí también se fue el "· recibido en X" que repetía el título.
   - **Y ESO OBLIGA A QUE LA LÍNEA CHICA NO MUESTRE LA UNIDAD**, porque muestra `nombreUnidad(b.unidad_negocio_id)` y en una transferencia esa unidad **ES el destino**: sin la rama propia, "Mengui" aparecería DOS VECES en la misma fila. No da ningún error ni se ve mal — una fila que repite el nombre se lee perfectamente hasta que alguien la lee. Tiene assertion propia, y no compara strings: parte el ternario y exige que la rama de transferencia **no** imprima la unidad y la general **sí**, así cubre las dos formas de reintroducirlo (cambiar la rama, o borrar el ternario y mostrarla para todos).
   - **POR QUÉ NO ES UNA INCONSISTENCIA con los ingresos por comprobante**, donde el título es el proveedor — y hay que dejarlo escrito, porque sin esta nota alguien "corrige" la transferencia para que sea consistente con el resto y vuelve al problema: en los dos casos el título responde **la misma pregunta**, *"¿de qué es esta fila?"*. Lo que cambia es la información. En un comprobante la unidad es **una sola y obvia** —quien carga está en su unidad— y lo que identifica la fila es de quién llegó; en una transferencia hay **DOS unidades**, y lo que hay que decir es **a cuál de las cuatro entró**.
   - **El avatar sigue al título** —es su representación gráfica, no un dato aparte: un círculo "CN" al lado de la palabra "Mengui" se lee como una tarjeta mal armada—, pero con el ternario `inicialesEmpresa(e.esTransferencia ? titulo : b.razon_social)` y **NO `inicialesEmpresa(titulo)` a secas**, que rompía el avatar de los ingresos **sin proveedor**: ahí el título cae al placeholder `'(sin proveedor)'`, y las iniciales daban `"(P"` en vez del guion que devuelve `inicialesEmpresa` cuando no hay nada. Es daño colateral **fuera de las transferencias** y solo se ve abriendo un ingreso viejo sin proveedor, así que tiene assertion y mutación propias.
   - El buscador matchea **las dos puntas**: el origen ya salía por `razon_social`, y el destino no estaba en ningún campo de texto — con el destino de título, alguien que busca "Mengui" espera encontrarla.

   **DETECTOR DE COMPROBANTES YA CARGADOS** (`2663810`). Al salir del paso de datos del encabezado, el wizard llama a `buscar_comprobante_cargado()` y avisa si el mismo papel ya está cargado acá, en Gastos o en Cuentas Corrientes. **NO BLOQUEA**: un bloqueo duro empuja a inventar un número para poder cargar, y eso es peor — el duplicado se ve, el número inventado no.
   - **Una sola caja, FUERA de los paneles de paso**, debajo de la barra de progreso: la detección ocurre al SALIR del paso de datos, así que adentro de ese paso no se vería nunca; y desde ahí sigue visible en la confirmación, que es donde se decide guardar.
   - **Distingue los tres orígenes con chips de color, y la consecuencia es DISTINTA**: un duplicado acá significa que la mercadería se va a contar dos veces en el stock; uno en contabilidad, que el papel ya se registró pero la mercadería puede no estar.
   - **Sin comprobante no dispara**, con dos guardas: el flag `sinComprobante` (intención) y un número sin dígitos (mecánica, el mismo criterio de `normalizar_numero_doc`, que devuelve NULL y hace que la RPC no matchee nada).
   - **Sin caché, a propósito**: el resultado depende de lo que la persona acaba de tipear, y un aviso calculado con un número viejo es peor que ninguno.
   - Si la RPC falla, **loguea y sigue**. El `try/catch` envuelve la llamada porque sin él una caída de red deja el botón de continuar deshabilitado para siempre: **un detector que rompe la carga es peor que no tener detector.**
   - El importe **rechaza `null`/`undefined`/`''` ANTES de convertir**: la rama de `materia_prima` lo devuelve en NULL siempre, así que sin ese guard todo duplicado de este módulo mostraría **"$0"**.

   **DEEP LINK `?ingreso=<id>`** (`78c9b57`). Lo pidió el chat de Gastos: su aviso de duplicados hace clickeable cada fila para abrir el registro que encontró, y este archivo **no tenía ningún deep link** —cero `URLSearchParams`—.
   - **`buscarEntrega(id)` busca por la BASE O por cualquiera de los COMPROBANTES**, y esa es la pieza: el listado fusiona remito + factura en una entrega cuya base es el **remito**, y la RPC devuelve el id de la **factura**. Sin eso, el caso más común del detector no abriría nada.
   - **Cerró un no-op silencioso latente**: `abrirDetalleIngreso` hacía `find(e => e.base.id === id)` + `if (!entrega) return`. No se veía porque el listado siempre le pasa la base (`data-ingreso` es `b.id`), pero el deep link lo destapaba en el primer caso. Las dos puntas usan ahora la misma función.
   - **Las transferencias recibidas quedan cubiertas sin nada extra**: entran en `estado.entregas` con su id de base y `comprobantes: []`, así que el `some` no las rompe y `abrirDetalleIngreso` ya las rutea a su detalle propio. De la RPC no llegan —esa rama devuelve `materia_prima_ingresos.id`— pero una URL pegada a mano abre bien.
   - **Dos mensajes, cada uno con algo cierto.** Si no está en el listado se consulta por id, y **la consulta no es para encontrarlo** —si fuera visible ya estaría, porque `cargarIngresos()` no filtra nada, solo ordena— sino para que el mensaje no mienta: el aviso viene de una RPC `SECURITY DEFINER`, así que el registro EXISTE aunque la persona no pueda verlo. Sin permiso dice *"puede ser de otra persona o de una unidad que no ves"*; si existe y no está en la lista, lo dice tal cual.
     - Esa segunda rama es **CÓDIGO DEFENSIVO y hoy NO es alcanzable** — está comentada así en el código para que no la borren por "muerta". Se vuelve alcanzable cuando la tabla pase de las **1000 filas** que PostgREST devuelve por defecto y la lista quede cortada.
   - No se consulta si el id no es un uuid, y el parámetro **se limpia con `replaceState` ANTES de abrir**, para que recargar no reabra el modal solo.

   **QUIÉN CARGÓ EL INGRESO** (`cfc15dd`). El dato estaba en `materia_prima_ingresos.empleado_id` pero no se traía ni se mostraba: hubo que consultarlo por SQL para saber quién había cargado un remito con datos raros.
   - **Va POR COMPROBANTE y no una vez por entrega**: una entrega fusionada puede tener el **remito cargado por el de depósito y la factura por el de administración**, que es el circuito normal del módulo. Arriba se perdería uno de los dos — el mismo argumento que ya regía para `editado_por`.
   - El nombre se resuelve contra **`v_empleados_publico`, NUNCA contra `empleados`** (su RLS deja ver solo la fila propia), reusando `asegurarEmpleados()`. Si el empleado no aparece **se muestra el ingreso sin el nombre**: una baja no puede hacer desaparecer un dato de auditoría. (Esta entrada decía que "la vista filtra `activo=true`": **no filtra** —verificado con `pg_get_viewdef` el 22/09/2026, es un `select … from empleados` sin `where`—; la regla de mostrar el ingreso sin nombre sigue valiendo para cualquier id que no se encuentre.)
   - **EN UNA TRANSFERENCIA LA ETIQUETA ES OTRA: "Recibido por".** No hay fila en `materia_prima_ingresos`, así que sale de `stock_transferencias.respondido_por` —quien la aceptó—. El acto que corresponde a esta pantalla es **recibir**; quien la envió ya está dicho en la unidad de origen. **NO se unifican bajo un texto genérico:** en un comprobante alguien registró lo que llegó de un PROVEEDOR, en una transferencia alguien confirmó lo que llegó de OTRA EMPRESA NUESTRA.
   - **Un comentario que dejó de ser cierto y se actualizó a tiempo:** `asegurarEmpleados()` decía que *"en la práctica esta consulta no se ejecuta nunca"*, y era verdad mientras el único consumidor fuera `editado_por` (no hay pantalla de edición, así que `editado_en` está siempre en null). Con `empleado_id`, que es **NOT NULL**, la condición pasa a ser siempre verdadera: corre al abrir el primer detalle de cada sesión.

   **AVISO DE NÚMERO DE COMPROBANTE DEMASIADO CORTO** (`cfc15dd`). Se cargó un remito con `numero_doc = "1"`, y en el sistema hay **siete registros más con ese número, de cinco proveedores distintos**. Un número de uno o dos dígitos no identifica nada y deja al detector de duplicados sin con qué trabajar.
   - **Es un AVISO Y NO UN BLOQUEO**: hay comprobantes internos que legítimamente se numeran con un dígito. No toca ninguna validación.
   - **Se cuentan los DÍGITOS, no los caracteres del texto crudo**, con la misma normalización que usa el detector: `"0001"` son cuatro dígitos y no avisa; `"0-1"` son dos y sí, aunque tenga tres caracteres. Sin ningún dígito tampoco avisa: ahí el problema es otro.
   - **Vive DENTRO del campo y no en la caja de duplicados**, que está arriba de todo: así los dos avisos **conviven por construcción**, sin tener que coordinarlos.
   - Se recalcula al tipear, al prellenar desde el OCR —que no pasa por el listener— y al limpiar el formulario.

   **PREGUNTAR ANTES DE CREAR UN PROVEEDOR DUPLICADO** (`63d84e9`). El caso: se cargó un remito de FerPlast y el alta al vuelo creó un proveedor NUEVO llamado `FerPlast` con `FERPLAST S.R.L.` ya en la tabla desde agosto. Fallaron las dos vías de matcheo — el remito **no traía CUIT**, que es lo normal y además la única vía confiable, y por razón social `FerPlast` no es `FERPLAST S.R.L.`—. Ahora, cuando el match exacto no encuentra nada pero hay **PARECIDOS**, se pregunta *"¿Es alguno de estos?"* con la lista y una salida explícita **"No, es un proveedor nuevo"**. Sin parecidos no cambia nada: el caso normal no gana ningún paso.
   - **UN TERCER DUPLICADO QUE NADIE HABÍA NOTADO, encontrado midiendo y no buscándolo:** `EMBALAJES MERCOSUR` (CUIT 30080005331) y `EMBALAJES MERCOSUR S.R.L.` (30698885331) — la misma forma que FerPlast. Eran **TRES pares**, no dos.
     - **En los tres pares los dos CUITs son válidos y difieren EN EL MEDIO**, no en un dígito de más: los BADIC son `30717659327` y `30716593327`. O sea que **uno de cada par está mal tipeado y NO se puede saber cuál desde el código** — hay que mirar una factura de cada uno. Por eso el índice único de CUIT no frenó ninguno de los tres.
     - **UNIFICADOS — pendiente CERRADO el 17/09/2026.** Los hizo Facu a mano. Verificado contra la base: queda **una sola fila de cada uno** (`FERPLAST S.R.L.` 30708959320, `EMBALAJES MERCOSUR S.R.L.` 30698885331, `BADIC S.A.S.` 30716593327) y **CERO filas huérfanas** en `gastos`, `facturas_pendientes`, `materia_prima_ingresos` y `proveedor_insumo_alias`. Este cambio del wizard sigue siendo lo que evita que se creen nuevos.
     - **OJO AL UNIFICAR EL PRÓXIMO: borrar un proveedor le borra TODOS sus alias**, porque esa FK es la única de las cinco que está en `ON DELETE CASCADE`. Hay que reapuntarlos ANTES. Está explicado en la tabla `proveedor_insumo_alias`, en la sección de Materia Prima.
   - **LA MEDICIÓN DE LOS TRES CRITERIOS, contra los 36 proveedores reales y ANTES de escribir código.** Está acá para que nadie agregue de vuelta lo que se midió y se descartó:
     - **NORMALIZACIÓN FUERTE** (la misma regla que `normalizar_razon_social()` de la base). Agrega **EXACTAMENTE un par** que antes no matcheaba —`BADIC S.A.S` y `BADIC S.A.S.`, la misma empresa— y ningún otro. **Cero falsos positivos.**
     - **CONTENCIÓN sobre la fuerte**: resuelve FerPlast, porque `ferplast` está adentro de `ferplastsrl`. Encuentra 2 pares (FerPlast y Embalajes Mercosur).
     - **SUFIJOS SOCIETARIOS (`srl`/`sa`/`sas`/`saic`) y DISTANCIA DE EDICIÓN: NO ENTRAN, y eso se midió.** Sacar el sufijo encuentra **los MISMOS dos pares** que la contención —el sufijo va siempre AL FINAL, así que la contención ya lo cubre— y el único caso de una letra de diferencia era BADIC, que lo resuelve la normalización fuerte. Serían **dos reglas que hoy no encuentran nada y que habría que mantener para siempre.**
   - **LA NORMALIZACIÓN DÉBIL SE REEMPLAZÓ, no se sumó una segunda.** El archivo tenía una `normalizarRazonSocial` débil —solo mayúsculas y colapsar espacios— y la fuerte ocupó su lugar en el match exacto también. **Dos normalizaciones conviviendo es exactamente cómo alguien usa la equivocada**, y acá el costo sería silencioso: el match exacto dejaría pasar duplicados que la búsqueda de parecidos sí ve, o al revés. La medición de arriba es la que hizo seguro reemplazarla.
   - **EL MÍNIMO DE 4 CARACTERES no es "por si acaso":** sin él, un OCR que devuelva `"S.A."` da `sa`, que está contenido en **media tabla**. Sobre los 36 de hoy, 4, 5 y 6 dan idéntico resultado, así que se elige el más bajo que cierra esa puerta. El guard va **UNA sola vez**, sobre el más corto de los dos: `corto` es por definición el más corto, así que un segundo chequeo sobre el texto tipeado no podría cambiar el resultado nunca — y dos guards para una regla es como alguien cambia uno y cree que cambió la regla.
   - **El `nombre_fantasia` entra en la búsqueda de candidatos** (no en el match exacto, que sigue mirando solo la razón social): **el OCR lee el MEMBRETE**, que muchas veces trae la fantasía. Caso real de la tabla: `AVACA YANINA Y LIBRETTI F.A.S.` se anuncia como *"La casa de la Manguera"*.
   - **EL CUIT DE CADA CANDIDATO SE MUESTRA COMPLETO**, con `word-break: break-all` y NO con ellipsis: con dos proveedores del mismo nombre es lo único que los distingue, y como difieren en el medio, recortarlo dejaría a quien tiene la factura en la mano sin con qué elegir. Que se parta en dos renglones, pero que esté entero.
   - **LAS DOS DECISIONES QUE NO SON OBVIAS:**
     - **EL BLOQUEO DEL AVANCE ES LA ÚNICA BARRERA REAL.** El paso Datos no deja continuar mientras la pregunta esté abierta. Sin eso la pregunta se puede ignorar y **el proveedor se crea igual al confirmar**, que es el bug entero — la pregunta sería decorativa. Solo corre cuando hay candidatos.
     - **ELEGIR UN PARECIDO NO PISA EL TEXTO del campo, al revés que gastos.html.** `materia_prima_ingresos.razon_social` guarda **lo que decía el COMPROBANTE**, y esa diferencia con el padrón es dato, no ruido (ya está documentado así en la sección de tablas). Se vincula el proveedor y el texto queda como vino.
   - Editar la razón social resetea la respuesta —era sobre otro texto—, y eso vive dentro de `intentarMatchProveedor()`, el único lugar que recalcula. El listener es `change` y no `input`, así que tocar un candidato no dispara un re-matcheo que pisaría la elección.
   - **NO se tocó `cargarProveedores()`**: ya traía los cuatro campos y ya estaba **sin filtrar por `estado_alta`**, con su comentario explicando por qué. La trampa de "la columna no llega" no aplicó acá.

   **EL VÍNCULO DE PROVEEDOR SE SUELTA AL VACIAR EL CAMPO** (`095fb47`). Se elegía o matcheaba un proveedor, se borraba el texto de la razón social, y la etiqueta *"Proveedor identificado"* **seguía en pantalla con el vínculo VIVO** sobre un campo vacío: el ingreso se podía guardar apuntando a un proveedor que no tenía nada que ver con el comprobante.
   - **LA CAUSA, encontrada EJECUTANDO el camino y no leyéndolo:** el recálculo colgaba solo del listener `change`, **que no dispara hasta que el campo pierde el foco**. Hasta el blur, pantalla y estado decían cosas distintas.
   - **El pedido traía un segundo síntoma —que el proveedor sobrevivía a "Repetir" + otra foto— y ESE NO SE REPRODUCE.** Se auditó campo por campo ejecutando el camino real: `wizardVacio()` trae los cuatro campos de proveedor, `reiniciarLecturaWizard` reemplaza el wizard entero y `limpiarFormularioDatos` apaga las dos cajas del DOM. Quedó cubierto con assertions de regresión en lugar de con un arreglo que no hacía falta. La causa de arriba explica lo que se vio: sin blur en el medio, la etiqueta que queda en pantalla es la anterior.
   - **LA REGLA PRECISA, y es más fina que "sin razón social no hay proveedor que valga": VACIAR EL CAMPO SUELTA EL VÍNCULO QUE VINO DEL TEXTO, NO EL QUE VINO DEL CUIT.**
     - **NO LO "CORRIJAS" POR SIMETRÍA**, que es exactamente el riesgo: **las facturas X no traen la razón social impresa**, así que ahí el campo arranca vacío y el CUIT es la ÚNICA vía de identificación. Soltar el vínculo con el campo vacío dejaría sin proveedor **justo el caso donde el match automático anda bien**. El código lleva el comentario con ese motivo.
     - Sale gratis: `intentarMatchProveedor()` re-evalúa el CUIT primero y ese match no depende del campo de texto.
   - **Se recalcula SOLO si el campo quedó vacío, nunca en cada tecla**, y con `.trim()` —`"   "` no es el nombre de ningún proveedor—. Una regla que soltara el vínculo por falta de parecido soltaría también `Distribuidora del Centro SRL` → `DIST. CENTRO S.R.L.`, que es el caso legítimo para el que existen los dos campos: es el mismo argumento ya escrito en gastos.html.
   - `renderizarProveedorDetectado` además **VACÍA el `innerHTML` al ocultarse**, como ya hacía el de parecidos: el texto viejo debajo de un `[hidden]` es inofensivo hasta que alguien toca un guard, y ahí reaparece.

   **BUSCADOR DE PROVEEDOR EN EL PADRÓN** (`3a4e57a`). El caso: un remito que **no dice de quién es** —solo el destinatario y el producto—. La persona SABE que es de FerPlast y no tenía forma de elegirlo, porque hasta acá el proveedor solo se resolvía si el texto matcheaba o si aparecía como parecido.
   - **VA EN UN INPUT SEPARADO Y NO DENTRO DEL CAMPO DE RAZÓN SOCIAL, al revés que gastos.html, y el motivo es el caso mismo:** tipear `"ferp"` en ese campo dejaría **`"ferp"` guardado como `razon_social`**, que es el texto histórico de lo que decía el papel — y el papel no dice nada. Se sigue el patrón del **buscador de catálogo del paso 3**, que es el que este módulo ya tiene para lo mismo: input propio y panel dibujado en el sitio.
   - **EL HUECO QUE ENCONTRÓ LA SUITE, y es el que más transfiere: elegir del padrón vinculaba EN SILENCIO.** `renderizarProveedorDetectado` solo dibujaba si había texto o CUIT, y **en el caso que motiva el buscador no hay ninguno de los dos**: la persona tocaba FerPlast y la pantalla no cambiaba en nada. La condición mira ahora también `proveedorMatch`. Ver el aprendizaje sobre la acción que ocurre sin que la interfaz lo confirme.
   - **El panel se dibuja SOLO A ÉL, nunca se re-renderiza el bloque**: un render por tecla le saca el foco al input, que es el bug que este archivo ya tuvo dos veces.
   - **Sin resultados SE DICE**, con el término buscado y qué hacer si es nuevo. Un panel vacío se lee como que el buscador no anda, y con 36 proveedores escribir algo que no está va a pasar seguido.
   - **El CUIT completo en cada resultado**, por lo mismo que en la pregunta de parecidos.
   - **Elegir no pisa el texto de la razón social** —reusa `elegirProveedorParecido`, que ya lo garantiza— y limpia el término de búsqueda: la pregunta que la persona vino a responder ya está respondida.
   - Filtra en el **CLIENTE** sobre `estado.proveedores`, que ya está en memoria y **ya viene sin filtrar por `estado_alta`** —un pendiente que no apareciera se volvería a crear y chocaría contra el índice único de CUIT—. Cero consultas nuevas, tope de 8 como gastos.html, y busca por razón social **y** fantasía con la misma normalización fuerte.
   - Detalle de CSS que se paga solo: la regla del input va **DESPUÉS de `.wz-campo input` y con más especificidad**. Vive adentro de un `.wz-campo`, así que si no heredaría los 48px del campo de razón social y **se leería como un segundo campo del comprobante**, que es justo lo que no es.

   **EL GATEO ASIMÉTRICO — lo más importante de este cambio, y no estaba previsto.** `stock_transferencias` se lee con **`stock:ver` sobre ORIGEN *O* DESTINO**, no con una tarea de `materia_prima`. Las dos consecuencias:
   - **Alguien con `materia_prima:ver_todo` pero sin `stock:ver` ve el listado INCOMPLETO y sin ningún error** — la trampa de "las filas no llegan", ahora entre dos módulos con criterios de permiso distintos. Por eso hay un **aviso al pie que lo dice**, con el mismo criterio que la LIMITACIÓN ya documentada del filtro de remitos: un listado incompleto que no se declara es peor que no tener el dato. La regla del aviso es PRECISA para no volverse ruido: se dibuja solo si hay una unidad que APARECE en el listado y sobre la que no se ve stock, así que un usuario de una sola unidad que ve su stock no recibe ningún aviso.
   - **Y el efecto del "O ORIGEN":** la policy entrega la fila también a quien tiene `stock:ver` en el ORIGEN aunque no pueda ver el destino. O sea que alguien con permiso en Cucuruchos y sin permiso en Mengui **recibe del servidor** esa transferencia y no debería verla en el listado de Mengui. **El filtro por `unidad_destino_id` contra `v_mis_unidades_stock` es una DECISIÓN EXPLÍCITA para atajarlo, no una consecuencia accidental de cómo quedó armada la consulta**: está comentado en el código y tiene **mutación propia** —cambiarlo a mirar el origen— para que nadie lo "simplifique" sacándolo.

7. **Stock** (`modulos/stock.html`, clave `stock`, nombre visible "Stock", orden 7, sin grupo): fila de `modulos` y las 6 tareas del CHECK ya existen en la base (migración del 12-13/08/2026). El módulo tiene **DOS pestañas**: "Stock" (fase 2) y "Catálogo de insumos" (fase 1), que se lleva la administración del catálogo que antes vivía como `materia_prima:gestionar_catalogo`.
   - Ojo con los guiones: acá `stock` es igual en los dos namespaces (`modulos.clave` y `empleado_tareas.modulo`), a diferencia de `materia-prima` / `materia_prima`.

   **`stock_movimientos` — el libro único.** id, unidad_negocio_id, insumo_id, lote, lote_ilegible, cantidad, **contenido_por_bulto**, tipo, motivo, lote_ambiguo, materia_prima_item_id, empleado_id, recuento_id, fecha, created_at. Una fila por cada entrada o salida, **con signo**: el saldo es siempre una suma, nunca un campo que alguien mantiene.
   - **`contenido_por_bulto` (numeric, nullable) vive en el MOVIMIENTO y no en el catálogo, y es la decisión de fondo de todo el tema bultos.** El mismo insumo puede entrar en tacho de 215 kg y en bidón de 5 kg, así que un campo fijo por insumo mentiría en cuanto llegara la segunda presentación. Null = entró sin presentación conocida.
   - **Los ONCE `tipo` válidos** (CHECK `chk_tipo_movimiento`): `ingreso`, `inventario_inicial`, `baja`, `ajuste`, `transferencia_salida`, `transferencia_entrada`, `transferencia_devolucion`, `consumo_sala_masa`, `consumo_produccion`, `consumo_envasado`, `despacho`.
     - **Los últimos cuatro TODAVÍA NO SE USAN, y están a propósito.** Cuando llegue Producción o Despacho, consumir stock es insertar una fila con un tipo que ya existe: cero cambios en Stock, cero migración del CHECK.
   - Los otros tres CHECK, todos verificados contra `pg_get_constraintdef`:
     - `chk_signo_tipo`: `cantidad <> 0` **y** el signo atado al tipo — positivo en `ingreso`, `inventario_inicial`, `transferencia_entrada` y `transferencia_devolucion`; negativo en `baja`, `transferencia_salida` y los cuatro consumos/despacho. **`ajuste` cae en el `ELSE` y acepta los dos signos**, que es justamente lo que un ajuste necesita.
     - `chk_motivo_obligatorio`: en `baja` y `ajuste` el motivo no puede quedar vacío. Son los dos tipos que una persona escribe a mano y los dos que hay que poder auditar después.
     - `chk_lote_normalizado`: `lote` es NULL o viene sin espacios en los bordes y no vacío. **Nunca `''` ni `' L-1'`**: el saldo se agrupa por lote, así que un mismo lote escrito distinto se partiría en dos filas que no suman.
   - **INMUTABILIDAD, con UNA excepción declarada.** Los movimientos que nacen en Stock (`baja`, `ajuste`, transferencias) **no se editan ni se borran**: se corrigen con otro movimiento que los compensa, y el libro conserva las dos filas. La excepción son los de tipo **`ingreso`**, que son ESPEJO de `materia_prima_items` y se sincronizan con él —el trigger borra y reinserta en cada UPDATE—: ahí la fuente de verdad es el documento, y el módulo de Ingreso ya tiene su propia auditoría de edición (`editado_por` / `editado_en`).
   - **Dos triggers de espejado**, los dos sobre tablas de Materia Prima:
     - `trg_espejar_stock_ingreso` → `fn_espejar_stock_ingreso()` (SECURITY DEFINER), AFTER INSERT/UPDATE/DELETE sobre `materia_prima_items`.
     - `trg_espejar_stock_cabecera` → `fn_espejar_stock_ingreso_cabecera()`, AFTER UPDATE sobre `materia_prima_ingresos`: propaga la unidad de negocio y la fecha si se corrigen en la cabecera.
   - **EL CONTRATO CON EL MÓDULO DE INGRESO, y es lo que hay que no romper:** el trigger suma **exactamente los ítems con `ya_recibido_con_remito = false`** (`v_suma := (new.ya_recibido_con_remito = false)`). Es por **RENGLÓN y no por documento**: una misma factura puede traer renglones que suman y renglones que no. Si esa columna cambia de nombre o de semántica, el espejo se rompe en silencio — el stock quedaría de menos o de más sin ningún error.

   **Las cuatro vistas, TODAS con `security_invoker=true`** (verificado el 26/08/2026):
   - `v_stock_por_lote` (unidad_negocio_id, insumo_id, insumo_nombre, marca, unidad_medida, tipo, lote, **contenido_por_bulto**, saldo, **`desde`**, aclaracion) — la base de la trazabilidad: saldo por lote, con `HAVING sum(cantidad) <> 0`. **Agrupa por `(lote, contenido_por_bulto)`, así que un lote con dos presentaciones da DOS filas** — y no se fusionan a propósito: un tacho de 215 y un bidón de 5 no son "3 bultos". `desde` es `min(fecha)` por lote y existe para ordenar por antigüedad **del lado de Postgres**: calcularlo en el cliente obligaba a traerse `stock_movimientos`, y PostgREST corta en 1000 filas, así que el mínimo salía de un subconjunto arbitrario sin ningún error.
   - `v_stock_insumos` (… tolerancia_merma_pct, cantidad_total, lotes_distintos, **presentaciones, contenido_unico, kilos_sueltos, categoria, aclaracion, vista_preferida**) — las **15** columnas verificadas contra `pg_attribute` el 10/09/2026. Cada una se agregó **AL FINAL** porque `CREATE OR REPLACE VIEW` solo deja agregar ahí, y el replace se hizo **repitiendo `WITH (security_invoker=true)`**: omitirlo resetea las opciones y la vista pasa a saltear el RLS, en silencio. Verificado después contra `reloptions`. — el total por insumo y unidad. Solo trae saldo distinto de cero. `lotes_distintos = 0` es el insumo que no lleva lote.
     - `presentaciones` = cuántos `contenido_por_bulto` distintos hay (los null no cuentan). `contenido_unico` = ese valor **solo cuando hay exactamente uno, y null en cualquier otro caso**. Antes era un `min()` pelado, y con dos presentaciones devolvía la más chica: quien lo leyera sin condicionar mostraría "1.250 bultos de 5 kg" para un stock que está en tachos de 215. **El nombre de la columna prometía una garantía que la columna no daba** — la misma trampa del comentario mentiroso, pero en un nombre. Se arregló en la vista, que es donde correspondía, en vez de exigir disciplina en cada cliente.
     - `kilos_sueltos` = el saldo con `contenido_por_bulto` null. Existe porque `presentaciones = 1` **no significa que todo el stock esté en esa presentación**: 6.250 kg en bolsas de 25 más 100 kg sueltos daba `presentaciones = 1` y dividir el total mostraba 254 bultos, que nadie puede ir a buscar al depósito.
   - `v_stock_negativo` — `v_stock_por_lote` filtrada a `saldo < 0`. **LISTA SUS COLUMNAS EXPLÍCITAMENTE, no usa `select *`**, así que agregar una columna a `v_stock_por_lote` **NO se propaga sola**: hay que recrear las dos. Se descubrió agregando `aclaracion` (`04c5042`) — era una fuente más donde la columna no llegaba. **Verificado el 31/08/2026: `modulos/stock.html` NO la consulta** — el badge de saldo negativo de la lista sale de `cantidad_total < 0` sobre `v_stock_insumos`. La vista existe y es correcta, pero hoy no tiene consumidor conocido; antes de darla por usada, verificar. (La versión anterior de este documento decía que la pantalla la mostraba como "Falta cargar un remito": ese texto además ya no existe, ver el badge más abajo.)
   - `v_mis_unidades_stock` (id, nombre, ciudad) — las unidades activas donde la persona puede ver stock, resueltas con la **MISMA** `tiene_tarea_alcance('stock','ver', id)` que la RLS. **Existe para que los chips de unidad no reimplementen la lógica de alcance en el cliente**: duplicarla significa que el día que cambie el servidor la UI muestre unidades de más (chips que no llevan a nada) o de menos (alguien deja de ver algo que sí puede ver, que es un bug funcional). Además funciona con el libro en cero, que derivar los chips de `v_stock_insumos` no hace.
   - **`v_stock_insumos_presentacion` fue ELIMINADA** (verificado: no existe). Desglosaba por bulto; el recuento se hace en la unidad base del catálogo.
   - **La condición por `remito_vinculado_id` ya no existe en NINGUNA vista de stock** — el filtro por documento lo reemplazó `ya_recibido_con_remito`, que es por renglón.

   **RLS de `stock_movimientos`: SOLO SELECT**, con `tiene_tarea_alcance('stock','ver', unidad_negocio_id)`. **CERO policies de escritura**: todo lo que entra al libro pasa por el trigger (SECURITY DEFINER) o por RPCs. Un `insert` directo desde el cliente no tiene por dónde entrar.

   **La pantalla** (`modulos/stock.html`):
   - **Abre en "Stock"**, que es lo que se mira todos los días; el catálogo lo toca poca gente muy de vez en cuando. Pero **el default se resuelve por PERMISO y no por costumbre**: es Stock si la persona tiene `stock:ver`, y Catálogo si solo tiene `gestionar_catalogo`. Sin ninguna de las dos, mensaje de sin acceso.
   - Las pestañas **solo se dibujan si hay DOS** vistas para elegir. Con una sola no hay elección, hay ruido.
   - La lista de stock usa la misma receta que el catálogo (`.tarjeta-lista`, tres líneas, sin botones, tocable). La **cantidad** va primera en la meta y con más peso: es lo que se viene a ver.
   - **LA ACLARACIÓN TIENE QUE LLEGAR A SIETE FUENTES DISTINTAS, y en todas falla igual: mostrando vacío, sin ningún error.** Basta olvidar una para que quede UNA pantalla sin el campo. Son (verificado contra el código, no contra una lista de memoria):
     1. y 2. los **DOS `.select()` de `insumos`** — el mismo par que ya trajo `categoria`;
     3. **`v_stock_insumos`**, de donde salen el listado de stock y el título del detalle por lote;
     4. el **embed `insumos(...)` del recuento abierto** — la pantalla donde más importa;
     5. y 6. los **DOS embeds del historial de recuentos**, que son los que más fácil se olvidan porque no están cerca de los otros;
     7. **el REMAPEO intermedio de `estado.itemsRec`** — la que faltaba en la primera versión de esta lista y la que mordió (ver la trampa en Aprendizajes clave). El recuento es la ÚNICA pantalla del módulo que remapea la fila del servidor a un objeto propio en vez de usarla como viene, así que tiene un punto de pérdida que las otras no: `aclaracion` llegaba del embed, el render la usaba, y se caía en el medio. **Los otros cinco `=> ({` del archivo pasan la fila por referencia o no vienen del servidor** — este es el único remapeo campo-por-campo.
     - **`v_stock_por_lote` y `v_stock_negativo` la tienen también, pero hoy no la consume nadie:** sus `.select()` piden saldo por `insumo_id` y no traen el nombre. Está para cuando alguna pantalla lo muestre.
     - **Cómo se encontró el alcance real, que es la parte que transfiere:** el plan traía una tabla de "las cinco fuentes" armada leyendo por encima, y al mapear el código resultó que `v_stock_por_lote` **no** era una y que los dos embeds del historial **sí**. Guiarse por la tabla en vez de por el código habría dejado dos pantallas sin el campo. La tabla sirve para no olvidarse de buscar; no sustituye la búsqueda. Y la SÉPTIMA la encontró Facu tocando la pantalla — el harness estaba en verde y las dos puntas (consulta y render) también.
   - **LAS DOS LISTAS VAN AGRUPADAS EN DOS NIVELES** (`62f3459`, septiembre 2026): primero `tipo` y adentro `categoria`. Con 53 insumos la lista plana no dejaba encontrar nada y mezclaba cosas sin relación — el film aparecía entre las cajas y el desengrasante entre las bolsas.
     - **El orden de las categorías es el del NEGOCIO y no alfabético**, y vive como un array en el código porque es una decisión, no un dato. Una categoría fuera del array **cae al final sin romper nada**: el CHECK de la base puede ganar un valor antes que esta pantalla. `Sin categoría` va siempre última.
     - **Las secciones se arman DESDE las filas visibles**, así que una categoría sin filas no se dibuja y con filtros o búsqueda activa las vacías desaparecen solas — no hay que acordarse de esconderlas.
     - El encabezado **reusa la receta de `.grupo-empleados` de empleados.html** —punto de color + texto chico— en vez de inventar un componente.
     - **El nivel de TIPO hoy NO agrega información y va liviano a propósito:** ninguna categoría cruza el tipo, así que "Harinas" ya implica materia prima. **No se saca** porque es la distinción que decide si el sistema exige lote, y mañana puede aparecer una categoría que cruce — pero dos niveles de encabezado sobre 53 filas en un celular se vuelven ruido si pesan igual. Hay un test que exige que el de tipo sea más chico que el de categoría, así que no se pueden igualar por descuido.
     - **Los DOS `.select()` de `insumos` traen `categoria`**, y son dos: olvidar uno rompe UNA pantalla y no la otra, así que el bug aparece en un solo lugar y cuesta el doble encontrarlo. Mismo patrón que ya mordió con `oculto_como_contraparte`.
   - **LA CANTIDAD VA A LA DERECHA, EN NEGRO Y CON PESO, SIEMPRE** (`62f3459` primero y `6cf7e27` completando), con el criterio de `.movimiento__monto` de Caja: texto a la izquierda, número a la derecha. La regla de `62f3459` era condicional —solo con desglose de bultos, para no repetir el mismo número dos veces— pero eso dejaba el número a la IZQUIERDA en las tarjetas sin presentación, y **los 19 insumos que entraron por el recuento inicial no tienen presentación declarada**, o sea el caso normal y no el borde. Se generalizó: la cantidad va a la derecha SIEMPRE, y el desglose de bultos baja a la línea de meta como contexto en lugar de duplicarse. El razonamiento de no repetir se mantiene — lo que cambia es lo de la IZQUIERDA.
   - **CONSECUENCIA que hay que decir en voz alta: los bultos DEMOTARON de dato principal a contexto.** La entrada anterior decía "los bultos son el dato principal y los kilos pasan a la línea gris" — ahora es al revés: la cantidad en unidad base va en el renglón destacado, y los bultos en la meta gris junto al resto. Vale la pena escribirlo porque **un renglón viejo que describe lo anterior es lo que invita a "arreglar" algo deliberado**, y a hacer un commit que devuelva la regla condicional creyendo que corrige un olvido.
   **LAS DOS RPCs DEL CATÁLOGO, y el patrón que NO hay que romper** (verificado contra `pg_proc` el 10/09/2026, **una sola firma de cada una**):
   - `crear_insumo(p_nombre, p_marca, p_unidad_medida, p_tipo, p_tolerancia_merma_pct, p_aclaracion, p_categoria)` — **NO recibe `p_vista_preferida`**: un insumo nace en `'base'` por el default de la columna.
   - `editar_insumo(p_insumo_id, p_nombre, p_marca, p_unidad_medida, p_tipo, p_tolerancia_merma_pct, p_estado_alta, p_aclaracion, p_categoria, p_vista_preferida)`.
   - **TODOS LOS PARÁMETROS CON DEFAULT SE MANDAN SIEMPRE, NUNCA OMITIDOS.** `p_aclaracion`, `p_categoria` y `p_vista_preferida` se asignan **DIRECTO** —sin `coalesce` contra el valor actual—, así que **omitir la clave no deja el campo como estaba: LO PISA**. El diseño directo es CORRECTO y deliberado: sin él sería imposible BORRAR una aclaración o DESclasificar un insumo. El precio es que el cliente no puede "simplificar" la llamada sacando una clave. Ver la trampa del borrado por omisión en Aprendizajes.
     - **`p_estado_alta` es la EXCEPCIÓN y sí usa `coalesce`**: ahí null significa "no lo toques", y por eso la llamada manda `'activo'` explícito cuando el insumo estaba pendiente. Que dos parámetros de la misma función se comporten distinto es exactamente lo que hace que valga la pena tenerlo escrito.
   - **`crear_insumo` y `editar_insumo` reciben `p_aclaracion` AL FINAL**, y las firmas viejas se dropearon explícitamente: con `SECURITY DEFINER`, un `CREATE OR REPLACE` con firma distinta crea una **SOBRECARGA** y PostgREST tira ambigüedad — ya costó un bug de producción. Verificar después que quede **UNA sola firma de cada una**.
     - **En `editar_insumo` la asignación es DIRECTA y NO `coalesce`**, y hay que dejarlo escrito porque es lo que alguien "corrige" por simetría: el `estado_alta` de la línea de arriba **sí** usa `coalesce(p_estado_alta, estado_alta)`, y copiar ese patrón acá haría **imposible BORRAR una aclaración** — el null se leería como "no la toques" en vez de como "sacala". La RPC lleva el comentario al lado.
     - **Los 100 se validan en el cliente ANTES de llamar**: el error crudo del CHECK nombra el constraint y no le sirve a nadie en el depósito. Y el vacío se manda como `null` aunque el servidor también lo normalice (`nullif(btrim(coalesce(...)),'')`): que las dos puntas digan lo mismo es lo que evita depender de cuál corre primero.
   - **La pestaña activa va en `--verde-oscuro`, NO en `--verde`:** es NAVEGACIÓN, no acción. Con el verde pleno competiría con "+ Nuevo insumo" y con "Empezar recuento", que sí son la acción principal de su pestaña, y la regla del proyecto es un solo primario por pantalla. Un tono más oscuro se lee como "estás acá" en vez de "tocá esto". Las inactivas ganan borde: sin contorno se leían como texto suelto.

   **EL MODAL DEL CATÁLOGO CAPTURA CATEGORÍA Y VISTA PREFERIDA** (`352ef5e` y `0c41698`, septiembre 2026). Hasta ahí el modal no tenía ninguno de los dos campos, así que **la única forma de categorizar un insumo era por SQL**.
   - **La categoría es una LISTA CERRADA, no un campo de texto**, y reusa la constante `CATEGORIAS` que ya ordena los grupos de las dos listas — no una segunda copia, que se desincronizaría de aquella en silencio. Con texto libre tendríamos "Cajas", "cajas" y "Caja" como tres grupos distintos, que es la razón del CHECK. **"Sin categoría" va primera, con value vacío y se manda como `null`**: es un valor legítimo y tiene que poder elegirse para desclasificar.
   - **El selector de vista preferida SOLO aparece EDITANDO** (ver la columna arriba). Las etiquetas nombran la unidad concreta de ESE insumo —"En su unidad (kilos)" / "En bultos"—, porque **"base" no es una palabra para nadie**; `kg`→kilos, `lt`→litros, `un`→unidades, y una unidad desconocida cae al literal en vez de inventar.
   - En los dos, **el poblado del `<select>` corre ANTES de asignar el value**: sobre un select vacío la asignación no engancha, y el campo quedaría en blanco sobre un insumo que SÍ tiene el dato — llevándoselo al guardar.

   **EL NÚMERO DE BULTOS VA A SECAS, Y LA MARCA SE DESTACA CUANDO IDENTIFICA** (`39df132`, septiembre 2026) — solo presentación, sin lógica ni consultas nuevas.
   - **En vista `'bulto'` el "de 25 kg" NO cuelga del número.** A la derecha va **"250 bultos"** —con la palabra, que sin ella no se entiende de qué son 250— y la cola baja a la línea gris junto al total. Antes competía con el número, que es lo que se busca de un vistazo.
     - **Y LOS SUELTOS VAN CON LA COLA, NUNCA CON EL NÚMERO:** "+ 100 kg" **no son bultos**, así que colgarlos de "200 bultos" diría algo falso. Es el tipo de detalle que miente sin que nadie lo note.
     - `textoBultos` **no cambió de firma** —tiene NUEVE usos—: se le extrajo la cabeza a `cabezaBultos()` y ella la reusa, así que cómo se dice "250 bultos" —el signo, el singular, la fracción— se decide en un solo lugar.
   - **LA MARCA SE DESTACA POR NOMBRE REPETIDO, NO POR TIPO.** Cuando hay **más de un insumo ACTIVO con el mismo nombre normalizado**, la marca sube al lado del nombre en `--verde`, porque es lo único que los distingue. Va en las **dos** listas (stock y catálogo) y **sale de la línea gris cuando sube**: nunca se dibuja dos veces.
     - **EL CRITERIO POR TIPO SE CONSIDERÓ Y SE DESCARTÓ, con los datos delante — no lo "simplifiques" de vuelta.** Parecía razonable (la marca de una harina es identidad, la de un pegamento es contexto), pero de los **11 insumos activos con nombre repetido, CINCO son `tipo='insumo'`**: dos "Caja capelina" y **TRES "Caja N°1"**, que difieren ÚNICAMENTE en la marca (Dolce Pasta / Nuss / Sin impresión) — y ahí eso ni siquiera es una marca comercial, es para qué empresa es la caja. Por tipo, esas tres quedaban indistinguibles. **Lo que hace que la marca sea identidad no es el tipo: es que el nombre solo no alcance para saber cuál es cuál.**
     - **AL LADO DEL NOMBRE *Y* CON COLOR, las dos juntas: sola, ninguna alcanza.** Las cuatro "Harina 000" tienen el nombre IDÉNTICO, así que en negro quedan cuatro renglones que difieren recién en el carácter 12 — para escanear es casi tan malo como el gris. El color es lo que la hace saltar.
     - **`--verde` pleno y no `--verde-oscuro`**: acá no compite con ningún primario, que es el único motivo por el que la pestaña activa usa el oscuro. Medido sobre blanco: **5,04:1**, AA. **NUNCA `--rojo` ni `--bordo`**, que en este proyecto significan alerta — esto es un dato, no una advertencia. Y pesa **menos** que el nombre (600 contra 650): acompaña, no lo reemplaza.
     - Se cuenta sobre lo **visible y activo** y una sola vez por render, sobre las filas que ya están en memoria: sin consulta nueva. Un insumo desactivado no obliga a destacar la marca del que quedó solo en pantalla, y en stock las filas ya vienen filtradas por unidad y buscador.

   **LA RECETA DE TARJETA Y ENCABEZADO** (`f8fd486`, septiembre 2026) — a partir de un mockup de Claude Design y para las DOS listas del módulo (Stock y Catálogo).
   - **Tarjeta:** radio `1.0625rem` (17px), borde `rgba(26,42,82,.06)` casi invisible + sombra `0 1px 2px rgba(26,42,82,.04)` en lugar del borde marcado, padding vertical `0.8125rem` (13px), gap entre tarjetas `0.5rem` (8px). Las tres rgba viven como **variables locales en el `body`** —`--borde-tarjeta`, `--sombra-tarjeta`, `--sombra-tarjeta-hover`—: es el patrón del proyecto para color de módulo, no hay equivalente global, y repetir el literal en las dos listas se desincroniza.
   - **`.tarjeta-lista` de `css/main.css` NO se toca**, y esto es lo que hay que dejar escrito: la comparten los otros cuatro módulos, así que la receta se aplica **local** vía `.fila-stock, .fila-insumo { ... }`. Un cambio en la clase global se propaga a Gastos, Caja, Cuentas Corrientes y Empleados sin que nadie lo pida.
   - **Encabezados:** línea divisoria ocupando el resto del ancho, y el contador de categoría en píldora gris (`background: rgba(26,42,82,.07)`, `border-radius: 999px`). El de TIPO usa `--borde-tarjeta` (más sutil) y el de CATEGORÍA usa `--color-borde` (más marcado) — la jerarquía entre los dos niveles queda MÁS marcada que antes.
   - **EL HALLAZGO que la explica:** el prompt pedía "fondo más oscuro que las tarjetas" copiando el `#eef1f6` del mockup. **Ese hex ES `--color-superficie`, y `.pagina-modulo` ya lo aplica contra tarjetas en `--color-fondo` desde el primer día**: la separación existía. Lo que aplanaba la pantalla era **el borde marcado (`--color-borde`) compitiendo con esa separación**. El arreglo real fue apagar el borde, no cambiar el fondo. Es del mismo género que la lección de "verificar antes de asumir" y aparece por el otro extremo: **antes de copiar un valor de un mockup, verificar que no lo tengamos ya con el mismo hex**.
   - **EFECTO COLATERAL, del género "trampa del orden CSS por un cambio ajeno":** al agregar `border-color: var(--borde-tarjeta)` a la receta compartida, la variante `.fila-insumo--pendiente { border-left: 4px solid var(--amarillo) }` **quedaba pisada** si la variante estaba antes en el archivo — la barra ámbar de "sin revisar" se volvía casi invisible. La variante está después y se mantiene, con una assertion propia que lo cuida. Es la trampa del orden aparecida por un cambio que no tenía nada que ver con esa variante — sumar `border-color` en una regla compartida obliga a auditar todas las variantes de esa clase.

   **PANTALLA DE GESTIÓN DE ALIAS DE PROVEEDOR** (`9db29d6`, septiembre 2026). La libretita la ESCRIBE el wizard de Ingreso, que es INSERT-ONLY a propósito; faltaba el lugar donde CORREGIRLA, y un alias mal mapeado se propaga en silencio a todas las cargas futuras de ese proveedor.
   - **Drill-down del CATÁLOGO, no una cuarta pestaña**: cuatro no entran en 375px, y lo toca la misma gente con la misma tarea que el catálogo. Reusa `PESTANA_DE_VISTA` —`{ historial: 'stock', alias: 'catalogo' }`—, así que mientras se lo mira **la pestaña marcada sigue siendo Catálogo**: es lo que hace que no se sienta una pantalla suelta. Se entra por un botón de la barra de acciones del catálogo, gateado por `stock:gestionar_catalogo`.
   - **Agrupada por proveedor**, por la misma razón por la que el índice único es por `(proveedor_id, texto)` y no global: un alias solo significa algo dentro del proveedor que lo escribió. Cada fila muestra la traducción COMPLETA —el texto del papel en monoespaciado y entrecomillado, para que se lea como transcripción y no como una frase del sistema, y el insumo del catálogo al que apunta— más quién lo creó y cuándo. El buscador filtra por **las dos puntas**: se puede llegar desde el texto raro del papel o desde el insumo.
   - **`creado_por` se resuelve en consulta aparte contra `v_empleados_publico` y NUNCA por embed a `empleados`**, cuyo RLS solo deja leer la fila propia. Los embeds a `proveedores` y a `insumos` sí van, que son inequívocos: una sola FK a cada uno.

   - **QUÉ SE PUEDE EDITAR Y QUÉ NO: SOLO EL INSUMO.** El texto del proveedor se muestra y no se edita, por dos motivos que se refuerzan: es **la clave con la que se busca** el renglón contra el comprobante, y es **la transcripción del papel** — un papel que después no está más. Cambiarlo rompe el alias en las dos direcciones y además lo mueve dentro del índice único. Si el texto está mal, **se borra el alias y se rehace en la próxima carga**, que es donde se lo puede comparar contra el comprobante real.
   - **EL AVISO DE QUE CORREGIR NO CORRIGE EL PASADO, Y POR QUÉ VA EN EL PASO DE GUARDAR.** Los ingresos ya cargados siguen apuntando al insumo viejo, y su stock también; eso se arregla con un **ajuste de inventario**, no cambiando el alias. Va en el paso de confirmación —que REEMPLAZA al formulario, mismo criterio que el cambio de tipo del modal de insumo— y **no en un texto de ayuda, que nadie lee**. Sin él, alguien corrige, cree que quedó resuelto, y **el stock sigue mal en DOS insumos a la vez**: de más en el equivocado y de menos en el correcto. El aviso nombra el insumo viejo, no uno genérico.
   - **UN ALIAS QUE APUNTA A UN INSUMO DESACTIVADO NO FALLA. Está escrito así a propósito, porque la intuición dice lo contrario** — la primera redacción de este chip decía "va a fallar en la próxima carga" y era falsa. **Verificado:** `modulos/materia-prima.html` carga el catálogo **SIN filtrar por `activo`** (lleva su propio comentario explicando por qué: si un insumo inactivo no matcheara, se intentaría crear de nuevo y chocaría contra el índice único de `insumos`). Así que el alias **resuelve igual**, la carga se guarda sin un solo error, y **el stock se acumula en un insumo muerto que ya nadie mira**. El daño no es un error: es un saldo creciendo donde nadie lo busca. El chip de la fila dice esa consecuencia y ofrece las dos salidas —reapuntar o borrar—, en bordó y no en rojo.
     - Y el modo de falla de la redacción vieja era peor que no avisar: quien la leyera **esperaría un error que nunca llega** y daría por sano el alias mientras el daño real sigue pasando.

   **EL STOCK MUESTRA LOS BULTOS COMO CONTEXTO** (`ec28bf5`, agosto 2026, ajustado en `6cf7e27`) — "250 bultos de 25 kg" al lado del total, así se maneja la fábrica.
   - **AVISO SOBRE LA REDACCIÓN VIEJA de esta entrada:** decía "los bultos son el dato principal y los kilos pasan a la línea gris". Desde `6cf7e27` es al revés (ver "LA CANTIDAD VA A LA DERECHA, EN NEGRO Y CON PESO, SIEMPRE" más arriba): la cantidad en unidad base va en el renglón destacado a la derecha, y el desglose de bultos baja a la línea de meta gris junto al resto. **"Bulto" genérico**: si era bolsa, tacho o bidón no está guardado en ningún lado.
   - **Los `kilos_sueltos` NO se dividen**: se restan antes y se muestran aparte ("250 bultos de 25 kg + 100 kg"). No se sabe en qué venían.
   - **EL EQUIVALENTE SALE DE DIVIDIR LOS KILOS, NO DE CONTAR BULTOS**, y está escrito al lado del helper. Puede haber 250 bolsas en el depósito con 2 medio usadas y el sistema decir "248 + ¾". Para *cuánto tengo* es correcto; para *cuántos bultos cerrados hay* NO, y eso solo lo responde un recuento físico. La pantalla no lo presenta nunca como un conteo.
   - La fracción se redondea a la más cercana de `0 ¼ ⅓ ½ ⅔ ¾ 1` — cuartos y tercios, que es como se maneja Facu. Redondear a 1 sube el entero; a 0 no muestra fracción; el empate exacto cae al corte de abajo. **El error máximo es un OCTAVO de bulto, y por eso los kilos exactos van SIEMPRE al lado en gris.**
   - En el movimiento puntual: con una sola presentación en el lote elegido se hereda sin preguntar; con varias hay que elegir de cuál sale, o el movimiento entraría con presentación nula y ensuciaría el desglose para siempre.
   - **El desglose por lote lista una línea por (lote, presentación)**, con los bultos y los kilos de cada una. Las dos fechas que antes se veían pegadas —el lote de una harina suele SER una fecha, normal en molinos— ahora se nombran: **"Lote 10/08/2026"** y **"primer ingreso 21/08/2026"**. Antes decía "10/08/2026" con "desde 21/08/2026" abajo y no había forma de saber cuál era cuál.
   - **El desglose por lote incluye la fila de lote NULL**, al final y etiquetada "Sin lote identificado". `lote_ilegible` existe para las etiquetas rotas, así que esa mercadería entra al libro con lote null y cantidad real: filtrándola, el desglose no sumaba el total y faltaban kilos que existen, sin ninguna explicación en pantalla.
   - **La pestaña "Recuento" es la fase 3** (ver abajo), que además trajo el historial de recuentos y los movimientos puntuales. (Acá decía que las **transferencias entre unidades** seguían sin pantalla: quedó viejo, son la fase 4 y están documentadas más abajo.)

   **FASE 3 — RECUENTO DE INVENTARIO** (`e2df7a1`, agosto 2026). Tercera pestaña, con `stock:ajustar_inventario`. Es lo que permite cargar el stock inicial, así que es lo primero del módulo que se usa de verdad. Dos usos, y el diseño sirve a los dos: la carga inicial (se cuenta todo un domingo, con el sistema en cero, así que da diferencia en absolutamente todo) y el control mensual (las diferencias son merma, rotura o faltante).

   - `stock_recuentos`: id, unidad_negocio_id, estado, observaciones, abierto_por/_en, cerrado_por/_en, anulado_por/_en, motivo_anulacion, created_at. `estado` ∈ `abierto`/`cerrado`/`anulado` (CHECK `chk_estado_recuento`), motivo obligatorio al anular (`chk_motivo_anulacion`), y un **índice único parcial `idx_un_recuento_abierto_por_unidad`: UN SOLO recuento abierto por unidad de negocio.**
   - `stock_recuento_items`: id, recuento_id, insumo_id, lote, **contenido_por_bulto**, cantidad_contada, cantidad_sistema, diferencia, observacion, created_at. Índice único `idx_recuento_item_unico` sobre `(recuento_id, insumo_id, coalesce(lote,''), coalesce(contenido_por_bulto, 0))` — **lote null y lote vacío son la MISMA clave, y la presentación forma parte de ella**. CHECK `chk_contada_no_negativa`.
     - Hay **una fila por (insumo, lote, presentación)** y se cuentan por separado, porque así se cuentan físicamente. `cerrar_recuento` congela el saldo comparando por las TRES claves: con dos de tres, el ajuste se calcularía contra el saldo de otra presentación.
     - **`cantidad_contada` NULL = todavía sin contar. Es el guion de la UI.** `cantidad_sistema` y `diferencia` se llenan AL CERRAR, no antes: en un recuento abierto o anulado quedan en null, así que **la foto congelada del conteo solo existe en los cerrados**. Rellenarlas con el saldo de hoy sería inventar una foto que nunca se sacó.
   - `stock_movimientos.recuento_id` (uuid, nullable, FK a `stock_recuentos`): cada ajuste sabe de qué conteo salió.
   - `v_recuentos` (`security_invoker=true`): id, unidad_negocio_id, unidad_nombre, estado, abierto_en, cerrado_en, observaciones, motivo_anulacion, abierto_por_nombre, cerrado_por_nombre, items, con_diferencia. **`con_diferencia` solo significa algo en los cerrados**: en un abierto o anulado siempre da 0, porque `diferencia` está en null. Mostrarlo igual sería un número que parece un resultado y no lo es.
   - `v_mis_unidades_ajuste` (`security_invoker=true`) — las unidades activas donde la persona tiene `ajustar_inventario`. **NO se usa `v_mis_unidades_stock` para los chips del recuento: esa resuelve `ver`, que es OTRA tarea**, y alguien puede ver stock sin poder ajustarlo.
   - **Las policies de las DOS tablas de recuento aceptan `ver` OR `ajustar_inventario`.** Fue una corrección: leyendo solo con `ver`, alguien con `ajustar_inventario` y sin `ver` abría un recuento y **no podía verlo** —consulta vacía, sin ningún error— y encima ya había consumido el único recuento abierto de esa unidad. **La del libro `stock_movimientos` NO se amplió y sigue en `ver` a secas**, y es correcto que así sea: cualquier pantalla que ajuste stock necesita mostrar el saldo para elegir qué tocar, y ese saldo sale de vistas `security_invoker` sobre el libro. Sin `ver` no hay nada que mostrar, así que no es una restricción agregada — es que no hay pantalla posible.

   **Las 5 RPCs del recuento** — todas SECURITY DEFINER, todas revalidan con `tiene_tarea_alcance('stock','ajustar_inventario', …)`. Sus mensajes de error ya vienen redactados en español y **se muestran TAL CUAL** ("Ya hay un recuento abierto en esa unidad. Cerralo o anulalo antes de abrir otro."): uno genérico taparía justo el dato que hace falta.
   - `abrir_recuento(p_unidad_negocio_id)` → uuid. **Precarga todo lo que tiene saldo, una fila por (lote, presentación), más los insumos activos con `tipo='insumo'` que no hayan quedado con NINGUNA fila.** Ese `not exists` es una corrección: antes los insumos comunes se insertaban siempre con `(null, null)`, y como la presentación forma parte de la clave única, uno que tuviera stock en bultos de 25 recibía DOS filas —la suya con saldo y una fantasma en cero— y quien contaba veía dos "Cajas" idénticas y tenía que poner cero en una. La versión original precargaba solo lo que tenía saldo y eso rompía los dos usos: la carga inicial abría un recuento **vacío** —el libro está en cero—, y el control mensual omitía justo los insumos en cero, **que son donde aparece la mercadería que el sistema no registró**. Una materia prima sin ningún lote conocido no se puede precargar (el lote se lee de la etiqueta del envase, no lo inventa el sistema) y se agrega a mano.
   - `guardar_conteo(p_items jsonb)` — `[{item_id, cantidad_contada, observacion}]`. Se llama muchas veces mientras se cuenta. **PISA `observacion` con lo que venga en el jsonb**, así que el payload tiene que llevar SIEMPRE los dos campos: mandar solo la cantidad borra en silencio la observación ya escrita. `cantidad_contada` en `''` se guarda como null, o sea que **se puede des-contar** un ítem mal cargado.
   - `agregar_item_recuento(p_recuento_id, p_insumo_id, p_lote, p_contenido_por_bulto)` → uuid — **CUATRO parámetros**. Para la mercadería de algo que el sistema tenía en cero y para las materias primas cuyo lote todavía no conoce. La presentación es obligatoria en el sentido de que sin elegirla el ítem entra en "sin presentación" y crea una fila paralela a la que sí tiene saldo.
   - `anular_recuento(p_recuento_id, p_motivo)` — motivo obligatorio. No genera ajustes ni toca el stock.
   - `cerrar_recuento(p_recuento_id)` → `{ajustes_generados: n}`. Rechaza si queda algún ítem sin contar. **Congela el saldo AL CERRAR, no al abrir**: si entró un remito mientras se contaba, el ajuste se calcula contra el saldo de ese instante y no lo borra. Consecuencia para cualquier pantalla: el saldo que se ve mientras se cuenta es VIVO, y todo resumen previo al cierre es una **estimación** — el número definitivo lo devuelve la RPC. La tolerancia de merma NO evita el ajuste: el sistema tiene que reflejar la realidad, la tolerancia solo marca si la desviación es esperable.

   **La pantalla del recuento:**
   - **EL GUION ES LA REGLA CENTRAL.** Cada ítem arranca en `—`, nunca en cero: un cero por defecto es un dato que nadie escribió, y al cerrar borraría stock real sin que nadie lo note. Poner cero es una acción explícita, "conté y no hay". La pantalla no deja cerrar mientras quede un guion, y la RPC lo revalida.
   - **Orden ESTABLE, que NO se reordena al contar** — reordenar la lista bajo el dedo de alguien que camina por un depósito es desorientador. Desde el 23/09/2026 (`905c040`) va **agrupada por categoría**, con la MISMA `agruparPorTipoYCategoria` / `htmlAgrupado` de las otras dos listas del módulo: se cuenta recorriendo el depósito por sector. La `categoria` sale del embed `insumos(...)` y **hay que copiarla en el remapeo de `estado.itemsRec`** —el séptimo lugar donde una columna puede no llegar, el que ya mordió con `aclaracion`—. Para saltar a lo que falta está el chip **"Ver solo los que faltan"**, y el contador dice **"Contados 34 de 55"** (mide el avance, no la deuda).
   - **"Actualizar"**: un recuento es de la UNIDAD, así que dos personas pueden contar sectores distintos a la vez. Guarda lo propio y recién después relee. Y agregar un ítem ya no puede perder un conteo tipeado sin guardar: se guarda antes de recargar y, si ese guardado no entra, `cargarItemsRecuento` conserva el valor local de toda fila que siga sucia.
   - **CONTAR EN BULTOS**: el renglón con presentación tiene su propio campo de bultos al lado del de la cantidad ("bultos de 25 kg"); los dos dicen lo mismo y **lo que se guarda es siempre la unidad base**. Los bultos admiten 3 decimales (`DECIMALES_BULTOS`) **también en un insumo que se cuenta por unidades enteras** —medio bidón es real, y la regla de "sin decimales" es de la cantidad— y se leen con `parsearBultos`, no con `parsearCantidad`. **Nada de bultos se guarda en ninguna columna**: no confundir con materia-prima.html, donde `cantidad_bultos` es una columna con CHECK y **nunca** puede ser fraccionaria.
     - **UNA CANTIDAD INVENTADA, cerrada en `a3bc858`:** 2,5 bultos de 3 cajas dan 7,5 cajas, y como el campo de una unidad entera tiene 0 decimales, `ponerNumero` **redondeaba a 8** y el renglón quedaba contado en algo que nadie contó, sin ningún error. Ahora se frena, se dice la cuenta que no cierra y la cantidad no se toca. Misma familia que el dato ausente mostrado como un número plausible.
   - **"Quitar del recuento", SOLO en los renglones AGREGADOS A MANO** (`7af559d`); un precargado se pone en 0. Llama a **`quitar_item_recuento(p_item_id)`**, que ya existía y no tenía pantalla (SECURITY DEFINER, `search_path=public`; borra la fila solo si el recuento está abierto y quien llama tiene `ajustar_inventario` en esa unidad). Diálogo propio que NOMBRA el renglón, no `confirm()`.
     - **Cómo se distingue un agregado a mano de un precargado, sin ninguna columna que lo diga:** `abrir_recuento` inserta el recuento y sus ítems en UNA transacción y el default de `stock_recuento_items.created_at` es `now()`, así que un precargado tiene `created_at` **exactamente igual** a `abierto_en` (medido: los 39 ítems de cada uno de los dos recuentos que existen). Mismo razonamiento que el `created_at > cerrado_en` del historial, **y la misma advertencia: si ese default pasa a `clock_timestamp()`, todos los precargados se verían como agregados a mano.** Ante la duda, `esAgregadoAMano` responde que NO.
   - **Franja permanente en el recuento abierto** (`f37583d`) con el riesgo operativo: el saldo se congela AL CERRAR, así que un remito, una baja o una transferencia cargados en esa unidad mientras se cuenta los compensa el ajuste del cierre y esa mercadería desaparece sin ningún error. **No es pegajosa a propósito**: `.rec-barra` ya ocupa el `top: 0`.
   - **"Imprimir la planilla"** (`a3bc858`, `window.print()` + `@media print`): la hoja para contar sin la tablet, agrupada por categoría, con un casillero vacío por renglón y su unidad. **Imprime la lista ENTERA** (ni el buscador ni el filtro la recortan) y **no imprime el saldo del sistema ni lo ya contado**: un número al lado de un casillero vacío se copia. `#rec-planilla` **no lleva `hidden`** —la regla global `[hidden] { display: none !important }` lo dejaría sin imprimir— y es **hijo directo de `<body>`**, que es de lo que depende que `body > *:not(.rec-planilla)` tape lo demás.
   - **`hoyLocal()`** (`a3bc858`): el módulo sacaba "hoy" de UTC (`new Date().toISOString().slice(0, 10)`) en la fecha de la planilla y en el `p_fecha` de `crear_transferencia_stock`, y después de las 21:00 de Argentina eso ya dice mañana. Los dos pasan por `hoyLocal()`, que usa la zona del dispositivo; hay una assertion sobre el fuente que impide que el patrón vuelva.
   - Guardado automático con debounce, flush al salir del campo y flush en **`visibilitychange → hidden`**: el caso real no es cerrar la pestaña, es el celular que se bloquea. Si una fila cambia mientras el guardado está en vuelo, esa fila sigue sucia y se reprograma otro intento.
   - Guard de 0 ítems: cerrar sin ítems **no da error del lado del servidor** —cierra, devuelve 0 ajustes y consume el único recuento abierto de la unidad—, así que lo bloquea la UI. Alguien podría creer que hizo el inventario inicial sin haber cargado nada.
   - **El resumen previo al cierre** (`c6aec0a`, `f37583d`) ordena por el tamaño de la diferencia y **marca en bordó las que superan el 20% del saldo del sistema**, con el texto de por qué. **Solo cuando hay saldo contra qué comparar**: con el sistema en 0 marcaría las 55 filas de una carga inicial. Y si **la relectura del saldo falla**, el modal lo dice (`#cierre-aviso-saldos`) y no pisa los saldos que ya tenía, en vez del `if (!error)` pelado de antes.
   - **Siguen abiertos, porque necesitan la base:** lote ilegible en el recuento (`stock_recuento_items` no tiene `lote_ilegible` y `agregar_item_recuento` exige lote en materia prima), abrir un recuento vacío, y la fecha de corte del saldo (que volvería innecesaria la franja). El `confirm()` de cancelar una transferencia sigue ahí.
   - Suites: `test-stock-recuento` **158/158** con `mut` **75/75**; `test-stock-numeros` 112/112 con `mut` 46/46; `test-stock-xss` 234/234 con `mut` 189/189 (+4 equivalentes); **`controles-stock.js` 988/988**, baseline fijo `2cd547a` (declara `data-insumo` como referencia que su inventario no ve —se escribe en una plantilla anidada dentro de una interpolación— y la excepción exige igual que el atributo esté en el archivo). Detalle y guion en `.claude/traspasos/2026-09-23-stock.md`, que se borró al aplicarse (queda en el historial: `git show 1e46793:.claude/traspasos/2026-09-23-stock.md`).

   **LOS CAMPOS NUMÉRICOS VAN `type="text"` + `inputmode="decimal"`, NUNCA `type="number"`** (`f6f96b3`) — ver la trampa en Aprendizajes clave. `parsearCantidad(crudo, {permitirNegativos, unidad})` acepta coma o punto como decimal, y el vacío sigue siendo el guion. `permitirNegativos` va en false por defecto y existe para el campo de AJUSTE, que lleva la cantidad con signo.
   - **`UNIDADES_ENTERAS`:** si la unidad del insumo se cuenta de a enteros (`un` y variantes), el campo **rechaza cualquier separador**. Con el punto y la coma valiendo los dos como decimal, `"47.000"` cajas daría 47 — un error de mil veces que en un recuento inicial, donde TODO da diferencia, no llama la atención de nadie. Verificado contra el catálogo: **32 de los 51 insumos activos se miden en `un`**, 14 en kg y 5 en lt, y los de mayor volumen (bolsas por decenas de miles) son justo los de unidades. En kg y lt el decimal sigue andando: `0,300` de lecitina es un caso real. **OJO: una unidad contable nueva ("cajas", "bolsas") hay que sumarla a esa lista o pierde la protección.**
   - Lo que no se pudo leer **se dice en la fila**. Sin el mensaje, el campo volvería al guion en silencio y la persona no tendría forma de saber que su número no entró — la misma clase de falla que el arreglo vino a cerrar. Y el valor ya guardado se repinta con coma y **sin separador de miles**: `formatearCantidadStock` daría `"1.234,5"`, que al releerlo tiene dos separadores y el parser rechaza, así que el campo se vaciaría solo al re-renderizar la lista.

   **`claveSaldo(insumoId, lote, contenido)` LLEVA TRES COMPONENTES**, con `?? ''` en los dos últimos — el mismo criterio que el `coalesce(contenido_por_bulto, 0)` del índice único. Es lo que indexa el Map de saldos del recuento contra `v_stock_por_lote`. **Modo de falla si se le saca el tercero:** esa vista agrupa por presentación, así que un lote en tacho de 215 y en bidón de 5 devuelve dos filas con la misma clave vieja, la segunda pisa a la primera en el Map, y al cerrar el recuento el conteo de una presentación se compara contra el saldo de la otra — ajuste enorme, sin ningún error a la vista.

   **SEGUNDA PARTE DE LA FASE 3** (`554aa3c`, agosto 2026): el historial y los movimientos puntuales.

   **`v_mis_unidades_baja`** (`security_invoker=true`) — id, nombre, ciudad; las unidades activas donde la persona tiene `dar_baja`. **Es una TERCERA vista de unidades y no un lujo**: `ver`, `ajustar_inventario` y `dar_baja` son tres tareas con tres alcances, y sacar los chips de un modo de la vista de otro ofrece unidades donde la RPC va a rechazar.

   **HISTORIAL DE RECUENTOS** — drill-down dentro de la pestaña **Stock**, no una cuarta pestaña: cuatro no entran en 375px, y el historial es `stock:ver`, la misma tarea que gatea esa pestaña. Mientras se lo mira, la pestaña marcada sigue siendo Stock (`PESTANA_DE_VISTA`). Lista desde `v_recuentos`, con chips de unidad; los anulados van atenuados y con su motivo, los abiertos con badge "En curso".
   - **`con_diferencia` solo se muestra en los cerrados.** En un abierto o un anulado `diferencia` está en null y la vista devuelve 0 siempre: un número que parece un resultado y es ausencia de dato.
   - **EL DETALLE ES LA FOTO CONGELADA Y NO SE RECALCULA NUNCA.** Contado, sistema y diferencia salen de `stock_recuento_items` tal como los dejó `cerrar_recuento`, aunque el stock se haya seguido moviendo. Es literalmente para lo que se pidió la pantalla: *"quiero ver cuánto contó Emanuel el 1 de agosto"*.
   - Y de ahí sale la regla que no hay que romper: **`cantidad_sistema` y `diferencia` SOLO existen en los cerrados.** En un abierto o un anulado quedaron en null, así que el detalle muestra únicamente lo contado, con un aviso que explica por qué no hay comparación. **Rellenarlas con el saldo de hoy sería inventar una foto que nunca se sacó.**
   - Los nombres se resuelven contra `v_empleados_publico` en consulta aparte, **NUNCA por embed a `empleados`**: su RLS solo deja leer la fila propia, así que un embed dejaría todos los nombres en blanco menos el tuyo.

   **MOVIMIENTO PUNTUAL** — un modal con dos modos y **tres puntos de entrada**: suelto desde la vista de Stock, precargado desde el detalle por lote, y precargado **y vinculado** desde el detalle de un recuento cerrado. Con un recuento de por medio el selector de modo no se dibuja: un movimiento vinculado es por definición un ajuste, y `registrar_baja_stock` ni siquiera recibe `p_recuento_id`.

   | | Ajuste | Baja |
   |---|---|---|
   | Tarea | `ajustar_inventario` | `dar_baja` |
   | Unidades | `v_mis_unidades_ajuste` | `v_mis_unidades_baja` |
   | Cantidad | magnitud + botones de signo | siempre positiva |

   - **Cambiar de modo resetea la unidad** si no está en el alcance del modo nuevo. Dejarla apuntando a una unidad donde la RPC rechaza es el dead-end de siempre.
   - Los movimientos exigen **además `stock:ver`**: la pantalla muestra el saldo para decidir qué tocar, y ese saldo sale de vistas `security_invoker` sobre el libro, cuyo RLS sigue siendo `ver` a secas. No es una restricción agregada — sin `ver` no hay nada que mostrar.
   - El lote sugiere los que tienen saldo pero es **campo libre**: puede haber entrado mercadería que todavía no se cargó.

   **EL SIGNO DEL AJUSTE VA EN DOS BOTONES ("Apareció" / "Falta"), NUNCA TIPEADO.** `inputmode="decimal"` **no ofrece la tecla del menos** en Android: es la misma familia que el bug de la coma —el teclado no da el carácter— y el modo de falla habría sido el simétrico, la persona escribe `12` porque es lo único que puede escribir y el ajuste **suma cuando tenía que restar**. Aparte, "Apareció / Falta" se entiende sin traducir mentalmente que un menos borra stock. Si alguien igual tipea `-12` —posible desde el escritorio— manda el signo tipeado y no se aplica dos veces. La **baja** no tiene signo que elegir y **rechaza un negativo tipeado**: sería una doble negación que terminaría sumando stock.

   **Las dos RPCs de movimiento**: `registrar_ajuste_stock(p_unidad_negocio_id, p_insumo_id, p_lote, p_cantidad, p_motivo, p_recuento_id default null, **p_contenido_por_bulto default null**)` → uuid, cantidad **con signo**; y `registrar_baja_stock(p_unidad_negocio_id, p_insumo_id, p_lote, p_cantidad, p_motivo, **p_contenido_por_bulto default null**)` → uuid, recibe la cantidad **siempre positiva** y la guarda en negativo. En las dos la presentación va **última**, y en el ajuste **después** de `p_recuento_id`. Las dos exigen motivo, y lote si el insumo es materia prima.
   - **NINGUNA valida que haya saldo, y es una decisión tomada**: frenar una operación por papeleo es peor que el número mal. Dar de baja más de lo que hay deja el saldo negativo; la UI **avisa el saldo que quedaría antes de confirmar y no bloquea**.
   - **INVARIANTE SOSTENIDA POR LA UI, NO POR EL SERVIDOR:** `registrar_ajuste_stock` no valida que `p_recuento_id` sea de la misma unidad ni que el recuento esté cerrado. Hoy solo se manda desde el detalle de un recuento cerrado, con su propia unidad. Si alguna vez se la llama desde otro lado, hay que revalidar eso ahí.

   **EL BADGE DE SALDO NEGATIVO DICE "Saldo negativo" Y YA NO AFIRMA UNA CAUSA.** Decía "Falta cargar un remito", que era cierto mientras la única forma de llegar a un negativo fuera consumir mercadería sin cargar. Desde esta fase también puede venir de una **baja intencional**, así que la etiqueta quedó neutra y las dos causas posibles se explican en el detalle por lote, que es donde hay lugar para nombrarlas. Una etiqueta no debería afirmar una causa que la pantalla no conoce.

   **Separar los ajustes del cierre de las correcciones posteriores** se hace con **`created_at > cerrado_en`**, y conviene saber de qué depende. `now()` en Postgres es el timestamp de la **transacción**: `cerrar_recuento` fija `cerrado_en = now()` e inserta sus ajustes en esa misma transacción, con `created_at` default `now()`, así que los del cierre tienen `created_at` **exactamente igual** a `cerrado_en` y el `>` los excluye bien. **Si algún día se cambia el DEFAULT de `stock_movimientos.created_at` a `clock_timestamp()`, los ajustes del cierre pasarían a tener `created_at` unos microsegundos MAYOR y esta pantalla los contaría a todos como correcciones posteriores** — en silencio, y a nadie se le va a ocurrir mirar el historial al tocar un default. Está escrito al lado de la comparación, en el código.

   **EL MODAL DE CORREGIR STOCK** (`d5ed5c8`, septiembre 2026) — seis bugs encontrados usando la app, todos en el mismo modal.
   - **EL CERO QUE SE INVENTA.** `saldoActualMov()` devolvía `0` en dos situaciones distintas: **sin unidad elegida** —`cargarLotesMov` corta temprano, la lista queda vacía, el `find` no encuentra nada, DATO AUSENTE— y **con unidad e insumo pero sin fila para ese lote y presentación** —cero REAL, la vista tiene `HAVING sum <> 0`—. Ahora devuelve `null` en el primero, y con `null` no se dibuja NADA: ni la cifra, ni el "después del movimiento", ni el aviso de negativo, que se calculaban todos sobre ese cero. **El cero real ahora se dice CON PALABRAS** ("El sistema no tiene saldo registrado para este lote y presentación en esta unidad"): sin eso, arreglar el cero falso dejaba el verdadero indistinguible del que se acababa de sacar.
     - **ES LA CUARTA APARICIÓN DE LA MISMA FAMILIA** — la coma descartada por `type="number"`, `Number(null)` devolviendo 0, el espacio interno que "8 5" volvía en 85, y ahora el saldo sin unidad. Todas: **un dato AUSENTE presentado como un número plausible, sin error y sin señal**. Y esta es la PEOR de las cuatro, porque el número que inventaba era **justamente el que dice "no hay nada"**: alguien podía dar de baja mercadería creyendo que el sistema estaba desactualizado, o no darla creyendo que ya estaba en cero. La regla que ya está escrita para las tres primeras vale igual: **rechazar `null`, `undefined` y `''` ANTES de convertir, y devolver `null` cuando no se puede saber, no un cero que se lee como un dato**.
   - **UN TERCER EFECTO DEL MISMO CERO:** sin unidad, la lista de presentaciones conocidas viene vacía **por no haberse consultado** y `renderizarPresentacionesMov` **elegía "Sin presentación" sola**. Esa decisión sin datos SÍ viajaba al `insert`. Corregido en el mismo lugar, mirando `m.unidadId` antes de decidir.
   - **EL ORDEN INVERTIDO, que resuelve el problema de raíz.** La unidad de negocio es lo PRIMERO y **nada se habilita hasta elegirla**: insumo, lote, cantidad, motivo y el botón de confirmar quedan deshabilitados con el motivo escrito en pantalla. Sin unidad no hay saldo que mostrar, así que se pide primero lo que decide el resto. Con **una sola unidad disponible se elige sola y el paso no se muestra** — pedir una decisión que no existe es ruido. `disabled` se aplica DESDE JS y no como atributo del HTML: si el script fallara, los campos quedan usables como antes en vez de dejar el formulario inutilizable (mismo criterio que los botones de contraseña).
   - **EL RESETEO VA AL ABRIR, NO AL CERRAR**, a propósito: cerrar puede pasar por caminos que la función no controla —el gesto de atrás del celular, sin ir más lejos— y el reseteo tiene que valer igual. `abrirModalMovimiento` llama a `quitarInsumoMov()` como primera cosa; la precarga desde el detalle por lote y desde un recuento se aplica DESPUÉS y sigue funcionando. Antes el alta limpiaba seis campos pero no el chip del insumo elegido: el DOM mostraba "Caja capelina" seleccionada y `estado.mov.insumo` era `null` — el formulario se abría con datos viejos y era un movimiento equivocado esperando a que alguien confirmara sin mirar.
   - **EL MODAL Y EL BOTÓN DE ATRÁS DEL CELULAR.** No había **NINGÚN** manejo de historial en todo el proyecto —ni `pushState` ni `popstate`, en ningún archivo— así que con un modal a pantalla completa abierto, el gesto de atrás **salía de la página** en vez de cerrarlo, y de ahí venía el bug reportado de "la pantalla queda rota". Este modal ahora empuja una entrada con `pushState({ modalStock: true }, '')` al abrir y se cierra con `popstate`; el cierre normal saca la entrada con `history.back()` para no tener que apretar atrás dos veces. **Los otros ocho modales del archivo siguen igual** —el helper `abrirConHistorial(cerrarModal)` está acotado a este caso porque los otros no se reportaron— y **queda anotado en el código que adoptarlo es una línea**, para que el próximo que toque un modal sepa que existe.
   - **LOS TEXTOS.** El botón pasa de "Registrar movimiento" a **"Corregir stock"** —no dice el nombre del sistema sino qué hace—; y los dos modos, de "Ajuste" / "Baja" a **"Igualar a lo que hay"** / **"Dar de baja"**, con explicaciones que dicen QUÉ PASÓ y no qué operación es. **Se descartó "Sumar o restar" para el modo de ajuste porque una baja también resta**: describe algo que hacen los dos y no distingue nada. "Igualar a lo que hay" dice lo único que es propio del modo. Y se descartó "Corregir el conteo" porque **"conteo" apunta al recuento, que es otra pantalla**: alguien podía entrar ahí buscando contar.
     - **UN COSTO DE MOBILE que hay que anticipar:** el rótulo largo no entra en media pantalla de 375px con el `white-space: nowrap` de `.pestana-stock`. Se aflojó SOLO dentro del modal (`#mov-modos .pestana-stock { white-space: normal }`); las pestañas de la pantalla siguen en una línea. **La pestaña se adapta al texto y no al revés** — el texto es el que le sirve a quien está en el depósito.
   - **LA JERARQUÍA DEL MODAL.** Había tres cajas con el mismo fondo gris —explicación del modo, saldo, aviso del motivo— pesando igual entre sí y más que los títulos de sección. **El saldo es EL DATO** por el que se abre esta pantalla y pasa a leerse como una cifra (`1.25rem/700`), con la receta de tarjeta del listado y el patrón de Cuentas Corrientes —etiqueta chica arriba, número grande abajo—, y con TRES estados: sin unidad no se dibuja, con saldo muestra la cifra, y en cero real suma la aclaración con palabras. La explicación del modo pierde la caja y queda como texto suelto; el aviso del motivo también, más chico y colgando del campo. Los títulos de sección suben de peso **SOLO dentro del modal** —`#modal-movimiento .campo-stock label`— para no propagar el cambio al resto del módulo.

   - Pendiente: **nada de las fases 2 y 3 se verificó en un navegador con datos reales, salvo el campo de cantidad** — ahí Facu encontró el bug de la coma tocando la pantalla, con 134 verificaciones del harness en verde. Con el libro en cero, el historial abre vacío y **un ajuste positivo es hoy la única forma de meter stock sin un remito**, así que es lo primero que se puede ejercitar de verdad.
   **FASE 4 — TRANSFERENCIAS ENTRE UNIDADES** (`52e6b6f` el envío y el tránsito; la recepción vive en Ingreso, `96932de`). Cucuruchos le presta mercadería a Dolce Pasta. Todo verificado contra la base el 15/09/2026.

   - `stock_transferencias`: id, unidad_origen_id, unidad_destino_id, estado, fecha, observaciones, creado_por, respondido_por, respondido_en, motivo_rechazo, created_at. `estado` ∈ `pendiente`/`aceptada`/`rechazada`/`cancelada` (CHECK `chk_estado_transf`), motivo obligatorio al rechazar (`chk_motivo_rechazo`) y `chk_origen_distinto_destino`.
   - `stock_transferencia_items`: id, transferencia_id, insumo_id, **lote**, **contenido_por_bulto**, cantidad_enviada, cantidad_recibida, motivo_diferencia. `cantidad_enviada > 0`; `cantidad_recibida` **null = todavía sin responder**, y si no es null tiene que ser `>= 0`.
     - **EL LOTE Y LA PRESENTACIÓN VIAJAN CON LA MERCADERÍA, y esa es la decisión de fondo de la tabla.** Los dos se guardan en el ítem y los dos se copian a los movimientos de las dos puntas. Sin eso la trazabilidad se corta JUSTO en la transferencia —el lote entra al destino como null y no se puede reconstruir de qué partida salió— y el desglose por bultos también: `v_stock_por_lote` agrupa por `(lote, contenido_por_bulto)`, así que la mercadería llegaría a destino "sin presentación" y el listado dejaría de poder mostrarla en bultos.

   **EL CIRCUITO, y por qué el saldo nunca queda al doble ni en el aire:**
   - **Enviar DESCUENTA DEL ORIGEN EN EL ACTO** (`transferencia_salida`, negativo). `crear_transferencia_stock` valida antes que haya saldo **de esa terna exacta** —insumo + lote + presentación, con `is not distinct from` para que los null matcheen—, así que no se puede despachar lo que no está.
   - **En tránsito la mercadería NO ESTÁ EN NINGÚN STOCK.** Salió del origen y todavía no entró al destino: eso es correcto y es lo que hace que el chip de días en tránsito importe —si nadie la recibe, esa mercadería no figura en ningún lado—.
   - **Recibir suma al destino LO REALMENTE RECIBIDO** (`transferencia_entrada`, positivo), no lo despachado.
   - **Rechazar y cancelar devuelven TODO al origen** con `transferencia_devolucion` por la `cantidad_enviada` completa. Cancelar solo corre sobre una pendiente, y la puede hacer quien la creó **o** quien tenga `enviar_transferencia` en el origen.

   **LA MERMA DEL TRASLADO NO DEJA NINGUNA FILA EN EL LIBRO, y hay que saberlo:** verificado sobre la única transferencia real —500 bolsas despachadas, 450 recibidas— el libro tiene **−500 en el origen y +450 en el destino, y nada más**. Las 50 que faltan no son un movimiento: son la resta entre dos. Por eso `v_mermas` las **calcula** en vez de leerlas, y por eso `perdida_traslado` no está en el CHECK de `motivo_tipo` ni se escribe nunca (ver el tablero más abajo).

   **Las tres RPCs** (SECURITY DEFINER, las tres con `search_path` fijo — verificado contra `pg_proc.proconfig`):
   - `crear_transferencia_stock(p_unidad_origen_id, p_unidad_destino_id, p_fecha, p_observaciones, p_items jsonb)` → uuid. Exige `enviar_transferencia` **en el origen**. `p_items` = `[{insumo_id, lote, contenido_por_bulto, cantidad}]`.
   - `cancelar_transferencia_stock(p_transferencia_id)` → void.
   - `responder_transferencia_stock(p_transferencia_id, p_respuesta, p_items jsonb, p_motivo_rechazo)` → jsonb. Exige `recibir_transferencia` **en el destino**. Sus tres cortes: **no se puede recibir más de lo enviado** ("si hay de más, es un ajuste de inventario aparte"), **una diferencia sin motivo se rechaza**, y **faltan ítems por confirmar** si alguno quedó en null. Un `0` sí se acepta —"conté y no llegó nada"— y no inserta movimiento.

   **Las vistas nuevas de la fase** (las tres con `security_invoker=true`):
   - `v_stock_en_transito` (id, unidad_origen_id, origen_nombre, unidad_destino_id, destino_nombre, fecha, creado_por, created_at, **dias_en_transito**, items) — solo las `pendiente`. `dias_en_transito` es `CURRENT_DATE - fecha`, o sea contra la fecha del DESPACHO y no contra `created_at`: lo que hace vieja a una transferencia es cuánto hace que la mercadería salió.
   - `v_mis_unidades_envio` y `v_mis_unidades_recepcion` — las unidades activas donde la persona puede enviar / recibir, resueltas con la MISMA `tiene_tarea_alcance` que las RPCs. **Son la CUARTA y la QUINTA vista de unidades del módulo** (con `stock`, `ajuste` y `baja`), y no es un lujo: son cinco tareas con cinco alcances, y sacar los chips de un modo de la vista de otro ofrece unidades donde la RPC va a rechazar.

   **LAS POLICIES SON "VER ORIGEN **OR** VER DESTINO", y tiene que ser así:** el destino necesita ver la transferencia para poder recibirla, aunque no opere en el origen; si la policy mirara solo el origen, la mercadería llegaría a una pantalla vacía. Las dos tablas tienen **SOLO policy de SELECT** —cero escritura desde el cliente—: todo entra por las tres RPCs.
   - **Ese `OR` tiene dos consecuencias documentadas, en módulos distintos:** en el listado de Ingreso obligó a filtrar por `unidad_destino_id` contra `v_mis_unidades_stock` (ver ese módulo), y en el tablero de mermas obligó a poner un `tiene_tarea_alcance` propio adentro de la vista (ver abajo). Las dos veces el mismo `OR`, correcto para recibir y de más para mirar.

   **LA PANTALLA DE RECEPCIÓN VIVE EN INGRESO Y NO CREA UN INGRESO.** Su única escritura es `responder_transferencia_stock`. Los TRES motivos están escritos también en el módulo de Ingreso, y van acá porque **es lo que alguien va a querer "simplificar"** —"si es un ingreso, que cree un ingreso"—:
   1. **`materia_prima_ingresos.tipo_doc` no contempla transferencias**: su CHECK admite `remito` / `factura_a` / `factura_x` / `sin_comprobante`, y una transferencia interna no es ninguno.
   2. **EL DOBLE ESPEJADO, que es el que hace daño en silencio.** `materia_prima_items` se espeja al libro por `trg_espejar_stock_ingreso`, así que si la recepción además creara ítems, la mercadería entraría **DOS VECES**: una por el espejo y otra por el `transferencia_entrada` de la RPC. **El stock quedaría al doble sin ningún error.** Este es el motivo que no se ve leyendo el esquema.
   3. **Se perdería el vínculo con la transferencia de origen**: el ingreso paralelo no sabría de qué envío salió y el circuito deja de ser auditable de punta a punta.

   **SE RECIBE EN BULTOS, NO PESANDO.** Con `contenido_por_bulto` cargado, la pantalla cuenta **bultos + fracción** (`¼ ⅓ ½ ⅔ ¾`) y el helper traduce a unidad base: un bidón que salió medio usado **se recibe como medio bidón**, no pesando los litros que quedan. Sin presentación se carga la cantidad directo — y ése es hoy el único caso que existe en la base, así que si se hubiera construido solo el camino de bultos la pantalla no habría servido para nada.
   - El helper de fracciones es el **INVERSO** del de este módulo y lleva nombre propio a propósito: `baseDesdeBultos()` va de bultos a unidad base para GUARDAR, y `equivalenteEnBultos()` de stock.html va de unidad base a bultos para MOSTRAR. Llamarlos igual invitaría a "unificarlos" y romper los dos.

   **EL TABLERO DE MERMAS** (`7f5ebbc`, septiembre 2026) — drill-down de la pestaña Stock con `PESTANA_DE_VISTA`, gateado por `stock:ver`. El listado de todo lo que se perdió, de cualquier origen, agrupado por mes y el más reciente primero.

   - `v_mermas` (`security_invoker=true`), **14 columnas**: unidad_negocio_id, unidad_nombre, fecha, mes, insumo_id, insumo_nombre, marca, unidad_medida, lote, cantidad, origen, motivo_tipo, motivo_texto, empleado_id. `cantidad` viene **SIEMPRE POSITIVA** —es cuánto se perdió, no un saldo— y `mes` es el primer día del mes, para agrupar.
   - **Las TRES fuentes**, una por rama: las **bajas** (todas), los **ajustes negativos con `motivo_tipo='faltante_recuento'`** (y solo esos), y la **diferencia de las transferencias aceptadas** (`cantidad_enviada - cantidad_recibida`, con `origen='transferencia'`).

   **EL TOTAL DEL MES ES UN CONTEO DE EVENTOS MÁS SUBTOTALES POR UNIDAD DE MEDIDA, Y NUNCA UN NÚMERO ÚNICO.** No se pueden sumar 50 bolsas con 20 kg de harina: "70" no significa nada. Y **no hay ningún número único posible, tampoco uno en plata**: este módulo NO captura precios por decisión de diseño explícita, así que no existe una unidad común a la que convertir y cualquier cifra única mentiría. Entonces el número que manda es **cuántas veces pasó** —lo único que se suma sin mentir— y los subtotales por unidad van como contexto, con su limitación escrita en el código: sumar `un` de bolsas con `un` de cajas es coherente en la DIMENSIÓN pero no dice cuánto costó.

   **LA MERMA DEL TRASLADO SE IMPUTA AL ORIGEN, y de ahí salió la corrección más fina de la fase.** La tercera rama usa `t.unidad_origen_id` como `unidad_negocio_id` —la mercadería salió de ese stock y no llegó— y **lleva su propio `tiene_tarea_alcance('stock','ver', t.unidad_origen_id)` adentro de la vista**. Hace falta porque la policy de `stock_transferencia_items` es **"origen OR destino"**, que es *correcta para recibir*, pero con `security_invoker` eso entregaba a alguien con permiso solo en el DESTINO una fila de merma de una unidad **que no ve en ninguna otra pantalla del módulo**. **Con ese filtro las tres ramas comparten criterio de visibilidad.**
   - **SE ARREGLÓ EN LA VISTA Y NO EN EL CLIENTE, a propósito:** un filtro de UI esconde la fila y el dato viaja al navegador igual. Los chips de la pantalla son un FILTRO, no una barrera — está comentado así en `cargarMermas()`, con una mutación que pone la suite en rojo si alguien "completa" el alcance del lado del cliente.
   - Consecuencia para leer el tablero: la fila dice la unidad que **PERDIÓ** la mercadería, y **el destino no está en la vista**, así que la fila no dice a dónde iba en vez de inventarlo.

   **LA PANTALLA VA EN `--bordo` Y NO EN EL VERDE DEL MÓDULO.** Es el listado de lo que se perdió, y `--bordo` ya es el color de alerta del proyecto. **La diferencia con los otros usos es que acá NO es una alerta puntual dentro de una pantalla verde: es el acento DE la pantalla.** Y por eso mismo no va en todo — lo llevan el mes, los números y el chip de baja; el nombre del insumo, el motivo y la meta siguen siendo texto normal, porque si se pinta todo el acento deja de señalar nada.
   - **El chip de ORIGEN son TRES tratamientos distintos y no uno:** bordó la **baja** (pérdida constatada), ámbar el **recuento** (falta y nadie sabe por qué: una pregunta abierta, y el ámbar ya significa "esto espera revisión" en este archivo) y gris neutro el **traslado**.
   - **La marca destacada sigue en `--verde` y NO se pasó a bordó**: significa IDENTIDAD, igual que en las otras dos listas, y pintarla del color de alerta la convertiría en una advertencia sobre un insumo que no tiene nada malo. Lo exige `test-stock-marca.js`.
   - El vacío **explica de dónde salen las mermas** en vez de un "no hay datos" seco: hoy el tablero casi vacío es el estado NORMAL.

   **`stock_movimientos.motivo_tipo`** (text, nullable, CHECK `chk_motivo_tipo` con **10 valores**, verificado el 15/09/2026):
   - de **ajuste**: `faltante_recuento`, `sobrante_recuento`, `aparecio_otro_lado`, `correccion_carga`, `carga_inicial`
   - de **baja**: `rotura`, `mojado`, `vencido`, `robo`, `otro`
   - **`perdida_traslado` LA GENERA LA VISTA Y NO ESTÁ EN EL CHECK NI SE ESCRIBE NUNCA**: la diferencia de una transferencia no es un movimiento con motivo, es la resta entre lo despachado y lo recibido (ver arriba: no deja fila en el libro).
   - **LA VALIDACIÓN CRUZADA VIVE EN LAS DOS RPCs, y el CHECK no alcanza:** `chk_motivo_tipo` admite los diez en **cualquier** `tipo`, así que sin esa validación una **baja marcada `carga_inicial` desaparecería del tablero sin que nadie lo note** — la rama de bajas de `v_mermas` las trae todas, pero el motivo mostrado sería una etiqueta de ajuste y el reporte quedaría mintiendo sobre la causa. `registrar_baja_stock` acepta solo los cinco de baja y `registrar_ajuste_stock` solo los cinco de ajuste, las dos con su `raise` propio. La UI ofrece lo correcto, pero eso es cosmético.
   - **Una baja SIN clasificar llega como NULL y la pantalla dice "Sin clasificar".** La vista tuvo un `coalesce(motivo_tipo,'otro')` y **se sacó**: `'otro'` es una de las diez opciones del CHECK, o sea una respuesta que alguien **eligió**, y mostrar así las bajas anteriores a la columna las volvía indistinguibles de las clasificadas de verdad.

   **SOLO `faltante_recuento` ENTRA AL TABLERO DE LOS AJUSTES, y hay que dejar escrito por qué** o alguien mete las correcciones de carga en el reporte de pérdidas: los otros cuatro son **correcciones administrativas** —la mercadería estaba mal ubicada, mal cargada, o recién se está cargando por primera vez— así que **nunca se perdió nada**. Contarlas como merma haría que el reporte de fin de mes midiera errores de tipeo. La rama de recuento de `v_mermas` filtra `motivo_tipo = 'faltante_recuento'` y deja los otros cuatro afuera.
   - En el modal, **los cuatro se mapean igual** con la nota de por qué no aparecen nunca: sin eso alguien lo lee como un olvido y los "arregla".
   - **La línea debajo del selector dice si esa opción cuenta como pérdida**, porque en un `<option>` no entra — **y solo en modo AJUSTE**: toda baja es una pérdida, así que repetirlo en las cinco opciones no distingue nada y se vuelve el cartel que se muestra siempre.

   **LOS SELECTORES DE `motivo_tipo`** (`5c1f0e1`, septiembre 2026) — sin ellos el tablero solo mostraba transferencias: la columna existía y `v_mermas` la leía, pero nadie la escribía.
   - **Sin preselección.** Un default silencioso afirmaría por la persona justo lo que decide si el movimiento entra al tablero — el mismo criterio por el que el wizard de Ingreso no preselecciona el tipo de insumo. El poblado corre **ANTES** de tocar el `value` (ya mordió con `vista_preferida`) y el selector se **repuebla entero** al cambiar de modo: los cinco motivos son otros, y arrastrar el elegido dejaría una clave que la RPC rechaza recién al confirmar.
   - **EL TEXTO LIBRE SIGUE SIENDO OBLIGATORIO Y NO SE REEMPLAZA.** El `motivo_tipo` **agrupa** para el reporte —es lo que permite separar un faltante real de una corrección de tipeo— y el texto **explica ESTE caso**. Ninguno sustituye al otro, y las dos RPCs exigen los dos.
   - Con **"Otra cosa"** el texto exige un mínimo de **10 caracteres**: esa etiqueta es la ausencia de etiqueta, así que el reporte depende entero del texto libre. El número es **arbitrario y está declarado como tal** — no garantiza que lo escrito sirva, pero descarta "roto" y "varios", que es lo que se escribe cuando el campo solo exige no estar vacío.

   **`cerrar_recuento(p_recuento_id uuid, p_clase text default 'control')`** — la firma cambió en esta fase, con DROP explícito. Mapea: en **`'control'`** los negativos van `faltante_recuento` y los positivos `sobrante_recuento`; en **`'carga_inicial'`** los dos signos van `carga_inicial`. Rechaza cualquier otro valor.
   - **POR QUÉ EL SIGNO NO ALCANZA, y esto salió de los datos:** de los **21 movimientos del recuento inicial, DOS son NEGATIVOS** y ninguno de los 21 fue un faltante —era la primera carga de ese stock—. Con la regla "negativo = faltante", **esos dos habrían entrado al tablero como pérdidas que nunca ocurrieron**.
   - **La clase es una propiedad DEL RECUENTO ENTERO, no del renglón**, así que se pregunta **UNA vez al cerrar** y no una por renglón. El signo sí alcanza para separar faltante de sobrante *dentro* de un control — eso queda automático y es la fuente que más va a alimentar el tablero sin trabajo extra.
   - **La pantalla no preselecciona nada**: el `default 'control'` del servidor es una **red de seguridad, no el camino esperado**. Sin clase elegida el botón de cerrar queda `disabled` —desde JS, no como atributo— y `confirmarCierre` revalida antes de llamar.
   - **El parámetro se manda SIEMPRE, nunca omitido.** Es la misma familia que el borrado por omisión, con una diferencia: acá no borra, **afirma** — una carga inicial cerrada sin el parámetro mandaría sus negativos al tablero como pérdidas.
   - **PENDIENTE ABIERTO** (sigue sin tocarse a septiembre 2026): **`confirmarTransferencia` muestra `error.message` de la RPC a ciegas** (`52e6b6f`): `mostrarError(error.message || '…')`. Es el mismo riesgo que se cerró en la pantalla de recepción de Ingreso con un diccionario y fallback al genérico —lo que puso `chk_presentacion_coherente` en pantalla fue exactamente un passthrough así—. La RPC de crear también tiene mensajes propios en castellano, así que hoy no es visible, pero cualquier error que no sea suyo llega crudo. Territorio de este chat.

8. **Cobranzas** (`modulos/cobranzas.html`, clave `cobranzas`, orden 8, sin grupo — `6654fac` el módulo y la Edge Function, `53e7cc8` el catálogo de tareas; la base se creó el 16/09/2026). Los repartidores registran lo que cobran en la calle: **cheques en papel con foto, y efectivo.**
   - **El cliente es TEXTO LIBRE y no una lista, a propósito:** hoy no existe un padrón de clientes en la base, y la conciliación contra uno es otra pantalla, todavía no construida. Lo que sí hay es `cliente_normalizado` (columna generada, ver la tabla), así que el buscador del historial encuentra "Don Pepe" escrito de cinco formas distintas — **el texto libre no impide agrupar, solo impide validar**.
   - **La fecha arranca en HOY y solo va hacia atrás**, con la hora de Argentina (`hoyArgentina()` en el cliente, `_cobranza_hoy_ar()` en el servidor). Las dos puntas lo validan: Supabase corre en UTC, así que un "hoy" mal resuelto adelanta el día nueve horas antes de medianoche — justo cuando el repartidor cierra la jornada.
   - **La foto del cheque es OBLIGATORIA**, y una misma foto puede tener varios cheques y se comparte entre ellos (es normal fotografiar tres juntos sobre el mostrador). Sin foto no hay forma de verificar después qué decía el papel, que es todo el punto del módulo.
   - **EL OCR PROPONE Y LA PERSONA CONFIRMA, cheque por cheque.** Cada uno se abre como una tarjeta con lo que leyó la Edge Function `ocr-cheques` y hay que confirmarlo; lo que se corrige queda marcado `ocr_corregido` y lo que se carga a mano, `manual` (ver `origen_datos` y `ocr_propuesto` en la tabla). Mismo criterio que el alias de Ingreso y las presentaciones aprendidas: **nunca se aplica callado**, porque una lectura equivocada que se guarda sola se propaga a la cartera entera.
   - **Estados `registrada` → `procesada` / `anulada`.** Una cobranza **procesada queda bloqueada** —no se edita— y para tocarla hay que reabrirla con motivo; anular exige motivo y **anula también sus cheques**, liberando el número para volver a cargarlo bien.
   - **FUNCIONA SIN SEÑAL, y no como un agregado:** el repartidor cobra en la calle, donde el celular no tiene datos. El borrador vive en **IndexedDB con las fotos ya comprimidas** (`localStorage` no guarda Blobs) y un sincronizador las sube cuando hay red. **El alta es idempotente por el `id` que genera el celular**, así que un reintento después de una respuesta perdida no duplica la cobranza — es lo que hace que reintentar sea seguro.
   - **NO TOCA CAJA, y es deliberado.** Una cobranza es el registro de que un papel llegó a la mano de alguien, no todavía un movimiento de plata: el ingreso a la caja de Empresa lo va a definir **la pantalla de asignación de pagos** (requisitos en Pendientes).
   - **LA UNIDAD DE NEGOCIO SE ELIGE AL ASENTAR** (22/09/2026, `b65ac10`, A1): el repartidor no la sabe. "Controlada, asentar" (id `cob-btn-procesar`) abre un diálogo propio (`#cob-dialogo-unidad`: foco atrapado, Escape cancela, 44 px) con las unidades activas —sin preselección, salvo que ya tenga una o haya una sola— y llama a `marcar_cobranza_asentada`; confirmar queda `disabled` desde JS hasta elegir. El detalle muestra "Unidad de negocio · <nombre>" o, en una asentada sin unidad, "Sin unidad" en gris; con `cobranzas:procesar`, "Asignar unidad" / "Cambiar" → `asignar_unidad_cobranza`. El historial muestra "Unidad asignada: <unidad> · quién · fecha". En la fila de celular de una asentada, la unidad va en la línea chica. Suite `test-cobranzas-unidad.js` (+ mut). Detalle en `.claude/traspasos/2026-09-22-cobranzas-3.md`.
   - **Solo pesos**, por CHECK y no por default (ver `cobranzas.moneda`).
   - **"Agregar un cheque que no se leyó"** (22/09/2026, `e7a73e2`): la foto sigue siendo obligatoria para cada cheque (`cobranza_cheques.foto_id` NOT NULL). Sin ninguna foto, `#cob-btn-cheque-mano` queda deshabilitado —desde JS— con "Primero sacá la foto" debajo (`#cob-ayuda-cheque-mano`); con fotos se llama "+ Agregar un cheque que no se leyó", con una sola se vincula a esa y con varias pregunta con el diálogo de miniaturas. Lo recalcula `pintarBotonChequeMano()`, llamada desde `pintarEstadoFotos()`. Detalle: `.claude/traspasos/2026-09-22-cobranzas-2.md`.
   - **LA CARTERA DE CHEQUES SE MUDÓ A SU PROPIO MÓDULO el 22/09/2026 (`c0f8124`)**: ver el módulo 9, Cheques. En Cobranzas quedó: el link **"Cartera de cheques →"** (`#cob-link-cheques`, solo con `ver_todo` o `procesar`), el **link directo `cobranzas.html?cobranza=<uuid>&volver=<url>`** (abre el detalle de esa cobranza, en escritorio en el panel; limpia la barra con `replaceState` antes de abrir; `volver=` validado IGUAL que Gastos 8f73654 —`new URL(decodeURIComponent(v), location.href)`, solo http/https y mismo origen—, y el botón dice "‹ Volver a los cheques" si va a `cheques.html`; vale solo para la cobranza del link), y en el detalle cada cheque muestra su estado y su salida **en solo lectura** con un **"Ver en Cheques"** (`cheques.html?cheque=<id>`). **Dar salida y volver a cartera ya NO existen en Cobranzas.** El detalle de lo que sigue es HISTÓRICO (cómo era la vista antes de mudarse); las reglas valen igual en Cheques. Detalle del cambio: `.claude/traspasos/2026-09-22-cobranzas.md`.
   - **VISTA "CHEQUES"** (`c682479`, 21/09/2026 — hoy en `modulos/cheques.html`): una planilla con una fila por cheque (número, banco por nombre, emisión, pago —"A la vista" en un común—, importe, cliente, estado, y fecha y destino de salida). Arriba, el total y la cantidad **de toda la cartera, sin aplicar filtros**; si hay un filtro puesto, lo aclara. Filtro de estado: en cartera (el default) / salidos / todos. **Los anulados solo aparecen en "todos".** El orden es por fecha de pago ascendente, y en un cheque común por la de emisión: lo que se cobra primero queda arriba. Los que salieron se ven atenuados, con una etiqueta de Depositado o Endosado. Tocar una fila abre su cobranza. A 375px la tabla scrollea de costado **dentro de su propio contenedor**, con la columna del número fija, y la página no.
     - **LOS FILTROS POR NÚMERO Y POR BANCO VIVEN ACÁ, y no en la lista de cobranzas.** Estuvieron un tiempo en la lista, y ahí obligaban a una consulta en dos pasos con `.in()` y un tope de 200 ids. Filtrando `cobranza_cheques` directamente, ese camino desapareció. El tope medido de `.in()`, por si vuelve a hacer falta: **650 ids entran, 700 se rechazan**.
     - El filtro por número arma el `like` **con los dígitos ya extraídos, nunca con el texto crudo**, y reusa `dvBcra()`. Un `%` tipeado da cero resultados y un aviso, nunca todos los cheques. La lista de bancos del select se arma desde **todos** los cheques visibles, sin aplicar los otros filtros, y se recarga al guardar, anular o marcar una salida.
     - El cliente de cada cheque sale de una segunda consulta, sin embed y sin `.in()`. Si una consulta llega a las 1000 filas que corta PostgREST, **la pantalla lo avisa**.
   - **SALIDA DE CHEQUES** (`0822501`): en cada fila en cartera, un botón "Salió", visible solo con `cobranzas:procesar` y con la cobranza procesada. Abre un diálogo propio, sin `prompt` ni `confirm`: Depositado / Endosado, una fecha que por defecto es hoy (nunca futura ni anterior a la cobranza) y el destino, obligatorio en el endoso. Los que salieron tienen "Volver a cartera", con motivo. El detalle muestra el estado y la salida de cada cheque, más un aviso neutro si hay alguno afuera, y el historial muestra las dos acciones nuevas.
     - **LA PRUEBA MANUAL DEL ERROR DEL TRIGGER NO SE HIZO:** hacía falta una sesión real. Está probado ejecutando el código con el texto exacto del trigger. Los pasos para hacerla a mano están en el traspaso del 21/09/2026.
     - **El pie de `completado_desde_cmc7` puede afirmar algo falso:** el flag es **por cheque**, no por renglón, así que dice "lo calculó el sistema" también en los renglones que se leyeron del papel. Arreglarlo de fondo requiere que `ocr-cheques` diga qué renglones completó. **Desde el 21/09/2026 (`a413663`) dice la opción A**, que no afirma nada falso: *"Puede que este renglón no se haya leído del papel: en este cheque el sistema completó datos desde la banda magnética y calculó su dígito. Comparalo con lo impreso."* Sigue saliendo en los tres renglones hasta que `ocr-cheques` diga cuáles completó. En el mismo commit, la vista "Cheques" en escritorio (desde 1100 px) pasó a usar el ancho del listado: a 1440 px entran todas las columnas; el banco va en una línea con el nombre completo en `title`.
   - **REDISEÑO CON ACENTO NARANJA (21/09/2026, `bbb0a79` → `44f6db3`), a partir de un handoff de Claude Design.** Baseline fijo del rediseño: **`fba6396`**.
     - **Color del módulo: `--naranja` `#C2410C` / `--naranja-suave` `#FDEEE4` / `--naranja-oscuro` `#8F2F08`**, en el `:root` de `css/main.css`; reemplazan a las tres del acento anterior, que **ya no existen en el repo** (`dashboard.html` usa `color: 'naranja'` en la tarjeta del módulo). Contraste medido: **5,18:1** blanco sobre naranja y **7,19:1** del oscuro sobre el suave (el README del diseño decía 4,9). **Por qué cambió:** el tono suave anterior se leía igual que el amarillo de aviso, y el naranja tiene que ser claramente más rojo que el marrón de Insumos.
     - **EL NARANJA NUNCA MARCA VALIDEZ:** verde y bordó son del sistema; el naranja es "esto es Cobranzas". Los bordes de validación del dígito verificador siguen verde/bordó y le ganan al foco naranja.
     - **Grosor 600 donde el diseño dice 650:** `main.css` carga Inter en pesos estáticos (400…800) y no se tocó el import global. Los `650` que ya había en el módulo se ven como 700.
     - **Listado (3.2):** el total va en **tinta neutra, nunca en el color del módulo**; el estado va en una **franja izquierda** (naranja si falta procesar, bordó si está anulada) más un texto chico en mayúsculas, sin chip; la cantidad de cheques en naranja oscuro solo si está registrada. Los filtros de estado de Cobranzas y Cheques son un **segmentado** (`.cob-segmento`, `aria-pressed`, 44 px, fondo `--cob-segmento-fondo` = naranja al 9% con `color-mix`); `.cob-chip` queda solo para el tipo de cheque del formulario.
     - **NOMBRES DE LOS ESTADOS, SOLO EN PANTALLA** (21/09/2026): `registrada` → **"Por controlar"**, `procesada` → **"Asentada"** (segmentado "Asentadas"), `anulada` → "Anulada". Una sola tabla `ETIQUETA_ESTADO_COBRANZA` la usan la fila y el detalle. Botón del detalle **"Controlada, asentar"** (conserva el id `cob-btn-procesar`), dato **"Asentada por"**, historial **"Asentada · nombre · fecha"**, éxito "Cobranza asentada.". El segmentado quedó **Por controlar / Asentadas / Anuladas / Todas** (el default sigue siendo "Todas"). **LO QUE VIAJA A LA BASE NO CAMBIÓ:** los `data-estado`, los filtros por valor, las RPCs (`marcar_cobranza_procesada`/`reabrir_cobranza`/`anular_cobranza`) y el historial guardado (`accion='procesada'`/`'reabierta'`) siguen en `registrada`/`procesada`/`anulada`; las clases CSS (`cob-fila--registrada`, `cob-estado--procesada`) también, porque son el valor de la base y no texto. Hay un test que se pone en rojo si un valor enviado a la base pasa a decir "asentada". El label de la tarea `cobranzas:procesar` en `accesos.html` pasó a **"Controlar y asentar cobranzas"**. **PENDIENTE DE SERVIDOR:** cuatro mensajes de las RPCs (`editar_cobranza`, `marcar_cobranza_procesada` ×2, `marcar_salida_cheque`, `reabrir_cobranza`) todavía dicen "procesada"/"registrada" y se muestran tal cual — no hay diccionario en el cliente; se corrigen cuando se autorice tocar esas RPCs.
     - **Tarjeta de cheque en tres niveles (3.3):** importe / **Emisión · Paga el · N° cheque** con etiqueta / banco y cuenta al pie (`htmlDatosCheque`, compartida entre el detalle y la tarjeta plegada). **La tarjeta PLEGADA del FORMULARIO DE CARGA es una fila compacta de 72 px** (`.cob-cheque--fila`, 21/09/2026), no la de tres niveles: izquierda el importe + "Nº … · paga …" (o "a la vista") + banco en una línea con ellipsis y el nombre completo y la cuenta en el `title` escapado; derecha Editar y Quitar de 44 px. **Toda la izquierda es el botón con `data-mini`** que abre la foto —no hay tercer botón, no entraba en 375 px—. Lo que salió de la fila: emisión y tipo (se ven en Editar), la cuenta (al `title`) y el "✓ Confirmado" (lo dice el borde y el `aria-label`). Con tres cheques entran sin scroll en 390 px (3×72). **El detalle de la cobranza sigue en tres niveles.** Un diferido dice "en N días" (`textoDiasHastaPago(fecha, hoyArgentina())`; con una fecha inválida no dice nada, nunca "NaN"). **Un común dice "A la vista" en TODO el módulo**, tabla de Cheques incluida (antes decía "Al día"). Las miniaturas salieron de las tarjetas: "Ver la foto" es un botón con `data-mini` y la foto se pide recién al tocarlo. "En cartera" y "Endosado" son estados **neutros**. Los renglones de la banda magnética y los titulares no tenían estilo de campo (se veían chicos y sin los 44 px táctiles): corregido.
     - **ESCALA DE AVISOS (3.4): dos escalones.** `.cob-aviso` a secas **informa** (neutro: fondo `--color-superficie`, franja gris, texto secundario); `.cob-aviso--grave` (bordó, 600) **solo si un dato está mal o no se puede seguir** (dígito que no cierra, letras distintas de números, fechas que no cierran, CUIT inválido, dólares, cheque ya cargado completo, errores de la tarjeta, cobranza anulada). **El ámbar se retiró del módulo** —con el naranja quedaba a un paso y dejaba de leerse como aviso— y hay un test que exige que no vuelva. Los estados de las fotos, el banner "Hay cobranzas solo en este celular", el endoso y "lo que falta para guardar" son neutros; el error del lector también, porque ningún dato cargado está mal: faltó leer. **No existe un chip "SIN SUBIR"** (el diseño lo describía): las cobranzas solo locales no son filas del listado.
     - **ESCRITORIO MASTER-DETAIL (3.5), desde 1100 px (`MQ_ESCRITORIO`, el mismo corte en el CSS):** dentro de `#cob-maestro` (`.cob-maestro--activo`), tabla a la izquierda (Fecha · Cliente · Cheques · Efectivo · Total · Cargada por · Estado; **entre 1100 y 1399 px se esconden Efectivo y Cargada por** — subió de 1279 el 21/09/2026 porque a 1280 el cliente quedaba en 54 px; la columna Cheques pasó a 72 px y Estado a 108 px, que es lo que "POR CONTROLAR" necesita) y la cobranza abierta a la derecha en un panel de 468 px con scroll propio, que **reusa el detalle y las acciones existentes** sin comportamiento nuevo. Estado nuevo: `cobranzaSeleccionadaId` y `vista`. **La franja naranja significa SIEMPRE "por controlar"** (estado `registrada`), en celular y en escritorio; corregido el 21/09/2026 —antes en escritorio marcaba la fila elegida—. **La fila elegida en escritorio se marca solo con fondo `--naranja-suave` y `aria-current`, sin franja propia**; una elegida que además está por controlar lleva las dos cosas. Si el filtro deja afuera la elegida, el panel se vacía con un texto neutro. `abrirDetalle` lleva un **turno** para que la respuesta de la cobranza anterior no pise el panel. Un detalle abierto desde la vista Cheques sigue a pantalla completa. Abajo de 1100 px el flujo de celular quedó idéntico (capturas a 390 px byte a byte iguales a las de la sub-parte anterior).
     - **CIFRAS DE CABECERA — "POR CONTROLAR" Y "TOTAL DEL MES", con `resumen_cobranzas()`** (21/09/2026, reemplaza la nota vieja de que no existían). Dos tarjetas arriba del listado, en celular y en escritorio: **"Por controlar: N"** (en naranja oscuro si es > 0) y **"Total del mes"** / **"Total del período"** con filtro de fecha. **Salen de la RPC, NUNCA de sumar filas cargadas** (el listado pagina y PostgREST corta en 1000). Mismos filtros que el listado, por nombre; el listado y la función buscan igual (la clave normalizada solo tiene `[a-z0-9]`, así que `like` y `position` dan lo mismo). Se recalculan cuando el listado arranca de cero (cualquier filtro, y después de guardar, asentar, reabrir, anular o marcar salida), con un **turno** para que una respuesta vieja no pise la nueva; mientras carga muestran **"…"**, si falla **"No se pudo calcular"**, y un null **nunca** termina en "$ 0,00" (`numeroDeResumen` rechaza null/undefined/'' antes de convertir). **Decisiones:** sin filtro de fecha, "Por controlar" cuenta **todas las fechas** (dos llamadas: una sin fechas para "Por controlar", otra del 1º del mes a hoy para el total) — mandar el mes escondería las cobranzas del mes pasado sin controlar, que son las que importan; con el filtro **"Anuladas"** el total dice **"Las anuladas no suman"** en vez de una cifra (la función excluye las anuladas siempre y ahí daría 0 legítimo); en celular las dos tarjetas reparten 2/3 (un total largo no se parte).
     - **La barra de módulos del prototipo de escritorio es placeholder** y no se implementó.
     - **REGLA DE CONTROLES:** `pruebas/controles-cobranzas.js` inventaría cada id, `data-*` y control (button/input/select/textarea/a) del HTML estático **y de las plantillas del `<script>`**, y da rojo si falta alguno respecto del baseline **`fba6396`** (constante del archivo, nunca HEAD; `ARCHIVO_BASE` para cambiarlo, `LISTAR=1` para ver el inventario, `RENOMBRADOS` para declarar un cambio de texto de un control sin id con su motivo). Lo nuevo se permite y se lista. **Se corre al cerrar todo commit que toque la pantalla.**
   - **PENDIENTES DEL MÓDULO — correcciones de `cobranzas.html` salidas de una auditoría del código, todas verificadas como abiertas el 16/09/2026:**
     - **`guardarBorrador()` no recibe el objeto**: lee `estado.form`, así que un guardado que termina después de que la persona cambió de cobranza escribe el borrador equivocado. Ver el aprendizaje sobre la referencia implícita — **es el más grave de esta lista, porque el camino que lo dispara es el normal del módulo.**
     - **No hay ningún manejo de sesión vencida** (verificado: cero menciones en el archivo). Si el token expira mientras se carga —probable, porque la carga puede durar horas sin señal— el guardado falla sin ofrecer reintentar y con el borrador todavía a salvo en el celular, pero sin decirlo.
     - **Sin control de concurrencia en las ediciones**: dos personas con `editar_anular` sobre la misma cobranza se pisan, y como la edición es un reemplazo total, la segunda gana entera. El historial deja el rastro, pero nadie se enteró en el momento.
     - **Fotos pendientes del formulario abierto**: el sincronizador toma los borradores cerrados; las fotos del formulario que está abierto quedan esperando a que se cierre.
     - **Los renglones plegados** de la tarjeta de cheque y **los errores del detalle** necesitan una pasada de UX.
     - **CERRADO el 21/09/2026 (`d79765b`):** `formatearImporte(null)` devolvía `$ 0,00`; ahora `—`. Era `Number(null)` es `0` y es finito, así que pasa el `Number.isFinite`. Es **la quinta aparición** de la familia ya documentada (la coma de `type="number"`, `Number(null)` en `equivalenteEnBultos`, el `"8 5"`, el saldo sin unidad elegida) — un dato ausente mostrado como una cifra plausible, en una pantalla de plata.
     - **CERRADO el 21/09/2026 (`ed6391e`):** el `window.prompt` para elegir la foto de un cheque cargado a mano y el `window.confirm` para descartar un borrador local pasaron a diálogos propios (botones de 44 px, foco atrapado, Escape cancela, el foco vuelve al botón que abrió). Mismo comportamiento; la elección ahora se espera, así que si el formulario en pantalla cambió, el cheque no se agrega al anterior. **Siguen sin foco atrapado ni Escape** los diálogos que ya existían ("Salió" y el de motivo). Texto de antes: El proyecto ya decidió no usarlos —no se pueden estilar y algunos webviews los bloquean—; con una sola foto se elige sola, así que el camino común está bien, pero con varias hay que reemplazarlo por una tirita de miniaturas.
     - **Evaluar modelos de OCR con las fotos originales** (ver el aprendizaje sobre la resolución): hoy corre con el default `claude-opus-5` y **sin ninguna medición contra cheques reales**.
   - **EL MÓDULO ESTÁ EN USO EN PRODUCCIÓN desde el 16/09/2026** (verificado contra la base el 17/09/2026: 11 cobranzas cargadas por 2 personas —3 registradas, 7 procesadas, 1 anulada—, 11 cheques, 10 fotos y 21 eventos de historial). **Una versión anterior de esta entrada decía "NADA DEL MÓDULO SE PROBÓ CON UN CHEQUE REAL", y quedó falsa en un día.** Importa más de lo que parece: no es una corrección cosmética, **cambia el costo de cualquier cambio que se le haga al módulo** — una regresión no se ve en la próxima revisión, se ve el mismo día y en la mano de alguien que está cobrando en la calle. Lo que sí sigue sin ejercitarse de punta a punta es el **flujo offline y el sincronizador**.
   - **PRIMERA MEDICIÓN DEL OCR (17/09/2026), que es para lo que existen `origen_datos` y `ocr_propuesto`:** de **11 cheques**, **7 quedaron en `ocr`** (confirmados sin tocar), **4 en `ocr_corregido`** y **CERO en `manual`**. Los 11 tienen su `ocr_propuesto` guardado, así que se puede ir a ver **qué campo** se corrigió en cada uno. Corre con el default **`claude-opus-5`**. Eso cierra **parcialmente** el pendiente de "evaluar modelos sin ninguna medición contra cheques reales": ya hay una medición, lo que falta es la **comparación entre modelos** — y para eso hace falta correr el mismo lote con otro y comparar contra lo que la persona confirmó.
   - **FASE 2 — BCRA** (pedida, no empezada): consultar **cheques denunciados automáticamente al confirmar banco + número** —la API de denunciados devuelve sucursal y cuenta, así que el aviso puede ser preciso y no genérico— y la **situación del titular por CUIT**, las dos vía Edge Function. Un cheque denunciado que se acepta es plata perdida, así que el aviso tiene que llegar **antes** de guardar.

9. **Cheques** (`modulos/cheques.html`, tarjeta `cheques` del dashboard, sin fila en `modulos`; 22/09/2026, `c0f8124` → ver el traspaso `.claude/traspasos/2026-09-22-cheques.md`). **La cartera de cheques**: salió de Cobranzas porque Cobranzas es para cobrar, cargar, ver y controlar/asentar, y la cartera es administración de la plata de la empresa. Los cheques siguen siendo de su cobranza: **tocar un cheque abre su cobranza** (`cobranzas.html?cobranza=<id>&volver=<url>`), y `cheques.html?cheque=<uuid>` muestra todos los estados y destaca ese cheque.
   - **Permisos, sin tareas propias:** ver la cartera pide `cobranzas:ver_todo` o `cobranzas:procesar` (con bypass de super_admin); sin ninguna, la pantalla lo dice y vuelve al dashboard. Dar salida y volver a cartera piden `cobranzas:procesar` (lo exigen las RPCs). **La cartera de quien tiene `procesar` sin `ver_todo` está INCOMPLETA** —la policy de `cobranzas` es "leer propias o con ver_todo"— y la pantalla lo dice; hoy nadie está en ese caso (22/09/2026).
   - **Lo que vino de Cobranzas, con las mismas reglas:** filtros de número (`filtroNumeroCheque`: 8 dígitos exacto, 9 con el de control, menos parcial, un `%` no consulta) y de banco (la lista sale de TODOS los cheques), segmentado En cartera / Salidos / Todos, total en cartera de toda la cartera en centavos ("—" si no se pudo), diálogo de salida (`marcar_salida_cheque`) y "Volver a cartera" con motivo (`volver_cheque_a_cartera`). Los filtros y el orden se guardan en `sessionStorage` para la ida y vuelta a Cobranzas.
   - **Columna "Salida"** (Parte 2): "Dar salida" (en cartera, cobranza asentada, con `procesar`), "Asentá la cobranza para darle salida" (por controlar), o la fecha y el destino con "Volver a cartera"; "—" si no hay nada. Todas las filas miden lo mismo (56px). Sin permiso, un salido sigue mostrando su fecha y su destino.
   - **Orden por columna** (Parte 3): número, banco por nombre, emisión, pago (un común usa su emisión), importe, cliente, cargó y estado; ▲/▼ y `aria-sort`; lo sin dato siempre al final. Por defecto pago ascendente. **Se ordena en el cliente** (cliente, banco y cargó no son columnas de `cobranza_cheques`); si la consulta llega a las 1000 filas, se dice que el orden es parcial. En el celular, UN select con columna y sentido.
   - **Columna "Cargó"** (Parte 4): sale de `v_cobranzas.cargada_por_nombre` (las cobranzas se leen de esa vista, `security_invoker`).
   - **Celular** (Parte 5, debajo de 900px): una tarjeta de dos renglones por cheque (importe y "paga dd/mm/aa" / "a la vista"; "Nº · banco · cliente" con el estado al final) y la acción a la derecha con 44px. Medido a 390×844: entran 8 sin scroll (6 con las barras de Safari). Desde 1440px la tabla entra entera; a 1280px entra con el filtro "En cartera" y, con cheques salidos a la vista, scrollea adentro de su caja (desde la columna Unidad, 22/09/2026); entre 900 y 1279 scrollea adentro de su caja.
   - **Vencimiento del plazo para depositar** (Parte 6): 30 días desde el pago, o desde la emisión si es común — **`plazoPresentacion()` replica `public.cheque_plazo_presentacion(p_tipo, p_emision, p_pago)`** (`case when p_tipo='diferido' then p_pago else p_emision end + 30`; IMMUTABLE; EXECUTE `authenticated` y `service_role` — **el conector de solo lectura no tiene EXECUTE**) y `pruebas/test-cheques-plazo.js` las compara con 9 casos medidos. A 7 días o menos (el mismo 7 de `mis_pendientes`): la fila o la tarjeta entera en `--bordo-suave`, la fecha de pago en bordó y "Vence el 22/10 · quedan 5 días"; vencidos, `--chq-vencido-fondo` (bordó más cargado, por `color-mix` de variables) y "Plazo vencido el 22/10". **Nunca naranja.** Arriba, "N cheques vencen esta semana · $ TOTAL" (vencidos incluidos, como la burbuja), tocable, filtra a esos.
   - **Selección con resumen** (Parte 7): "Seleccionar" pone casillas; tocar marca o desmarca (Shift + clic, el tramo); barra fija abajo (safe-area) con "N cheques · $ TOTAL · plazo X días (ponderado Y)" y "Vencen pronto: M". Días = máx(0, pago − hoy), un común 0; ponderado = Σ(días × importe) / Σ importe; redondeados. Reordenar la mantiene; un filtro la limpia con aviso. La selección le gana al fondo bordó.
   - **Dar salida a los seleccionados** (Parte 8): "Dar salida a N" con `procesar`, el mismo diálogo, y **`marcar_salida_cheques(p_cheque_ids uuid[], p_tipo, p_fecha, p_destino)` → integer, que es TODO O NADA**: recorre los ids llamando a `marcar_salida_cheque` y ante el primero que falla hace `raise` con "No salió ninguno. El cheque <banco> Nº <número> no puede salir: <motivo>" (la transacción entera se revierte); rechaza lista vacía, más de 100 e ids repetidos; devuelve cuántos salieron. La pantalla la habilita solo si TODOS están en cartera y son de cobranzas asentadas (y ≤ 100), marca los que no pueden, y muestra el error de la base entero.
   - **Suites:** `test-cheques-{vista,orden,cargo,celular,plazo,seleccion,lote}.js` con sus `mut-*`, `controles-cheques.js`, `sandbox-cheques.js` y `seguras-cheques.js`. **Ningún control se perdió en la mudanza:** `controles-movidos.js` dice a dónde fue cada uno, `controles-cobranzas.js` los acepta y `controles-cheques.js` los exige en cheques.html, los dos contra el baseline `fba6396`.
   - **La unidad de negocio de cada cheque** (22/09/2026, `f4d1a0f`, A2): un cheque es de la unidad de su cobranza (`v_cobranzas.unidad_negocio_id` / `unidad_negocio_nombre`). Columna "Unidad" ordenable ("Sin unidad" en gris, siempre al final); filtro por unidad (`#chq-filtro-unidad`, la lista sale de TODOS los cheques, en `sessionStorage`; se muestra solo si hay al menos dos grupos); y en el resumen de la selección, el desglose por unidad cuando los marcados son de más de una. En el celular la unidad va en el `title` de la tarjeta (no entra en el renglón). Suite `test-cheques-unidad.js` (+ mut). Detalle en `.claude/traspasos/2026-09-22-cheques-2.md`.
   - **EL COLOR DE CADA ESTADO** (22/09/2026, traspaso `.claude/traspasos/2026-09-22-cheques-3.md`; solo presentación: cero cambios de base, de RPCs, de policies y de permisos, y no se tocó `css/main.css` ni `js/cobranzas-comun.js`). Lo trajo Facu: los cheques que ya habían salido **parecían deshabilitados** en vez de "este cheque ya salió", y los demás estados no se distinguían de un vistazo. Ahora **cada fila (y cada tarjeta del celular) lleva un fondo suave según su estado, con el texto a pleno contraste**, y el chip lo NOMBRA.
     - **En cartera** con la cobranza asentada: **blanco**. En cartera con la cobranza **por controlar**: blanco más una **franja `--naranja`** a la izquierda. **Depositado / endosado**: `--verde-suave`, con el chip en `--verde-oscuro`. **Anulado**: `--bordo-suave`, con el chip "ANULADO" en `--bordo-oscuro` y en mayúsculas. **Por vencer / vencido**: `--bordo-suave` / `--chq-vencido-fondo`, con la etiqueta "Vence el …".
     - **La franja naranja de "cobranza por controlar" ES NUEVA en este módulo.** El pedido decía "como hoy" y acá no estaba: lo único que lo decía era el texto "Asentá la cobranza para darle salida" de la columna Salida, **y ese solo lo ve quien tiene `cobranzas:procesar`**. La decide `esPorControlar(ch, cob)` = el cheque en cartera **y la cobranza en `registrada`**, o sea que **mira la COBRANZA y no el permiso**: es el estado del cheque, no una acción.
     - **El anulado tacha SOLO EL IMPORTE**, no la fila entera. Tachar todo tachaba también el chip "Anulado", que es justo lo que tiene que leerse: es lo único que lo distingue del fondo del plazo, que es el mismo `--bordo-suave`. (En los datos no se pueden confundir: `estadoVencimiento()` solo mira los `en_cartera`, así que un anulado nunca lleva además la marca del plazo.)
     - **NINGÚN estado se atenúa.** Ni con texto gris —medido, `--color-texto-suave` sobre `--verde-suave` da **2,68:1**, por debajo de AA; ahora esas filas usan el color de todas las demás, 12,37:1 sobre `--verde-suave` y 11,78:1 sobre `--bordo-suave`— ni con **`opacity`**, porque **una celda sticky con `opacity` se vuelve transparente** y la columna fija del número deja de tapar lo que pasa por abajo. Hay assertions y mutaciones que lo exigen.
     - **Los chips van sobre fondo BLANCO y no sobre el tinte de su propio estado**: encima de una fila ya tintada del mismo color desaparecerían. Medido sobre blanco: `--verde-oscuro` 8,02:1 y `--bordo-oscuro` 12,13:1.
     - Con el filtro **"Salidos"**, `.chq-tabla-scroll` y `.chq-lista` toman **`--chq-salidos-caja`** (variable local nueva en el `body`: `color-mix(in srgb, var(--verde) 5%, var(--color-fondo))`), **y el encabezado de la tabla también** — si no, de la caja solo se vería el borde.
     - **La LEYENDA** (`#chq-leyenda`), arriba de la tabla y de la lista: una muestrita de color con su nombre, **solo con los estados que están en la lista que se está viendo**, en un orden fijo (`LEYENDA_ESTADOS`): Cobranza por controlar, En cartera, Vence esta semana, Plazo vencido, Salió de cartera, Anulado. **Devuelve vacío —y la leyenda se esconde— cuando no hay ningún color que explicar**: una leyenda que dice "blanco = en cartera" sobre una lista toda blanca es el cartel que se muestra siempre y que nadie lee. Un cheque puede aportar **más de una** entrada (la franja y el fondo del plazo son dos canales distintos sobre la misma fila); uno marcado por el plazo **no aporta además "En cartera"**, porque su fondo no es blanco y una muestra blanca nombraría un color que esa fila no tiene. La muestra del anulado lleva **su tachado**, que es lo único que la distingue de la del plazo. La muestrita va `aria-hidden`: lo que se lee es el nombre.
     - **La PRECEDENCIA de los fondos, que es lo que no se puede romper:** **selección / destacada (`--naranja-suave`) > estado (verde / bordó) > blanco**, y **franja bordó de "no sale" > franja naranja de "cobranza por controlar"** (responde por qué ese cheque elegido no puede salir, que es más urgente que repetir el estado de su cobranza). Es la misma especificidad, así que **decide el orden en el archivo**, y las assertions miran la **ÚLTIMA** aparición de cada fondo de estado y la **primera** de la que le gana: una copia agregada más abajo ganaría, y hay mutaciones que agregan justamente esa copia.
     - Suites `pruebas/test-cheques-colores.js` y `pruebas/mut-cheques-colores.js` (93/93 y 52/52 +2 eq.). **Los contrastes los recalcula la suite leyendo los hex de `css/main.css`**, así que si alguna variable cambia, la prueba lo dice.
   - **Nada se probó con sesión en un navegador real**: las medidas son sobre una foto estática de la pantalla con datos de prueba (que es donde se leyeron los colores computados con `getComputedStyle`). **Falta la pasada de Facu en la app, sobre todo en el celular:** que con la cartera de verdad los seis colores se distingan de un vistazo y que la leyenda no ocupe demasiado a 390 px. El guion para Facu está en el traspaso.

10. **Producción** (`modulos/produccion.html`, clave `produccion`, orden 9). **La tablet de la fábrica, en modo kiosco**, con acento `--azul`. Nació el 22/09/2026 (B1 `9a70841` → B7 `94b3356`, traspaso `.claude/traspasos/2026-09-22-produccion.md`) y se **rehizo entero con un diseño nuevo** el 22 y 23/09/2026, en 7 partes: `f502c3d` (modos y PIN) · `d3f8203` (tablero y abrir turno) · `54a216d` (la planilla) · `9e178ae` (sala de masa) · `7865d76` + `41a1761` (configuración) · `c6f035d` + `071bf64` (dashboard y el PIN en Empleados) · `6487dff` (historial y stock terminado). El detalle y los números por parte, en `.claude/traspasos/2026-09-23-produccion-2.md`. **El rediseño no tocó ni una RPC, ni una tabla, ni una policy, ni un permiso: cero SQL corrido.**
   - **CÓMO TRABAJA LA FÁBRICA, y es lo que manda sobre el diseño:** (1) el ENCARGADO (en Cucuruchos Nuss rotan Federico Silva y Agustín Barrera) abre el turno en la tablet: quién es, el turno (Mañana/Tarde/Noche) y qué máquinas arrancan, cada una con sus operarios; cada máquina abierta recibe su LOTE (7023, 7024…); un turno dura de 6 a 12 horas. (2) Recién ahí esas máquinas le aparecen al MASERO, que hace las masas para las máquinas abiertas. (3) El encargado carga lo producido **durante** el turno y al terminar cierra la planilla de CADA máquina: hora en que se apagó el fuego, scrap en kg y observaciones; cada renglón de lo producido es un SUBLOTE (7023-1, 7023-2…), el número que se sella en la caja, y suma al stock de producto terminado. (4) Una máquina que sigue al turno siguiente se abre de nuevo: es un lote nuevo. (5) Cada unidad tiene sus máquinas y se pueden agregar o quitar.
   - **Condiciones de uso, que son las que deciden las medidas:** manos con harina y grasa, de pie, a un metro de la pantalla, luz de galpón con reflejo. Texto **mínimo de 18 px**, botones de **56 px** (64 px los de uso frecuente), separación mínima de 8 px, y cada pantalla entra completa en **1280×800 sin scroll**. **Configuración, el Historial y el Stock terminado son la excepción: se usan en la COMPU** (`VISTAS_OFICINA`) y van más densos (14–16 px, filas de 52 px).
   - **LAS REGLAS VIVEN EN LA BASE y la pantalla no las repite ni las contradice:** el origen de la masa, que la doble es ×2 (se manda SIEMPRE una masa simple), que la "anterior" es la última masa de esa máquina y ese tipo, el descuento del lote de cada renglón con insumo y `lote_fuera_de_stock`, que los ingredientes sin insumo no descuentan, el sublote en el orden de carga, y que con puestos configurados solo un encargado abre y solo un masero figura en una masa.
   - **Acceso:** con cualquiera de las tres tareas; sin ninguna, aviso y vuelta al dashboard. Las unidades de cada tarea se resuelven con la MISMA regla que `tiene_tarea_alcance()` (`unidadesCon()`): no hay una vista `v_mis_unidades_*` de producción. La cabecera vieja con su menú **queda solo para la oficina** —quien no tiene `produccion:cargar`— y para las `VISTAS_OFICINA`; en la tablet manda la barra de modos y no hay ninguna otra cabecera. Para que **cambiar de fábrica** no quede inalcanzable, "¿Quién sos?" lleva un "Cambiar de fábrica" discreto, visible solo si esta cuenta puede cargar en más de una unidad.
   - **LA BARRA DE MODOS Y EL PIN** (parte 1). Arriba de todo, fija, una barra de 64 px con **los dos modos repartiéndose el ancho** —PRODUCCIÓN y SALA DE MASA— y, a la derecha, el rol y el nombre de quien está adentro, la unidad y **Salir**. Sin nadie adentro dice "Nadie adentro" y no hay Salir. **No hay "Volver": la tablet está en modo kiosco**, y ya no existen la pantalla "¿Para qué se usa esta tablet?" ni el menú "Cambiar el modo de esta tablet" — el cambio de modo son esos dos botones. Una tablet sin modo guardado **arranca en PRODUCCIÓN**, que además es el único modo usable mientras no haya ninguna máquina abierta.
     - **El fondo de TODA la pantalla toma el color del modo** (`body.pr-modo-*`), para reconocerlo desde la puerta sin leer la barra. Los dos modos se separan por **luminosidad y no por tono** —Producción oscuro con letra blanca, Sala de masa claro con letra oscura—, así se distinguen en escala de grises, con el reflejo del galpón y para quien no distingue colores.
     - **SALA DE MASA está DESHABILITADA mientras no haya ninguna máquina abierta**, con **borde punteado** (no solo otro color) y la leyenda "· El encargado tiene que abrir el turno" adentro del mismo botón. **TERCER ESTADO, y es el que importa: si todavía no se pudo medir, el botón queda HABILITADO** — negarlo sin haberlo medido sería mostrar un dato ausente como si fuera un dato. Lo decide `salaDeshabilitada()`, que exige `estado.abiertasConocido === true`.
     - **Entrar a un modo pide "¿Quién sos?"**: los nombres a la izquierda con buscador y **el teclado del PIN siempre a la derecha**, para elegir y tipear sin cambiar de pantalla. Aparece quien tenga ESE puesto **fijo o temporal**, con la etiqueta "hoy" en los temporales. Es la misma regla de la base en sus dos mitades: `unidad_restringe_puesto()` mira los puestos **FIJOS** y `tiene_puesto_produccion()` acepta fijo **o** temporal vigente.
     - **El teclado es PROPIO, 3×4 de 84 px, y nunca el del sistema**: ningún campo del PIN es un `<input>`. Los puntos son **formas**, no el número.
     - **EL PIN NO SE GUARDA EN NINGÚN LADO** —ni el común, ni el maestro, ni el `pin_temporal`— fuera de la memoria: ni `localStorage`, ni `sessionStorage`, ni el DOM. **La persona verificada sí** queda en `sessionStorage` (`produccion.persona`), atada a SU puesto: cambiar de modo obliga a verificar de nuevo.
     - **Los mensajes salen del `motivo` que DEVUELVE la RPC**, no del texto de un error: `sin_pin`, `sin_puesto`, `pin_vencido`, `pin_repetido`, `bloqueado` (con la hora en zona argentina y la cuenta regresiva) y `pin_incorrecto` ("te quedan N intentos"). **Ninguno dice si falló el nombre o el PIN.**
     - **El bloqueo del PIN común es a los 5 errores** (así está la base), no a los 3 que decía el handoff. **Y en el QUINTO la base devuelve todavía `pin_incorrecto` con `intentos_restantes` en 0**, sin `bloqueado_hasta` —ese llega recién en el intento siguiente—, así que ahí la pantalla dice "quedó bloqueado unos minutos" **sin inventar la hora**.
     - **Si la base devuelve `debe_cambiar`, NO se entra**: hay que elegir un PIN propio, dos veces. **No hay ningún otro camino al modo**: el único `entrar(...)` del flujo cuelga de `if (res.debe_cambiar)`.
     - **"Acceso maestro"**, discreto abajo a la izquierda: PIN de **8 números**, panel gris y tecla de acción **en el gris de Producción y NO naranja** —es un acceso de excepción y no compite con la acción de la pantalla—. Mientras está activo un cartel fijo lo dice, esa persona entra a cualquier modo sin volver a poner el PIN y **todo lo que haga queda a SU nombre**. **NO se guarda en `sessionStorage`**, a diferencia de la persona del modo: su PIN tiene que quedar en memoria para poder llamar a `otorgar_puesto_temporal`, así que **recargar la tablet lo cierra**, que es lo correcto para un acceso de excepción. Vive en un `let` del módulo (`pinMaestro`) y no en `estado`, para que se vea que es volátil.
     - **Con el maestro activo, "Dar acceso por hoy"**: persona, puesto y hasta cuándo (por defecto hoy, y ahí `p_hasta` va en **null** para que la medianoche argentina la ponga la base). Si devuelve `pin_temporal` se muestra **UNA vez** y se borra de la memoria y de la pantalla al cerrar. **`pin_temporal` puede venir null** —esa persona ya tenía PIN—: ahí no se muestra ningún número.
     - **PRODUCCIÓN se cierra sola a los 15 minutos sin tocar la pantalla; SALA DE MASA NO**, porque el masero la usa todo el turno y volver a poner el PIN en cada masa sería insoportable. El acceso maestro también se cierra a los 15 minutos.
   - **EL TABLERO Y ABRIR TURNO** (parte 2). Tarjetas por máquina, en orden **abierta de ayer → paradas → abiertas → libres**; la abierta de ayer va primera, de ancho completo y en bordó. Abrir turno: la fecha con ‹ › (**ya no es un `input[type=date]`**), el turno en tres botones, y las máquinas libres con casilla y **varios operarios por máquina como chips** (antes era un `<select>` de uno solo), con buscador que se abre **dentro de la fila** y marca en negrita la coincidencia. UNA llamada a `abrir_turnos` y los lotes, grandes, para copiarlos al papel desde lejos.
   - **LA PLANILLA DE UNA MÁQUINA** (parte 3). La pantalla donde más tiempo pasa el encargado. Arriba, el **LOTE en 64 px**, porque es lo que se copia a la planilla de papel desde un metro; a la derecha **Cerrar planilla** y **Parada**. Abajo, dos columnas: a la izquierda **lo que se MIRA** —operarios, masas y paradas— y a la derecha **lo que se CARGA: lo producido**.
     - **Las masas son SOLO LECTURA y lo dice la pantalla** ("las carga el masero"). No hay ni un control para editarlas, y hay una prueba que se pone en rojo si aparece uno.
     - **Las paradas: la EN CURSO primero y en bordó.** Mientras dura una, una **franja fija de 80 px bajo la barra** con el sello PARADA, desde cuándo, el motivo, hace cuánto y **Reanudar**.
   - **LO PRODUCIDO SE CARGA DURANTE EL TURNO, no al cerrar** (el cambio más grande de la parte 3). Cada renglón se manda con **`registrar_produccion_item`**, que **suma el stock terminado en el acto** y devuelve el **sublote definitivo**: **ya no existen los sublotes provisorios**.
     - Se **corrige** con `corregir_produccion_item` y se **borra** con `anular_produccion_item`, los dos **con motivo**. **Anular NO borra la fila: le pone `anulado`**, así que el renglón **se sigue viendo, anulado** —ese sublote existió, se selló en una caja y su stock entró y salió— y **no suma al total**.
     - **Ya no se reordena nada**: el orden es el de carga y lo pone la base. **`agregar_produccion_item` NO se usa acá**: es solo para un turno ya CERRADO y con `produccion:configurar`.
     - **Agregar un producto va EN PASOS** (1 Producto → 2 Con o sin cono → 3 Presentación → 4 Cono → 5 Cajas), con los pasos a la izquierda: el actual pintado, los hechos **con lo elegido y tocables para volver**, los que faltan apagados y **sin ningún valor**. **El paso del cono desaparece al contestar "Sin cono"**, y la numeración sale de la posición. **Los de chocolate van ABAJO y separados** por una línea con la palabra "Chocolate", y **lo decide `tipo_masa`, no el nombre** (los valores reales son `Común` y `Chocolate`). En el paso del cono, el usado en el sublote anterior va marcado —se PROPONE, nunca se aplica solo— y **"Común" es una opción de verdad**, no un placeholder: `produccion_items.marca_id` es nullable.
     - **Un cono que no está: "+ Agregar cono nuevo"** llama a **`proponer_marca`**, que guarda el nombre en MAYÚSCULAS y, con solo `produccion:cargar`, lo deja `pendiente_revision`. **Queda pendiente y se puede usar igual**: lo dice un chip "nuevo, a revisar". Un nombre ya rechazado hace que la función LANCE, y ese mensaje ya viene escrito para una persona, así que se muestra tal cual. Si el cono **ya existía**, no se le pone ningún chip: afirmar "a revisar" sobre uno aprobado sería mentir.
   - **CERRAR LA PLANILLA** (parte 3). **`p_productos` va VACÍO (`[]`)**: lo producido ya está cargado sublote por sublote.
     - **SE PUEDE CERRAR CON UNA PARADA ABIERTA.** `cerrar_turno` **primero** cierra la parada que quedó abierta con **`hasta_fin_de_turno = true`** —"no volvió en todo el turno"— y recién después valida hora y scrap. Antes la pantalla lo BLOQUEABA, con un comentario que decía que "la base rechaza las dos cosas": **era falso**. Ahora el botón se puede tocar siempre y la pantalla avisa y hace confirmar, **en una sola pregunta** que junta el otro caso ("no cargaste nada producido").
     - **La casilla "La máquina se rompió y no volvió"** cambia la etiqueta de la hora a "Hora en que se rompió" y hace **obligatorio** "Qué pasó". **La hora va de a 5 minutos** con − / +, y tocando el número se escribe; el scrap igual, de a 1 kg.
     - **El error va PEGADO al botón y el campo que falta se marca.** **El botón NO se deshabilita por lo que falta** (ver el aprendizaje). El borrador del cierre guarda `{ hora, scrap, obs, rota }` y **ya no lo producido**, que está en la base desde que se cargó.
   - **CERRAR A LA FUERZA UNA PLANILLA DE OTRO DÍA.** Con **`forzar_cierre_turno(p_turno_id, p_persona_id, p_motivo)`**, donde `p_persona_id` es **la persona de la tablet** (la que entró con su PIN), no la cuenta con la que la tablet está logueada. La máquina **se libera**, la planilla queda **`pendiente_completar`** y **NO suma nada al stock**. Como el tablero solo muestra los turnos ABIERTOS, arriba aparece **"N planillas quedaron pendientes de completar"** con un botón por cada una; si esa consulta falla no se dibuja nada. **Completarla después es EL MISMO CIERRE**: `cerrar_turno` funciona sobre un `pendiente_completar` y ahí completa `completado_por` / `completado_en`.
   - **LA SALA DE MASA** (parte 4). Máquina abierta → simple o doble → tres botones grandes (**Usar la original / Usar la anterior / Modificar**) → **LA RECETA COMPLETA** → Registrar. **SIN pantalla de resumen**: registrar guarda y vuelve a las máquinas.
     - **La receta es una planilla**: un renglón de 64 px por ingrediente, **TODOS a la vista sin scroll**, incluso los que están en cero. Columnas: Ingrediente · Marca · Cantidad (− / número / +) · vs. receta · Lote · **Queda**.
     - **"Queda" es lo que queda de ese lote DESPUÉS de esta masa.** En bordó, con "no alcanza para otra", cuando queda menos de lo que lleva una masa, y el renglón se tinta. **"Se aleja de la receta" es más del 10 %** para ese ingrediente; **con la receta en 0 la diferencia se muestra pero nunca en bordó**, o el bordó saltaría en toda masa de chocolate, que es lo normal.
     - **LOS LOTES VIENEN PUESTOS de la masa anterior de esa máquina**, se elija original, anterior o modificada; solo se tocan si se terminó uno. En la **primera masa del día** llegan vacíos y hay que cargarlos: Registrar se bloquea y el pie dice qué falta. **NO se pregunta "¿mismos lotes?". Nunca** — esa pregunta existía y se eliminó.
     - **El desplegable de lote es UNO solo** y cada opción es el par (insumo, lote) con su stock; la columna Marca muestra el insumo del lote elegido. Dos desplegables no entran en un renglón de 64 px.
     - **CHOCOLATE: NO SE ELIGE.** Si el cacao lleva cantidad, la cabecera suma el chip "Chocolate", igual que hace la base con `es_chocolate`. **No hay ningún selector común/chocolate**, y el de tipo de masa **no se muestra si la máquina tiene una sola receta** (hoy es el caso).
     - **"+ Otro"** en el pie: nombre y cantidad, con el sello OTRO y "Quitar". Viaja como **`ingrediente_libre`** y **no descuenta stock**. Un "otro" en 0 kg no pasa la validación local, porque `registrar_masa` lanza ahí y **sin señal esa masa quedaría esperando un reintento que nunca iba a entrar**.
     - **Si la anterior fue de chocolate, se dice ANTES de copiarla**, en el propio botón "Usar la anterior".
     - **Lista de masas del turno con su chip de origen, que se distingue también sin color**: Original blanco con borde, Anterior gris lleno, Modificada naranja suave.
     - **El "masa N del turno" que se muestra ANTES de registrar es una ESTIMACIÓN** (cuenta las no anuladas + 1, y la base usa `max(nro)+1`), así que con una masa anulada en el medio puede decir uno menos. El número que vale es el que devuelve la base, y es el que muestra la banda verde de "Masa registrada", que dura **6 s**.
   - **LA REGLA DEL UUID, y es la que no se toca:** `crypto.randomUUID()` se llama en UN solo lugar, al empezar la masa, y queda en su borrador; el payload se congela al mandarlo. **Todo reintento manda el MISMO uuid** (al volver la conexión, cada 30 s, al entrar a la sala y también después de recargar la tablet), y `registrar_masa` devuelve la masa ya guardada sin duplicarla ni descontar dos veces. Un uuid nuevo recién en la masa siguiente. Un error con código (un `raise`) es un rechazo: se muestra tal cual y se puede corregir. Una masa pendiente de envío **no se puede descartar**. **El borrador se guarda bajo su uuid y no bajo el turno**, para que la masa siguiente no pise a una pendiente sin enviar. Sin conexión, la banda bordó: *"La masa 13 quedó guardada en esta tablet. Se manda sola cuando vuelva la señal. **No la cargues de nuevo.**"*
   - **CONFIGURACIÓN** (parte 5, `configurar`, **en la compu**), por pestañas: Máquinas · Recetas · Ingredientes · Productos · **Marcas / Conos (con contador de pendientes)** · Personal.
     - **Personal:** una fila por persona con casillas **Encargado / Masero / Operario** y el **estado del PIN**, y **UN SOLO botón "Guardar los cambios"** que dice **cuántas filas se tocaron**; las tocadas se marcan y hay **aviso si se quiere salir sin guardar** (un panel propio, no un `confirm()`; el `beforeunload` queda porque cerrar la pestaña no tiene otra salida). Antes había un botón por fila: cargarle los tres puestos a cinco personas eran quince toques y quince viajes al servidor. **`guardar_puestos` se llama una vez por fila tocada y para en la primera que falla**, conservando las que no entraron: la base no tiene una RPC de varias filas.
     - **"Generar PIN para los que no tienen"**, por unidad, con **`generar_pines_iniciales`**: se muestra **UNA sola vez** como **hoja para imprimir** (`window.print` con `@media print`), **una tira recortable por persona** con nombre, PIN y "PIN de un solo uso: la primera vez vas a elegir uno tuyo". **Al cerrar, los PINes se borran de la memoria**, y hay una prueba que barre `localStorage`, `sessionStorage` y el DOM. Si no había nadie sin PIN, **se dice**, en vez de abrir una hoja vacía.
     - **"Asignar PIN" / "Resetear PIN"** por persona, los **puestos temporales vigentes** con quién los dio, hasta cuándo y **"Revocar"**, **"Dar acceso temporal"**, y **"Mi PIN maestro"** (`asignar_mi_pin_maestro`) solo para quien puede tenerlo.
     - **Marcas / Conos:** los **pendientes de revisar arriba**, con **Aceptar** y **Rechazar** (`revisar_marca`) y **la posibilidad de corregir el nombre al aceptar**.
     - **DOS PIEZAS DEL DISEÑO QUE NO SE PUDIERON HACER PORQUE LA BASE NO LAS TIENE**, y se saltearon en vez de inventarlas: el **chip "Bloqueado · 3:10"** —`personal_produccion` devuelve `tiene_pin`, `pin_temporal`, `debe_cambiar_pin` y `es_maestro`, pero **no** si el PIN está bloqueado ni hasta cuándo, y `pines_produccion.bloqueado_hasta` no tiene policy de lectura— y **de qué sublote salió cada cono pendiente**, que `marcas_personalizadas` no guarda (solo `creada_por` y `creada_en`). Hay una assertion que exige que la palabra "Bloqueado" **no** aparezca. **Para tenerlos hace falta un cambio de servidor.**
   - **HISTORIAL Y STOCK TERMINADO** (parte 7, `ver` o `configurar`, **en la compu**, con la clase propia `.pr-of` — **no reusa `.pr-cfg`**, que significa Configuración). Tabla de turnos (Lote · Fecha · Turno · Máquina · Encargado · Estado) con filtros, y el filtro de estado ganó **"Pendientes de completar"**: el valor existe desde la parte 3 y no se podía elegir. El detalle: encargado, **operarios con sus horas** (el que se fue sigue en la lista con su rango y su duración), masas con ingredientes, lotes, diferencia contra su receta y **chip de chocolate** (de `masas.es_chocolate`, que escribe `registrar_masa` — **no se recalcula**), paradas **con la que no volvió marcada**, scrap, **sublotes con sus correcciones** (`produccion_correcciones`: de cuántas a cuántas cajas, motivo, quién y cuándo; el anulado se sigue viendo tachado y no suma), el cierre forzado con su motivo, y **los totales consumidos por insumo y lote**. El **stock terminado** por producto · presentación · marca, con el total del grupo y la tabla de sublotes.
   - **Pantalla encendida** con la Wake Lock API mientras haya un turno abierto (try/catch; se vuelve a pedir al volver a la pestaña). Los números con `enlazarCampoNumero` / `leerCampoNumero` / `ponerNumero` (kilos con 3 decimales, cajas enteras), y **el PIN NO**: es un IDENTIFICADOR y va como texto, sin separador de miles.
   - **Suites:** `test-produccion-{acceso,quien,pin,abrir,cierre,masa,config,historial,xss}.js` con sus `mut-*`, `sandbox-produccion.js`, `seguras-produccion.js`, `controles-produccion.js` (baseline: un commit fijo por parte, en `BASES`), `test-accesos-produccion.js` y `test-dashboard-produccion.js` (con su `mut-dashboard-produccion.js`). Las suites corren con `TZ=UTC` para que una hora sin la zona de Argentina dé rojo.
     - **`test-produccion-pin.js` es nueva** y cubre los cinco motivos del PIN, el cambio obligatorio que no se puede saltear, el acceso maestro, "Dar acceso por hoy" y —lo que más importa— que **ni el PIN común, ni el maestro, ni el `pin_temporal` queden en `localStorage`, `sessionStorage` o el DOM**.
     - **`controles-produccion.js` tiene tres listas de declaración:** `RENOMBRADOS`, **`RETIRADOS`** (un control que desaparece a propósito, con su motivo; **y un RETIRADO que ya no hace falta pone la prueba en rojo**, para que la lista no se llene de declaraciones muertas que tapen la próxima) y **`MENOS_COPIAS`** (`[clave, cuántas veces ahora, motivo]` para un control que sigue estando pero aparece menos veces en el fuente; si aparece **más** veces que lo declarado, la declaración sobra y se dice).
   - **Lo que NO se probó:** **nada con sesión en un navegador real, en ninguna de las 7 partes** — ni la tablet, ni el teclado del PIN en una pantalla táctil, ni el cierre por inactividad, ni el Wake Lock, ni la impresión de la hoja de PINes, ni el circuito sin señal de verdad, ni las medidas a 1280×800, 800×1280 y 390 px. **Ninguna RPC se ejecutó**: el conector está en solo lectura y todas las suites corren contra un doble. La **única excepción** es la parte 7, donde el Historial y el Stock terminado **sí se miraron renderizados** en un navegador (una foto estática con el CSS y los renders reales, midiendo con JS sobre el DOM: la grilla sin desborde horizontal, los fondos y chips de cada estado, y los totales que cierran sin contar lo anulado). El guion para Facu está en el traspaso.
11. **Pedidos** (`modulos/pedidos.html`, clave `pedidos`, orden 10, acento `--naranja`). **Los pedidos de los distribuidores**: llegan por WhatsApp, son pocos clientes y todos distribuidores. Nació el 23/09/2026 en 5 partes: `41fe94c` (Accesos, dashboard y el subagente) · `87d3c9d` (clientes) · `d525f8f` (cargar un pedido) · `4062707` (la lista y el detalle) · `51d3749` (imprimir). Traspaso con las decisiones y el guion: `.claude/traspasos/2026-09-23-pedidos.md`. **Cero SQL corrido: Supabase estaba en solo lectura.**
   - **Esta versión se carga A MANO.** Más adelante un programa va a leer los mensajes y precargar los pedidos, y la base ya tiene lo que hace falta: `texto_original`, los `apodos` de cada cliente y los renglones de TEXTO LIBRE. **La pantalla los respeta y no los implementa**: no inventa un `texto_original` (un pedido nuevo lo manda null; al corregir se conserva el que había) y muestra el mensaje original en un desplegable del detalle cuando existe.
   - **LAS REGLAS VIVEN EN LA BASE y la pantalla no las repite ni las contradice.** El estado pendiente / en producción / listo lo decide el AVANCE; **no hay ningún control para elegir el estado a mano**, salvo "Marcar entregado" (con confirmación) y "Anular" (con motivo obligatorio). Lo que la pantalla sí hace es decir las cosas ANTES de mandar, con la misma regla: más cajas cumplidas que pedidas, anular sin motivo (3 letras, `LARGO_MINIMO_MOTIVO`), un pedido con cumplidos que se quiere reemplazar entero.
   - **Permisos:** se entra con cualquiera de las tres tareas de `pedidos` en alguna unidad (si no, se vuelve al dashboard); las unidades salen de la MISMA regla que `tiene_tarea_alcance()` (`unidadesCon`), sin vista `v_mis_unidades_*`. Con más de una unidad hay un segmento arriba y la elegida se recuerda en `localStorage` (`pedidos.unidad`). Clientes con `configurar`; "+ Pedido nuevo", el avance, entregado y anular con `cargar`; la lista con `ver` (sin ella, la lista dice que no hay permiso y deja cargar igual). **Imprimir, con cualquiera que vea el pedido.**
   - **CLIENTES** (`configurar`): lista con buscador **por nombre Y por apodo**, sin acentos ni mayúsculas. Alta y edición con nombre, localidad, **teléfono como TEXTO** (es un identificador: nunca pasa por el formato de números), observaciones, activo y **los APODOS como etiquetas** que se agregan (botón o Enter) y se quitan con su ✕; la pantalla dice para qué sirven: *"cómo lo nombran en los mensajes, para reconocerlo después"*. **Un apodo escrito y no agregado se agrega al tocar Guardar.** Repetidos (sin acentos ni mayúsculas) y vacíos no se agregan y se dice. Viajan a `guardar_cliente` como **arreglo**, y su error se muestra tal cual.
   - **CARGAR UN PEDIDO** (`cargar`): cliente con buscador (**solo los activos**: la base rechaza uno inactivo), fecha (**hoy en Argentina**, `hoyArgentina()` con `ZONA_AR`), entrega opcional, observaciones y los renglones. Arranca con un renglón ya puesto.
     - **Cada renglón:** producto (**los de chocolate al final, separados por una línea**; lo decide `tipo_masa`, no el nombre) → **SIN CONO viene puesto**, con cono a un toque, y **recién ahí aparece el buscador de conos** con "Cono común" (marca null) elegido → presentación (**se elige sola si hay una**) → cajas (`enlazarCampoNumero`, 2 decimales; se escriben con `ponerNumero` y se leen con `leerCampoNumero`) y una nota opcional. Cambiar el producto o el cono suelta la presentación elegida.
     - **"No sé qué es"** agrega un renglón de **TEXTO LIBRE**: en bordó, con el sello **"Falta identificar"**, lo que dice el mensaje y las cajas si las dice (sin cajas la base pone 1). **"Ya sé qué es"** lo pasa a producto y guarda el texto como nota, para no perderlo.
     - **UN RENGLÓN VIAJA CON SUS CLAVES Y NADA MÁS** (`itemParaBase`): uno de producto lleva `presentacion_id`, `marca_id` (null si es sin cono, aunque haya quedado uno elegido), `cajas` y `observacion`; uno de texto lleva `texto_libre`, `cajas` y `observacion`. **Nunca viajan `presentacion_id` y `texto_libre` juntos, ni siquiera uno en null**: el CHECK de la tabla los hace excluyentes y la RPC decide el tipo por la presencia de `presentacion_id`.
     - **Una sola llamada a `guardar_pedido` con todos los renglones.** Lo que falta se dice pegado al botón y en cada renglón, y **el botón NO se deshabilita por lo que falta** (la regla de Producción: si no, el error pegado no se vería nunca); se traba solo mientras se manda. Guardado, se abre el pedido.
     - **Si no se ve ningún producto** (el hueco de las policies de Producción, arriba en las tablas), la pantalla lo explica y ofrece "No sé qué es".
   - **LA LISTA** (la pantalla principal, `ver`): con `pedidos_de`, una fila por pedido con número, fecha, entrega, cliente, estado y una **BARRA DE AVANCE** ("22 de 40 cajas", `role="progressbar"`, verde cuando está completa), más el sello **"N sin identificar"** en bordó. Filtros: **estado** (segmento de seis, en la base), **fechas** (en la base) y **"Solo los que tienen renglones sin identificar"** (en la pantalla, sin volver a pedir). Con un **turno** para que una respuesta vieja no pise la nueva.
     - **Los estados:** pendiente neutro, en producción naranja suave (el color del módulo, no una validez), **listo con su chip VERDE**, entregado verde suave, anulado bordó (y la fila entera en `--bordo-suave`). Un estado que no es de la lista no inventa una clase.
     - **Los seis filtros de estado no se achican por debajo de su texto** y scrollean de costado adentro del segmento: en 375 px se pisaban (visto en un navegador con datos de prueba).
   - **EL DETALLE:** las acciones van **arriba** de los renglones (con un pedido largo quedarían a varias pantallas). Datos, el motivo si está anulado, el mensaje original si existe, el aviso de cuántos renglones faltan identificar y un renglón por item con su barra; **un renglón cumplido se ve TACHADO**. Con `cargar` y el pedido abierto, cada renglón tiene **"Cajas cumplidas"** (`enlazarCampoNumero` con `max` en las pedidas) + "Guardar avance" + **"Cumplido"**, que lo deja en el total. El estado que queda es **el que devuelve `marcar_avance_pedido`**, y el error de la base va tal cual, pegado al renglón.
     - **"Corregir el pedido" con renglones cumplidos NO abre el formulario**: avisa que hay que corregir de a uno. Y si igual se llegara al formulario, `guardarPedido` tampoco manda nada. Sin nada cumplido, abre el formulario con el pedido reconstruido (productos, conos, textos libres y el `texto_original`).
   - **IMPRIMIR EL PEDIDO** (`window.print()` + `@media print`): la hoja que baja al depósito para armarlo. Número, cliente con localidad y teléfono, fecha, entrega, observaciones, y un renglón por item con **un casillero para tildar**, las cajas en grande, la descripción, lo que ya está hecho y la nota; los de texto libre dicen "FALTA IDENTIFICAR"; al pie el total. `#pe-impresion` es **hijo directo de `<body>`** y se esconde en pantalla con su clase, no con `hidden` (el mismo motivo que la planilla de Stock).
   - **Todo texto de la base va escapado con `esc()`** (nombre, apodos, texto libre, observaciones, motivo, el mensaje original, la nota): lo prueban las suites ejecutando los renders con HTML malicioso, y `test-pedidos-xss.js` recorre cada interpolación del archivo.
   - **Suites:** `test-pedidos-{clientes,carga,lista,imprimir,xss}.js` con sus `mut-*` (**90/90 · 146/146 · 127/127 · 33/33 · 6/6**; mutaciones **36/36 (+1 eq.) · 68/68 (+3 eq.) · 71/71 (+11 eq.) · 22/22 (+5 eq.)**), `sandbox-pedidos.js`, `seguras-pedidos.js`, `controles-pedidos.js` (**315/315**, baseline un commit por parte), `test-accesos-pedidos.js` (25/25, mut 6/6) y `test-dashboard-pedidos.js` (12/12, mut 5/5).
   - **Lo que NO se probó:** nada con sesión ni contra la base real (ninguna RPC se ejecutó: el conector estaba en solo lectura y todas las suites corren contra un doble). Lo único que se miró renderizado es la pantalla en un navegador a 375 px con un Supabase falso (lista, carga, detalle y el panel de anular), no la hoja impresa.

### Planificados
1. **Materia Prima — lo que falta**: la pantalla de edición de un ingreso (la columna `editado_en` y su trigger ya existen, esperándola) y el gateo de UI por tareas dentro del módulo. El alta/edición del catálogo de insumos **ya no es de este módulo**: se mudó a Stock.
2. **Cobranzas — pantalla de ASIGNACIÓN DE PAGOS.** Es la que cierra el circuito. Lo decidido y lo propuesto van separados, para que nadie construya sobre algo que todavía no se confirmó:
   - **DECIDIDO por Facu:**
     - **Genera el ingreso en la caja de EMPRESA de la unidad** que corresponda. Hoy Cobranzas no toca Caja a propósito, y este es el lugar donde eso pasa a existir.
     - ~~La unidad de negocio la pone la ASIGNACIÓN~~ — **cambió el 22/09/2026: la pone el ASENTAR** (`marcar_cobranza_asentada`), porque para pagar la deuda de una unidad con un cheque de otra primero hay que saber de quién es cada cheque. La asignación de pagos parte de esa unidad.
   - **PROPUESTO, pendiente de confirmar con Facu:**
     - **Que asignar pase la cobranza a `procesada` automáticamente**, en vez de pedir dos acciones separadas para un solo hecho.
     - **Que anular la asignación la deshaga**: revertir el ingreso de caja y devolver la cobranza a `registrada`. Si se confirma, con el mismo criterio que `anular_gasto` y la misma trampa a evitar — las dos puntas tienen que resolver la caja **igual**, o la plata entra por un camino y no vuelve por el otro.
3. Salamasa (futuro)
4. Mantenimiento de máquinas (futuro)
5. Trazabilidad de lotes (futuro)

## Estilo visual
- Mobile-first, bordes redondeados (12px cards, 8px inputs/botones), sombras muy sutiles
- Fondo de página: clase global compartida `.pagina-modulo` (en `css/main.css`) → `background-color: var(--color-superficie)` (#eef1f6, gris muy claro) — **todo módulo nuevo necesita `<body class="pagina-modulo">`**, si falta, la página queda blanca lisa por error (bug real que pasó en Cuentas Corrientes).
- `.tarjeta-lista`: clase global compartida para cada ítem de una lista (fondo blanco, borde, radius, padding) — reusar siempre, no reinventar el estilo de tarjeta en cada módulo nuevo.
- **Campos de fecha en iPhone** (21/09/2026, `css/main.css`): `input[type="date"]`, `input[type="time"]` e `input[type="datetime-local"]` traen en iOS un **ancho mínimo propio del control nativo** y se salían de la tarjeta. La regla global les pone `appearance: none` + `min-width: 0` (lo que suelta ese piso), `width: 100%`, `max-width: 100%`, `box-sizing: border-box`, `display: block`, y alinea el valor a la izquierda con `::-webkit-date-and-time-value`. Trae borde/alto/fuente base, pero el `<style>` de cada módulo carga después de main.css y **gana en empate de especificidad**, así que un campo de fecha con estilo de módulo conserva su borde. **Cuando hay dos fechas lado a lado en un flex/grid**, los hijos necesitan `min-width: 0` para poder achicarse: `.cob-filtros__fila > *` (Cobranzas) ya lo tenía; a `.filtros-cc input[type="date"]` (Cuentas Corrientes, fechas hijas directas del flex) se le agregó `flex: 1 1 8rem; min-width: 0` para que compartan la fila en vez de apilarse. Las barras de filtro de Gastos y Caja ya son `flex-wrap` con las fechas en wrappers `flex-shrink:0`, así que con el arreglo global las fechas quedan compactas y no desbordan. **Sin verificar en un iPhone real:** no hay Playwright/WebKit en la máquina — la prueba final la hace Facu.
- **Carteles de error y de éxito** (22/09/2026, `d0f7431`, `js/utils.js` + `.toast` de `css/main.css`): el cartel mide como mucho la pantalla menos 1rem por lado y el safe-area (`max-width: calc(100vw - 2rem - env(safe-area-inset-left) - env(safe-area-inset-right))`, `box-sizing: border-box`), el texto baja de renglón (`white-space: normal`, `overflow-wrap: anywhere`) y el borde de abajo respeta la barra de gestos. Antes era una píldora con `nowrap` y en el celular un error largo se cortaba a los dos costados. Lleva `role="alert"` si es error y `role="status"` si es éxito, y dura según el largo (`duracionToast`: 60 ms por carácter, entre 3,5 y 12 s) porque los errores de la base vienen enteros. Ningún módulo tiene otro sistema de carteles. Suite `pruebas/test-toast.js` (+ `mut-toast.js`).
- Colores de acento por módulo: variables CSS locales en el `body {}` de cada archivo (no tocan las globales de `css/main.css`). Valores definitivos, no cambiarlos sin pedido explícito:
  - Gastos: `--color-acento-highlight` (cian, global)
  - Accesos: `--rojo` (#C23B3B)
  - Empleados: `--violeta` (#6E56CF / suave #F1EDFC / oscuro #5B45AD)
  - Caja: `--amarillo` (#C99A2E / suave #FBF3E0 / oscuro #8C6A1A)
  - Cuentas Corrientes: `--turquesa` (#3FBFAE / suave #E8FBF7 / oscuro #1E8C7C)
  - Insumos (Materia Prima): `--marron` (#A8601F / suave #F7EDE1 / oscuro #7A4315) — definidas en el `:root` de `css/main.css` (mismo precedente que `--rojo`/`--violeta`/`--amarillo`; `--turquesa` es la excepción, vive local en dashboard.html y cuentas-corrientes.html)
  - Cobranzas: `--naranja` (#C2410C / suave #FDEEE4 / oscuro #8F2F08) — también en el `:root` de `css/main.css`, desde el 21/09/2026. Contraste 5,18:1 sobre blanco, pasa AA. En Cobranzas el ámbar de aviso se retiró: la escala es neutro (informa) / bordó (un dato está mal). Ver el módulo.
  - Pedidos: `--naranja`, el mismo de Cobranzas (la cara comercial). Verde y bordó solo dicen algo del dato: listo / entregado y anulado / falta identificar. Sin azules.
  - Producción: `--azul` (#1F5FAD / suave #E7EFFA / oscuro #164680) — en el `:root` de `css/main.css`, desde el 22/09/2026. Contraste 6,37:1 sobre blanco. Es un acento de módulo, distinto del azul marino de la marca (`--color-acento`).
  - **Producción — colores de MODO** (22/09/2026, rediseño parte 1): quince variables **locales en el `body {}` de `produccion.html`**, no en el `:root` global (precedente de `--borde-tarjeta` en stock.html): son valores de una sola pantalla, y el `:root` se reserva para el acento del módulo, `--azul`, que ya existe. `--pr-prod-activo` `#3F4655` · `--pr-prod-apagado-borde` `#9AA1AE` · `--pr-prod-apagado-letra` `#55617d` · `--pr-prod-fondo` `#E4E7EC` · `--pr-masa-activo` `#F3D774` · `--pr-masa-activo-borde` `#B8961F` · `--pr-masa-letra` `#3B2E00` · `--pr-masa-apagado-borde` `#D9C476` · `--pr-masa-apagado-letra` `#6B5A1E` · `--pr-masa-fondo` `#FBF4D9` · `--pr-masa-barra-borde` `#EADFAE` · `--pr-off-fondo` `#F1F2F4` · `--pr-off-borde` `#C3C8D1` · `--pr-borde-control` `#D5D9E0` · `--pr-panel-gris` `#F4F5F7`. **Donde el hex ya tiene nombre en `css/main.css` se usa la variable global** (`--color-fondo`, `--color-texto-suave`, `--naranja*`, `--bordo*`, `--marron-oscuro`, `--color-borde`), nunca el literal repetido. Contraste medido: blanco sobre `#3F4655` **9,6:1** y `#3B2E00` sobre `#F3D774` **11:1**.
    - **Los dos modos se separan por LUMINOSIDAD y no por tono** —uno oscuro con letra blanca, el otro claro con letra oscura—, así se distinguen en escala de grises, con el reflejo del galpón y para quien no distingue colores. Y **el color nunca va solo**: siempre acompaña al nombre del modo en mayúsculas grandes. El deshabilitado se marca con **borde punteado**, que distingue "deshabilitado" de "apagado" sin depender del color.
    - **El color de modo NUNCA marca validez**: verde y bordó siguen siendo del sistema, y el naranja sigue siendo la acción de la pantalla.
  - **Qué significa cada color en Cheques** (22/09/2026): ahí el `--verde` / `--verde-suave` / `--verde-oscuro` quiere decir **"este cheque ya salió de cartera"**, y **no validez ni éxito**; el `--bordo` sigue siendo el del plazo y el del anulado; y el `--naranja` sigue siendo "por controlar" y la selección. Es el mismo criterio que en Producción y en Stock: **un módulo puede darle a un color del sistema un significado propio, pero tiene que estar escrito** — si no, el verde se lee como "está todo bien" sobre una fila que solo dice que el papel ya no está en la mano.
- Rediseño en curso: se está adoptando un lenguaje visual más redondeado/con tarjetas (headers en tarjeta blanca propia, banners con gradiente, chips de color) módulo por módulo, vía mockups de Claude Design revisados antes de implementar. Cuentas Corrientes y Caja ya lo tienen, e Insumos (materia-prima) nació directamente con este lenguaje; Gastos/Empleados/Accesos quedan con el estilo anterior hasta que se rediseñen (decisión: Empleados y Accesos NO se van a rediseñar, están bien como están y son los menos usados).

## Seguridad
- **RLS activado en las 36 tablas de `public`, las 36** (re-verificado contra `pg_class.relrowsecurity` el 17/09/2026: cero tablas sin RLS; eran 35 hasta que se creó el respaldo de la migración de URLs). El pendiente de "el RLS de las tablas nuevas no se re-verificó" queda cerrado. Lo que sigue abierto es otra cosa: las URLs firmadas a diez años del bucket `comprobantes`, más abajo. (El `EXECUTE` de `buscar_empleado_por_cuil`, que también figuraba acá, quedó cerrado el 16/09/2026.)
- `empleados`: **CERRADO** (el pendiente viejo de "se lee completo por cualquier logueado" ya no aplica). La policy vigente se llama "leer propia o con tarea de alcance" y su `qual` es:
  ```
  (auth_user_id = auth.uid())
  OR tiene_tarea_alcance('empleados', 'ver_editar', unidad_negocio_id)
  ```
  El acceso migró del chequeo por rol al sistema de tareas granulares, con alcance por unidad de negocio. Un usuario común solo lee SU PROPIA fila; el CUIL, el teléfono, el domicilio y la fecha de nacimiento de los demás dejaron de ser legibles. Los super_admin siguen viendo todo, pero no por un chequeo de rol en la policy: `tiene_tarea_alcance` es SECURITY DEFINER y lleva la excepción de super_admin adentro (`e.rol_app = 'super_admin' or exists(...)`).
- `cuentas_caja`: **SIGUE ABIERTA, Y ES A PROPÓSITO. No es un pendiente.** Cualquier empleado logueado ve el nombre, el medio y la moneda de las cuentas de cualquier otro, y hace falta para dos cosas concretas: elegir la cuenta destino en las transferencias entre personas (caja.html) y el selector de contraparte del wizard de Gastos. Es segura porque `cuentas_caja` NO tiene columna de saldo: la plata vive en las vistas `v_caja_saldos*`, que sí respetan el RLS de `caja_movimientos` gracias a `security_invoker`. O sea: se ve QUE la cuenta existe, no CUÁNTO tiene. Si alguna vez se le agrega una columna sensible, esta decisión hay que revisarla — hoy es segura por la forma de la tabla, no por una restricción de acceso.
- **`REVOKE` a `anon` sobre las TABLAS de `public`**, más `ALTER DEFAULT PRIVILEGES` para que las tablas futuras nazcan sin permisos para `anon`. Verificado en su momento con una consulta a `information_schema.role_table_grants`, que devolvió CERO filas para `anon`. Los DEFAULT PRIVILEGES no son opcionales: sin ellos el revoke se degrada solo, porque cubre lo que existía ese día y no lo que se cree después. CONSECUENCIA A TENER PRESENTE: cualquier funcionalidad futura que necesite leer algo sin sesión (una pantalla pública, un healthcheck) va a fallar con permission denied, y el motivo no va a ser obvio.
  - **ESO VALÍA SOLO PARA LAS TABLAS.** Las funciones siguieron naciendo con EXECUTE para `anon` hasta el 16/09/2026, y este documento decía "sobre todo el esquema `public`", que se leía como si `anon` no pudiera nada. Esa mañana, **66 de las 80 funciones de `public` eran ejecutables por `anon`**, con el grant escrito en cada ACL.
    - **El default de funciones del rol `postgres` en `public` ya no incluye `anon`.** El chat de permisos corrió `alter default privileges in schema public revoke execute on functions from anon, public` (16/09/2026, su registro de escrituras). Verificado: esa entrada de `pg_default_acl` es `{postgres=X, authenticated=X, service_role=X}`.
    - **Y CIERRA LO QUE ESTE DOCUMENTO TENÍA COMO "ORIGEN SIN DETERMINAR":** el `anon=X` explícito que tenían 65 funciones venía de ese default viejo. **Nadie las abrió a propósito: nacieron así.**
    - **MATIZ 1 — queda la fila de `supabase_admin`** en `public`, con `anon`. Solo aplica a funciones que cree ese rol interno de Supabase; las 80 del proyecto son de `postgres`.
    - **MATIZ 2 — el `PUBLIC` NO se cerró con la forma `IN SCHEMA`, y se verificó midiendo** (16/09/2026, chat de permisos). Se creó `public.__test_acl_descartable()` con esa forma ya aplicada: nació con `{=X/postgres, postgres=X, authenticated=X, service_role=X}` y **`has_function_privilege('anon', …, 'EXECUTE')` dio TRUE**. El `=X/postgres` es el grant a `PUBLIC`, y `anon` lo hereda por ser miembro. O sea: **la forma `IN SCHEMA` no cerraba nada**, porque los default privileges por esquema se suman a los globales y no pueden revocar lo que el global otorga.
    - **Lo que SÍ cerró: la forma GLOBAL**, `alter default privileges for role postgres revoke execute on functions from public`. La misma función de prueba pasó a nacer con `{postgres=X, authenticated=X, service_role=X}` —sin `PUBLIC`— y **`anon` = FALSE**, con `authenticated` = TRUE en los dos casos. La función de prueba se borró (verificado: no existe).
      - Verificado en `pg_default_acl`: ahora hay una **entrada GLOBAL** del rol `postgres` para funciones, `{postgres=X/postgres}`, y la de `public` sigue en `{postgres=X, authenticated=X, service_role=X}`. Juntas dan exactamente lo que se midió: una función nueva de `postgres` en `public` la ejecutan `authenticated` y `service_role`, y nadie más.
      - **La global vale para cualquier esquema**: una función que `postgres` cree fuera de `public` ya no nace ejecutable por `PUBLIC`, y quien la necesite abierta tiene que otorgarlo a mano.
    - **REGLA para código nuevo, se MANTIENE aunque el default ya cierre: toda función nueva de `public` lleva su `REVOKE EXECUTE ... FROM public, anon` en el mismo bloque que la crea**, como hicieron las de Cobranzas. Es la barrera que no depende de la configuración: un default se puede cambiar sin que nadie lo note, y la lección de este mismo episodio es que el default se leía cerrado cuando no lo estaba.
  - **LAS EXISTENTES — EL HITO DEL DÍA: HOY CERO FUNCIONES DE `public` SON EJECUTABLES POR `anon`.** Verificado el 16/09/2026 a la noche: sobre **83** funciones, la consulta de las ejecutables por `anon` devuelve **lista vacía**, y **ninguna** tiene EXECUTE a `PUBLIC`. `authenticated` ejecuta **77**; las **6** que no son las 4 internas de Cobranzas, `listar_factores_mfa` y `buscar_empleado_por_cuil` (las dos últimas, solo `service_role`).
    - **El recorrido, todo el mismo día: 66 → 11 → 1 → 0.** A la mañana eran **66**. Un revoke masivo de un chat sin identificar las bajó a **11**; el chat de permisos cerró las **10** que quedaban abiertas por default y quedó **1**; el chat de seguridad cerró **`buscar_empleado_por_cuil`**, la última y **la única que entregaba datos**, y quedaron **0**.
    - **El detalle de los primeros dos saltos:** el revoke sobre ~54 funciones ajenas a Cobranzas —sin reporte, ver el registro de escrituras— es el de 66 → 11. El chat de permisos cerró después las **10** que no eran deliberadas: `tiene_tarea`, `tiene_tarea_alcance`, `tiene_tarea_explicita`, `mi_empleado_id`, `mi_rol_app`, `normalizar_texto`, `normalizar_numero_doc`, `normalizar_razon_social`, `fn_fecha_a_periodo` y `fn_inicializar_saldo_pendiente`, con `REVOKE` de `PUBLIC` y `anon` y `GRANT` explícito a `authenticated` y `service_role`.
    - **LA VERIFICACIÓN PREVIA QUE HIZO QUE NO FUERA TEMERARIO:** las tres de permisos corren dentro de **todas** las policies RLS del sistema, así que si `authenticated` las ejecutaba solo a través de `PUBLIC`, el revoke habría dejado a todos sin poder leer nada. Antes de revocar se confirmó que **`authenticated` tenía grant EXPLÍCITO propio en las diez**, no heredado. Después: `anon = false` y `authenticated = true` en las diez (re-verificado).
    - **Contexto histórico, que explica por qué no era urgente:** de esas 10, cinco son `SECURITY DEFINER` de identidad y permisos que resuelven todo contra `auth.uid()` —sin sesión devuelven null o `false` y no exponen nada— y las otras cinco no son `SECURITY DEFINER` ni leen tablas con privilegios ajenos (normalizadores puros y una de trigger). Estaban abiertas sin que eso entregara datos. `buscar_empleado_por_cuil` era la única que entregaba algo sin sesión, y por eso fue la última: cerrarla necesitaba primero mover el registro a una Edge Function (ver abajo).
- **CERRADO el 16/09/2026 — la enumeración de CUILs por `buscar_empleado_por_cuil(p_cuil)`. Era el pendiente de seguridad más viejo del proyecto.** La función es `SECURITY DEFINER`, sin ningún control de identidad, y devuelve `TABLE(nombre, rol)` del empleado cuyo CUIL coincide.
  - **El riesgo que tenía:** era ejecutable por `anon`, y la anon key es pública —está en el JS de la app—, así que cualquiera podía **enumerar CUILs** (se derivan del DNI) y sacar **nombre y puesto** de cada empleado. Era `anon` a propósito, porque `registro.html` la usa antes de que exista la cuenta: revocarla sin más rompía el auto-registro.
  - **Estado actual, verificado contra la base:** se le revocó EXECUTE a `public`, `anon` y `authenticated` y quedó **solo para `service_role`**. ACL `{postgres=X/postgres, service_role=X/postgres}`; `has_function_privilege` da `false` / `false` / `true` para `anon` / `authenticated` / `service_role`.
  - **La consulta ahora pasa por la Edge Function `buscar-empleado-cuil`** (`f49bdd9`), que exige un **token de Turnstile ANTES de consultar la RPC con `service_role`** y **FALLA CERRADA**: si `TURNSTILE_SECRET_KEY` no está configurada, rechaza la búsqueda (500) en vez de consultar sin verificar; sin token o con token inválido, 400. **Sus logs no registran ni el CUIL ni el nombre.** Detalle del circuito en Login y roles.
  - **Resultado: ya no es enumerable con la anon key pública.** Cada consulta cuesta resolver un desafío de Turnstile.
  - **El orden fue parte del arreglo:** primero las Edge Functions desplegadas, después el frontend en producción, y **recién ahí** el `REVOKE`. Invertido, el `registro.html` viejo —que llamaba a la RPC directa— quedaba roto.
- **EL BUCKET `comprobantes` — policies CERRADAS; URLs firmadas a diez años, ABIERTO.**
  - **1) Las policies: cerradas por el chat de permisos el 16/09/2026.** Las viejas solo chequeaban `bucket_id = 'comprobantes'`: **cualquier usuario logueado listaba y descargaba los comprobantes de todos** (**153** archivos de **7** dueños). Estado verificado contra `pg_policy` el 16/09/2026 a la tarde, **las dos `TO authenticated`**:
    - **`comprobantes: subir a la carpeta propia`** (INSERT): `foldername(name)[1] = auth.uid()`.
    - **`comprobantes: ver los propios o los que la tarea habilita`** (SELECT): la carpeta propia siempre; la subcarpeta **`{uid}/mp/`** (Ingreso) solo con **`materia_prima:ver_todo` RESPETANDO EL ALCANCE**, con **DOS `EXISTS`** que resuelven la unidad de la foto:
      - contra **`materia_prima_ingresos`** donde `foto_url` contiene la ruta, con `tiene_tarea_alcance('materia_prima','ver_todo', i.unidad_negocio_id)`;
      - contra **`materia_prima_items`** donde `ficha_tecnica_url` **o** `foto_lote_url` contienen la ruta, con join a `materia_prima_ingresos` para resolver la unidad y el mismo `tiene_tarea_alcance`.
      - y la raíz **`{uid}/`** (Gastos y facturas) con `gastos:ver_exportar` o `cuentas_corrientes:ver_todo`. **El acceso no es cruzado a propósito**: la tarea de Gastos no abre las fotos de Ingreso ni al revés.
    - **Verificado antes de aplicar** (chat de permisos): los archivos tienen el uid del dueño como primer nivel, y `gastos.html` (~3970) y `materia-prima.html` (`subirArchivoMp`) construyen la ruta con `estado.sesion.user.id`. Re-verificado a la tarde: los **153** archivos tienen el uid del dueño primero, **7** dueños, 47 bajo `mp/`.
    - **El `mp/` de la ruta dejó de ser cosmético**: es lo que la policy usa para separar Ingreso de Gastos. Cambiar el formato de ruta de cualquiera de los dos módulos deja las fotos nuevas fuera de su rama.
    - **EL HUECO DE LOS ADJUNTOS, CERRADO.** La primera versión de la rama `mp/` buscaba **solo** en `materia_prima_ingresos.foto_url`, así que la ficha técnica y la foto de lote —que salen de la misma `subirArchivoMp()`— quedaban afuera: cuando se firmaran al mirar, alguien con `ver_todo` no iba a poder abrir la ficha que cargó otra persona. Lo reportó el chat de Ingreso y el chat de permisos agregó el segundo `EXISTS` (verificado contra `pg_policy` el 16/09/2026).
    - **LO QUE SIGUE ABIERTO AHÍ — el match es por `LIKE` contra la URL firmada guardada** (`foto_url ~~ '%' || objects.name || '%'`). Cuando la fase 2 guarde la **ruta** en vez de la URL, ese `LIKE` va a poder pasar a una **comparación exacta**. Es una mejora y no una rotura —la ruta sigue estando contenida en sí misma—, pero **hay que revisar la policy en ese momento**.
    - **PENDIENTE NUEVO — las fotos HUÉRFANAS** (del análisis del chat de Ingreso). Una foto subida por una carga que después se abandonó no la referencia ninguna fila, así que ninguna de las dos ramas la matchea: **es invisible para todos salvo para quien la subió**, y **solo se puede limpiar con `service_role`** (una Edge Function o un script), porque ninguna policy le da a nadie acceso a un archivo huérfano ajeno. **Conviene hacer esa limpieza DESPUÉS de la fase 2**, cuando el match sea por ruta exacta y "¿la referencia alguna fila?" tenga una respuesta sin ambigüedad. Borrar archivos es destructivo: va con aprobación de Facu (ver también las fotos huérfanas del módulo Ingreso).
      - **Desde `4b677fa` Gastos también las produce, a sabiendas:** la foto se sube al elegirla, así que un wizard abandonado o una foto cambiada deja un archivo en la raíz `{uid}/` que ningún gasto referencia. Es un costo aceptado, no un bug — ver la entrada del módulo Gastos.
    - **El comentario que describía este control cuando no existía** está en el aprendizaje *"el comentario que describe una seguridad inexistente"*.
  - **2) URLs FIRMADAS A 10 AÑOS — GASTOS Y FACTURAS MIGRADOS; INGRESO Y LOS LINKS YA CIRCULADOS, ABIERTOS.** El cierre de las policies no lo resolvía. Eran **115** guardadas en la base (16/09/2026): `gastos.foto_url` (**86**), `facturas_pendientes.comprobante_url` (**19**) y `materia_prima_ingresos.foto_url` (**10**). **Hoy quedan las 10 de Ingreso** (verificado el 17/09/2026; ver la migración más abajo). **Una firma saltea el RLS por diseño**: esos archivos abren **SIN SESIÓN** para quien tenga el link, **hasta 2036**, aunque la policy lo excluya. La única forma de invalidar una hoy es mover el archivo.
    - `materia_prima_items.ficha_tecnica_url` y `foto_lote_url` tienen **el mismo problema en potencia**: salen de la misma función a 10 años, y están en 0 solo porque todavía nadie cargó un adjunto.
    - **LA FASE 2:** guardar la **ruta** y firmar **al mirar** con `createSignedUrl` corto — el patrón de Cobranzas, que firma a **300 segundos**. **Ingreso la tomó para sus 10**; al 16/09/2026 el código todavía firma a 10 años (`materia-prima.html`, `subirArchivoMp`), así que está asignada y no hecha.
    - **GASTOS: CÓDIGO Y FILAS, LAS DOS COSAS HECHAS.** El código es `21289ef` (16/09/2026): `subirFoto()` guarda la ruta y "Ver foto" firma a 300 s al tocarla —también sobre una URL vieja, de la que extrae la ruta—, así que el token viejo ya no aparece en la página. Las filas, con la migración de abajo.
    - **Distinguir "sin permiso" de "no existe" no se puede con el error de Storage:** medido contra la API real, los dos devuelven `NoSuchKey` / `404` (HTTP 400). La pantalla lo decide ANTES de pedir la firma, replicando la policy (carpeta propia, o raíz con `gastos:ver_exportar` / `cuentas_corrientes:ver_todo`); un token inválido es `AccessDenied` / `403` y se reintenta una vez tras refrescar la sesión.
    - **LA MIGRACIÓN DE LAS 105 FILAS SE CORRIÓ** (con aprobación de Facu, porque toca datos; ver el registro de escrituras). Pasó las **86** URLs de `gastos.foto_url` y las **19** de `facturas_pendientes.comprobante_url` a la **ruta** en Storage. Verificado contra la base el 17/09/2026:
      - **0 URLs viejas** en las dos columnas.
      - **Los gastos y las facturas cargados después ya nacen con ruta**, así que el total con ruta sube solo y no se anota acá: lo que importa es que las viejas sean **cero**, re-verificado el 17/09/2026 en las dos columnas.
      - **0 rutas rotas**: todas apuntan a un objeto que existe en el bucket `comprobantes`.
      - Probado en la app: las fotos viejas migradas se abren bien.
    - **EL LÍMITE QUE NO CAMBIA: LOS LINKS QUE YA CIRCULARON SIGUEN ABRIENDO HASTA 2036.** La migración **saca los tokens de la base pero no los invalida**: una URL firmada que alguien copió, reenvió o guardó antes sigue abriendo el archivo **sin sesión** hasta que vence. La única forma de invalidar una es **mover el archivo** (cambiarle la ruta). No es un pendiente de la migración: es el costo, ya pagado, de haber firmado a diez años.
    - **EL RESPALDO: `_respaldo_urls_comprobantes_20260916`** (`tabla`, `id`, `url`), con las **105 filas originales** (86 de `gastos`, 19 de `facturas_pendientes`).
      - **GUARDA LAS 105 URLs FIRMADAS, que siguen siendo válidas hasta 2036**: es la tabla más sensible que dejó la migración. Hoy está bien cerrada —**RLS activo y CERO policies**, así que `authenticated` no lee ninguna fila aunque tenga el grant, y `anon` no tiene permiso (verificado el 17/09/2026)—. **No agregarle una policy ni desactivarle el RLS**: sería publicar los 105 tokens.
      - Borrarla es borrar datos: va con la decisión de Facu, cuando ya no haga falta para revertir.
    - **INGRESO, CÓDIGO HECHO el 21/09/2026 (`8e9cfbb`):** `subirArchivoMp` guarda la RUTA (`{uid}/mp/...`) de las tres fotos y el detalle firma a 300 s al tocar, extrayendo la ruta también de un link viejo. **Las 12 filas viejas siguen con su URL a 10 años** (pasarlas a ruta es tocar datos: con aprobación de Facu) y el `LIKE` de la policy `mp/` sigue igual (matchea la ruta). **Efecto en Gastos:** "Cargar gasto" copia la foto de un ingreso NUEVO por Storage, así que quien tenga `gastos:ver_exportar` sin `materia_prima:ver_todo` en esa unidad ya no la puede copiar (Gastos avisa y deja subirla a mano); si se quiere, hay que ampliar la rama `mp/` de la policy. Lo que decía esta entrada antes: **Lo que sigue abierto de esta fase:** las de `materia_prima_ingresos.foto_url` —**DOCE al 17/09/2026, eran 10**— y la policy `mp/` que matchea por `LIKE` contra la URL. **El número sube solo:** `subirArchivoMp` sigue firmando a diez años, así que **cada ingreso nuevo agrega una llave sin sesión que no se puede revocar**, y el pendiente se agranda sin que nadie toque nada. `materia_prima_items.ficha_tecnica_url` y `foto_lote_url` siguen en cero, pero solo porque todavía nadie cargó un adjunto.
    - **Efecto sobre las fotos huérfanas:** para `gastos` y `facturas_pendientes` ya se puede responder con comparación exacta si una foto "la referencia alguna fila", así que su limpieza no depende más de la fase 2. La de `mp/` sí.
- **Todas las vistas llevan `security_invoker=true`** para que respeten el RLS de quien consulta y no el del dueño de la vista. Re-verificado el **16/09/2026** contra `pg_class.reloptions`: de las **19** vistas del proyecto lo tienen **18**, con UNA excepción deliberada. (Eran 18 y 17 el 15/09 —la que se sumó es `v_cobranzas`—, y 13 y 12 el 31/08. El salto anterior fueron las **cuatro de la fase 4 de Stock** —`v_stock_en_transito`, `v_mis_unidades_envio`, `v_mis_unidades_recepcion` y `v_mermas`—, y antes habían sido seis de las fases 2 y 3 menos `v_stock_insumos_presentacion`. La afirmación de fondo no cambió en ninguna de las cuatro auditorías: la única sin la opción sigue siendo `v_empleados_publico`.) REGLA: toda vista nueva se crea con `security_invoker=true`. Sin eso, una vista sobre una tabla con RLS es un agujero: corre como su dueño.
  - EXCEPCIÓN JUSTIFICADA — `v_empleados_publico` NO lo tiene, y es a propósito. Si respetara el RLS de `empleados`, un usuario común solo se vería a sí mismo y los selectores de empleados de Gastos y Caja le quedarían vacíos — que es exactamente el problema que esa vista vino a resolver. Es segura por dos motivos: sus columnas son solo `id`, `nombre`, `unidad_negocio_id`, `tipo`, `activo`, `rol_app`, `caja_raiz`, `oculto_como_contraparte`, `tiene_acceso` (NADA de CUIL, teléfono, domicilio, fecha de nacimiento ni email); y el `REVOKE` a `anon` también le aplicó, así que no es legible sin sesión. Si alguna vez se le agrega una columna, revisar esta lista antes: es la única vista del proyecto donde una columna nueva se expone salteando el RLS.
    - **`tiene_acceso`** (agosto 2026, `3cd5ef7`) es `auth_user_id is not null` **calculado en la vista**, y la forma importa: exponer el uuid de Auth de las 34 personas en la única vista que saltea el RLS sería una ampliación real de superficie, mientras que "¿tiene login?" no es dato sensible y además ya es inferible. Reemplazó al `.not('auth_user_id','is',null)` que caja.html hacía contra `empleados`. El filtro **no es opcional**: hay 27 personas activas sin acceso contra 7 con acceso, y ofrecer a una de ellas como contraparte crea una solicitud que nadie va a poder aceptar nunca.
    - La anterior había sido `oculto_como_contraparte` — booleano de UI, tampoco dato personal. **Las dos últimas ampliaciones fueron booleanos derivados y no columnas crudas de `empleados`; conviene que siga siendo el patrón.**
    - **Al ampliarla, el `CREATE OR REPLACE VIEW` va SIN `WITH`**, porque esta vista no lleva opciones. Para CUALQUIERA DE LAS OTRAS DOCE es al revés: hay que repetir `WITH (security_invoker=true)` en el `CREATE OR REPLACE`. La doc de `CREATE VIEW` dice que al reemplazar una vista "only the view's defining SELECT rule, plus any `WITH ( ... )` parameters and its `CHECK OPTION` are changed" — omitir el `WITH` **resetea las opciones**, y la vista pasaría a correr como su dueño salteando el RLS, en silencio y sin ningún error.
- **DIECISIETE funciones `SECURITY DEFINER` de `public` NO tienen `search_path` fijo** (eran VEINTE; re-verificado contra `pg_proc.proconfig` el 17/09/2026). Que este documento mencione `search_path = ''` solo al describir `listar_factores_mfa` da a entender que es la práctica general del proyecto, y **no lo es**.
  - **Las TRES centrales del sistema de permisos —`tiene_tarea`, `tiene_tarea_alcance` y `tiene_tarea_explicita`— YA LO TIENEN** (17/09/2026, ver el registro de escrituras). Eran justo por donde este mismo pendiente decía que había que empezar, porque corren adentro de cada policy RLS del sistema. Las diecisiete que faltan: `agregar_interes_factura`, `anular_factura_pendiente`, `anular_gasto`, `aplicar_credito_a_factura`, `aprobar_proveedor`, `asignar_proveedor_factura_pendiente`, `crear_proveedor_activo`, `crear_proveedor_pendiente`, `editar_factura_pendiente`, `editar_proveedor`, `importar_proveedores_excel`, `rechazar_proveedor`, `registrar_pago_directo_proveedor`, `registrar_pago_proveedor`, `sincronizar_pago_directo_proveedor`, `sugerir_facturas_fifo` y `tiene_cuenta_corriente_activa`.
  - **No es un agujero explotable hoy**, y conviene decirlo sin dramatizar: para aprovecharlo haría falta que alguien ya pudiera crear objetos en un esquema del `search_path` del rol que llama, y el `REVOKE` a `anon` más los `ALTER DEFAULT PRIVILEGES` cierran esa puerta. Es **endurecimiento estándar de Postgres** para `SECURITY DEFINER`, no una vulnerabilidad abierta.
  - **REGLA para código nuevo: toda función `SECURITY DEFINER` nueva lleva `SET search_path` explícito.** El resto de las funciones del proyecto ya lo tiene, así que estas veinte son deuda, no el criterio.
  - **PENDIENTE:** agregárselo a las diecisiete que quedan. Las de mayor superficie ya están hechas, así que lo que queda es deuda de menor consecuencia — pero sigue abierto.
- **`mi_rol_app()` está HUÉRFANA.** Se creó para resolver el chequeo por afuera de las policies y evitar la recursión (ver Aprendizajes clave), pero tras la migración de la policy de `empleados` a `tiene_tarea_alcance()` no le quedó ningún uso: verificado contra `pg_policies` (cero usos), `pg_proc` (ninguna función la invoca) y el repo completo (el frontend tampoco). Pendiente de `DROP` en la limpieza final. **NO usarla en código nuevo** — para policies nuevas, usar `tiene_tarea()` / `tiene_tarea_alcance()` / `tiene_tarea_explicita()`.
- CORS de las Edge Functions usa `Access-Control-Allow-Origin: '*'` (default del scaffolding de Supabase) — no es una puerta abierta real porque cada función valida el token de sesión igual, pero si se quiere cerrar del todo, cambiar a `https://cucuruchosnuss-gastos.github.io` en las **7** funciones (`ocr-comprobante`, `ocr-materia-prima`, `ocr-cheques`, `crear-solicitud-acceso`, `buscar-empleado-cuil`, `rechazar-solicitud-acceso`, `quitar-mfa-empleado`). Ojo: `crear-solicitud-acceso` y `buscar-empleado-cuil` son públicas —no tienen sesión que validar— y su barrera es el token de Turnstile.

### Hallazgo de PROCESO — dos conectores de Supabase con permisos distintos en la misma sesión (16/09/2026)

**Una sesión de Claude Code tenía DOS servidores MCP de Supabase conectados, los dos apuntando a producción: uno en modo solo lectura y otro con escritura.** Apareció al crear el módulo Cobranzas: el primer bloque falló con `cannot execute ALTER TABLE in a read-only transaction` por el primer conector, y el mismo SQL, sin cambiarle una coma, entró por el segundo.

**Con la regla de escritura vigente desde ese día, escribir estructura por el segundo conector es el camino autorizado** (ver la regla de oro de escritura en "Cómo trabajar"). Lo que queda para aprender es otra cosa:
- **Saber en cada sesión qué conector escribe.** El síntoma que delata la mezcla es el de este caso —el mismo SQL falla por uno y entra por el otro—, y confundirlos lleva a diagnosticar un "error" que en realidad es un permiso.
- **El límite de los DATOS no lo impone ningún conector.** Uno con escritura puede hacer un `DELETE` o un `UPDATE` igual de fácil que un `CREATE`; lo que impide borrar o editar filas ya cargadas es **la regla**, no la herramienta. Y un conector en solo lectura frena todo —estructura y datos por igual—, así que tampoco sirve como barrera del límite que importa.
- **Por eso cada escritura se reporta**: qué se corrió, con qué verificación y qué resultado. Es lo que hace visible después que la regla se cumplió.

## Aprendizajes clave (bugs recurrentes ya resueltos — no repetirlos)
- **`hidden` de HTML puede quedar pisado por una regla CSS con más especificidad** (ej. `#vista-lista { display: flex }` sin `:not([hidden])`) — la primera vez causó que dos pantallas se vieran superpuestas. Es el bug más recurrente del proyecto: volvió a aparecer varias veces desde entonces, y la última con consecuencia de PERMISOS — un botón quedó visible para quien no debía verlo, que es mucho peor que un problema estético. REGLA: todo contenedor con un `display` de autor que además se oculte con `[hidden]` necesita su propia regla `.clase[hidden] { display: none }`. Auditar todo el archivo por el mismo patrón, no parchear un solo caso.
  - **DESDE EL 21/09/2026 `css/main.css` TIENE `[hidden] { display: none !important; }` GLOBAL** (`e0c9a94`), así que el atributo gana siempre en todas las pantallas. Consecuencia: para mostrar algo hay que sacarle el atributo (`el.hidden = false`), **nunca** pisarlo con `style.display` (quedaría oculto para siempre). Lo verifica `pruebas/test-css-hidden.js`. Los guards locales siguen valiendo y no molestan.
  - **`modulos/stock.html` TIENE UNA REGLA GLOBAL DE `[hidden]` CON `!important` en la línea 19** (`12fd7f8`, agosto 2026), y con `!important` le gana a cualquier `display` de autor. **En este módulo un `[hidden]` sin regla propia NO es un bug: la global lo cubre.** Antes de "encontrar" uno acá, mirar la línea 19.
  - **CORRECCIÓN de una entrada que decía lo contrario** (`9db29d6`, septiembre 2026, corregido en la doc que sigue): se agregaron dos guards redundantes —`.aviso-pendientes[hidden]` y `.modal-stock__acciones[hidden]`— creyendo que eran bugs vivos ("Hay 0 insumos" y los botones bajo la confirmación), **y DOS PERSONAS lo dieron por bueno teniendo la línea 19 a la vista en el mismo grep**. La regla global ya los tapaba. Los guards son inofensivos y se dejaron, pero la justificación era falsa: **no era un caso real, era un hallazgo que no existió**. El principio del aviso que se muestra cuando no hay nada que avisar sigue valiendo por sí solo — la entrada que sigue lo conserva sin caso adosado.
- **Un aviso que se muestra cuando no hay nada que avisar entrena a ignorarlo.** El costo real no es el cartel de más, es el cartel que deja de funcionar: el día que de verdad haya algo que avisar tampoco lo van a mirar. Vale para toda notificación que la pantalla dibuje "por si acaso" — un badge en cero, un banner con el conteo puesto siempre, un aviso permanente de que "puede haber" algo. Si no hay algo, no se dibuja nada.
- **Una variante CSS (`.clase--modificador`) definida ANTES de su regla base queda pisada por la base**, porque tienen la MISMA especificidad y ahí decide el orden (`.x--m` y `.x` son 0,1,0; `.x--m:hover` y `.x:hover` son 0,2,0). Es el mismo patrón del bug recurrente del `[hidden]`. Aparecieron TRES casos en materia-prima.html, y el peor era invisible: `.wz-remito--sel` perdía el `border-color` pero conservaba el anillo `inset` y el texto en marrón, así que **parecía** funcionar y el borde real seguía gris. REGLA: al agregar o mover una variante, verificar que quede DESPUÉS de su base — y auditar el archivo entero por el mismo patrón, no solo el caso que se está tocando.
- **Postgres NO permite sacar ni reordenar columnas de una vista con `CREATE OR REPLACE VIEW`, solo agregar al final.** Para sacar una columna (o cambiar el orden) hay que hacer `DROP VIEW` + `CREATE VIEW`. Pasó dos veces con `v_cuenta_corriente_movimientos`.
- **Nunca asumir que un CHECK constraint acepta un valor nuevo: verificarlo con `pg_get_constraintdef` ANTES de escribir el código que lo use.** Pasó con `facturas_pendientes.estado` y con `creditos_proveedor.estado` — en los dos casos el código ya estaba escrito cuando apareció el rechazo del constraint.
- **Formato de número argentino (punto de miles, coma decimal) vs. el formato con punto decimal que usa `toFixed()`/JS nativo** — mismo bug apareció dos veces en dos lugares distintos del código (un valor formateado se re-parseaba mal, terminaba en `NaN`/`null`). Regla: el número canónico vive como valor JS plano, se formatea solo para mostrar, nunca se re-parsea un string ya formateado como si fuera la fuente de verdad.
- **`input type="number"` DESCARTA la tecla de la coma, y en un teclado es-AR la coma es el único separador decimal que el celular ofrece.** Al escribir "8,5" el campo queda en `"85"` —los dígitos se pegan— y `.value` devuelve un string perfectamente válido: **no hay error, no hay excepción, no hay `badInput`, no vuelve al guion y no se ve nada raro en pantalla.** Y no se arregla normalizando: la coma se pierde ANTES de llegar al JS, así que no hay nada que normalizar. REGLA: todo campo numérico va `type="text"` + `inputmode="decimal"` —que mantiene el teclado numérico del celular— y la normalización se hace al leer. Caso real (`f6f96b3`, agosto 2026): el campo de conteo del recuento de Stock. Alguien contaba 8,5 kg de lecitina, veía 85, seguía, y al cerrar se generaba un ajuste de +76,5 kg de mercadería inexistente con motivo "ajuste por recuento" y nadie que lo cuestionara. **Lo encontró Facu tocando la pantalla en el celular, con 134 verificaciones del harness en verde**: ningún test que lea el código puede ver lo que hace el navegador con una tecla. Corolario del mismo caso: con el punto Y la coma valiendo como decimal, el separador de MILES se vuelve ambiguo (`"47.000"` daría 47). Donde la unidad se cuenta de a enteros, la salida es rechazar cualquier separador en vez de adivinar.
  - **SEGUNDO CASO (`6862597`): los CINCO campos de cantidad de `materia-prima.html`**, donde se cargan kilos de harina todos los días. Mismo bug, más superficie. De ahí salieron dos reglas más:
    - **Un espacio interno NO se borra, se rechaza.** El parser de Stock hacía `.trim().replace(/\s/g, '')`, que además de los bordes come los de adentro: `"8 5"` —un dedazo donde iba la coma— daba 85, que es exactamente el error que el parser viene a evitar. `trim()` para los bordes y basta; un espacio no es un separador decimal, así que no hay nada que interpretar.
    - **Un helper por FUENTE, no uno solo "que sirva para todo".** `numeroDesdeInput()` servía a dos contratos distintos: valores del OCR (números JSON, ya canónicos) y texto tipeado por una persona. Aplicarle la regla estricta al camino del OCR lo habría hecho descartar datos en silencio el día que la Edge Function devuelva un string con separadores — cambiar un bug callado por otro. Quedaron `numeroDesdeOcr()` y `parsearCantidad()`. Y el NOMBRE importaba: "numeroDesdeInput" se lee como *"esto ya viene validado de un campo"* cuando cuatro de sus cinco usos eran del OCR, y eso es parte de por qué el bug sobrevivió.
- **`Number(null)` y `Number('')` son `0`, que es finito, así que un helper que valida con `Number.isFinite()` convierte "no hay dato" en una AFIRMACIÓN.** Caso real (`ec28bf5`): `equivalenteEnBultos(null, 25)` devolvía "0 bultos" en vez de rechazar. Es la misma familia que la coma —un dato ausente transformado en un número plausible— y el modo de falla es el mismo: nadie ve un error, ve una cifra. REGLA: rechazar `null`, `undefined` y `''` **ANTES** de convertir, no después con `isFinite`. Y una nota sobre cómo apareció: **los call sites lo tapaban** —todos guardaban el null antes de llamar— así que la suite estaba verde; salió mutando el helper. Un guard que hoy es redundante deja de serlo en cuanto alguien agrega el sexto call site.
- **Cambiar el `GROUP BY` de una vista cambia la CARDINALIDAD de sus filas, y cualquier `Map` indexado por la clave vieja empieza a pisar entradas en silencio.** Caso real (`ec28bf5`): `v_stock_por_lote` pasó a agrupar por `(lote, contenido_por_bulto)`, así que un lote con dos presentaciones pasó a devolver DOS filas. El `claveSaldo(insumo, lote)` del recuento seguía siendo el de antes: la segunda fila pisaba a la primera, y al cerrar, el conteo de una presentación se comparaba contra el saldo de la otra — un ajuste enorme, sin ningún error. REGLA: al tocar un `GROUP BY`, auditar **todos los índices que se construyen sobre esa vista** en el frontend (`new Map(...)`, `find`, `some`, cualquier clave compuesta a mano), no solo las consultas que la leen. El SQL compila igual y la pantalla sigue andando; lo que cambia es cuántas filas volvieron.
- **Nombres de columnas/parámetros de un RPC: nunca adivinar variantes.** Cuando no se tiene el cuerpo real de la función a mano, preguntar o verificar antes de escribir código defensivo con múltiples nombres posibles — cuesta más tiempo debuggear después que preguntar antes.
- **`CREATE OR REPLACE FUNCTION` con firma distinta NO reemplaza — crea una sobrecarga duplicada** que rompe las llamadas por ambigüedad (costó un bug de producción). Si cambia la firma: `DROP FUNCTION` primero, después `CREATE`.
- **`DEFAULT NULL` EN UNA RPC DE ACTUALIZACIÓN PARCIAL BORRA EN SILENCIO.** Caso real (`352ef5e`, septiembre 2026): se le sumó `p_categoria` a `editar_insumo` con `DEFAULT null` y se asigna **directo** —`categoria = nullif(btrim(coalesce(p_categoria,'')),'')`—, así que **cualquier llamada que no mandara la clave escribía null**. El frontend no la mandaba: corregirle la tolerancia a una harina **la sacaba de "Harinas"**, sin error, sin señal y sin que nadie lo mirara.
  - **LA CAUSA DE FONDO, que es lo que transfiere: con `DEFAULT null`, "no mandé este campo" y "borrá este campo" son INDISTINGUIBLES para la función.** El parámetro nuevo es **opcional para el compilador y obligatorio para la corrección**, y nada en el sistema marca la diferencia — ni un error, ni un warning, ni un tipo.
  - **REGLA: al agregar un parámetro a una RPC de UPDATE QUE YA ESTÁ EN USO, el default seguro es CONSERVAR el valor actual** (`coalesce(p_campo, campo)`), no null. Y **auditar TODOS los call sites en el MISMO commit** — el parámetro entra a la base y al cliente por caminos separados (el SQL lo aplica un chat o Facu, y el HTML lo toca otro chat, en momentos distintos), así que la ventana entre los dos es justo donde vive el borrado.
  - **La asignación DIRECTA no siempre está mal, y por eso la regla es sobre el DEFAULT y no sobre la asignación:** acá es correcta y deliberada —igual que en `p_aclaracion`— porque sin ella sería **imposible BORRAR** una categoría mal puesta. Lo que faltaba era que el cliente mandara la clave siempre. Las dos formas son válidas; lo que no puede pasar es elegir la directa y dejar call sites que omitan el parámetro.
  - **EL BORRADO ES MÁS O MENOS DETECTABLE SEGÚN LA COLUMNA, y con `NOT NULL` es INVISIBLE.** Pasó dos veces seguidas en la misma función, y la segunda es peor:
    - Con **`categoria` (nullable)** el borrado dejaba un `null`, así que se podía **ir a contar** cuántos habían quedado sin categoría para saber si había pasado — y así se verificó que no.
    - Con **`vista_preferida` (NOT NULL default `'base'`)** el borrado escribe **`'base'`**, que es **indistinguible de "nunca se cambió"**. No deja null ni nada: el daño es invisible **incluso consultando la base**. Ahí no hay forma de reconstruir si pasó; solo queda no dejar que pase.
    - REGLA: **mandar siempre el parámetro, nunca omitirlo, con una mutación que lo OMITA y ponga la suite en rojo.** Es la única verificación que sirve cuando el borrado no deja rastro.
  - **Es la misma falla que el passthrough a medias de más abajo** (`0e70708`), donde un null pisaba el ALCANCE de una tarea en cada guardado: un valor existente sobrescrito con null por un camino que nadie declaró. Ahí entró por el payload de un modal, acá por el default de una función.
  - **LÍMITE PARA RECONSTRUIRLO DESPUÉS: `insumos` NO tiene columnas de auditoría de edición** —sus columnas son `id, nombre, unidad_medida, activo, created_at, tipo, marca, estado_alta, tolerancia_merma_pct, categoria, aclaracion`— **ni triggers propios** (verificado el 10/09/2026). O sea que un borrado silencioso ahí **no deja ningún rastro**: se puede afirmar qué falta HOY, no qué se perdió y se repuso. Si alguna vez importa poder reconstruirlo, hay que agregarlas — otras tablas del proyecto ya lo hacen (`materia_prima_ingresos.editado_por`/`editado_en`, `gastos.anulado_por`/`anulado_en`).
    - En este caso no hizo falta: al auditar había **2 insumos sin categoría y los dos eran productos creados al vuelo desde Ingreso**, no víctimas del borrado; y hoy son **0 sobre 56**. Si alguna se hubiera perdido sin reponerse, se vería.
- **UN OPERADOR QUE DEVUELVE NULL EN EL `WHERE` NO SIGNIFICA "NO FILTRA": SIGNIFICA "FILTRA TODO".** El `WHERE` deja pasar solo las filas donde la condición es `true`; `null` queda afuera igual que `false`. Caso real (17/09/2026, chat de Ingreso): la primera versión de la exclusión de `buscar_comprobante_cargado` era `not (p_excluir_modulo = 'gastos' and g.id = p_excluir_id)`. **Con los dos parámetros en `NULL` —el caso de todas las llamadas de tres parámetros, o sea los dos wizards— la comparación da `NULL`, el `not` de `NULL` es `NULL`, y el predicado entero filtraba TODAS las filas**: el detector dejaba de encontrar cualquier duplicado, en los tres módulos, sin ningún error.
  - **La forma correcta es `(p_excluir_modulo is distinct from 'gastos' or g.id is distinct from p_excluir_id)`**: `is distinct from` nunca devuelve `NULL`, así que con los parámetros vacíos la condición es `true` y no excluye nada.
  - **REGLA: toda condición opcional que dependa de un parámetro que puede venir en `NULL` se escribe con `is distinct from` / `is not distinct from`, o con un `p is null or …` explícito, y se prueba con el parámetro en `NULL`.** Probar solo el caso "con exclusión" deja pasar exactamente este bug: ahí anda perfecto.
  - Es prima del `DEFAULT NULL` de arriba: las dos son un `NULL` que entra por un parámetro opcional y cambia el comportamiento de todas las llamadas que no lo mandan, sin que ninguna se entere.
- **Una VISTA tiene lista FIJA de columnas: agregar una columna a la tabla NO la propaga.** Si el código lee esa columna desde la vista, recibe `undefined` y falla **en silencio** — sin error, sin excepción, sin nada en consola. Caso real (agosto 2026): se agregó `empleados.oculto_como_contraparte` y el filtro del selector de contraparte de Caja se escribió contra las dos listas de candidatos. Una sale de `empleados` (faltaba la columna en el `.select()`) y la otra de `v_empleados_publico` (faltaba en la vista). El modo de falla es el peor posible: **habría andado en el mock y habría andado para un super_admin —que lee de la tabla— y habría fallado justo para los usuarios comunes, que leen de la vista y eran el caso que motivó el cambio.** REGLA: cuando una columna nueva tiene que llegar al frontend, seguir el camino completo de cada consulta que la va a leer —`.select()` incluido— y ampliar la vista si el dato pasa por una. Es de la misma familia que las sobrecargas de `CREATE OR REPLACE FUNCTION`: el sistema no se queja, simplemente hace otra cosa.
- **Una tabla con RLS restrictivo, leída para armar una lista DE OTRAS PERSONAS, no devuelve un error: devuelve UNA FILA.** La consulta anda, no tira nada, y la lista sale truncada. Caso real (agosto 2026, `3cd5ef7`): `cargarPersonasConAcceso()` de caja.html leía de `empleados`, cuyo RLS es `auth_user_id = auth.uid() OR tiene_tarea_alcance('empleados','ver_editar', unidad_negocio_id)`. Quien no tiene `empleados:ver_editar` recibía **solo su propia fila**, que el `.filter(e => e.id !== miEmpleado.id)` de la línea siguiente eliminaba: **selector de contraparte vacío, sin ningún error**. Y de esa misma consulta comen el listado general, el Directorio y los filtros de "Retiros socios" y "Todos los movimientos". Se resolvió leyendo de `v_empleados_publico`, que no lleva `security_invoker` y por eso saltea el RLS de `empleados` (ver la excepción justificada en Seguridad).
  - **El bug era ASIMÉTRICO, y eso es lo que lo hacía difícil de ver:** de las dos personas para las que se hizo la función, una tenía `empleados:ver_editar` y la otra no. Probando con la primera andaba todo. Corolario: cuando un permiso decide cuántas filas vuelven, **la prueba que vale es con la cuenta MENOS privilegiada**, no con la primera que haya a mano.
  - REGLA: cuando una consulta arma una lista de OTRAS personas, verificar el RLS de la tabla antes de asumir que devuelve todo. Si la tabla tiene RLS por fila propia, hay que leer de una vista sin `security_invoker` o la lista sale truncada en silencio.
  - Es prima de la de arriba, por el otro mecanismo: aquella es **"la columna no llega"**, esta es **"las filas no llegan"**. Las dos fallan sin error y las dos se ven distinto según quién mire.
- **`empleado_tareas` y `empleado_modulos` tienen columna `habilitado`, así que LA EXISTENCIA DE UNA FILA NO SIGNIFICA QUE EL PERMISO ESTÉ ACTIVO.** Las dos son `boolean not null default true` (verificado contra `information_schema.columns` el 31/08/2026), y ese default es parte de la trampa: una fila nace habilitada, así que un `false` es siempre alguien que la apagó a mano — y se ve idéntica a una otorgada, con su `alcance` cargado y todo. **Toda consulta de auditoría de permisos tiene que filtrar por `habilitado = true`**, o va a reportar permisos que en la práctica no están.
  - Caso real (agosto 2026): al revisar quién podía leer `empleados` se contaron las FILAS de `empleado_tareas` de una persona y salieron 12, incluida `empleados:ver_editar` con un alcance sobre una unidad concreta. Habilitadas eran 9, y esa era una de las tres apagadas. Como `tiene_tarea_alcance()` exige `and t.habilitado` en su cuerpo, el permiso EFECTIVO era falso: el diagnóstico salió al revés y por poco se prueba el arreglo con la cuenta equivocada.
  - **El frontend NO tiene este bug**: las 10 consultas que leen esas dos tablas —accesos, caja, cuentas-corrientes, empleados, gastos, stock y dashboard— filtran todas por `habilitado`. La trampa es de las consultas ad-hoc que uno escribe para auditar, que es justo cuando más se confía en el resultado.
  - Cierra la serie con las dos de arriba: **"la columna no llega"**, **"las filas no llegan"** y **"las filas llegan pero no significan lo que parecen"**. Las tres fallan igual — el sistema no se queja, simplemente devuelve algo distinto de lo que uno cree que pidió.
- **`CREATE OR REPLACE VIEW` sin repetir `WITH (security_invoker = true)` RESETEA las opciones de la vista**, que pasa a correr como su dueño y a saltear el RLS. Es silencioso del peor modo: no hay error, la vista sigue existiendo, sigue respondiendo, y devuelve **de más** — filas que quien consulta no tendría que ver. La doc de `CREATE VIEW` lo dice al pasar ("only the view's defining SELECT rule, plus any `WITH ( ... )` parameters and its `CHECK OPTION` are changed"), y por eso se lee como si conservara lo que no se menciona. REGLA: repetir la cláusula en CADA replace de las vistas que la llevan —hoy 12 de 13, ver Seguridad— y verificar `reloptions` después de cada una. La única que va SIN `WITH` es `v_empleados_publico`, que no lleva opciones a propósito.
- **CUANDO UN CONSTRAINT EXIGE UNA RELACIÓN ENTRE DOS CAMPOS, DERIVÁ UNO DEL OTRO EN EL PUNTO DONDE SE ARMA LA FILA — no mantengas la relación a mano en cada camino que los toca.** `chk_presentacion_coherente` exige el par `cantidad_bultos` / `contenido_por_bulto` **completo o los dos en null**, nada intermedio. Eso dependía de que cada rama del código se acordara de anular los dos juntos, **y falló DOS VECES**.
  - El arreglo (`73d0ebd`) es una línea en el payload: `contenido_por_bulto: bultos == null ? null : item.contenido`. Un solo lugar, los tres modos, y el par a medias pasa a ser **estructuralmente imposible** en vez de depender de disciplina.
  - Lo obligó un caso que no se podía resolver de otra forma: en modo `'bultos'` el contenido es **el que se tipea**, así que anularlo en el estado le vaciaría el campo a la persona. La única salida era derivarlo al armar la fila — y eso, que parecía la excepción, resultó ser la regla que faltaba.
  - Es la pareja de la de abajo: aquella **valida** la fila contra el predicado del constraint, esta hace que la fila **no pueda** violarlo. Validar avisa; derivar previene.
- **VALIDAR CONTRA EL PREDICADO DEL CONSTRAINT, Y SOBRE LA FILA QUE SE VA A INSERTAR — no campo por campo.** Caso real (`341937d`, septiembre 2026): el wizard de Insumos validaba cada campo por separado —que haya bultos, que haya contenido, que la cantidad sea positiva— y ninguna de esas reglas mira la que el CHECK realmente exige, que es `cantidad = bultos * contenido`. Un camino que dejaba esos tres desincronizados pasaba la validación entera y reventaba en el `insert`, con un mensaje que nombra un constraint y no dice qué renglón fue.
  - **El arreglo que transfiere no es tapar el camino que falló, sino mover la validación al BORDE**: se corre sobre `filasItemParaBase()`, o sea sobre exactamente lo mismo que viaja a la base, replicando el predicado del CHECK. Así **cualquier camino de desincronización —los de hoy y los que se agreguen— cae ahí solo**, sin que nadie tenga que acordarse de validarlo aparte. Es la diferencia entre arreglar el bug y arreglar la clase de bug.
  - **Y CON LA IGUALDAD EXACTA DE LA BASE.** El archivo comparaba cantidades con una tolerancia de `0,01`, y Postgres compara `numeric` contra `numeric` sin perdonar un centésimo. **Una validación más permisiva que la base es una validación que miente**: deja pasar exactamente las filas que el insert va a rechazar, que son las únicas que importaba atajar.
  - Corolario del mismo caso: **al agregar una SEGUNDA CAUSA a un estado, revisar quién lo estaba leyendo con la primera.** El arreglo hizo que `contenido == null` pasara a significar dos cosas —"lo del papel no se reparte exacto", que es un error a corregir, y "lo recibido no entra en bultos enteros", que es legítimo—, y la validación que ya miraba ese null bloqueaba el segundo caso con un mensaje **falso**: *"300 no se reparte exacto en 25 bultos"*, cuando 300 sí se reparte en 25. Pasó a mirar la división del papel directamente. Un estado con dos causas necesita que cada lector diga cuál le importa.
- **PostgREST corta en 1000 filas por defecto, así que un agregado calculado EN EL CLIENTE da bien hasta que la tabla crece, y ahí empieza a mentir sin ningún error.** Caso real (agosto 2026): el detalle por lote de Stock ordenaba por antigüedad con un `min(fecha)` hecho en JS sobre `stock_movimientos`. Con el libro en cero andaba perfecto; pero cada consumo de sala de masa es un movimiento, así que un insumo de alta rotación llega a mil en meses — y a partir de ahí el mínimo salía de un subconjunto arbitrario y el orden quedaba mal, sin nada que lo delatara. REGLA: los agregados (`min`, `max`, `sum`, `count`) van en la VISTA, no en el JS. Se resolvió agregando `desde` a `v_stock_por_lote`.
- **No codificar contra una columna que alguien dio por creada.** Caso real (agosto 2026): se dio por corrido el SQL que agregaba `desde` a `v_stock_por_lote` — en realidad se había pasado para que lo corrieran y no se había ejecutado. Escribir el `.select('… desde')` contra la vista sin esa columna habría devuelto un 400 de PostgREST y el detalle por lote no habría abierto más. REGLA: verificar con `pg_get_viewdef` o `information_schema.columns` ANTES de escribir el código que la use. Es la misma regla de "verificar antes de asumir", y vale igual cuando quien lo afirma es de confianza: lo que falla no es la persona, es el paso intermedio entre pasar un SQL y correrlo.
- **Toda FK nueva hacia `empleados` desde una tabla que ya tiene otra FK a empleados rompe los embeds de PostgREST por ambigüedad** — `gastos.anulado_por` se dejó SIN FK a propósito por esto.
- **Una FK hacia `auth.users` SIN cláusula `ON DELETE` rompe cualquier borrado de usuario.** Sin `ON DELETE`, Postgres usa `NO ACTION`: mientras alguna fila referencie al usuario, `auth.admin.deleteUser()` falla con `SQLSTATE 23503 (violates foreign key constraint)`. Caso real: `rechazar-solicitud-acceso` marcaba la solicitud como 'rechazada' dejando `usuario_id` apuntando a la cuenta, y acto seguido intentaba borrar esa misma cuenta. **Falló el 100% de las veces desde que existe** — nunca borró un solo usuario, y la consecuencia era exactamente lo que esa función vino a evitar: la persona no podía volver a registrarse con su email. Era invisible porque la función atrapaba el error y devolvía HTTP 200 con `cuentaAuthNoEliminada: true` en el body, así que el status no delataba nada. Se arregló por dos lados a la vez: la FK pasó a `ON DELETE SET NULL`, y la función además pone `usuario_id: null` en el mismo UPDATE que marca el estado. REGLA: toda FK nueva hacia `auth.users` lleva `ON DELETE SET NULL` o `ON DELETE CASCADE` explícito. Nunca se deja el default.
- **Una política RLS sobre una tabla NO puede consultar esa misma tabla: es recursión, Postgres la rechaza y la tabla queda inaccesible para TODOS.** Caso real: la política original de `empleados` hacía un `EXISTS (SELECT ... FROM empleados ...)` dentro de una política SOBRE `empleados`. Resultado: nadie podía entrar a la app, ni siquiera los super_admin. Y el síntoma no delataba la causa — el dashboard mostraba su cartel genérico "Hubo un problema con tu registro", que es el mismo mensaje del caso "cuenta de Auth sin fila en empleados". REGLA: dentro de una policy RLS nunca consultar directo la tabla que esa policy protege. El chequeo se resuelve por afuera, con una función **SECURITY DEFINER** — `tiene_tarea()`, `tiene_tarea_alcance()`, `tiene_tarea_explicita()`. Lo que corta la recursión es el SECURITY DEFINER (la función corre con los permisos de su dueño y no re-dispara la policy), no una función puntual: cualquier helper nuevo que se escriba para usar dentro de una policy tiene que serlo también.
- **Dos páginas que se redirigen entre sí generan un bucle infinito del que no se sale por la UI.** Caso real: `dashboard.html` manda a `mfa.html` cuando no puede determinar el AAL (falla cerrada), y `mfa.html` mandaba al dashboard cuando la persona no tenía ningún factor verificado. Con las dos reglas juntas, cualquier error al leer el nivel dejaba a TODOS los usuarios rebotando entre las dos pantallas. REGLA: cuando la página A redirige a B bajo la condición X, y B redirige a A bajo la condición Y, verificar que X e Y no puedan ser verdaderas a la vez. Si pueden, uno de los dos lados tiene que cortar con un mensaje y una acción manual en vez de redirigir. Cómo quedó en mfa.html: sin factores → mensaje + botón de cerrar sesión, sin redirección; pero con la sesión ya en aal2 SÍ sigue redirigiendo al dashboard, porque ahí el dashboard la deja pasar y no hay rebote posible. La diferencia es entre "ya estás verificado" y "no hay nada que verificar".
- **Una pantalla de detalle que se abre desde más de un lugar necesita saber de dónde vino, o el "‹ Volver" devuelve siempre al mismo lado y se pierde el contexto** (la búsqueda, los filtros de la pantalla anterior). En Cuentas Corrientes se resuelve pasando el origen como **parámetro con default de la función que abre** — `abrirFicha(..., origen = 'lista')` — y NO como una variable suelta que se setea antes de llamar: así un call site nuevo no hereda en silencio el origen del anterior. Mismo criterio que el `?volver=` entre Gastos y Cuentas Corrientes.
- **Un passthrough a medias es peor que ninguno.** Caso real (`0e70708`): el modal de Accesos preservaba el permiso de una tarea que no está en `CATALOGO_TAREAS`, pero le pisaba el ALCANCE con null en cada guardado — una revocación parcial silenciosa, del tipo que no tira ningún error. Lo peor: el prefill cargaba ese alcance al Map con un comentario que decía *"para que el round-trip no lo pierda"*, y el payload hacía exactamente lo contrario. REGLA: cuando un comentario promete una garantía, verificar que la cumpla **todo el camino**, no solo la mitad donde está escrito el comentario. Un comentario que promete algo que el código no hace es peor que no tener comentario, porque desactiva la sospecha justo donde hacía falta. SEGUNDO CASO, y todavía más engañoso porque la frase era *literalmente cierta*: un comentario de `modulos/materia-prima.html` afirmaba que el `.value` de sus cinco campos de cantidad "ya viene con punto decimal" porque eran `type="number"`. Era verdad — pero solo porque la coma se descartaba, que es el bug de arriba. Describía correctamente el síntoma y hacía pasar por garantía lo que en realidad era la falla. **Se eliminó en `6862597`** (y esta entrada citaba su número de línea, que dejó de existir con el arreglo: un puntero a una línea es lo primero que se pudre).
TERCER CASO (`7d145d7`), y el peor de los tres por dónde estaba: en `ocr-materia-prima/index.ts`, el bloque que justifica **por qué `crudo` y `datos` van separados** daba dos razones, y `9d52317` dejó una falsa —decía que el desglose se anulaba cuando no cerraba contra el total, que es justo el descarte que ese commit eliminó—. No es un comentario decorativo: es la justificación de una decisión de arquitectura. Quien lo leyera, verificara que no pasa, y concluyera que la separación no hace falta, **unificaría los dos objetos y perdería el filtro de renglones con `cantidad <= 0`**, que sí es real. REGLA que suman los tres: **al cambiar un comportamiento, buscar los comentarios que lo describen** — sobre todo los que justifican una decisión, porque ahí un comentario obsoleto no confunde: invita a deshacerla.
- **"EL ACL YA NO MUESTRA ESE ROL" NO ES LO MISMO QUE "ESE ROL NO PUEDE".** Caso real (16/09/2026): se revocó EXECUTE de `anon` sobre funciones con `ALTER DEFAULT PRIVILEGES ... IN SCHEMA public`; `pg_default_acl` dejó de listar `anon` y **pareció cerrado. No lo estaba**: el EXECUTE a `PUBLIC` es un default **GLOBAL** de Postgres, los default privileges por esquema **se SUMAN** a los globales y no pueden revocar lo que el global otorga, y **todo rol es miembro de `PUBLIC`**. El permiso llegaba por una puerta que el catálogo no mostraba.
  - Se resolvió **midiendo**: una función de prueba descartable, creada antes y después, con `has_function_privilege('anon', …)` — ver el detalle en Seguridad. La lectura de la doc ya lo anticipaba, pero lo que lo cerró fue la medición.
  - **REGLA: un permiso se verifica con `has_function_privilege()` / `has_table_privilege()` sobre un objeto REAL, no leyendo `pg_default_acl` ni `proacl`.** Si hace falta, se crea un objeto de prueba descartable, se mide y se borra.
  - Es la familia de **"las filas llegan pero no significan lo que parecen"** (ver `habilitado` en `empleado_tareas`), del lado de los privilegios: el catálogo responde exactamente lo que se le preguntó, y lo que se le preguntó no era la pregunta.
- **EL COMENTARIO QUE DESCRIBE UNA SEGURIDAD INEXISTENTE.** Caso real: `modulos/materia-prima.html`, arriba de `subirArchivoMp()`, afirmaba desde el **10/08/2026** (`4fad55f`) que el uid iba primero en la ruta *"porque es el patrón de RLS de Storage (foldername[1] = auth.uid()) que ya usa gastos.html"*. **Era FALSO cuando se escribió**: las policies del bucket `comprobantes` solo chequeaban `bucket_id`, y cualquier usuario logueado veía todo. **Durante cinco semanas ese comentario dio tranquilidad sobre un control que no existía, y por eso nadie fue a mirar las policies.** Con el cierre del bucket (16/09/2026) pasó a ser verdadero — **la realidad se alineó con el comentario, no al revés**.
  - **Por qué es peor que un comentario desactualizado común:** uno que miente sobre SEGURIDAD no solo desinforma, **desactiva la revisión**. Nadie audita lo que cree ya resuelto. Es la versión más cara de la familia del comentario que promete una garantía que el código no da (ver el passthrough a medias, justo arriba).
  - **Y se repitió al corregirlo, en chiquito:** el comentario nuevo (`a63eb61`) describía la policy de lectura **sin** alcance por unidad; horas después el chat de permisos la reescribió **con** alcance, y el comentario quedó describiendo un hueco que ya no estaba (`8361046`). Un comentario sobre un objeto que mantiene OTRO chat envejece solo, sin que nadie toque el archivo — por eso lleva la fecha de la verificación y el nombre de la policy.
  - **REGLA: un comentario que afirme un control de seguridad tiene que NOMBRAR EL OBJETO VERIFICABLE** (la policy, la función) para que se pueda chequear en una consulta, **y verificarse cuando se lo escribe**. Si describe un control que "debería" existir, va como **TODO**, no como afirmación.
- **Las opciones de un filtro salen de la tabla base, nunca de los datos ya filtrados en pantalla.** Caso real: el selector de unidad de la ficha de proveedor se alimenta de `facturas_pendientes` en cualquier estado, y NO de los movimientos (que vienen filtrados por fecha: poner "Hoy" habría borrado unidades del selector) ni de `v_saldo_proveedor` (que solo trae saldos distintos de cero: una unidad con todo pagado habría desaparecido). Las dos serían regresiones silenciosas — el filtro seguiría "funcionando", solo que con opciones de menos, y nada avisaría que faltan.
- **Un permiso puede tener criterios distintos según el módulo, y eso puede ser deliberado.** Las tareas de Cuenta de Empresa se verifican SIN bypass de super_admin (`tiene_tarea_explicita` en Caja, `tieneTareaExplicita()` en Cuentas Corrientes): la plata de la empresa no es de nadie en particular, así que operarla exige la fila otorgada en `empleado_tareas` aunque quien mire sea super_admin. En Cuentas Corrientes ese criterio aplica a UN solo gate —el grupo "Cuenta de Empresa" del selector de pago— mientras los otros seis usan `tieneTarea()` con bypass. Parece una inconsistencia y no lo es: está documentado en el código para que nadie lo "corrija" unificándolo, porque hacerlo reabriría el acceso a la plata de la empresa sin permiso explícito.
- **UNA ASSERTION SOBRE EL *CALL SITE* NO DICE NADA DEL *CALLEE*: el único test que prueba que un helper EXISTE es ejecutar el código.** Caso real (`babb1fe`, septiembre 2026): el render del aviso de duplicados de `gastos.html` se adaptó del de `materia-prima.html`, y quedaron **cuatro referencias a `esc()`, que no existe en ese archivo**. La suite verificaba `/esc\(f\.numero_doc/` —o sea, que el render LLAMARA a `esc()`— y **pasaba en verde con `esc()` sin definir en ningún lado**. En producción era un `ReferenceError` cada vez que el aviso dibujaba una fila.
  - **Ninguna assertion de texto puede atrapar esto**, por más precisa que sea: todas verifican que la llamada esté escrita, y la llamada estaba perfecta. Lo que faltaba era el otro lado.
  - REGLA: **el código que arma HTML se EJECUTA en la suite**, con un `document` falso y datos de prueba, no se verifica por regex. Un `new Function(...)` con las funciones extraídas alcanza, y si un helper no existe la suite se pone en rojo con el `ReferenceError` en vez de en verde. En `test-gastos-duplicado.js` quedó así, con una mutación que **borra `esc()` y reproduce el bug exacto de producción**.
  - Corolario para el copiado entre módulos, que es de donde salió: **al adaptar una función de otro archivo, la lista de lo que hay que llevarse incluye sus helpers**. Los módulos de este proyecto duplican helpers a propósito (no hay JS compartido), así que uno que existe en un archivo no existe en el otro por defecto.
- **PROBAR EL HELPER NO ES PROBAR SU USO: un helper correcto llamado mal es un bug igual.** Caso real (`cfc15dd`, septiembre 2026): el aviso de "número de comprobante demasiado corto" cuenta DÍGITOS y no caracteres. La suite evaluaba `digitosNumeroDoc()` con todos sus casos borde y estaba perfecta — pero **no decía nada de si el render lo usaba**. Una mutación que cambiaba el render a contar el texto crudo **escapó**: el helper seguía impecable y la assertion de texto seguía matcheando.
  - **El caso que los distingue tiene que existir en los datos de prueba**, o la ejecución tampoco sirve: `"0-1"` tiene **2 dígitos y 3 caracteres**, así que avisa contando dígitos y no avisa contando caracteres. Con `"1"` o `"0001"` las dos implementaciones dan lo mismo y la mutación se escapa igual.
  - REGLA: **ejecutar el RENDER, no solo el helper**, y elegir un caso donde las dos implementaciones difieran. Es la hermana de la entrada del `esc()`: allá faltaba el callee, acá el callee está bien y lo que falla es el call site.
- **LA ACCIÓN QUE OCURRE SIN QUE LA INTERFAZ LO CONFIRME.** Una acción se ejecuta, el estado cambia, y **la pantalla no muestra nada** — no porque el render falle, sino porque **su condición de dibujo depende de datos que en ese caso no existen**. No hay error, no hay excepción: hay una persona que tocó algo y no sabe si pasó.
  - **Caso real (`3a4e57a`, septiembre 2026):** el buscador de proveedor del wizard de Ingreso vinculaba bien, pero `renderizarProveedorDetectado` solo dibujaba si había texto en la razón social o CUIT leído. **El caso que motiva el buscador es justamente el remito que no dice de quién es**, así que ahí no hay ninguno de los dos: se elegía FerPlast del padrón y la pantalla no cambiaba en nada. El arreglo es una condición más, `|| proveedorMatch` — lo difícil no era arreglarlo, era verlo.
  - **ES LA SEGUNDA VEZ EN ESTE MÓDULO**, y por eso es un patrón y no una anécdota. La primera fue el alias que se aplicaba mostrando el texto del papel y **no a qué producto del catálogo se había mapeado**: la marca existía, pero sin el dato que la hacía verificable no servía para detectar un alias mal cargado, que es para lo que estaba puesta.
  - **Dónde aparece:** siempre que la condición de dibujo de una confirmación se apoye en los datos de **entrada** (lo que se tipeó, lo que leyó el OCR) en vez de en el **resultado** de la acción. En el camino común los datos de entrada están, así que anda; el caso que rompe es el que motivó la funcionalidad, porque justamente es el caso donde faltan.
  - **REGLA: EJECUTAR EL RENDER CON EL CASO LÍMITE, no leerlo.** Leyendo, la condición parece razonable —"dibujá si hay algo que mostrar"— y el hueco no se ve. Ejecutando la acción con el caso que no tiene datos de entrada, la assertion de "la etiqueta dice el proveedor" se pone en rojo sola. Es la hermana de la entrada del `esc()` y de la de PROBAR EL HELPER NO ES PROBAR SU USO: las tres se detectan **ejecutando** y ninguna leyendo.
- **LA PROPIEDAD DE UN OBJETO COMPARTIDO SE VERIFICA, NO SE HEREDA DE UN TRASPASO.** Caso real (septiembre 2026): `buscar_comprobante_cargado` se trató como territorio del chat de Cuentas Corrientes durante varias tandas —se le mandaron dos traspasos pidiéndole cambios— **porque un traspaso lo dijo, no porque alguien lo hubiera comprobado**. Ese chat devolvió que la función es de Gastos/Ingreso y que **él ni la consume**: no tiene ninguna pantalla donde se tipee un número de comprobante, las facturas entran siempre por el wizard de Gastos.
  - El costo fue tiempo, no un daño: los pedidos quedaron esperando a un chat que no iba a implementarlos.
  - REGLA: **el dueño de un objeto de la base es quien lo mantiene, y eso se chequea mirando quién lo creó y quién lo LLAMA** (`pg_proc`, más un grep de `.rpc('...')` sobre el repo). Un traspaso puede equivocarse de destinatario igual que se equivoca de número, y la verificación vale en los dos lados — el que traspasa y el que recibe.
- **UN `try/catch` MAL UBICADO CULPA AL COMPONENTE EQUIVOCADO, y manda a debuggear el lugar sano.** Mismo caso: las tres llamadas del detector se agregaron **DENTRO del `try` del OCR**, así que el `ReferenceError` del render lo agarraba el `catch` del OCR y la pantalla decía **"Error al procesar la imagen. Continuá cargando los datos manualmente."** El OCR funcionaba perfecto y sus datos ya estaban en el formulario, escondidos detrás de un mensaje de error que acusaba a otro.
  - El `try/catch` que el detector ya tenía **no alcanzaba**: cubría la llamada a la RPC, pero ni el match de proveedor —que toca el DOM— ni el renderizado estaban adentro de nada.
  - REGLA: **todo lo que se agregue adentro del bloque protegido de otro necesita su propio `try/catch`**, o hereda un mensaje de error que miente. Se resolvió metiendo las tres llamadas en una función con su propio guard, así una cuarta llamada futura tampoco puede escaparse.
  - Y el criterio de fondo: **un detector que rompe la carga es peor que no tener detector**. Ahí el OCR ya terminó, así que lo único que se pierde si el detector falla es el aviso.
- **UN BUG LATENTE PUEDE APARECER RECIÉN CUANDO SE ARREGLA OTRO — y el commit que lo destapa parece el culpable.** El `esc()` faltante entró en `6f40317` y no rompió nada durante días, porque el render salía siempre por su `return` temprano: **el detector casi nunca encontraba duplicados, que era justamente el otro bug**. Cuando `19b36b2` hizo que empezara a encontrarlos de verdad, la primera fila que dibujó explotó.
  - Consecuencia práctica para diagnosticar: **la sospecha natural apunta al último commit, y el último commit puede ser inocente**. Acá el reporte señalaba `19b36b2` con buenos argumentos y la falla venía del anterior; lo que hizo `19b36b2` fue alcanzar un camino que hasta entonces estaba muerto.
  - REGLA: al arreglar algo que hacía que un camino no se ejercitara, **revisar ese camino como código nuevo**, porque en la práctica lo es. Un `return` temprano que casi siempre se toma es código sin probar esperando.
- **UN UMBRAL CALIBRADO AL FILO DEL VALOR REAL SE ERODA SOLO.** Un guard cuyo número está pegado a la medición de hoy lo empuja cualquier commit normal, y ahí la salida obvia —bajarlo un poco más— se toma sin pensar, hasta que el guard deja de detectar lo que vino a detectar. **Calibrarlo contra EL CASO QUE TIENE QUE ATAJAR, no contra el valor actual.**
  - Caso real (`5c1f0e1`, septiembre 2026): el guard de "el pelado no se comió el archivo" estaba en **0.8** y `modulos/stock.html` tiene ~20% de comentarios de línea, así que el ratio real era **0.8035** — a tres milésimas del corte. Un commit con comentarios lo cruzó y puso cuatro suites en rojo sin que nada estuviera mal.
  - **El número correcto sale del caso malo, no del bueno:** lo que ese guard ataja es el `/* */` falso que se comía el 77% del archivo y dejaba el **23%**. Con eso, **0.5** separa perfectamente el destrozo de cualquier variación normal de comentarios, y deja de moverse con cada commit.
  - Vale para todo umbral del andamio —tamaños, conteos, tolerancias—: si el valor de hoy está a menos de un cambio de distancia del corte, el corte está mal puesto.
- **UNA MUTACIÓN CUYA ANCLA DEJA DE EXISTIR AL REFACTORIZAR SE REPORTA COMO "NO DETECTADA", y manda a buscar cobertura que ya estaba.** Es la tercera forma de la misma familia —la mutación mal apuntada, la que muta un solo lado de un comparador— y la más fácil de creerse: el texto que buscaba era correcto cuando se escribió. Caso real del mismo commit: al mover una regla CSS de lugar, la mutación que la apagaba dejó de aplicar. **El guard de unicidad la corta antes de correr** (`NO EXISTE` / `AMBIGUA`) y por eso se ve enseguida; sin él sería un hueco fantasma que empuja a agregar assertions para tapar algo que no falta.
- **EL TEST QUE VERIFICA PRESENCIA Y NO COMPORTAMIENTO.** Probar que una llamada **esté escrita** no prueba que llegue a ejecutarse: una mutación que anula la CONDICIÓN deja la llamada intacta y la assertion sigue matcheando. Caso real (`5c1f0e1`): `chk('sin motivo_tipo no se confirma', /marcarError\('mov-error-motivo-tipo'/...)` pasaba en verde con la validación cambiada a `if (false)`. **La assertion tiene que anclar a la condición** (`/if \(!motivoTipo\) \{/`), no al efecto que cuelga de ella. Es la hermana de PROBAR EL HELPER NO ES PROBAR SU USO, un escalón más abajo: allá el call site estaba bien y se probaba el callee; acá lo que no se prueba es que el bloque sea alcanzable.
- **EL TEST VERDE QUE MIDE OTRA COSA.** Todas comparten el modo de falla: la verificación sigue en verde mientras dejó de medir lo que dice medir, que es peor que no tenerla porque además da confianza. Las tres primeras aparecieron DOS VECES cada una en el mismo módulo, así que son un patrón y no anécdotas; el resto salió de los harness de Stock y de Insumos. **Ninguna se detecta leyendo la suite: se detectan mutando el código y exigiendo que se ponga en rojo.**
  - **EL HARNESS VALIDA UN FRAGMENTO Y EL NAVEGADOR NO PUEDE NI CARGAR EL ARCHIVO. Es la peor de toda esta familia, porque el modo de falla no es un dato mal: es el módulo entero sin cargar.** Caso real (`0c41698` → `4511880`, septiembre 2026): se declaró `const nombreUnidad` para traducir `kg`/`un`/`lt` a palabra, y ese identificador **YA EXISTÍA 700 líneas más abajo** como `function nombreUnidad(id)` del módulo de transferencias, que traduce un **uuid de unidad de negocio**. Dos declaraciones top-level con el mismo nombre en el mismo `<script type="module">` son un `SyntaxError`, y **el script entero no parsea**: pantalla gris, módulo muerto, en producción.
    - **SE ESCAPÓ CON 32 MUTACIONES EN VERDE Y LAS NUEVE SUITES DE STOCK PASANDO.** El andamio **extrae funciones sueltas y las evalúa por separado**, así que verifica LÓGICA sobre fragmentos mientras el archivo COMPLETO no puede cargarse. **Un fragmento válido no dice nada sobre el archivo:** el choque cruza dos zonas que ninguna extracción mira juntas, y `node --check` sobre un bloque tampoco lo ve.
    - **REGLA: además de probar la lógica por fragmentos, VALIDAR EL ARCHIVO ENTERO COMO LO VE EL NAVEGADOR.** Está implementado en **`pruebas/check-scripts.js`** —versionado en el repo desde el 17/09/2026; antes vivía en el scratchpad, que es donde no tenía que estar— y hace dos cosas:
      1. **Parsea cada `<script>` inline con `node --check` sobre un `.mjs`** —que es exactamente lo que el navegador hace antes de ejecutar una línea—. **BLOQUE POR BLOQUE y no todo concatenado**: cada `<script type="module">` tiene su propio scope, así que concatenarlos daría falsos positivos.
      2. **Escanea identificadores duplicados en el top-level**, porque el parse SOLO los agarra cuando alguno es `const`/`let`. **Con dos `function` o dos `var`, JavaScript los deja pisarse EN SILENCIO** — y eso es peor que el `SyntaxError`: no rompe nada, simplemente una de las dos implementaciones desaparece y nadie se entera.
    - Verificado que habría atajado este bug: sobre `0c41698` reproduce el error exacto del navegador; sobre el arreglo, pasa. Corrido sobre **todos** los HTML del proyecto, ninguno tiene otro choque latente (se corre sin argumentos y los busca solo, así que el número no hay que mantenerlo en ningún lado).
    - **CORRERLO ES PARTE DEL CIERRE DE TODO COMMIT DE UN MÓDULO**, junto con las suites — no es una herramienta para cuando algo ya se rompió. Y antes de declarar un identificador nuevo en un archivo de miles de líneas, **verificar que el nombre esté libre** cuesta un `grep`.
    - **EL PATRÓN DE FONDO YA HABÍA APARECIDO Y NO EXPLOTÓ POR SUERTE:** `enBultos` existe como dos cosas distintas en este mismo archivo, solo que en scopes que no colisionan. **Un nombre genérico —`nombreUnidad`, `enBultos`, `esValido`— en un archivo de 6.000 líneas es una colisión esperando**, y la defensa es que el nombre diga QUÉ hace: `nombreDeUnidadMedida` y `nombreUnidad` no se pisan porque nombran cosas distintas.
    - **Y la lección operativa, que es la que costó cara:** un bug de CARGA se ve **en el primer segundo** en un navegador. No hace falta sesión ni datos — alcanza con servir el archivo y mirar la consola. Ninguna cantidad de mutaciones reemplaza ese minuto.
  - **PELAR COMENTARIOS `/* */` SOBRE UN HTML ES INSEGURO, y no da error: deja el fuente corrupto y las assertions midiendo basura.** Caso real (`96932de`, septiembre 2026): el harness de la recepción hacía `src.replace(/\/\*[\s\S]*?\*\//g, '')` para no matchear comentarios. En `modulos/materia-prima.html` el `accept="image/*"` de los inputs de foto **abre un comentario falso** —la barra y el asterisco de `image/*`— que cierra recién en el siguiente `*/` del archivo, **4.600 líneas después**. Se comía **252k de los 327k** del archivo, y las dos assertions que corrían contra el resultado pasaban igual: una contaba llamadas en un fuente donde la función ni existía, la otra buscaba un `prompt(` que se había ido con el resto.
    - **Por qué es de esta familia y no de las otras:** las demás entradas del grupo son assertions que miden otra cosa; esta es el **insumo** de todas las assertions quedando corrupto de una sola pasada, así que puede volver verde un bloque entero de la suite a la vez.
    - **REGLA: un harness que analiza el fuente tiene que validar que lo que quedó después de pelar sigue teniendo sentido** — comparar el largo antes y después, y abortar si la pérdida es desproporcionada. Un pelado que se come el 77% del archivo no es un pelado, es otro archivo.
    - Corolario práctico: sobre un HTML, pelar `//` y `<!-- -->` es seguro; `/* */` NO, y para el CSS alcanza con cortar en `</style>`, que es antes de cualquier `accept`. Y ojo con la simetría: el `*/` que cerró el comentario falso fue **un comentario mío legítimo** dentro de un `catch {}`, o sea que el alcance del destrozo depende de dónde caiga el próximo `*/` y puede cambiar con cualquier commit.
  - **`git stash` PUEDE CONVERTIR EL ARCHIVO A CRLF, y eso rompe TODAS las suites a la vez.** Caso real (`63d84e9`, septiembre 2026): para comparar el trabajo en curso contra el estado limpio se hizo `git stash push` del archivo, se corrieron las suites, y `git stash pop`. El round-trip devolvió `modulos/materia-prima.html` con **8.146 CR** que antes no tenía —el repo trabaja en LF y git avisa `LF will be replaced by CRLF` en cada operación, así que la conversión es esperable y silenciosa a la vez—.
    - **El daño no fue el archivo, fue el diagnóstico.** Las suites de este proyecto comparan funciones extraídas del working copy contra `git show <commit>:archivo`, que devuelve **siempre LF**: con el working copy en CRLF, `extraerFn(x) === extraerFn(x, antes)` falla para cualquier `x`. **Doce suites se pusieron en rojo de golpe** y por un rato pareció que el cambio había roto medio módulo.
    - **LA FORMA CORRECTA de comparar contra el estado limpio no toca el working copy:** `git show HEAD:modulos/x.html > limpio.html` y apuntar el harness ahí con `ARCHIVO_TEST`. Es además lo que los harness ya soportan, así que el stash nunca hizo falta.
    - **EL COROLARIO, que es lo que transfiere: una falla que rompe MUCHAS suites a la vez casi nunca es el código, es el INSUMO.** Un cambio de una tanda toca dos o tres cosas; doce suites en rojo simultáneo significa que lo que cambió es algo que todas comparten — el archivo que leen, el baseline contra el que comparan, el pelado que le aplican. **Es la hermana de la entrada de arriba** (`/* */` comiéndose 252k del fuente): las dos son el insumo corrompido, y por eso son la única familia donde conviene sospechar del andamio antes que del código.
    - **`git checkout -- archivo` HACE LO MISMO** (21/09/2026): con el autocrlf de este repo, restaurar un archivo con checkout lo dejó con miles de CR, y el runner de mutaciones reportó 15 anclas como "NO EXISTE" sobre un código que estaba bien. **Antes de creerle a un "NO EXISTE" masivo, contar los CR.** Y al normalizar, tocar solo los archivos propios: hay blobs versionados en CRLF (`supabase/functions/ocr-comprobante/index.ts`) que un barrido ciego modifica.
    - **RESUELTO DE RAÍZ el 21/09/2026 con un `.gitattributes`** (`* text=auto eol=lf`, más `binary` para png/jpg/jpeg/webp/pdf/zip). El atributo `eol=lf` le gana al `core.autocrlf=true` de la máquina, así que git ya no convierte a CRLF nada que saque del repo. Cómo se verificó: `git add --renormalize .` **no cambió ningún blob** —el índice ya estaba todo en LF, incluido `ocr-comprobante/index.ts`, que esta entrada creía versionado en CRLF: el blob era LF y solo la copia de trabajo tenía CR—; después `git ls-files --eol` dio **`i/lf w/lf` en los 67 archivos de texto** (el único `w/crlf`, esa función, se reescribió a LF a mano, solo ese); `git checkout -- modulos/cobranzas.html` y `git checkout -- CLAUDE.md` dejaron **0 CR**; y `check-scripts` más las siete suites dieron los mismos números que antes. **La regla de no usar `git stash` sigue** (reescribe el working copy y no hace falta), pero ya no es una trampa que rompa las suites.
    - Verificación antes de commitear, y vale la pena hacerla sobre el **archivo entero** y no sobre los primeros bytes: `git show HEAD:archivo | python -c "import sys; print(sys.stdin.buffer.read().count(b'\r'))"` tiene que dar 0.
  - **Un harness anclado a `HEAD` deja de probar en cuanto el cambio se commitea.** Mientras el trabajo está sin commitear, `HEAD` es el "antes" y la comparación tiene sentido; después del commit el "antes" pasa a ser el propio cambio, las dos mitades se vuelven idénticas y todas las aserciones de "antes era X, ahora es Y" quedan verdes para siempre sin mirar nada. REGLA: anclar el baseline a un COMMIT FIJO, no a `HEAD`.
  - **`indexOf` devuelve −1 cuando no encuentra, así que una comparación de ORDEN entre dos bloques da verdadera justo cuando uno de los dos desapareció.** `indexOf(a) < indexOf(b)` con `a` ausente es `-1 < n`, o sea `true`: la assertion pasa exactamente en el caso que tenía que detectar. Se descubrió con una mutación que borraba el bloque y la suite siguió verde. REGLA: verificar primero que los DOS existan, y recién después comparar posiciones.
  - **Una assertion contra el código fuente matchea también los COMENTARIOS.** Un test de "ya no queda X" falla —o, peor, pasa— por la frase del comentario que explica por qué se sacó X, no por el código. Pasó en las dos direcciones: falsos negativos al afirmar que algo se fue, y un `!/--rojo/` que saltaba por el comentario que prohíbe usar rojo. REGLA: pelar comentarios (`/* */` y `//`) antes de comparar contra el fuente, y reservar el fuente crudo para cuando lo que se quiere verificar es justamente el comentario.
  - **El EXTRACTOR de funciones del harness truncaba en silencio cualquier función con parámetros desestructurados.** Saltaba al primer `{` después de la firma, que en `function f(a, { b = 1 } = {})` es la llave del *destructuring* y no la del cuerpo, así que devolvía la firma cortada. **Todo el harness se apoya en esa función**: acá rompió con un `SyntaxError` lejos del problema real, pero si el fragmento hubiera quedado sintácticamente válido —muy posible con otra forma de función— habría estado probando un pedazo y reportando verde. REGLA: cerrar primero la lista de parámetros y recién después buscar la llave del cuerpo, y que el extractor **valide su propia salida** (que termine en `}`, que tenga más de una línea) para gritar en el punto de la extracción y no doscientas líneas después.
  - **UNA SOLA ASSERTION QUE FALLA SIEMPRE CONVIERTE TODAS LAS MUTACIONES EN "DETECTADAS".** Es la más grave de este grupo, y de otra categoría que las demás: las otras hacen que UNA assertion mida otra cosa, esta **rompe el mecanismo entero de verificación**. El runner decide "mutación detectada" preguntando si el sub-proceso falló; si el sub ya venía fallando por cualquier otro motivo —una assertion vieja que quedó obsoleta al mover el baseline, por ejemplo— entonces **toda mutación se reporta como detectada** y el 20/20 no mide absolutamente nada. Caso real (`554aa3c`): al sacar esa assertion apareció una mutación que se escapaba —el ajuste perdía su `p_recuento_id`— y que a nivel de fuente era invisible, porque `p_recuento_id: null` y `p_recuento_id: m.recuento?.id` se parecen demasiado. REGLA: **si la suite no está verde sobre el archivo LIMPIO, las mutaciones no corren**, y el harness lo dice en vez de reportar cobertura. Corolario: al mover el baseline hay que revisar las assertions que lo comparan — las que hablaban del commit anterior pasan a comparar el cambio consigo mismo y hay que borrarlas, no dejarlas.
  - **UN COMANDO DE VERIFICACIÓN QUE NO LEE EL EXIT CODE DE LO QUE VERIFICA.** Caso real (`26f7a20`): el chequeo de tipos de la Edge Function era `npx esbuild ... 2>&1 | tail -3 && echo "TS OK"`, y estaba mal de DOS formas a la vez. El `&&` se evalúa contra el exit code del **`tail`**, no del compilador, así que el "TS OK" se imprimía pasara lo que pasara; y el flag `--loader=ts` sin leer de stdin es un error de esbuild, así que nunca llegó a parsear el archivo. El archivo **no compilaba** —un backtick dentro de un template literal— y se pusheó igual; lo cazó el deploy, no la verificación. REGLA: un comando de verificación tiene que leer el exit code **de lo que verifica**, sin un pipe en el medio que lo tape.
    - **SEGUNDO CASO (17/09/2026), y lo grave es que el cero que informó era justo el resultado que se quería ver.** Al chequear los line endings de un blob ya pusheado, Git Bash sobre Windows **convirtió la ruta** `origin/main:.claude/...` (la conversión de rutas de MSYS la dejó como `origin\main;.claude\...`), el `git show` falló, **python leyó stdin vacío** y el chequeo informó **"0 CR"** — un cero que venía de **no haber leído nada**, no de no haber CR. Se rehízo con `MSYS_NO_PATHCONV=1` y `git cat-file -p`. El resultado final coincidió, pero **podría no haberlo hecho**.
      - Lo cazó **quien lo había corrido, revisando su propia verificación**, y eso es lo que hay que retener: la salida era exactamente la esperada, así que nada invitaba a mirar dos veces. **Un chequeo que puede fallar en silencio y cuyo modo de falla produce el resultado deseado no es un chequeo** — tiene que gritar cuando no pudo leer, en vez de devolver cero.
  - **UN TEST QUE PARECE CUBRIR LA REGLA Y NO TOCA LA LÍNEA.** Tercera aparición en la misma tanda. La assertion existe, está bien escrita y afirma exactamente lo que la regla dice — pero el caso elegido no ejercita la línea que importa. Caso real (`9d52317`): "un renglón ya-recibido también necesita bultos" dejaba vacío `bultos` y cargado `contenido`, mientras que la mutación tocaba el chequeo de **`contenido`**: pasaba por al lado. REGLA: cuando una regla se valida en más de un campo, el test necesita un caso POR CAMPO. Y esto **solo se ve mutando** — es lo que separa un test que prueba de uno que acompaña.
  - **Un runner de mutaciones que a veces lee el archivo LIMPIO no da un falso negativo: invierte el significado del resultado.** Pasando la ruta del archivo mutado por `argv`, el sub-proceso a veces no la recibía, corría contra el archivo real, pasaba, y la mutación se reportaba como "no detectada" — o sea, lo que se leía como un hueco de cobertura era en realidad el test midiendo otra cosa, y al revés. Era intermitente, así que podría haber vivido meses. REGLA: el sub-proceso tiene que **VERIFICAR que leyó el archivo mutado, no asumirlo** — que imprima el largo de lo que leyó y que el padre lo compare contra lo que escribió. Y una mutación que no cambia nada (`mutado === src`) se reporta como error del test, nunca como cobertura.
  - **UNA MUTACIÓN QUE ESCAPA NO SIEMPRE ES UN HUECO DE COBERTURA: puede ser la mutación pegándole al renglón equivocado.** Es la variante ESPEJO de la de más arriba — no "el test que no toca la línea" sino **"la mutación que no toca la línea"**. Caso real (`242573a`, agosto 2026): la mutación "saca «no se hereda por rol»" escapó, y el diagnóstico obvio era que a la suite le faltaba una assertion. No le faltaba. El `.replace()` buscaba la frase *"No se hereda por rol: un super admin también la necesita tildada"*, que aparece **DOS VECES** en accesos.html, así que mutó la descripción de `ingreso_externo_empresa` en vez de la de `transferir_entre_personas`. **La suite tenía razón en quedarse verde: lo que se rompió no era lo que ella asegura.**
    - Es peor que las otras por dos motivos. **El guard de "la mutación no cambió nada" NO lo detecta**, porque sí cambió algo — solo que en otro lado. Y el daño no es un falso verde sino un **falso ROJO**: se lee como cobertura faltante, lo que empuja a agregar una assertion para tapar un agujero que no existe. Queda ruido permanente en la suite, salido de un diagnóstico equivocado, y encima con la sensación de haber mejorado algo.
    - REGLA: **una mutación que escapa se investiga ANTES de asumir que falta cobertura** — verificar que el cambio cayó donde se creía. Con `.replace()` sobre un fuente, anclar el reemplazo a algo ÚNICO (la clave de la tarea, no una frase de la descripción, que se repite entre entradas parecidas) o verificar el renglón mutado, no solo que el archivo cambió.
  - **BASELINE-DRIFT: una assertion que exige que una función quede BYTE-IDÉNTICA deja de proteger y empieza a ESTORBAR en cuanto esa función cambia POR DISEÑO.** Caso real (`2f6afd1`, septiembre 2026): la suite de la recepción congelaba `cargarIngresos` para probar que ese commit no había tocado el listado —cierto en su momento—, y el commit siguiente tenía que modificarla justamente para sumar las transferencias recibidas. La assertion se puso en rojo sin que nada estuviera mal.
    - **La lección no es "no congelar funciones", es ELEGIR BIEN CUÁL.** La que de verdad no se podía tocar era `fusionarEntregas` —con sus casos borde de factura huérfana y de anomalía—, y esa sigue congelada y sirvió: es la que hace verificable la decisión de concatenar las transferencias por afuera. Congelar `cargarIngresos` era congelar el punto de entrada de todo el listado, o sea justo donde cualquier feature futura tiene que escribir.
    - **Regla práctica:** antes de escribir un `extraerFn(x) === extraerFn(x, antes)`, preguntarse si `x` es una función cuyo comportamiento es un INVARIANTE del sistema o simplemente una que este commit no tocó. Lo primero se congela; lo segundo ya lo cubren las assertions de comportamiento.
    - Es prima de la que sigue: aquella clava un VALOR y esta clava un CUERPO ENTERO, y las dos terminan igual —un rojo que no señala nada, que se "arregla" actualizándolo, y un test que pasó a ser un trámite—.
  - **UNA ASSERTION CLAVADA A UN VALOR CONCRETO SE ROMPE CON CUALQUIER AJUSTE VISUAL AUNQUE EL INVARIANTE SE RESPETE.** Caso real (`f8fd486`, septiembre 2026): una assertion afirmaba "el rótulo de TIPO pesa menos que el de CATEGORÍA" clavando los dos tamaños (`0.6875rem` y `0.8125rem`). Al ajustar el visual del listado, el de CATEGORÍA subió a `0.9375rem` — la jerarquía se respetaba MÁS que antes, y la assertion se puso en rojo igual. **Ese rojo INVITA a "arreglarla" copiando el número nuevo sin mirar si sigue valiendo**: el test deja de proteger y pasa a ser un trámite —tocar el visual, actualizar el número, volver a verde—, y el día que la jerarquía se rompa de verdad nadie va a sospechar. El invariante era "menor que", no "0.6875 y 0.8125": la assertion pasó a **comparar los dos valores** en vez de clavarlos, así sobrevive a cualquier ajuste que respete la relación.
    - Regla general: **cuando el invariante es una relación —mayor, menor, antes, después—, la assertion tiene que expresar la relación**. Clavar los operandos es la forma más común de escribir un test que verifica el símbolo `<` sin verificar nada más.
  - **EL GUARD DE UNICIDAD DEL RUNNER ATAJÓ MUTACIONES AMBIGUAS EN TRES TANDAS SEGUIDAS** (`f8fd486`, `6cf7e27`, `d5ed5c8`). En cada tanda hubo al menos una mutación cuyo texto aparecía en más de un lugar del archivo, y el guard cortó antes de correr. **Ya no es una precaución teórica**: es una barrera activa, y en las tres tandas evitó que una mutación pegara en el renglón equivocado y se reportara como "escapó" —un falso rojo que empuja a agregar assertions para tapar un hueco que no existe—. La lección de fondo, ya escrita: en un `.replace()` sobre el fuente, anclar a algo único o abortar; no confiar en que el texto de hoy sea único mañana.
  - **LA MISMA TRAMPA APARECIÓ TRES VECES, así que dejó de ser un accidente: es una PROPIEDAD del runner.** Un runner que muta por reemplazo de texto muta la PRIMERA aparición, y si el string existe en más de un lugar, muta el renglón equivocado. Los tres casos, todos en el módulo de Insumos: una frase de `descripcion` repetida entre dos tareas de `accesos.html` (`242573a`); el comentario `"No se hereda por rol: un super admin también la necesita tildada"`, igual en dos entradas; y `p_proveedor_id: w.proveedorId ?? null`, que aparece en la llamada a `remitos_vinculables` **y** en la de `presentaciones_sugeridas` (`6d021f2`).
    - El daño es el de siempre pero al revés: **no un falso verde sino un falso ROJO**. Se lee como cobertura faltante y empuja a agregar una assertion para tapar un agujero que no existe — ruido permanente en la suite, salido de un diagnóstico equivocado y con la sensación de haber mejorado algo.
    - **REGLA, ya implementada en el runner (`1228d19`): antes de correr, verificar que el texto a reemplazar sea ÚNICO en el archivo** —o anclarlo a suficiente contexto para serlo— **y ABORTAR nombrando las ambiguas** en vez de reportar cobertura. Es el mismo criterio que el guard de suite verde: si el mecanismo de verificación no está en condiciones de medir, lo dice, no informa un número.
    - Corolario del mismo runner: una mutación que **no aplica** (`mutado === src`) se reporta como ERROR DEL TEST, nunca como cobertura. Pasó una vez por un comentario en medio del texto buscado.
  - **UN `const` DECLARADO DENTRO DE UN `eval` NO ESCAPA AL SCOPE DE AFUERA — una `function` sí.** Los harness del proyecto extraen funciones del fuente y las evalúan, y eso viene funcionando desde siempre; al empezar a extraer también CONSTANTES (`62f3459`), el eval corría sin error y las constantes simplemente **no existían después**. El `ReferenceError` aparece en la primera línea que las usa, o sea lejos del problema real. REGLA: al extraer una constante, convertirla a `var` antes de evaluarla —`s.replace(/^const /, 'var ')`— que sí se filtra al scope de afuera. Es la misma familia que el extractor que truncaba las funciones con parámetros desestructurados: el mecanismo de extracción fallando en silencio y rompiendo en otro lado.
  - **UNA MUTACIÓN QUE LE SACA UNA COLUMNA A UN `.select()` NO LA PUEDE VER EL DOBLE DE `supabase`.** El doble devuelve la tabla entera sin mirar el select, así que sacarle una columna a una consulta **no cambia nada en la prueba**, aunque en producción deje ese dato en `undefined` — la trampa de "la columna no llega", ya documentada, ahora entrando por el andamio. Caso real (23/09/2026, Producción parte 7): **cinco** mutaciones escaparon por esto en una sola corrida y se leían como cobertura faltante. **La única forma de medirlo es afirmar sobre el TEXTO del select**, y así quedaron las cinco assertions (`desde`/`hasta`, `es_chocolate`, `ingrediente_libre`, `hasta_fin_de_turno`, `id`+`anulado`). Corolario: en una suite que corre contra un doble, **lo que el doble ignora no está probado**, por más que la assertion pase.
  - **DOS RUNNERS DE MUTACIONES A LA VEZ SE PISAN EL ARCHIVO TEMPORAL, y el resultado se lee como cobertura faltante.** Caso real (22/09/2026, rediseño de Producción): se lanzaron sin querer dos corridas del mismo `mut-produccion-quien.js` en paralelo. Los dos usan el mismo `mut-tmp-<suite>.html`, así que cada uno mutaba sobre lo que el otro acababa de escribir. Salieron **nueve "ESCAPÓ"** y **cinco eran falsos**: verificados a mano, sus mutaciones sí se detectan.
    - **El guard de "el sub-proceso leyó X y se escribieron Y" atajó solo una parte** —los casos donde los largos no coincidieron—, así que **no alcanza** cuando dos mutaciones distintas dan archivos del mismo tamaño.
    - **REGLA: un runner de mutaciones por vez.** Y ante un "ESCAPÓ", antes de agregar assertions, **reproducirlo a mano**: copiar el archivo, aplicar esa única mutación y correr la suite.
  - **UNA ASSERTION SOBRE UN VALOR QUE LA PROPIA PRUEBA YA DEJÓ PUESTO NO PRUEBA NADA.** Mismo trabajo: `marcarAbiertas()` marca un dato como conocido, y la assertion que lo verificaba corría sobre un sandbox donde **la prueba ya había puesto `abiertasConocido = true` a mano** unas líneas antes, para armar otro caso. Sacarle esa línea a la función no cambiaba nada y la mutación escapaba. Se arregló midiendo en un sandbox NUEVO. Es la hermana de "probar el helper no es probar su uso": acá **el estado de partida lo había fabricado el propio test**.
  - **MUTAR UN SOLO LADO DE UN COMPARADOR LO DEJA INCONSISTENTE, Y PUEDE PASAR POR CASUALIDAD.** Caso real (`62f3459`): la mutación "Sin categoría deja de ir última" tocaba solo el `oa` de un `sort` y dejaba el `ob` intacto. El comparador quedaba contradictorio —`a` antes que `b` Y `b` antes que `a`— y el resultado pasó a depender de la implementación del `sort`; para ese caso de prueba dio **el mismo orden esperado, por otro camino**, y la mutación se reportó como escapada.
    - No es que al test le falte una assertion: **es que la mutación no produjo el error que su nombre dice producir**. Es de la familia de la mutación mal apuntada, pero más difícil de ver — ahí el texto cae en otro renglón y se puede comprobar; acá cae en el renglón correcto y aun así no rompe lo que debía.
    - REGLA: **al mutar un comparador, tocar los DOS lados de la comparación.** Y si una mutación de ordenamiento escapa, sospechar primero de esto antes de agregar casos de prueba.
- **UN BOTÓN DESHABILITADO HACE INALCANZABLE EL ERROR QUE CUELGA DE ÉL.** Si la regla del proyecto es *"el error va pegado al botón que lo provoca"*, entonces **ese botón no se puede deshabilitar por lo que falta**: el error aparece al intentar, y si no se puede intentar, no aparece nunca. La persona queda mirando un botón apagado sin saber qué falta.
  - **Caso real** (Producción, cierre de la planilla, 23/09/2026): el cierre ponía `btn.disabled = faltan.length > 0` **y además** mostraba el texto de lo que falta solo si `intentado`, que se pone en el handler del clic. Las dos condiciones juntas hacían el mensaje **imposible de ver**.
  - **Y la suite estaba en VERDE**, porque llamaba a `intentarCerrar()` directo en vez de simular el clic: es la familia del **test que mide otra cosa**. La assertion que lo cierra no es sobre el mensaje sino sobre el botón —*"el botón sigue pudiéndose tocar"*— y tiene su mutación.
  - **REGLA:** el botón se traba **solo mientras se está mandando**. Lo que falta se dice al tocarlo, y además se marca el campo.
- **UN REPINTADO POSTERIOR PUEDE TAPAR EL ERROR QUE ACABA DE ESCRIBIR EL `catch`.** Con un `finally` que vuelve a pintar, el orden es `catch` (escribe el error) → `finally` (repinta y lo borra), así que el mensaje de la base **se ve un instante o no se ve nunca**.
  - **Caso real** (mismo cierre): el `catch` hacía `err.textContent = e.message; err.hidden = false` y el `finally` llamaba a `pintarCierre()`, que recalcula `err.hidden` a partir de lo que falta. Sin nada faltando, lo escondía. **La suite pasaba igual porque afirmaba sobre `textContent` y no sobre `hidden`** — el texto quedaba puesto, invisible.
  - **REGLA: el error va al ESTADO, no al DOM.** El `catch` guarda el mensaje (`estado.cierre.errorBase`) y el único que escribe en el DOM es el render, que decide qué mostrar: lo que falta, o lo que contestó la base. Y la assertion afirma **`hidden === false`**, no solo el texto.
- **UN CHEQUEO DE CR ESCRITO COMO `grep -c $'\r'` PUEDE DEGRADARSE A `grep -c ''` Y DEVOLVER LA CANTIDAD DE LÍNEAS**, que se lee como un desastre de CRLF. Pasó **dos veces el mismo día, a dos lectores distintos** (23/09/2026): uno informó que unos archivos "venían enteros en CRLF, 6.897 líneas con CR", y la verificación posterior informó 6.916 CR en el blob ya subido. **Los dos números eran la cantidad de LÍNEAS de esos archivos** — medido con Python sobre los bytes, `CR = 0` en el blob, en el árbol y en la copia guardada. **Nunca hubo CRLF.**
  - **Por qué se degrada:** con el `$'\r'` anidado adentro de `"$( … )"` la comilla ANSI-C no siempre se procesa y el patrón queda **vacío**, y un patrón vacío matchea todas las líneas. No hay ningún error: `grep` responde un número perfectamente plausible.
  - **Por qué es de la familia de "un comando de verificación que no lee el exit code de lo que verifica", con una diferencia que conviene anotar:** acá el modo de falla **no** produjo el resultado deseado sino uno **alarmante**, y por eso se miró dos veces. Con el signo al revés —un cero falso— habría pasado derecho, que es exactamente lo que pasó con el `git show` de las rutas de MSYS.
  - **REGLA: contar CR sobre los BYTES**, no con una comilla que el shell puede comerse: `python -c "print(open(f,'rb').read().count(bytes([13])))"`, o `git ls-files --eol`, que lo dice sin ambigüedad.
- **CUANDO UN ARREGLO PUSHEADO "NO FUNCIONA", VERIFICAR PRIMERO QUE EL NAVEGADOR ESTÉ CORRIENDO ESE CÓDIGO — antes de dudar de la lógica.** Caso real (septiembre 2026): el bug de `chk_presentacion_coherente` se reportó como no arreglado después de pushear el arreglo. Se perdió una vuelta entera buscando el defecto en el estado del wizard, y no había ninguno: la pestaña estaba corriendo el HTML anterior al push.
  - **LA PISTA MÁS BARATA SON LOS CAMBIOS COLATERALES DEL MISMO COMMIT.** El commit traía dos cosas independientes: la corrección de la lógica y un mensaje de error nuevo, que nombra el renglón en vez de citar el constraint. **El error reportado seguía siendo el crudo de Postgres.** Que falte también el cambio de TEXTO —que no depende de ningún estado ni de ningún camino— no se explica con un bug de lógica: se explica con que el archivo no es el que se cree. Un cambio de mensaje, de rótulo o de color del mismo commit sirve de testigo, y cuesta cero mirarlo.
  - **GitHub Pages sirve con caché HTTP**, y una pestaña abierta desde antes del push conserva el HTML viejo. El service worker del proyecto está desactivado y se autodesregistra, así que **una recarga forzada alcanza** — no hace falta desinstalar nada.
  - Cómo se confirma sin adivinar: `curl` al archivo desplegado y buscar en él un identificador que solo exista en el commit nuevo, más comparar el tamaño contra el local. Si el fuente desplegado lo tiene, el problema está entre el servidor y el navegador y no en el código.
  - Es hermana de la de abajo —"lo que corre puede haber divergido del repo"— pero por el otro extremo del camino: aquella es **el servidor atrasado respecto del repo**, esta es **el navegador atrasado respecto del servidor**.
- **Lo que corre en producción puede haber divergido del repo: verificar contra el FUENTE DESPLEGADO.** Vale sobre todo para las Edge Functions, que **se despliegan aparte del push** (`supabase functions deploy`): un push sin deploy deja el repo adelantado, y un deploy desde otra máquina lo deja atrasado. Caso real (`7d145d7`): el comentario falso del `crudo`/`datos` apareció leyendo la función desplegada al verificar un deploy, no revisando el repo. `mcp__supabase__get_edge_function` devuelve el fuente que está arriba; para la base, `pg_get_functiondef` y `pg_get_viewdef` cumplen el mismo papel.
- **CUANDO EL DOMINIO TRAE MATEMÁTICA, VALIDAR EN EL BORDE CON ELLA — es la única validación que no depende de que alguien se acuerde.** Los códigos del cheque traen sus **dígitos verificadores** (norma BCRA, ponderador 9713), así que se guardan y los valida un CHECK con `dv_bcra()`: **un código mal leído no entra a la base**, venga del OCR o de un dedazo en el celular. No hace falta confiar en el modelo ni en la persona — la aritmética rechaza sola lo que no cierra.
  - Es la forma FUERTE de la regla que este documento ya tiene en varias versiones débiles: *validar contra el predicado del constraint*, *validar sobre la fila que se va a insertar*, *con la igualdad exacta de la base*. La diferencia es que acá el predicado **no lo inventamos nosotros**: viene con el dominio, y eso lo hace inmune a "lo aflojo un poquito para que pase este caso".
  - **El precio es tener la misma fórmula en tres lugares** (la base, la Edge Function y el HTML), porque el proyecto no comparte JS entre módulos y Postgres no comparte código con Deno. Se paga con el comentario cruzado en los tres: *"si cambia una, cambian las tres"*. Una réplica declarada y verificada es mejor que una validación que vive en un solo lado y por eso no corre donde hace falta.
- **REGLA DEL PROYECTO — TODO TEXTO QUE NO SEA LITERAL DEL CÓDIGO SE ESCAPA ANTES DE ENTRAR A `innerHTML`. Un texto libre sin escapar es ESCALACIÓN DE PRIVILEGIOS, no un bug visual.** El dato lo escribe alguien con pocos permisos —o sin cuenta— y lo mira alguien con muchos: con `<img src=x onerror=…>` en ese campo, el código corre **en la sesión de quien mira**, que casi siempre es un super_admin.
  - **POR QUÉ ES UNA REGLA Y NO UN INCIDENTE:** el patrón correcto entró TARDE y los módulos viejos quedaron atrás. El primer helper de escape del proyecto apareció el **10/08/2026** con el wizard de Ingreso (`4fad55f`), y lo siguieron Stock (`12fd7f8`, 13/08) y Cobranzas (`6654fac`, 15/09, como `escCob`). **Los cinco módulos de julio o anteriores no tenían ninguno.** Verificado el 16/09/2026 con grep:
    - **Gastos:** arreglado ese día (`9a0e9a8`).
    - **SIN NINGÚN HELPER: ninguno.** Los cinco módulos de julio o anteriores tienen hoy su `esc()`.
    - **`empleados.html` — CERRADO en `3ee44c5` (17/09/2026).** Los 5 `value="..."` de los formularios, el listado, la ficha, los dos selects de unidad y el chip de módulo. **El barrido propio sumó un sink fuera de la lista:** el NOMBRE de la unidad en los dos `<option>` (edición e importación) — la lista traía solo su `value`. Y descartó con la base dos que parecían sinks: las fechas de la ficha son columnas `date`. La prueba da **70/70** (y **11/70** contra el archivo anterior) y **37/37** mutaciones detectadas, con el mismo chequeo estático de interpolaciones que Caja.
    - **`caja.html` — CERRADO en `a3fd993` (17/09/2026).** Con barrido propio del archivo entero, que sumó dos sinks fuera del traspaso: la clase `chip-tipo-movimiento--${m.tipo}` y los labels `"dueño — cuenta"` del multiselect de la ficha de Empresa. Y confirmó que los datos bancarios ya se dibujaban por DOM con `textContent`, así que ahí no había nada que hacer. La prueba da **148/148** (y **8/148** contra el archivo anterior) y **88/88** mutaciones detectadas.
    - **`cuentas-corrientes.html` — CERRADO en `1664384` y `6f09e8e` (16/09/2026).** Con barrido propio del archivo entero, que encontró sinks fuera del traspaso: **la moneda en ~20 interpolaciones** de importes (ver abajo) y `gastos.medio_pago` en el detalle del pago, que tampoco tiene CHECK. La prueba ejecutada da **161/161** (y **12/161** contra el archivo anterior) y **100/100** mutaciones detectadas, más 4 equivalentes contadas aparte (importes con la moneda `'ARS'` constante del código, donde sacar el escape no cambia ninguna salida posible).
    - **`accesos.html` — el peor, CERRADO en `d638cd9` (16/09/2026).** `solicitudes_acceso.nombre` / `apellido` / `email` los carga alguien **SIN CUENTA** en `registro.html`, y la pantalla la abre un super_admin para aprobar: un desconocido ejecutaba código en la sesión de quien reparte los permisos. Ahora tiene su `esc()` y **los 11 `innerHTML` están cubiertos**; la prueba ejecutada da **55/55** verificaciones (y falla contra el archivo anterior) y **34/34** mutaciones detectadas. Además, desde ese día el CHECK `chk_solicitud_texto_sin_html` y la validación de `crear-solicitud-acceso` rechazan `<` y `>` en esos tres campos — defensa en profundidad, no reemplazo.
    - **LECCIÓN DE PROCESO DEL BARRIDO DE ACCESOS: lo hicieron TRES revisores y cada uno encontró sinks que los otros dos no vieron.** El chat de Gastos, los críticos; el chat de permisos, el `<option>` del filtro de módulos; y Claude Code, **tres más con datos de la tabla `modulos`** (el label del modal de módulos, el atributo `data-modulos` y el aviso de dependencia que nombra módulos). Ninguna lista estaba completa.
      - **REGLA: cuando llegue un traspaso de XSS con una lista de sinks, asumir que está INCOMPLETA y hacer el barrido propio igual** —todo `innerHTML`, todo atributo interpolado, todo helper que devuelva un pedazo del dato—. La lista sirve para no olvidarse de buscar; no sustituye la búsqueda.
      - **`materia-prima.html` — BARRIDO HECHO el 21/09/2026 (`afe4217`):** 514 interpolaciones, 329 arman HTML, **8 sinks cerrados**. El más grave: el `href` de "Ver foto →" aceptaba un `javascript:` guardado por alguien con solo `cargar` (después, con la fase 2 de fotos, ningún dato de la base va a un href). Suite `test-materia-prima-xss.js`.
      - **`stock.html` — BARRIDO HECHO el 21/09/2026 (`be51610`):** 414 interpolaciones, 324 arman HTML, 1 sink real (la unidad de medida en `poblarSelectorVista`; `insumos.unidad_medida` no tiene CHECK) y 5 no explotables, escapados igual. Suite `test-stock-xss.js`.
      - **Tres puntos ciegos del método de Cobranzas**, que las suites nuevas ya cubren: el estático cortaba las asignaciones largas a `innerHTML` en la primera línea; no revisaba lo que devuelve un `.map` que no arma HTML propio; y un texto armado sin HTML propio asignado directo a `innerHTML`.
      - **SUITES DE REGRESIÓN RECUPERADAS el 21/09/2026** para Gastos (`test-gastos-xss.js`), Caja (`test-caja-xss.js`), Cuentas Corrientes (`test-cuentas-corrientes-xss.js`) y Empleados (`test-empleados-xss.js`): sus barridos se sostienen. **Hallazgo en Gastos fuera del barrido:** `?volver=` navegaba a cualquier dirección (`javascript:` incluido) — arreglado en `8f73654`: solo http/https del mismo origen.
      - **REGLA NUEVA: un parámetro de la URL (o un dato de la base) que termina en `location.href`, `window.open`, `href` o `src` es un sink igual que `innerHTML`**, y un barrido que solo busca `innerHTML` no lo ve. `encodeURIComponent` al ARMAR el link no protege a quien lo LEE.
      - **`cobranzas.html` — BARRIDO HECHO el 17/09/2026, y NO había ningún sink abierto: CERO líneas cambiadas** (`8f6b0ee`, que commitea solo el traspaso). Es el primer módulo del proyecto donde el barrido no encuentra nada, y tiene explicación: **nació con su `escCob`** —los cinco viejos lo sumaron después— así que el escapado se escribió junto con cada render en vez de agregarse encima. Se clasificaron las **237** interpolaciones del `<script>` (las **152** que entran a una plantilla que arma HTML y las **85** que no) más las **14** asignaciones a `innerHTML`, **ninguna sin justificar**; **89 llevan `escCob`**. La prueba ejecutada da **90/90**, **85/90 con un solo escape sacado**, y **90/90 mutaciones detectadas** (más 2 equivalentes declaradas aparte: las dos ramas de `'Diferido'`/`'Común'` no tienen ningún carácter escapable, así que la página sale idéntica).
        - **Que el barrido no encuentre nada NO lo vuelve inútil, y conviene dejarlo escrito:** es el único resultado que se puede afirmar *después de mirar*, y lo que queda del trabajo es la verificación que impide que se abra un sink mañana — el mismo valor que en los módulos donde sí había agujeros.
        - **La moneda no es sink en este módulo, por una razón propia:** `formatearImporte()` de `cobranzas.html` tiene `currency: 'ARS'` **hardcodeado en el código**, así que ningún valor de la columna `moneda` entra nunca al HTML — a diferencia de Caja y Cuentas Corrientes, donde venía de la base y hubo que escaparla. Sumado al CHECK `= 'ARS'` de la tabla, son dos barreras independientes.
    - **`dashboard.html` y las pantallas de auth: AUDITADAS el 16/09/2026, sin sinks** (no tienen helper, y no les hace falta hoy). Todo dato de usuario va por `textContent` —el nombre y el email del header, los factores MFA, el match de `registro.html`, los mensajes de error—; los únicos `innerHTML` son el ícono del "ojito" (texto fijo) y la grilla del dashboard, que se arma con la constante `MODULOS` del código y un conteo numérico. **Si alguna vez la grilla pasa a leer la tabla `modulos`, deja de estar a salvo.**
    - **`registro.html` es la ENTRADA del vector, no un sink:** ahí se escribe el nombre que después explota en Accesos, pero registro no lo dibuja. Desde `f49bdd9`, **`crear-solicitud-acceso` rechaza `<` y `>` en `nombre`, `apellido` y `email`** antes de crear la cuenta (antes no validaba nada más que no estuvieran vacíos). Aun así, **la defensa real sigue siendo escapar en quien lo muestra**: el dato puede entrar por otro camino, y otros caracteres (una comilla en un atributo) no los frena ninguna de las dos validaciones.
  - **EL LISTADO ES PEOR QUE EL DETALLE:** un detalle hay que abrirlo; una tarjeta se dibuja con solo entrar al módulo.
  - **Las cinco lecciones, que valen para todo archivo:**
    1. **LOS ATRIBUTOS SON SINK IGUAL QUE EL CONTENIDO.** En `value="${x}"`, `alt`, `title` o `data-*` alcanza una comilla para cerrar el atributo e inyectar. Un formulario de edición que precarga `value="${emp.domicilio}"` es tan peligroso como un `<span>`.
    2. **LAS INICIALES TAMBIÉN.** `iniciales()` / `inicialesEmpresa()` devuelven los primeros caracteres del texto, así que `'<x'` da `<X`. Se escapa su resultado. Lo mismo con cualquier helper que devuelva un pedazo del dato —`formatearCuil()` de accesos devuelve el CUIL **crudo** si no tiene 11 dígitos— o que lo concatene (`'Entregado a ' + nombre`).
    3. **UN HELPER QUE ARMA HTML ESCAPA POR DENTRO**, y el caso que necesita HTML crudo va por una variante con **nombre propio**, con los datos escapados en el call site. En `gastos.html`, las cuatro `fila()` escapan siempre y `filaHtml()` existe solo para el ícono de Categoría; `poblarSelect()` escapa `value`, `label` y `placeholder`. Con decenas de usos, *"acordate de poner `esc()`"* se rompe en el primer descuido y sin ningún aviso.
    4. **NO ESCAPAR DOS VECES.** Al pasar un helper a escapar por dentro, **sacar los `esc()` de los call sites**, o el texto sale como `A &amp;amp; B`. Pasó al integrar `9a0e9a8`: los resúmenes que mandaban `esc(proyecto)` a `fila()` pasaron a mandarlo crudo.
    5. **QUÉ NO ES SINK, y no se toca:** `textContent`, `document.createTextNode` y `window.confirm`. Los toasts de `js/utils.js` usan `textContent`.
  - **LA MONEDA YA NO ES TEXTO LIBRE: LAS 8 TABLAS CON COLUMNA `moneda` TIENEN CHECK** (verificado contra `pg_constraint` el 17/09/2026, los 8 validados). `cobranzas` admite solo `'ARS'`; las otras **7** —`caja_movimientos`, `caja_solicitudes_movimiento`, `creditos_proveedor`, `cuentas_caja`, `facturas_pendientes`, `gastos` e `intereses_factura`— admiten las **12 monedas del selector de la app** (`ARS`, `USD`, `EUR`, `BRL`, `PYG`, `UYU`, `CLP`, `BOB`, `GBP`, `MXN`, `COP`, `PEN`). Antes de aplicarlo, los datos existentes solo tenían `ARS` y `USD`: ninguna fila rechazada.
    - **Hasta el 17/09/2026 las 7 aceptaban cualquier texto**, y eso volvía un sink cada importe interpolado: en `cuentas-corrientes.html` eran unas 20 interpolaciones que ningún traspaso armado por línea había contado.
    - **Con el CHECK, la moneda pasa de sink a CONSISTENCIA.** El riesgo real queda en el texto libre de verdad: nombres de cuenta, descripciones y razones sociales. Igual, **todo importe que entra a HTML va por `importeHtml()`** (Cuentas Corrientes y Caja): tener un solo lugar que decide cómo entra un importe a la página vale por claridad, y es la defensa si el CHECK algún día se afloja (ver *"la validación en el servidor es defensa en profundidad"*).
    - **Agregar una moneda es un `DROP` + `ADD` de los 7 CHECK**, leyendo antes el real, y además sumarla al selector de la app: las dos listas tienen que decir lo mismo.
  - **CÓMO SE VERIFICA, porque leyendo no se ve** (ver *"una assertion sobre el call site no dice nada del callee"*):
    - **Ejecutar los renders** con un `document` falso y **una marca distinta por campo** (`"><b data-xss="campo">`).
    - **Afirmar dos cosas:** que no aparece ninguna marca cruda, y que aparecen escapadas **todas** las esperadas.
    - **Correr la prueba contra el archivo ANTERIOR, donde tiene que fallar.** En Gastos, según reportó la sesión que hizo el arreglo, dio 44 fallas antes, verde después y 57/57 mutaciones detectadas.
    - **Mutaciones que saquen cada `esc()`** y den rojo por una verificación, no por una excepción.
    - **NO contar los `innerHTML` para detectar uno nuevo; verificar que ninguno quede sin escapar.** Una aserción de conteo exacto se rompe sola con cualquier cambio que agregue o saque una línea, y cuando se rompe nadie sabe si es una regresión de seguridad o alguien que sumó un render. Lo que sirve es un **chequeo estático**: recorrer el `<script>` y exigir que cada `${...}` de una plantilla que arma HTML esté escapado (`esc` / `importeHtml` / `encodeURIComponent`) o figure en una lista de valores seguros **con su motivo** (HTML ya escapado, un número, una constante, una columna con CHECK o `date`). Una interpolación nueva sin escapar pone la prueba en rojo **nombrando la expresión y la línea**. Así quedó la prueba de Caja (`a3fd993`).
      - **No reemplaza a ejecutar los renders**, lo complementa: hay mutaciones que solo ve uno de los dos (un importe con `'ARS'` fijo solo lo ve el estático; un `esc` que escapa el argumento equivocado solo lo ven los renders). En Caja, sin el estático los renders detectan 84/88; con él, 88/88. En Cobranzas, 71/90 contra 90/90.
    - **PELAR COMENTARIOS NO ES LA FORMA DE ANALIZAR EL FUENTE: HAY QUE TOKENIZARLO.** Más arriba está escrito que un `replace` de `/* */` sobre un HTML es inseguro por el `accept="image/*"`. La salida no es pelar con más cuidado: es que el analizador **distinga código de string, de template, de comentario y de regex**. Entre otras cosas porque sin eso **no se puede saber qué es una plantilla que arma HTML y qué no**, y ahí se pierden las interpolaciones de las plantillas **ANIDADAS** — en Cobranzas, 16 de las 152.
    - **NO ALCANZA CON PREGUNTAR SI LA EXPRESIÓN EMPIEZA CON `esc(`: HAY QUE MIRAR LAS POSICIONES DE VALOR.** Un ternario escapa en sus ramas y no afuera, un `a || b` puede imprimir cualquiera de los dos, y un `a && b` solo imprime `b` —si imprimiera `a` sería falsy y no sale nada—. El chequeo descompone la expresión en lo que puede llegar a la página y clasifica CADA hoja. Sin eso, la mitad de las interpolaciones de un render con ramas queda sin verificar, o hay que meterlas a mano en la lista de seguras, que es peor: la lista crece y termina tapando un hueco real.
    - **EL CONTEXTO IMPORTA TANTO COMO EL ESCAPE.** `esc()` alcanza para el contenido y para un atributo **entre comillas**. NO alcanza para un atributo **sin comillas** —un espacio ya rompe afuera—, ni dentro de un `on*=`, ni dentro de un `href`/`src`, donde el contenido es código y hace falta otra cosa. El chequeo de Cobranzas mira también eso; hoy no hay ninguno de los tres casos en el archivo, y la verificación existe para que siga siendo así.
  - **Un módulo nuevo nace con su `esc()`** —duplicado localmente, como el resto de los helpers—, después de confirmar con `grep` que el nombre está libre en el archivo (ver el caso `nombreUnidad`).
  - **La validación en el servidor es defensa en profundidad, no reemplazo.** Rechazar `<` y `>` en `crear-solicitud-acceso` y en `chk_solicitud_texto_sin_html` (los dos, desde el 16/09/2026) ayuda, pero el escapado va igual: el dato puede entrar por cualquier otro camino (Naaloo, SQL, una RPC futura).
- **UN ID GENERADO EN EL CLIENTE COMO CLAVE PRIMARIA ES LA FORMA BARATA DE HACER IDEMPOTENTE UN ALTA QUE SE VA A REINTENTAR.** Si el `id` lo pone el servidor, un reintento después de una respuesta perdida —la red de una fábrica, un celular que se bloquea— crea un segundo registro y nadie se entera: no hay error, hay dos cobranzas iguales. Con el uuid generado en el celular, la segunda llamada choca contra la PK y la RPC puede responder *"ya estaba"* en vez de duplicar.
  - **Lo que lo completa es que la RPC lo trate como caso NORMAL y no como error:** `guardar_cobranza` devuelve `{ya_existia: true}`, y solo corta si ese id es de otra persona. Sin eso, el cliente recibiría un `23505` y tendría que adivinar si fue un duplicado propio o un choque real.
- **GUARDAR LA RUTA DE STORAGE Y FIRMAR AL MIRAR. Una URL firmada larga es una llave sin sesión que no se puede revocar.** El proyecto tiene **115 URLs firmadas a diez años** (al 16/09/2026) guardadas en tres columnas (ver Seguridad): cada una abre el archivo para cualquiera que la tenga, sin login, y quitarla no sirve de nada porque el token ya está afuera. Cobranzas guarda la **ruta** y firma por **300 segundos** al abrir la foto.
  - El costo de la forma correcta es una llamada más al abrir; el de la otra es que la única manera de revocar es **mover el archivo**.
- **VARIOS ELEMENTOS EN UNA MISMA FOTO: LA RESOLUCIÓN A LA QUE EL MODELO MIRA LA IMAGEN ES PARTE DEL DISEÑO.** Los modelos de nivel estándar reducen la imagen a ~**1568 px** en el lado largo, y en una foto con tres cheques cada uno queda con un tercio de eso: **la letra manuscrita se vuelve ilegible**. El riesgo es un importe plausible y mal, sin ningún error; **todavía no está medido con cheques reales** (ver la evaluación de modelos pendiente en el módulo Cobranzas). Para documentos con varios elementos hay que usar modelos que miren a mayor resolución (**2576 px** en los 4.7+).
  - Si pasa, es de los peores modos de falla de este documento —**no hay ningún error**, hay una cifra creíble— y no se arregla con el prompt. Por eso la medición va antes que la confianza.
  - Corolario práctico: la resolución con la que se comprime la foto **antes** de subirla y el modelo elegido son la misma decisión, no dos. Comprimir de más ahorra datos y arruina el OCR.
- **UNA FUNCIÓN QUE GUARDA "LO QUE HAY EN PANTALLA" EN VEZ DEL OBJETO SOBRE EL QUE TRABAJÓ PIERDE DATOS EN CUANTO EL TRABAJO ES ASÍNCRONO.** `guardarBorrador()` lee `estado.form` —el formulario abierto **ahora**— así que si termina una subida o un OCR de una cobranza y en el medio la persona abrió otra, el guardado escribe el borrador equivocado: el trabajo de la primera se pierde sin ningún error. **Pasar siempre el objeto explícito** (`guardarBorrador(f)`), no leerlo del estado global.
  - Es la misma familia que el `p_recuento_id` que se medía por distancia de texto y el `item.bultos` que significaba dos cosas según el modo: **una referencia implícita que resuelve bien en el camino común y mal en el que importa**. Y acá el camino que importa es el normal del módulo, porque subir fotos sin señal tarda.
- **UN ENCARGO TAMBIÉN ENVEJECE: auditar el código ANTES de hacer lo que pide.** Caso real (23/09/2026, recuento de Stock): el encargo pedía arreglar un bug que ya estaba arreglado dos días antes (`da0bf70`) y dos funcionalidades que ya estaban hechas. Lo atajó leer el CÓDIGO antes de tocarlo, en vez de confiar en el pedido. Es "verificar antes de asumir" por el lado del que RECIBE el pedido, no del que lo escribe.
- **UN RANGO DE ACENTOS SE ESCRIBE CON SU SECUENCIA DE ESCAPE, NUNCA CON LOS CARACTERES.** Caso real (23/09/2026, `pedidos.html`): la herramienta que escribió el archivo convirtió la secuencia de escape del rango de marcas diacríticas del `normalize('NFD')` en los dos caracteres combinantes LITERALES. El regex funcionaba igual, pero **en el fuente se ve vacío** (un combinante se pega al carácter de al lado) y una mutación que buscaba el texto con la secuencia de escape no lo encontraba. Se detectó porque el runner abortó con "el texto a reemplazar NO EXISTE". Es la hermana del NUL literal del caché del sandbox: **un carácter invisible en un fuente se escribe con su escape**.
- Verificación de cada commit: bajar el `.patch` real de GitHub y leerlo, nunca confiar en el resumen que da Claude Code de lo que hizo.

## Cómo trabajar

### REGLA DE ORO — Territorio compartido

Cada chat de módulo trabaja EXCLUSIVAMENTE sobre su módulo: su HTML, sus RPCs, sus policies RLS.

Hay territorio COMPARTIDO con un único dueño, **el chat de arquitectura de permisos**. Ningún chat de módulo lo modifica directamente:
- el CHECK constraint `chk_tarea_valida`
- el `CATALOGO_TAREAS` de accesos.html
- la pantalla de Accesos (`modulos/accesos.html`)
- este archivo, `CLAUDE.md`
- **la carpeta `.claude/`**, donde viven las definiciones de los subagentes (`.claude/agents/*.md`, un archivo por módulo, con el territorio y los límites de ese subagente) y los traspasos que dejan al cerrar (`.claude/traspasos/*.md`, que se borran cuando se aplicaron). **Un subagente que puede editar su propia definición puede aflojarse sus propios límites**, así que ninguno la toca — la ÚNICA excepción es que cada uno escribe su propio archivo en `.claude/traspasos/`.
  - **Son ONCE subagentes, uno por módulo** (21/09/2026, más `cheques` y `produccion` el 22/09/2026 y `pedidos` el 23/09/2026): `gastos`, `caja`, `cuentas-corrientes`, `empleados`, `materia-prima`, `stock`, `cobranzas`, `cheques`, `produccion`, `pedidos` y `accesos`. **`pedidos`** es dueño de `modulos/pedidos.html` y de `guardar_cliente`, `guardar_pedido`, `marcar_avance_pedido`, `cambiar_estado_pedido` y `pedidos_de`; escribe `clientes`, `pedidos` y `pedido_items` SOLO vía esas RPCs y **lee sin tocar el catálogo de Producción** (`productos_terminados`, `producto_presentaciones`, `marcas_personalizadas`). **`produccion`** es dueño de `modulos/produccion.html` y de las RPCs de turnos, masas, paradas, cierre y configuración (lista en `.claude/agents/produccion.md`); escribe las 16 tablas del módulo SOLO vía esas RPCs, y **comparte con Stock `stock_movimientos`** (cada masa descuenta sus insumos con el trigger de `masa_items`, tipo `consumo_sala_masa`). **`cheques`** es dueño de `modulos/cheques.html` y de las RPCs `marcar_salida_cheque`, `marcar_salida_cheques` y `volver_cheque_a_cartera`; escribe `cobranza_cheques` y `cobranza_historial` SOLO vía esas RPCs y lee `cobranzas` (vía `v_cobranzas`), `bancos_bcra`, `v_empleados_publico`, `empleados` y `empleado_tareas`. Esas tablas son del esquema de Cobranzas y **COMPARTIDAS** entre los dos subagentes, igual que `js/cobranzas-comun.js`, que no edita ninguno de los dos. Los diez comparten el mismo molde —primera acción, límites de escritura, verificación, cierre y lenguaje idénticos— y difieren solo en el territorio: su HTML, sus RPCs, las tablas que **escribe** separadas de las que **solo lee** (sacadas de cada `.from`, `.rpc` y `.storage.from` del HTML y confirmadas contra `pg_proc`), y sus Edge Functions.
    - **`css/main.css`, `js/auth.js`, `js/utils.js` y `dashboard.html` no son de ningún subagente**: está escrito así en los ocho.
    - **Las tablas que escriben dos módulos figuran como COMPARTIDAS en los dos**: `empleados` (Empleados y Accesos), `caja_movimientos` (Caja, y Gastos por `anular_gasto` y el trigger del gasto), e `insumos`, `proveedor_insumo_alias`, `stock_movimientos` y las dos de transferencias (Stock e Ingreso). Antes de tocar la estructura o las policies de una compartida, el subagente mira qué hace el otro módulo y, si lo afecta, devuelve un traspaso.
    - **`accesos` es el único con una excepción dentro de su propio archivo:** el `CATALOGO_TAREAS` vive en `modulos/accesos.html` pero lo sigue decidiendo el chat de arquitectura de permisos, en sincronía con el CHECK; el subagente solo lo toca cuando un traspaso de ese chat lo pide. Las pantallas de auth y sus Edge Functions (`crear-solicitud-acceso`, `buscar-empleado-cuil`) no son suyas.

Cuando el trabajo de un módulo necesita tareas nuevas, cambios de permisos o cambios de catálogo, ese chat **DEBE generar un prompt de traspaso** para el chat de permisos, con la información completa:
- las claves exactas `modulo:tarea`
- qué significa cada una (la semántica, no solo el label)
- si lleva bypass de super_admin o no (`tiene_tarea` vs `tiene_tarea_explicita`)
- el alcance, si aplica
- qué RPCs cambiaron de firma o de valor de retorno

**Corolario, y es el que evita el daño más caro:** cualquier cambio a `chk_tarea_valida` se hace leyendo PRIMERO el constraint real con `pg_get_constraintdef` y agregando sobre eso. NUNCA regenerándolo desde una lista propia — regenerar pisa en silencio las tareas de otros módulos que ese chat no conoce.

### REGLA DE ORO — Cierre de tarea = doc al día

Ninguna tarea se considera terminada hasta que CLAUDE.md refleje sus cambios.

Si el chat que hizo el trabajo no puede editarlo (por la regla de arriba), tiene que **generar el prompt de actualización de doc y entregárselo a Facu como parte del cierre**. No es opcional ni "algo para después": una tarea sin la doc actualizada está incompleta.

### REGLA DE ORO — Quién escribe en la base, y cuál es el único límite (vigente desde el 16/09/2026)

**TODOS LOS CHATS TIENEN ACCESO DE LECTURA A SUPABASE (MCP).** Cualquier chat puede consultar el estado real: constraints, policies, funciones, vistas, columnas, filas.

**1. "Verificar antes de asumir" se queda sin excusas.** Antes, *"no puedo verificarlo desde acá"* era una limitación real, y por eso se aceptaban afirmaciones de segunda mano. Ahora verificar cuesta una consulta. Ningún chat debe afirmar, documentar ni traspasar un dato sobre la base sin haberlo consultado primero — **ni siquiera datos propios ni recientes**. **Los SELECT de verificación se corren libremente**, sin pedir permiso.

Caso real (agosto 2026): un traspaso reportó "31 tareas en el CHECK" cuando eran 32; el número venía arrastrado de la tanda anterior. Se detectó porque el chat receptor consultó antes de aceptarlo. **Esa verificación es obligatoria en los DOS lados**, el que traspasa y el que recibe: el que traspasa evita propagar el error, y el que recibe es la última red antes de que el dato entre al código o a este documento.

**2. ESTRUCTURA SÍ, DATOS NO.** Regla de Facu, que reemplaza a la anterior de *"leer no es escribir"*:
- **El chat de arquitectura y Claude Code tienen AUTORIDAD DE ESCRITURA en Supabase para CREAR y MODIFICAR ESTRUCTURA**: tablas, columnas nuevas, índices, vistas, funciones/RPCs, políticas, constraints y DDL en general. **Se aplican directamente, sin pedir permiso**, con guards (`IF NOT EXISTS` / `CREATE OR REPLACE` / `DROP` explícito ante un cambio de firma) y **verificación posterior** contra el catálogo (`pg_get_constraintdef`, `pg_get_functiondef`, `reloptions`, `information_schema`).
- **EL ÚNICO LÍMITE SON LOS DATOS.** Nunca borrar ni editar filas ya cargadas —un gasto, una persona, una cobranza— **salvo que Facu lo pida explícitamente**. También requieren **su aprobación previa** los cambios de estructura que puedan destruir o alterar datos existentes: `DROP` de tablas o columnas con datos, cambio de tipo o de nulabilidad de una columna con datos, `TRUNCATE`.
- **TODA escritura se reporta a Facu**: qué se corrió, con qué verificación y qué resultado. Es lo que conserva **un único punto donde se ve todo lo que entra a producción**, en un proyecto donde varios chats trabajan en paralelo sin verse entre ellos: antes lo daba que Facu corriera cada SQL a mano; ahora lo da el reporte.
- **Los demás chats de módulo mantienen el acceso que tengan configurado.** Si no tienen escritura, **entregan el SQL a Claude Code o a Facu** para que lo apliquen.
- **Lo que NO cambió:** leer antes de escribir; el traspaso entre chats cuando el cambio toca territorio de otro; y **nunca regenerar de memoria un CHECK o un catálogo compartido** —se lee el real y se agrega sobre eso (ver el corolario de la regla del territorio compartido)—. Tener permiso para escribir no vuelve más seguro escribir sin haber leído: la corrección chiquita de un chat sigue siendo el cambio invisible que otro chat va a pisar si no está reportada.

### REGLA DE ORO — Las suites de verificación viven en `pruebas/`, no en el scratchpad

**Toda suite, runner de mutaciones o chequeo que valga la pena volver a correr va versionado en `pruebas/` del repo.** El motivo es el que las justifica: **una verificación que se borra con la sesión no impide nada mañana**, que es justamente para lo que se escribió.

**El costo ya se pagó y no se recupera:** cuando se adoptó esta convención (`bb1f2b6`, 17/09/2026), Gastos, Caja, Cuentas Corrientes y Empleados figuraban en este documento con su **barrido de XSS cerrado y la prueba que lo demostraba ya no existía en ningún lado** — se fue con las sesiones que las escribieron. No se rehacen: la convención arranca con lo único que sobrevivía, la de Cobranzas.

**Qué hay hoy** (`pruebas/`, con su propio README):
- `test-cobranzas-xss.js` — el barrido de escapado, en sus dos mitades: los renders EJECUTADOS con un `document` falso y el chequeo estático.
- `mut-cobranzas-xss.js` — su runner de mutaciones.
- `check-scripts.js` — el archivo entero como lo ve el navegador. **Se corre en el cierre de TODO commit de módulo**, junto con las suites. Sin argumentos revisa todos los HTML del repo.
- **`test-materia-prima-circuito.js` / `test-gastos-circuito.js` / `test-cuentas-corrientes-circuito.js`** — el circuito Ingreso ↔ Gastos ↔ CC (21/09/2026): los renders NUEVOS ejecutados con texto malicioso, qué RPC se llama y con qué, que ningún null termine en "$ 0,00", y un chequeo estático **ACOTADO a las funciones nuevas** (no al archivo entero: materia-prima.html tiene su barrido pendiente). Con sus `mut-*-circuito.js`. Números al cerrar: **113/113, 57/57, 78/78; mutaciones 33/33, 21/21, 39/39.**
- **Cobranzas, además** (21/09/2026): `test-cobranzas-fotos.js` / `mut-cobranzas-fotos.js` (estados de cada foto, `enCurso`, reintentos con el formulario abierto), `test-cobranzas-escritorio.js` / `mut-cobranzas-escritorio.js` (master-detail y la franja "por controlar"), `test-cobranzas-cabecera.js` / `mut-cobranzas-cabecera.js` (nombres de estados en pantalla, cifras de cabecera con `resumen_cobranzas`, tarjeta compacta) y `controles-cobranzas.js` (ningún control perdido contra el baseline `fba6396`). Números al cerrar la segunda tanda: controles 351/351, xss 320/320, fotos 70/70, escritorio 45/45, cabecera 72/72; mutaciones cobranzas xss 221/221 (+1 eq.), fotos 27/27 (+2 eq.), escritorio 35/35, cabecera 45/45.
- **Noche del 21/09/2026:** `test-numeros.js` / `mut-numeros.js` (las funciones de números de utils.js; `numeros-comun.js` trae `fuenteNumeros()`, para meter el código real de utils.js en el preludio de un sandbox, e `inputFalso()`, un input que imita al navegador al tipear, borrar y pegar); `test-<modulo>-numeros.js` + mut para cobranzas, gastos, caja, cuentas-corrientes, materia-prima y stock; `test-<modulo>-xss.js` + mut para gastos, caja, cuentas-corrientes, empleados, materia-prima y stock; `test-materia-prima-fotos.js`, `test-stock-recuento.js`, `test-cobranzas-dialogos.js`, `test-dashboard-pendientes.js` (con sus mut), `test-accesos-textos.js` y `test-css-hidden.js`. Los números de cierre están en `.claude/traspasos/2026-09-21-noche.md`.
- **22/09/2026, Producción y la unidad de las cobranzas:** `test-produccion-{acceso,quien,abrir,cierre,masa,config,historial,xss}.js` con sus `mut-*`, `sandbox-produccion.js`, `seguras-produccion.js`, `controles-produccion.js` (baseline = un commit fijo por sub-parte, en `BASES`), `test-accesos-produccion.js`, `test-dashboard-produccion.js`, `test-cobranzas-unidad.js` y `test-cheques-unidad.js` (+ mut). Números de cierre en `.claude/traspasos/2026-09-22-produccion.md`.
- **22/09 y 23/09/2026, el rediseño de Producción en 7 partes:** se sumó **`test-produccion-pin.js`** (+ mut) —los cinco motivos del PIN, el cambio obligatorio, el acceso maestro, y que ningún PIN quede en `localStorage`, `sessionStorage` ni el DOM—, **`mut-dashboard-produccion.js`** (nuevo, para la tarjeta y el entrar derecho) y **`test-empleados-pin.js` / `mut-empleados-pin.js`**. `controles-produccion.js` ganó **`RETIRADOS`** y **`MENOS_COPIAS`** además de `RENOMBRADOS` (ver el módulo). Números de cierre por parte en `.claude/traspasos/2026-09-23-produccion-2.md`.
- **23/09/2026, Pedidos:** `test-pedidos-{clientes,carga,lista,imprimir,xss}.js` con sus `mut-*`, `sandbox-pedidos.js`, `seguras-pedidos.js`, `controles-pedidos.js` (un baseline fijo por parte, en `BASES`), `test-accesos-pedidos.js` y `test-dashboard-pedidos.js` (+ mut). Y de Stock, `controles-stock.js` (baseline `2cd547a`), el primer chequeo de controles fuera de Cobranzas/Cheques/Producción.
  - **`extraer.js` subió el tope de líneas de `extraerConst` de 200 a 600**: `CATALOGO_TAREAS` pasó de 200 líneas con el grupo de Pedidos y **tres suites de Accesos dejaron de poder leerlo a la vez**. Una falla que rompe varias suites juntas era, otra vez, el andamio.
- **22/09/2026:** `test-materia-prima-sin-stock.js` / `mut-materia-prima-sin-stock.js` ("mercadería que ya estaba" → lote opcional, la casilla en el paso Datos).
- **22/09/2026, módulo Cheques:** `test-cheques-{vista,orden,cargo,celular,plazo,seleccion,lote}.js` + sus `mut-*`, `sandbox-cheques.js`, `seguras-cheques.js`, `controles-cheques.js`; `controles-comun.js` (el inventario, compartido) y `controles-movidos.js` (qué salió de cobranzas.html y a dónde); `fuente-cobranzas.js` (el script + `js/cobranzas-comun.js`) y `mutar-cobranzas-comun.js`; `test-toast.js` / `mut-toast.js` (el cartel de error no se sale del borde). Números de cierre en `.claude/traspasos/2026-09-22-cheques.md`. Después se sumaron **`test-cheques-colores.js` / `mut-cheques-colores.js`** (el color de cada estado, la leyenda y la precedencia de los fondos; **los contrastes los recalcula la suite leyendo los hex de `css/main.css`**, así que si una variable cambia, la prueba lo dice) — traspaso `.claude/traspasos/2026-09-22-cheques-3.md`.
- Helpers compartidos: `escaner-interpolaciones.js` (tokeniza el `<script>`; exporta además `analizar()`, que devuelve `templates` y `rangos`), `clasificar.js`, `sandbox.js` y `extraer.js`. **`sandbox.js` tiene `construirCon()` y `clasificar()` acepta `{ escape, seguras, segurasRegex }`** para usarlos desde otro módulo; sin opciones hacen lo de siempre (así cerró el circuito, con Cobranzas en 256/256 y 170/170; hoy tiene más). **`clasificar.js` confundía `a ?? b` con un ternario** y dejaba `a` sin revisar: arreglado el 21/09/2026, sin agujeros reales detrás. `circuito-comun.js` (arnés, marcas, estático acotado) y `mutar.js` (runner genérico con los tres guards) son de las suites del circuito.
- **EL CACHÉ DEL SANDBOX** (`e0563b9`, 23/09/2026). `construirCon()` cachea la función compilada **por CONTENIDO del archivo**, y no por ruta ni por fecha: el runner de mutaciones escribe un archivo distinto por mutación, **a veces en el mismo milisegundo y siempre en la misma ruta**, así que cualquiera de las otras dos claves devolvería el sandbox de la mutación anterior — una suite que mide el archivo equivocado sin ningún error. Medido sobre `test-produccion-abrir`, que arma 16 sandboxes: **12 s → 0,83 s**.
  - **Y una lección de andamio que dejó** (`2ac124b`): la primera versión usaba un **byte NUL literal** (0x00) como separador del hash. El hash salía bien, pero **git detecta el NUL en los primeros 8000 bytes y pasa a tratar el archivo como BINARIO**: `git ls-files --eol` daba `-text`, los diffs no se mostraban y **el `eol=lf` del `.gitattributes` no se le aplicaba** — justo la trampa de CRLF que ese archivo vino a cerrar. Se reemplazó por la secuencia de escape escrita como texto (`'\u0000'`), que JS parsea al mismo carácter: **el hash es idéntico** (verificado calculando los dos) y el archivo vuelve a ser texto. Era el único del repo así. **Detalle para no confundirse al mirarlo: el diff de ESE commit sigue viéndose binario**, porque git marca el par cuando *cualquiera* de los dos lados lo es y el blob viejo tiene el NUL; de ahí en adelante es texto.
  - **REGLA: un separador dentro de un string no se escribe con el byte, se escribe con su escape.** Un carácter de control literal en un archivo de texto lo saca del régimen de texto de git sin que nada avise.

**Patrón de nombres:** `test-<modulo>-<tema>.js` para una suite y `mut-<modulo>-<tema>.js` para su runner; los helpers se llaman como lo que hacen. Se corren desde cualquier directorio —las rutas se resuelven contra `__dirname`— y el archivo bajo prueba se cambia con `ARCHIVO_TEST`.

**EL BASELINE SE ANCLA A UN COMMIT FIJO, NUNCA A `HEAD`.** Una suite que compara contra `HEAD` **deja de probar en el momento en que el cambio se commitea**: el "antes" pasa a ser el propio cambio, las dos mitades se vuelven idénticas y toda aserción de "antes era X, ahora es Y" queda verde para siempre sin mirar nada.

**Y NUNCA `git stash` para conseguir el archivo limpio**: antes del `.gitattributes` del 21/09/2026 el round-trip convertía el archivo a CRLF, las suites comparan contra lo que devuelve `git show` —siempre LF— y se ponen **todas en rojo a la vez**, lo que se lee como que el cambio rompió medio módulo. La forma correcta no toca el working copy:

```bash
git show <commit>:modulos/x.html > limpio.html
ARCHIVO_TEST=limpio.html node pruebas/test-x-xss.js
```

**SI LA SUITE NO ESTÁ VERDE SOBRE EL ARCHIVO LIMPIO, LAS MUTACIONES NO CORREN.** El runner decide "mutación detectada" preguntando si el sub-proceso falló, así que con la suite ya fallando por cualquier otro motivo **toda mutación se reporta como detectada** y el número final no mide nada. El harness lo detecta y **aborta diciéndolo**, en vez de informar cobertura. Mismo criterio en los otros dos guards: una mutación cuyo texto **no es único** en el archivo se aborta nombrándola —si no, pega en el renglón equivocado y se reporta como escapada—, y una que **no cambia nada** es un error del test, nunca cobertura.

**Qué NO va en `pruebas/`:** lo de un solo uso —scripts que aplican una edición, generadores de vistas para mirar en el navegador, volcados intermedios—. Eso sigue en el scratchpad de la sesión. En `pruebas/` va lo que tiene que poder volver a correrse dentro de seis meses.

### Registro de escrituras en la base

**POR QUÉ EXISTE:** el 16/09/2026 hubo **DOS agentes escribiendo en producción en paralelo sin coordinarse**. Las funciones de `public` ejecutables por `anon` pasaron de **65 a 11** durante una misma sesión por trabajo que nadie reportó, y lo detectó el chat de permisos al medir dos veces con media hora de diferencia. No rompió nada y el trabajo iba en la dirección correcta, pero **nadie sabía que estaba pasando** — que es exactamente lo que la regla de escritura (arriba) dice que el reporte tiene que evitar.

**65 y 66 NO son un error de tipeo: son dos mediciones distintas del mismo momento, y se dejan las dos a propósito.** **65** es la cantidad de funciones con `anon=X` **explícito** en `proacl`; **66** es la cantidad que `anon` **realmente podía ejecutar**, medida con `has_function_privilege`. La que hace la diferencia no tenía el grant explícito: lo recibía **por `PUBLIC`**, del que `anon` es miembro. Es el caso concreto, medido en este proyecto, del aprendizaje *"el ACL ya no muestra ese rol no es lo mismo que ese rol no puede"*. **No unificar el número:** uno solo borra el ejemplo, y con él la razón para medir con `has_function_privilege` y no leyendo el ACL.

**REGLA: todo chat que escriba en la base reporta, EN EL MISMO TURNO, el SQL exacto y la verificación posterior**, y se anota acá. El acceso de lectura es para todos; el de escritura deja rastro solo si se escribe.

**16/09/2026 — chat de permisos** (todo esquema y políticas, cero datos):
1. `alter default privileges in schema public revoke execute on functions from anon, public` → **insuficiente**: no saca el `PUBLIC` global (ver Seguridad y el aprendizaje sobre el ACL).
2. `alter default privileges for role postgres revoke execute on functions from public` → **el que sí cerró**.
3. Policies del bucket `comprobantes`: cerradas por carpeta propia, y después corregidas dos veces (`TO authenticated` en vez de `PUBLIC`; alcance por unidad en la rama `mp/`; segundo `EXISTS` para `materia_prima_items`).
4. CHECK `chk_tarea_valida`: 36 → 37 (`gastos:gestionar_proyectos`), leyendo el constraint real antes del `DROP` + `ADD`.
5. `REVOKE` de `PUBLIC` y `anon` sobre las 10 funciones listadas en Seguridad, con `GRANT` a `authenticated` y `service_role`.
6. Creada y borrada `public.__test_acl_descartable()` para la verificación empírica.
7. CHECK **`chk_solicitud_texto_sin_html`** sobre `solicitudes_acceso`: rechaza `<` y `>` en `nombre`, `apellido` y `email`. Defensa en profundidad del XSS de `accesos.html`; **cero filas existentes lo violaban** (validado).

**16/09/2026 — Claude Code, módulo Cobranzas** (autorizado por Facu bloque por bloque, reportado en su sesión; esquema y filas nuevas, cero datos existentes tocados): los 7 bloques de creación del módulo — CHECK 32 → 36; las 5 tablas, `v_cobranzas` y `dv_bcra()`; 59 filas en `bancos_bcra`; las **10 funciones** de Cobranzas con `REVOKE ... FROM public, anon` sobre **esas** solamente; el bucket `cobranzas` y sus dos policies; la fila de `modulos`; y las 4 filas de `empleado_tareas` + 2 de `empleado_modulos` del permiso inicial. Más el deploy de la Edge Function `ocr-cheques`.
- **Son las 11 funciones más nuevas de `public` por OID** (`dv_bcra` y las diez de Cobranzas, verificado el 16/09/2026). Así que las "funciones nuevas creadas por un chat sin identificar" que se midieron ese día **son, en su mayoría, estas**: no estaban sin reporte, sino reportadas en otra sesión que el chat de permisos no veía — que es el mismo problema de coordinación que motiva este registro, desde el otro lado.

**16/09/2026 — Facu, a pedido de Claude Code (Gastos, Bloque A):** índice único `uq_proyectos_nombre` sobre `normalizar_texto(nombre)` y las RPCs `crear_proyecto(p_nombre)`, `dar_de_baja_proyecto(p_proyecto_id)` y `reactivar_proyecto(p_proyecto_id)` — `SECURITY DEFINER`, `search_path = public`, `authenticated` sí y `anon` no, las tres gateadas por `gastos:gestionar_proyectos`. Verificado por Claude Code contra `pg_proc` y `pg_indexes`.

**16/09/2026 — Claude Code, Gastos:** deploy de la Edge Function `ocr-comprobante`, versión 8 → **9** (`verify_jwt: true`), con el fuente de `4b677fa`. Verificado: el fuente desplegado coincide con el del repo, sin token responde 401 y el preflight 200. Cero cambios de base y cero datos tocados.

**16/09/2026 — chat de seguridad** (esquema, cero datos): `REVOKE EXECUTE ON FUNCTION public.buscar_empleado_por_cuil(text) FROM public, anon, authenticated` + `GRANT EXECUTE ... TO service_role`, corrido **después** de que el frontend de `f49bdd9` estuviera en producción. Verificado: ACL `{postgres=X/postgres, service_role=X/postgres}`, `has_function_privilege` `false` / `false` / `true`, y **cero** funciones de `public` ejecutables por `anon`.
- **Despliegues de Edge Functions de ese traspaso (`f49bdd9`), hechos por Claude Code con el CLI:** `buscar-empleado-cuil` **v1** (nueva) y `crear-solicitud-acceso` **v4 → v5**, las dos con `verify_jwt: true`. Verificado: el fuente descargado es idéntico byte a byte al del commit, y pruebas sin efectos con la key pública (sin token → 400 `esErrorCaptcha`; token falso → 400, lo que prueba que `TURNSTILE_SECRET_KEY` está configurada; nombre con `<` → 400 que nombra el campo; contraseña de 9 → 400).

**17/09/2026 — chat de arquitectura general** (esquema, cero datos): `alter function public.tiene_tarea(text,text) set search_path = public`, y lo mismo para `tiene_tarea_alcance(text,text,uuid)` y `tiene_tarea_explicita(text,text)`. **Se usó `ALTER FUNCTION` y NO `CREATE OR REPLACE`, a propósito:** no toca el cuerpo —que en estas tres está guardado con CRLF— y evita cualquier riesgo de reescribir una función que corre adentro de **cada policy RLS del sistema**. Verificado después: `proconfig` = `search_path=public` en las tres, las tres siguen ejecutando y devolviendo `false` sin sesión, `authenticated` las ejecuta y `anon` no. SECURITY DEFINER sin `search_path`: **20 → 17**.

**17/09/2026 — chat de Ingreso** (esquema, cero datos): `buscar_comprobante_cargado` suma `p_excluir_modulo text default null` y `p_excluir_id uuid default null`, con la exclusión como `is distinct from` en las tres ramas. Reportado a Facu con su verificación: una sola función sin sobrecargas, y ejecutada contra la base con los tres casos (sin excluir → 1; excluyendo `('gastos', id)` → 0; excluyendo otro módulo con el mismo id → 1). Verificado también por Claude Code contra `pg_proc` (firma, `SECURITY DEFINER`, `search_path = public`, `anon` no y `authenticated` sí).

**16-17/09/2026 — migración de las URLs de comprobantes (DATOS, con aprobación de Facu):** las 86 URLs firmadas de `gastos.foto_url` y las 19 de `facturas_pendientes.comprobante_url` pasaron a ruta de Storage, con respaldo previo en `_respaldo_urls_comprobantes_20260916` (105 filas; RLS sin policies). Verificado el 17/09/2026: 0 URLs viejas, 0 rutas rotas, las 105 filas del respaldo tienen hoy ruta. Reportada por Facu; **falta anotar qué chat la corrió.**

**16/09/2026 — chat de Caja, con autorización de Facu** (IDENTIFICADO DESPUÉS; acá figuraba como "chat SIN IDENTIFICAR"): `REVOKE` de `anon` sobre **~54 funciones ajenas a Cobranzas** — el salto de 66 a 11 funciones ejecutables por `anon`. Verificado y correcto; en su momento **no se reportó**, y por eso el chat de permisos lo detectó midiendo. Claude Code (Cobranzas) **no** lo hizo: su revoke solo tocó las funciones que creó.

**17/09/2026 — chat de Caja, con autorización de Facu** (esquema; la verificación previa miró los datos sin tocarlos): **CHECK de `moneda`** en las 7 tablas que no lo tenían (`caja_movimientos`, `caja_solicitudes_movimiento`, `creditos_proveedor`, `cuentas_caja`, `facturas_pendientes`, `gastos`, `intereses_factura`), con las 12 monedas del selector de la app. Antes se verificó que las filas existentes solo tenían `ARS` y `USD`. Verificado después (Claude Code): los 8 CHECK de `moneda` existen y están validados.

**21/09/2026 — chat del rediseño de Cobranzas** (esquema, cero datos; registrado acá al detectarse desde otra sesión, no reportado en su momento): migración `20260922000832` que crea `resumen_cobranzas(text,date,date,text,uuid) → TABLE(por_controlar bigint, cantidad bigint, total numeric)`, `LANGUAGE sql STABLE`, SECURITY INVOKER sobre `v_cobranzas`, `search_path=public`. Verificado el 21/09/2026 (Claude Code, solo lectura) contra `pg_get_functiondef` y `proacl`: EXECUTE `authenticated` y `service_role`, no `anon`; es la única función de Cobranzas con parámetros DEFAULT.

### REGLA DE ORO — Verificar antes de asumir

Antes de documentar, modificar o dar por conocido cualquier objeto de la base (policies, funciones, vistas, constraints), **verificar su estado real primero** — aunque lo haya hecho uno mismo y aunque haya sido hace poco.

El modelo de "un chat por tema" hace que ningún chat vea lo que hicieron los otros. Con el MCP de Supabase en modo lectura verificar cuesta treinta segundos; asumir costó, en este proyecto, estar a punto de documentar una policy que ya no existía: el chat de seguridad había diseñado la de `empleados` como "fila propia o admin/super_admin" con `mi_rol_app()`, y mientras trabajaba fue reescrita desde otro chat para usar `tiene_tarea_alcance()`. No se rompió nada y la policy nueva es mejor, pero se estuvo a un paso de escribir documentación falsa — y podría haber salido al revés, con un chat pisando el cambio del otro.

### Resto
- Responder SIEMPRE en español
- Un chat de Claude por módulo — más fácil de trocklear que mezclar todo en uno solo
- Paso a paso detallado para alguien sin experiencia técnica, cuando se pide un plan
- Mostrar qué se va a hacer antes de hacerlo y esperar aprobación
- Auditar el código/esquema real antes de proponer cambios — nunca asumir en base a este documento solo, puede estar desactualizado
- No tocar lo que funciona
- Los SELECT de verificación contra Supabase se corren libremente, sin pedir permiso.
- SQL de estructura (DDL, RPCs, ALTER, vistas, políticas RLS, constraints): el chat de arquitectura y Claude Code lo aplican directamente, con guards (`IF NOT EXISTS` / `CREATE OR REPLACE` / `DROP` explícito ante cambio de firma), verificación posterior y **reporte a Facu** de qué se corrió y con qué resultado. Los chats sin escritura entregan el SQL a Claude Code o a Facu.
- **Datos: nunca se borran ni se editan filas ya cargadas sin pedido explícito de Facu**, y requieren su aprobación previa los cambios que puedan destruir o alterar datos existentes (`DROP` de tablas o columnas con datos, cambio de tipo o de nulabilidad de una columna con datos, `TRUNCATE`). Ver la regla de oro de escritura.
- RPCs: convención `p_` en los parámetros, siempre `SECURITY DEFINER`, siempre re-verifican permisos server-side con `tiene_tarea()`/`tiene_tarea_alcance()`/`tiene_tarea_explicita()` (nunca confiar en que el frontend ya validó; nunca gatear por `rol_app` — ver sección Sistema de permisos)
- Nunca exponer claves secretas en el código ni en commits — los secretos de las Edge Functions se leen con `Deno.env.get(...)`
