# Traspaso — Módulo Cheques (22/09/2026)

Prompt para el chat de arquitectura: actualizar CLAUDE.md con lo de abajo. Se
arma parte por parte; cada parte dice su commit y sus números. Quien lo recibe
no vio el trabajo: está todo acá. Lo que cambió dentro de `cobranzas.html` lo
detalla aparte `.claude/traspasos/2026-09-22-cobranzas.md` (del subagente de
Cobranzas).

Etiqueta de antes de empezar: `antes-de-cheques-2026-09-22` (subida a origin).

## Parte 1 — Módulo nuevo "Cheques": la cartera sale de Cobranzas

**Decisión de Facu:** Cobranzas es para cobrar, cargar, ver y controlar/asentar.
La cartera de cheques es administración de la plata de la empresa y va en su
propio módulo, `modulos/cheques.html`.

**Qué hay en `modulos/cheques.html`** (lo escribió el agente principal): la
vista "Cheques" que estaba en Cobranzas, mudada con su comportamiento: el
listado (tabla), los filtros de número (`filtroNumeroCheque`, la regla de los
8/9 dígitos y el `%` que no consulta) y de banco, el segmentado En cartera /
Salidos / Todos, el total en cartera (de toda la cartera, en centavos, "—" si
no se pudo calcular), el diálogo de salida (`marcar_salida_cheque`),
"Volver a cartera" con motivo en un diálogo propio (`volver_cheque_a_cartera`,
error de la base entero) y la carga del catálogo de bancos (sin caché de
IndexedDB: es una pantalla de administración, no se usa sin señal).
- Tocar un cheque abre su cobranza: `cobranzas.html?cobranza=<id>&volver=<url
  de cheques.html>` (los dos codificados). Cobranzas valida el `volver=`.
- `cheques.html?cheque=<uuid>` (lo usa el "Ver en Cheques" del detalle de una
  cobranza): muestra Todos los estados y destaca la fila; el parámetro se
  limpia con `replaceState`.
- Link "‹ Cobranzas" arriba (reemplaza a la pestaña "Cobranzas").
- Los filtros se guardan en `sessionStorage` (`cheques-preferencias`), para
  que la ida y vuelta a Cobranzas no los pierda. Lectura defensiva: un estado
  inventado o un banco que no sea `^[0-9]{3}$` no entran.
- Función de escapado propia, `esc()`.

**Permisos, sin cambiar la base:** ver la cartera pide `cobranzas:ver_todo` o
`cobranzas:procesar` (con bypass de super_admin, como `tiene_tarea`). Sin
ninguna, la pantalla lo dice, muestra el cartel de error y vuelve al dashboard
a los 2,5 s. Dar salida y volver a cartera piden `cobranzas:procesar` (lo exigen
las RPCs; la pantalla solo oculta).
- **HALLAZGO que la pantalla declara:** la policy de `cobranzas` es "leer
  propias o con ver_todo" (verificado contra `pg_policies` el 22/09/2026), así
  que alguien con `procesar` y SIN `ver_todo` vería solo los cheques de lo que
  cargó. La pantalla lo avisa ("Ves solo los cheques de las cobranzas que
  cargaste vos…"). Hoy no hay nadie en ese caso (la única persona con
  `procesar` tiene también `ver_todo`, verificado ese día). La burbuja del
  dashboard (`mis_pendientes`, SECURITY DEFINER) cuenta TODA la cartera: para
  esa persona el número de la burbuja y el de la pantalla no coincidirían.

**Helpers compartidos — `js/cobranzas-comun.js` (NUEVO).** Excepción consciente
a "cada módulo duplica sus helpers", pedida por Facu: lo que usan los DOS
módulos vive una sola vez: `dvBcra`, `formatearImporte`, `ZONA_AR`,
`hoyArgentina`, `esFechaIso`, `diasEntre`, `formatearFechaCob`,
`ETIQUETA_ESTADO_CHEQUE`, `nombreBancoDe(bancos, codigo)` y `textoSalidaCheque`.
El escape NO: cada módulo tiene el suyo. **dvBcra ya no está en
cobranzas.html**: sus tres lugares son `public.dv_bcra()`, la Edge Function
`ocr-cheques` y `js/cobranzas-comun.js`; el aviso cruzado se actualizó en el
repo en los dos archivos (el de la base no se tocó: solo lectura). **La versión
DESPLEGADA de `ocr-cheques` conserva el comentario viejo** (no se desplegó: el
cambio es solo de comentario).

**Cobranzas (`cobranzas.html`, subagente de Cobranzas):** sin la pestaña ni la
vista Cheques; link "Cartera de cheques →" (`#cob-link-cheques`, solo con
`ver_todo` o `procesar`); link directo `?cobranza=<uuid>&volver=<url>` con
`volver=` validado como Gastos 8f73654; en el detalle cada cheque sigue
mostrando su estado y su salida en solo lectura y tiene "Ver en Cheques"
(`cheques.html?cheque=<id>`). Dar salida y volver a cartera ya NO existen en
Cobranzas. Ver su traspaso para el detalle.

**Dashboard:** tarjeta "Cheques" (`clave: 'cheques'`, ícono `banknote`, color
naranja) con dos campos nuevos en `MODULOS`: `requiereModulo: 'cobranzas'` y
`requiereTareas: ['cobranzas:ver_todo', 'cobranzas:procesar']`. La decide la
función pura `moduloVisible()`: módulo cobranzas habilitado (o admin) Y alguna
de las dos tareas (super_admin las tiene todas). Una fila "cheques" en
`empleado_modulos` NO alcanza: cuelga de cobranzas. El dashboard ahora lee
`empleado_tareas` de la persona (solo si no es super_admin; si la consulta
falla, esas tarjetas no se muestran). `MODULO_DE_PENDIENTE` suma
`cheques: 'cheques'`, así la fila `('cheques', 'por_vencer', …, 'Cheques que
vencen esta semana')` de `mis_pendientes()` pone su burbuja en esa tarjeta.

**Controles (ningún control se pierde):**
- `pruebas/controles-comun.js` (nuevo): el inventario, sacado de
  controles-cobranzas.js para usarlo en los dos.
- `pruebas/controles-movidos.js` (nuevo): cada clave que salió de
  cobranzas.html → dónde está ahora y por qué. Las de la vista van a
  cheques.html con ids `chq-*`; la pestaña "Cheques" pasa a
  `a#cob-link-cheques` (en Cobranzas) y la pestaña "Cobranzas" a
  `a#chq-link-cobranzas` (en Cheques).
- `controles-cobranzas.js` acepta los MOVIDOS (y exige la clave nueva si quedó
  en Cobranzas). `controles-cheques.js` (nuevo) compara contra el MISMO
  baseline `fba6396` y exige cada control movido en cheques.html, más que cada
  referencia del JS apunte a algo que existe. Si un control desaparece de los
  dos, uno de los dos da rojo (probado sacando un id y un data-*).

**Pruebas:** `pruebas/test-cheques-vista.js` + `mut-cheques-vista.js` (las de
la vista que vivían en test-cobranzas-xss, mudadas y ampliadas: permisos,
preferencias, link a la cobranza, cartera parcial), `sandbox-cheques.js`,
`seguras-cheques.js` y `fuente-cobranzas.js` (pega al script del HTML lo que
importa de js/cobranzas-comun.js, así el sandbox prueba el código real).
`check-scripts.js` ahora también parsea `js/*.js` y verifica que **cada nombre
importado exista como export** (un import de un nombre inexistente deja la
pantalla muerta y `node --check` no lo ve; probado).

**Subagente nuevo `.claude/agents/cheques.md`** (mismo molde): territorio
`modulos/cheques.html` y las RPCs `marcar_salida_cheque`,
`marcar_salida_cheques` y `volver_cheque_a_cartera`; escribe
`cobranza_cheques` y `cobranza_historial` solo vía esas RPCs; lee `cobranzas`
(vía `v_cobranzas`), `bancos_bcra`, `v_empleados_publico`, `empleados` y
`empleado_tareas` (verificado contra el código y `pg_policies`). También se
tocó `.claude/agents/cobranzas.md` para decir que la cartera y esas RPCs ya
no son suyas y que `js/cobranzas-comun.js` es compartido. **Sumarlo a la lista
de subagentes de CLAUDE.md (son NUEVE).**

**Commit:** (ver abajo, "Hashes").

**Números:** check-scripts OK (+ `js/*.js` y 138 imports revisados);
test-cheques-vista 131/131, mut 43/43; controles-cobranzas 315/315;
controles-cheques 145/145; test-dashboard-pendientes 60/60, mut 20/20;
Cobranzas (subagente): cabecera 71/71 (mut 45/45), dialogos 68/68 (19/19),
escritorio 57/57 (45/45), fotos 70/70 (27/27 +2 eq.), numeros 145/145 (2/2 sobre
el común + 20/20), xss 282/282 (5/5 sobre el común + 191/191 +1 eq.); el resto
de las suites del repo, en verde.

**Nota que corrige CLAUDE.md:** en la sección de Ingreso se dice que
`v_empleados_publico` "filtra activo=true". **No filtra** (verificado con
`pg_get_viewdef` el 22/09/2026: es un `select … from empleados` sin `where`).
