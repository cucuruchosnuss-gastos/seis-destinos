# Traspaso de la noche del 21/09/2026

Tarea larga sin supervisión. Punto de partida: tag **`antes-de-la-noche-2026-09-21`** (sobre `b00057c`), pusheado. Para deshacer toda la noche: `git revert` de los commits posteriores a ese tag (nunca reset --hard sobre main).

Base de las suites al arrancar, todas en verde: check-scripts OK · cobranzas-cabecera 72/72 · cobranzas-escritorio 45/45 · cobranzas-fotos 70/70 · cobranzas-xss 320/320 · cc-circuito 78/78 · gastos-circuito 57/57 · mp-circuito 113/113 · controles-cobranzas 351/351.

---

## PARTE 0a — Inventario de números

El inventario completo, archivo por archivo con líneas (al commit `b00057c`), está en **`.claude/traspasos/2026-09-21-noche-inventario-numeros.md`**. Resumen:

- **Importes (16):** gastos 5 (importe, edición de gasto, edición de factura, monto de interés, tasa de interés), caja 3, cuentas corrientes 4, cobranzas 2 (efectivo, importe del cheque), materia prima 2 (total de la factura, reintento).
- **Cantidades (16):** materia prima 8, stock 6 (incluye tolerancia %), gastos 2 de kilometraje (dudosos: siguen `type="number"`).
- **Identificadores (~24):** los renglones de la banda CMC-7 y CUIT de titulares en cobranzas, número de comprobante, CUIT, CBU/alias/cuenta, lote, legajo, CUIL, teléfono, código MFA. **No se tocan.**
- **Cuatro parsers con reglas distintas antes de esta noche:**
  - `parseImporte` (gastos, caja, CC, MP): borra todos los puntos y hace `parseFloat` → **"387300.50" daba 38.730.050** y "1250.5" daba 12.505. Lo disimulaba el formateo en vivo, que borra los puntos al tipear: el error se veía pero nada lo bloqueaba (caso típico: pegar un número copiado).
  - `parseImporteCobranza`: el más estricto, ya con la regla del punto con 1-2 dígitos = decimal.
  - `parsearCantidad` (MP, stock): un solo separador, siempre decimal → "1.500" kg = 1,5.
  - `numeroDesdeOcr`: `Number()` a secas.
- **Formateadores que convertían ausencia en cero:** `formatearImporte` de cobranzas (null → "$ 0,00"), `formatearImporteCentavosSuaves` de caja y CC (null → "$ 0,00"), `formatearNumeroEditable` (undefined → "NaN"), `formatearCantidad`/`formatearNumero` de MP y `formatearCantidadStock` (null → "0").
- **Único almacenamiento local de números:** la IndexedDB `cobranzas-offline` de Cobranzas guarda el formulario entero con **strings mixtos**: lo tipeado ("12.500,50") y, lo que vino del OCR o de la base, `String(n)` con punto decimal ("387300.5"). `leerNumeroAr` lee las dos formas bien (el punto con 1-2 dígitos al final es decimal; "387300" sin separador es 387300).
- Accesos, registro y dashboard: sin importes ni cantidades.

## PARTE 0b — Conteo inicial del 1/10 (análisis del subagente stock, solo lectura)

**¿Sirve la pantalla de recuento?** Sí, con fricciones. Nada lo impide.
- Hay **55 insumos activos** (40 `insumo`, 15 `materia_prima`) y **4 unidades activas** (Cucuruchos Nuss, Dolce Pasta, Mengui, Taller). **No hay ningún recuento abierto** (los dos que existen, de Cucuruchos del 02/09, están cerrado y anulado).
- **Quién puede contar** (`v_mis_unidades_ajuste`): los dos super_admin en las 4 unidades; Franco solo Cucuruchos y Mengui. **En Dolce Pasta y Taller solo pueden contar los super_admin**: si lo va a hacer otra persona, hay que otorgarle `stock:ajustar_inventario` (y `stock:ver`) con alcance en esa unidad antes del 1/10.
- Se cuenta **una unidad por vez** (un recuento abierto por unidad), pero se pueden tener las 4 abiertas a la vez.

**Qué pide por renglón:** cantidad contada (arranca en "—", siempre en **unidad base**: no se puede escribir en bultos) y observación opcional. Lote y presentación no se editan en el renglón: son parte de la clave. En una materia prima el lote se escribe al agregar el renglón.

**Con el libro en cero:**
- Los **40 insumos comunes entran precargados en CADA unidad**, sin lote ni presentación, también en unidades que no los usan (las cajas de Nuss en Dolce Pasta, todo en Taller). No se puede cerrar con renglones sin contar, así que son **~160 ceros a mano** en total.
- **Las 15 materias primas no entran**: se agregan a mano, **un renglón por lote** ("+ Agregar insumo" → buscar → lote obligatorio → presentación → Agregar; el modal queda abierto para el siguiente).
- **Una etiqueta de lote ilegible NO se puede cargar**: `agregar_item_recuento` exige lote en materia prima y `stock_recuento_items` no tiene `lote_ilegible`. Hoy la única salida es inventar "ILEGIBLE".
- **No hay forma de borrar un renglón agregado por error** (no hay RPC): se pone en 0.

**Tiempo estimado de tipeo:** 8 a 12 minutos por unidad para los precargados; 45-60 s por lote de materia prima (15-30 min en Cucuruchos). **Total: 1 a 1,5 horas para las 4 unidades**, aparte del conteo físico.

**Qué habría que cambiar:**
- (a) Chicos, solo stock.html, sin base:
  1. **Bug**: el resumen previo al cierre (~línea 4658) pide `.select('insumo_id, lote, saldo')` sin `contenido_por_bulto`, así que `claveSaldo` arma mal la clave de todo renglón con presentación y las diferencias estimadas salen mal (el servidor calcula bien). Una línea.
  2. **Botón "0" por renglón**: los 160 ceros pasan a un toque. Sigue siendo una acción explícita.
  3. Contar en bultos en renglones con presentación (60-100 líneas).
  4. Agrupar la lista por categoría (~40 líneas).
  5. Botón "Actualizar" para cuando cuentan dos personas a la vez.
  6. Aviso al agregar una presentación nueva de un precargado (el renglón "sin presentación" hay que ponerlo en 0).
- (b) Necesitan la base: `lote_ilegible` en el recuento (medio); RPC para quitar un renglón (chico); que `abrir_recuento` pueda arrancar vacío (chico, con DROP de la firma vieja); **fecha de corte del saldo** (medio).

**Riesgos operativos del 1/10, para Facu:**
1. **El saldo se congela al CERRAR, no al abrir.** Si con el recuento abierto se carga un remito, una baja o una transferencia en esa unidad, el ajuste lo compensa y **esa mercadería desaparece sin error**. Regla hasta tener la fecha de corte: **no cargar movimientos en una unidad mientras su recuento esté abierto**, o contar y cerrar el mismo día.
2. Las facturas de octubre de mercadería ya contada van en Ingreso con la casilla **"Mercadería que ya estaba"**, o queda dos veces.
3. **Orden del borrado de datos de prueba**: la FK `stock_movimientos_recuento_id_fkey` es NO ACTION → borrar primero los movimientos y después los recuentos viejos. **Los 21 ajustes del recuento del 02/09 y los movimientos de transferencias NO se borran solos** con los ingresos: si quedan, `abrir_recuento` los precarga como saldo.

Recomendación del subagente: hacer (a)1 y (a)2 antes del 1/10 (ver Parte 13), y decidir si se hace (b)1.

---

## PARTE 1 — Números compartidos (`js/utils.js`)

Funciones nuevas, exportadas (excepción consciente a "cada módulo duplica sus helpers", mismo criterio que las contraseñas: una lectura distinta por módulo es exactamente como un cheque de 387 mil se guarda como 387 pesos sin error):

- **`leerNumeroAr(texto, { decimales = 2, negativos = false })`** → número o `null`. Punto de miles, coma decimal. Excepción por lo pegado: sin coma y con UN solo punto seguido de 1 o 2 dígitos al final, ese punto es decimal ("387300.50" → 387300.5); con 3 dígitos es de miles ("1.500" → 1500). Acepta un "$" adelante y espacios en los bordes; rechaza espacios internos, grupos de miles mal armados, dos comas, más decimales que los permitidos, signo sin `negativos`. **Un NÚMERO se devuelve tal cual** (lo que viene de la base o del OCR).
- **`formatearNumeroAr(n, { decimales = 2, minimos = decimales })`** → "2.000.000,50". null / undefined / '' / NaN / texto no numérico → **"—"**.
- **`enlazarCampoNumero(input, { decimales = 2, max, negativos })`**: pasa el input a `type="text"` con `inputmode` decimal (o numeric sin decimales), pone los puntos de miles mientras se escribe, mantiene el cursor al escribir y al borrar en el medio (borrar un punto de miles borra el dígito de al lado), el punto tipeado se toma como coma decimal, la coma entra solo si hay decimales, pegar lee con `leerNumeroAr` y **un pegado ilegible no cambia el campo**. Con `max`, lo que se pase no entra. Los eventos `input` que dispara el propio código llevan `__formateado`.
- **`ponerNumero(input, n)`**: la ÚNICA forma de escribir un número en un campo desde código. Entero sin decimales ("387.300"), no entero con al menos 2 ("387.300,50"). Un string se lee con `leerNumeroAr`; null/ilegible deja vacío. No dispara eventos.
- **`leerCampoNumero(input)`**: lee con la configuración con que se enlazó.

Pruebas: **`pruebas/test-numeros.js` 149/149**, **`pruebas/mut-numeros.js` 30/30** (+2 equivalentes declaradas en el archivo: el guard de dos comas y el `n === null` del formateador son redundantes con el camino que sigue). Helper nuevo `pruebas/numeros-comun.js` (`fuenteNumeros()`, `cargarNumeros()`, `inputFalso()`).

Decisión sin preguntar: `formatearNumeroAr(0)` da "0,00" — un cero de verdad sí se muestra; solo la AUSENCIA de dato da "—".

**Commit: `ae5569d`.**

---

## PARTE 2 — Números en Cobranzas

Hecho por el subagente cobranzas; detalle en `.claude/traspasos/2026-09-21-noche-cobranzas.md`.
- Enlazados los 2 importes (efectivo e importe de cada cheque); los 6 identificadores sin tocar. `parseImporteCobranza` y `milesValidos` eliminados: todo lee con `leerNumeroAr`. Escrituras con `escribirImporteEnCampo` → `ponerNumero`; la plantilla del importe ya no lleva `value=""` (un punto de inyección menos).
- `formatearImporte(null)` → "—" (antes "$ 0,00"). Se ve igual con importe.
- El OCR guarda el importe como número. Bug viejo cerrado de paso: un importe OCR 1234.567 se leía como 1.234.567.
- Borradores viejos del celular ("387300", "387300.5", "12.500,50") abren y viajan bien a `p_efectivo` y `p_cheques[].importe`; uno ilegible ("12,500.50") se muestra tal cual y la pantalla dice "El efectivo no se entiende".
- Suites: test-cobranzas-numeros 146/146 (nueva) · xss 320/320 · fotos 70/70 · escritorio 45/45 · cabecera 72/72 · controles 354/354. Mutaciones: numeros 22/22 · fotos 27/27 (+2 eq.) · escritorio 35/35 · cabecera 45/45 · xss 220/220 (+1 eq.; eran 221: la del `value=""` del importe ya no tiene dónde caer).

**Commit: `d79765b`.**

---

## PARTE 3 — Números en Gastos

Subagente gastos; detalle en `.claude/traspasos/2026-09-21-noche-gastos.md`.
- 5 importes (wizard, edición de gasto, edición de factura, monto y **tasa** de interés —la tasa es un porcentaje y se migró con 2 decimales—) y 2 kilometrajes **enteros** (`decimales: 0`; eran `type="number"` y "85.000" podía entrar como 85). 4 identificadores sin tocar.
- Eliminados `parseImporte` (el de "387300.50" → 38.730.050) y `formatearImporteEnVivo`. Una sola lectura del importe del wizard: `importeDelWizard()`.
- OCR, "Cargar gasto" y las dos ediciones escriben con `ponerNumero`; un importe vacío en la base abre el campo vacío (antes "0,00").
- `formatearImporte` igual para todo importe presente; ausente/ilegible → "—".
- **Decisión del subagente:** `importeDeOcr` NO pasa a `leerNumeroAr`: lee el total que guardó el OCR de Ingreso en `ocr_crudo`, que viene con punto decimal y sin miles; `leerNumeroAr("150.000")` daría 150 mil en vez de 150. Solo cambió cómo entra al campo (ponerNumero).
- El kilometraje se sigue MOSTRANDO sin puntos ("85000 km") en resúmenes y detalle (regla de no cambiar cómo se ve). Pasarlo a "85.000 km" queda para decidir.
- Suites: test-gastos-numeros 205/205 (nueva) · gastos-circuito 57/57. Mutaciones: numeros 33/33 · circuito 21/21.

**Commit: `b97b44c`.**

---

## PARTE 4 — Números en Caja

Subagente caja; detalle en `.claude/traspasos/2026-09-21-noche-caja.md`.
- 3 importes: el monto de los movimientos (ingreso externo, ingreso propio, egreso, retiro), el monto que sale en un traspaso y el que entra cuando las monedas son distintas. CBU, alias y número de cuenta sin tocar.
- Fuera `parseImporte` y `formatearImporteEnVivo`; los modales vacían con `ponerNumero`; importe ausente → "—". `importeHtml()` y el escapado de la moneda intactos; el Excel no pasa por los formateadores.
- Parámetros de las 4 RPCs que llama el módulo verificados contra la base (solo lectura).
- El "Total de la empresa" sin saldos sigue en "$ 0,00": ahí es un total calculado, no un dato que falta.
- Suites: test-caja-numeros 379/379 (nueva; rojo contra el archivo anterior). Mutaciones 24/24.

**Commit: `d8372ff`.**

---

## PARTE 5 — Números en Cuentas Corrientes

Subagente cuentas-corrientes; detalle en `.claude/traspasos/2026-09-21-noche-cuentas-corrientes.md`.
- 4 importes: monto del pago, montos por factura de la sugerencia FIFO, monto a aplicar de un crédito, "Cargar importe" (total o precio por unidad). 2 CUIT sin tocar. **El interés y la edición de factura no están en este módulo** (los llama gastos.html; ya migrados en la Parte 3).
- El prefill del crédito ya no re-lee el texto formateado de la opción: toma el saldo como número. "Cargar importe" guarda el número en el formulario (un "387.300," a medio escribir vuelve como "387.300" si se cambia de modo).
- Montos ausentes → "—". El Excel sigue dejando vacío, no 0, un monto que no existe. Firmas de las RPCs verificadas en solo lectura; `completar_importe_factura` recibe el TOTAL también con precio por unidad.
- **Para Facu:** el precio por unidad admite 2 decimales (como antes, cuando se cortaba sin avisar). 0,333 × 3.000 kg da 999; 0,33 × 3.000 da 990. Si hacen falta 3 o 4 decimales, es un cambio chico solo para ese modo.
- Suites: test-cuentas-corrientes-numeros 284/284 (nueva) · cc-circuito 78/78. Mutaciones: numeros 37/37 · circuito 38/38 (antes 39: la marca de XSS del value del campo ya no tiene dónde caer; la reemplaza una prueba de que el campo se dibuja sin valor).

**Commit: `a4c17a2`.**

---

## PARTE 6 — Números en Ingreso / Materia Prima

Subagente materia-prima; detalle en `.claude/traspasos/2026-09-21-noche-materia-prima.md`.
- 2 importes ("Total de la factura" y el total del reintento), con 2 decimales; el wizard guarda el número y no el texto.
- 8 cantidades: **bultos y unidades de a enteros (`un`) → 0 decimales; kg y lt → 3** (decisión de la noche: antes no había tope). Al cambiar la unidad o el modo, la tarjeta se redibuja y el campo nace con los decimales nuevos (probado: un campo de `un` no acepta "1,5").
- **CAMBIO DE SIGNIFICADO:** "1.500" kg ahora son 1500 kg, no 1,5. Tipeando no hay confusión (el punto tipeado se toma como coma decimal); solo al PEGAR: pegar "0.300" da 300, no 0,3.
- `numeroDesdeOcr` se mantiene para leer el OCR (son dos helpers a propósito); el valor entra al campo con `ponerNumero`.
- **Bug viejo arreglado:** en la recepción de transferencias, tipear "8,05" en un campo en kilos daba 85 (la pantalla se redibujaba en cada tecla y perdía la coma).
- Un valor del estado que el campo no puede mostrar (2,5 bultos leídos por el OCR) no se redondea: se muestra tal cual y el campo queda marcado, el wizard no avanza hasta corregirlo.
- Dato ausente → "—" en vez de "0" o "0 kg".
- Pendientes chicos: la pantalla sigue mostrando 2 decimales aunque kg admita 3 (0,305 kg se ve "0,31"); quedan "bultos de 0 kg" antes de cargar el contenido y "las presentaciones suman 0" en un mensaje.
- Suites: test-materia-prima-numeros 87/87 (nueva) · mp-circuito 115/115 (eran 113). Mutaciones: numeros 32/32 · circuito 33/33.

**Commit: `7f18b0c`.**

---

## PARTE 7 — Números en Stock

Subagente stock; detalle en `.claude/traspasos/2026-09-21-noche-stock.md`.
- 5 campos de cantidad (el del movimiento sirve para ajuste y baja) + tolerancia %. Sin importes; los 2 de lote sin tocar.
- Decimales: 0 en `un` y variantes, 3 en kg/lt; contenido por bulto 3 en cualquier unidad (como hoy); tolerancia 2 decimales con tope 100, se precarga con coma ("0,50", antes "0.5"). En el ajuste se puede tipear "-" desde escritorio; en la baja no.
- Recuento: lo guardado se repinta con puntos de miles; un re-render no cambia ningún campo y el que tiene el cursor lo conserva; `guardar_conteo` sigue mandando la observación siempre.
- Al cambiar de insumo o modo, el campo se recrea con la regla nueva y conserva lo escrito; si no entra en la regla (1,5 en `un`) se muestra tal cual y confirmar lo frena.
- Mismo cambio de significado que Ingreso: "1.500" kg = 1500 (solo afecta al pegar). En el Excel de insumos, una tolerancia "1.500" ahora queda fuera de rango.
- **Riesgo viejo, no tocado:** agregar un ítem al recuento recarga la lista del servidor; un conteo tipeado y todavía sin guardar podría perderse (casi siempre se guarda antes por el blur).
- Suites: test-stock-numeros 112/112 (nueva; rojo contra `5c1f0e1`). Mutaciones 46/46.

**Commit: `84b5367`.**

---

## PARTE 8 — Números en Empleados

**Nada que migrar**: según el inventario (0a), `empleados.html` no tiene importes ni cantidades; sus 2 campos numéricos son identificadores (CUIL, teléfono/legajo), que no se tocan. Lo mismo `accesos.html`, `registro.html` (CUIL, teléfono) y `dashboard.html` (código MFA). Sin commit.

---

## PARTE 9 — Pendientes de Cobranzas

Subagente cobranzas; detalle en `.claude/traspasos/2026-09-21-noche-cobranzas-2.md`.
- **a)** Vista "Cheques" desde 1100 px con el mismo ancho que el listado de escritorio (1440 px), solo cuando está visible. Banco en una línea con puntos suspensivos y el nombre completo en `title` escapado con `escCob`. **Agregado sin pedido:** entre 1100 y 1399 px la columna del banco se angosta de 16rem a 10rem (sin eso, a 1100 px la tabla se pasaba 77 px). Medido en un navegador con el CSS real y `htmlTablaCheques` real, 12 cheques de nombres largos: 1440 → 1383 en 1383 (antes 1040 en 826), 1300 → 1243 en 1243, 1100 → 1043 en 1043. **Abajo de 1100 (390, 768, 1099) cada elemento con la misma posición y tamaño que en `d79765b`.**
- **b)** El aviso de `completado_desde_cmc7` dice ahora la opción A, textual.
- Pendientes: en escritorio los filtros de Cheques (N°, Banco) se estiran a todo el ancho — funciona pero se ve feo; el aviso de la banda sigue saliendo en los tres renglones (el dato es por cheque; para que sea por renglón, `ocr-cheques` tendría que decir qué renglones completó).
- Suites: escritorio 61/61 (50/61 rojo sobre `d79765b`) · xss 323/323 · controles 354/354. Mutaciones: escritorio 45/45 · xss 222/222 (+1 eq.).

**Commit: `a413663`.**

---

## PARTE 10a — Textos de Cobranzas en Accesos

Subagente accesos (con la autorización explícita de Facu para tocar SOLO texto del `CATALOGO_TAREAS`); detalle en `.claude/traspasos/2026-09-21-noche-accesos.md`.
- `cobranzas:cargar`: "…mientras sigan por controlar, es decir, mientras nadie las haya asentado."
- `cobranzas:editar_anular`: label **"Editar cobranzas ajenas por controlar y anular cualquiera"**; descripción con "por controlar", "asentada" y "Controlar y asentar cobranzas".
- Búsqueda en todos los módulos: no queda otro texto visible con las palabras viejas referido a Cobranzas (solo comentarios y valores que viajan a la base). **Siguen pendientes, del lado del servidor, los 4 mensajes de las RPCs** que dicen "procesada"/"registrada" (ya anotado en CLAUDE.md; esta noche la base es solo lectura).
- Suite nueva `pruebas/test-accesos-textos.js` 16/16 (12/16 rojo sobre `a413663`).

**Commit: `98573d8`.**

---

## PARTE 10b — Mercadería en "Cargar importe" (Cuentas Corrientes)

Subagente cuentas-corrientes; detalle en `.claude/traspasos/2026-09-21-noche-cuentas-corrientes-2.md`.
- RPC verificada en solo lectura: `items_de_factura_pendiente(p_factura_id uuid)` → `insumo, marca, unidad_medida, cantidad, cantidad_bultos, contenido_por_bulto`; SECURITY DEFINER; `authenticated` sí, `anon` no; exige `cuentas_corrientes:ver_todo` o `registrar_pago` (sin ninguna devuelve cero filas, sin error).
- La mercadería sale de esa RPC; el archivo ya no lee `materia_prima_ingresos` ni `materia_prima_items`. **Ya no hace falta permiso de Ingreso** para cargar precio por unidad.
- Un producto → precio por unidad (2.000 kg × "1.234,50" manda `p_importe = 2469000`). Varios, o uno sin cantidad → solo total. Error o vacío → aviso honesto y total igual. Se fue el aviso de "no se pueden ver las cantidades". Insumo, marca y unidad escapados.
- Agregado: la fila muestra la marca y, si todo vino en la misma presentación, los bultos.
- Pendientes: CLAUDE.md no menciona `items_de_factura_pendiente` (no se sabe qué chat la creó); no filtra por unidad (hoy da igual: las tareas de CC no tienen alcance).
- Suites: cc-circuito 107/107 (eran 78) · cc-numeros 284/284. Mutaciones: circuito 48/48 · numeros 37/37.

**Commit: `eacf0ef`.**

---

## PARTE 10c — Lote con "Mercadería que ya estaba" (Materia Prima) — NO HECHA, necesita la base

Subagente materia-prima; SQL y plan completo en `.claude/traspasos/2026-09-21-noche-materia-prima-2.md`.
- Verificado en solo lectura: **`fn_validar_item_materia_prima` NO mira `sin_stock_motivo`**: exige lote a toda materia prima con `ya_recibido_con_remito = false`. En cambio `fn_espejar_stock_ingreso` SÍ la mira: hoy los dos triggers deciden distinto si un renglón "suma".
- **No se cambió nada**: si el wizard dejara pasar un renglón sin lote, la base lo rechazaría al confirmar, con todos los renglones ya cargados; y marcar `lote_ilegible` para esquivarlo sería un dato falso.
- El traspaso trae el `CREATE OR REPLACE` propuesto (misma firma, SECURITY DEFINER y search_path; lee `sin_stock_motivo` de la cabecera, que ya está guardada cuando se insertan los renglones y es inmutable), cómo verificarlo, y el cambio del wizard para después. **Detalle no obvio:** la casilla está en el paso de confirmación, DESPUÉS del paso de ítems, así que cuando se valida el lote todavía no está marcada: el cambio del cliente incluye mover la casilla al paso Datos.
- Hoy no afecta a nadie: ningún ingreso tiene `sin_stock_motivo` cargado.

**Commit: `496e3d0`** (solo el traspaso de 10c).

---

## PARTE 11 — Burbujas rojas en el dashboard (hecho por mí, `dashboard.html` + `css/main.css`)

- `mis_pendientes()` verificada en solo lectura: SECURITY DEFINER, `search_path=public`, `authenticated` sí y `anon` no; devuelve `(modulo, clave, cantidad bigint, texto)` y solo las filas de lo que la persona puede resolver. Módulos que devuelve hoy: cobranzas, accesos, materia_prima (dos filas: "Pagado sin ingresar" e "Insumos nuevos por revisar" —esta última con `stock:gestionar_catalogo`, pero la RPC la pone en materia_prima: se respeta la RPC—), gastos, cuentas_corrientes (dos filas), stock y caja.
- Una llamada al abrir y otra al volver a la pestaña (`visibilitychange` → visible), con un **turno** para que una respuesta vieja no pise la nueva.
- Círculo rojo arriba a la derecha de la TARJETA (`.tarjeta-modulo__burbuja`), con la suma de las cantidades del módulo; "99+" arriba de 99; no aparece si es 0. El detalle va en `title` y `aria-label`, escapado (`escDash`, helper local nuevo): "5 cobranzas por controlar", varias filas separadas con " · ".
- **Si la llamada falla, se sacan TODAS las burbujas**: nunca un número viejo o inventado. Una cantidad null/no entera no cuenta.
- Mapeo explícito `MODULO_DE_PENDIENTE` (guión bajo de la RPC → guión medio de `MODULOS`). Un módulo que la RPC devuelva y no esté en el mapa se ignora y se avisa por consola.
- **Reemplaza** a la burbuja vieja de Accesos (la consulta directa a `solicitudes_acceso` que hacía el dashboard para super_admin): ahora sale de la RPC. La clase `.tarjeta-modulo__badge` de main.css quedó sin uso.
- Sin probar en un navegador con sesión (el dashboard exige login).
- Suites: `pruebas/test-dashboard-pendientes.js` 47/47 (datos, cero, error, excepción, módulo inexistente, respuesta tardía, escapado, mapeo contra MODULOS). Mutaciones `mut-dashboard-pendientes.js` 15/15.

**Commit: `8eabde8`.**

---

## PARTE 12 — Regla global `[hidden]` (css/main.css)

- Agregada `[hidden] { display: none !important; }` en `css/main.css`, antes del reset de box-sizing, con el porqué en un comentario.
- **Qué cambia en la práctica:** Cobranzas, Gastos, Ingreso y Stock ya tenían esa misma regla local, así que para ellos nada. En Caja, Cuentas Corrientes, Empleados, Accesos, dashboard y las pantallas de auth, audité con un script los elementos con atributo `hidden` (estáticos y dentro de plantillas) contra las reglas de `display` de su `<style>` y de main.css: **ninguno quedaba visible por una regla de autor**, así que no cambia nada que hoy se vea. Todos los guards `:not([hidden])` / `.x[hidden]` locales siguen valiendo.
- Verificado también: ningún elemento con `hidden` se muestra con `style.display = 'flex'` (con la regla global quedaría oculto para siempre). Los `style.display` de Caja, CC y Gastos son todos sobre elementos sin el atributo.
- Consecuencia a recordar: para mostrar algo hay que sacarle el atributo (`el.hidden = false`), no pisarlo con `style.display`.
- Suite nueva `pruebas/test-css-hidden.js` 322/322 (rojo sin la regla).

**Commit: `e0c9a94`.**

---

## PARTE 13 — Recuento para el 1/10 (stock.html, sin base)

Subagente stock; detalle en `.claude/traspasos/2026-09-21-noche-stock-2.md`.
1. **Bug del resumen previo al cierre, arreglado**: la consulta no traía `contenido_por_bulto`, así que ningún renglón con presentación encontraba su saldo y las diferencias estimadas salían mal (el cierre real siempre calculó bien). Era el único lugar: las otras 3 llamadas a `claveSaldo` y las otras 4 consultas a la vista ya estaban bien.
2. **Botón "0" por renglón**: hace lo mismo que tipear un 0 (campo, renglón pendiente, autoguardado; conserva la observación). 44 px, `aria-label` con el insumo escapado, la lista no se reordena. Solo en un recuento abierto. **Decisión:** si el renglón ya está en 0 el botón queda deshabilitado (no se esconde) para que la fila no cambie de ancho bajo el dedo.
3. **Aviso** al agregar en bultos un insumo que ya estaba sin presentación (y no en 0): no contarlo dos veces, poner ese renglón en 0.
- Quedan sin hacer (anotados): contar en bultos, agrupar por categoría, botón "Actualizar", y todo lo que necesita la base (lote ilegible, quitar renglón, abrir vacío, fecha de corte).
- Falta probar en el celular a 375 px que el botón entre en la fila.
- Suites: test-stock-recuento 33/33 (nueva) · stock-numeros 112/112. Mutaciones: recuento 15/15 · numeros 46/46.

**Commit: `da0bf70`.**

---

## PARTE 14 — Barrido de XSS de materia-prima.html

Subagente materia-prima; detalle y lista completa en `.claude/traspasos/2026-09-21-noche-materia-prima-3.md`.
- **514 interpolaciones** en el `<script>`, **329** arman HTML, más 38 asignaciones directas a `innerHTML` (sin `insertAdjacentHTML`, `outerHTML` ni HTML concatenado con `+`).
- **8 sinks abiertos, los 8 cerrados:**
  1. **El `href` de "Ver foto →"** (`abrirDetalleIngreso`) — **el más grave**: `foto_url` acepta cualquier texto y crear un ingreso solo pide `cargar`, así que alguien con esa tarea podía guardar un `javascript:` que se ejecutaba cuando otra persona con `ver_todo` tocaba "Ver foto". Escapar no alcanza en un href: función nueva `hrefFoto()` que solo deja pasar `https://` (si no, "Foto no disponible"). Las 12 fotos de hoy son de Storage y se siguen viendo.
  2-3. La unidad de medida del insumo en el detalle ("N bultos de X" y la cantidad de cada renglón): `unidad_medida` no tiene CHECK.
  4. "Recibido por" (nombre del empleado) en el detalle de una transferencia.
  5. El nombre de la unidad en la cabecera del detalle.
  6-7. El nombre y el `data-unidad` de los chips de unidad del listado.
  8. La etiqueta de un tipo de comprobante desconocido en `chipTipoDoc` (hoy no explotable: CHECK de 4 valores).
- **Dos puntos ciegos del método de Cobranzas, que la suite nueva ya revisa:** el estático cortaba las asignaciones largas a `innerHTML` en la primera línea, y no revisaba lo que devuelve un `.map` cuando ese texto no arma HTML propio. El octavo sink estaba ahí: lo encontró el render ejecutado, no el estático.
- **Para el dueño de `pruebas/`:** `clasificar.js` separa mal `(items ?? []).map(…) || '…'`, y los lectores no reconocen una regex justo después de `return`. Hoy no esconden fallas en Cobranzas.
- Suites: test-materia-prima-xss 165/165 (nueva; 145/167 ROJO sobre `da0bf70`). Mutaciones 123/123, ninguna equivalente. Circuito 33/33, números 32/32.

**Commit: `afe4217`.**

---

## PARTE 15 — Barrido de XSS de stock.html

Subagente stock; detalle en `.claude/traspasos/2026-09-21-noche-stock-3.md`.
- **414 interpolaciones**, **324** arman HTML, 87 asignaciones a `innerHTML` (sin `insertAdjacentHTML`, `outerHTML`, `document.write` ni `innerHTML +=`).
- **6 sin escapar contra `afe4217`, los 6 escapados:**
  1. **Real y explotable:** la unidad de medida en el selector de vista preferida (`poblarSelectorVista`): una unidad distinta de kg/lt/un se mostraba tal cual; `insumos.unidad_medida` acepta cualquier texto y un producto creado al vuelo desde Ingreso la trae como la tipearon. Aparecía al abrir el modal de edición del catálogo.
  2-6. No explotables con datos reales (un uuid en `data-insumo`, y la clase y el texto del chip de tipo, con CHECK en la base), escapados igual porque la función no puede saber de dónde vienen.
- **Tercer punto ciego del método** (además de los dos de Ingreso): un texto armado sin HTML propio que se asigna directo a `innerHTML`. Hoy no hay ninguno en stock; la suite nueva lo controla.
- **Para el dueño de `pruebas/`:** `clausura()` de `test-materia-prima-xss.js` deja afuera las constantes con varios espacios antes del `=` (en la suite de stock está corregido).
- Suites: test-stock-xss 234/234 (nueva; 228/234 ROJO sobre `afe4217`). Mutaciones 178/178 (+4 equivalentes: `esc()` sobre textos fijos). Números 46/46, recuento 15/15.

**Commit: `be51610`.**

---

## PARTE 16 — Fotos de Ingreso, fase 2

Subagente materia-prima; detalle en `.claude/traspasos/2026-09-21-noche-materia-prima-4.md`.
- Las tres fotos nuevas (comprobante, ficha técnica, foto de lote) se guardan como **ruta** (`{uid}/mp/...`, el mismo formato). Ya no se firma a 10 años.
- "Ver foto →" pasa a ser un botón con la ruta; al tocarlo se firma a **300 s**. Las **12 filas viejas** se siguen viendo: se extrae la ruta del link viejo, que deja de aparecer en la página. Lo que no tiene forma de ruta del bucket (un `javascript:`) muestra "Foto no disponible". Mismo manejo de errores que Gastos (sesión vencida → refresca y reintenta una vez). `hrefFoto()` se eliminó: ningún dato de la base va a un enlace.
- La policy de `mp/` (LIKE contra la columna) matchea igual con la ruta: verificada en solo lectura. El OCR recibe el archivo, no el link.
- **EFECTO EN GASTOS, para decidir:** "Cargar gasto" desde una factura ingresada copia la foto con `archivoDeIngreso()`, que con una ruta la baja por Storage (no se rompe). Pero antes la bajaba con el link de 10 años, sin permisos; ahora pasa por la policy, así que **quien tenga `gastos:ver_exportar` y no `materia_prima:ver_todo` en esa unidad ya no puede copiar la foto de un ingreso NUEVO**: Gastos muestra su aviso y deja subir la foto a mano. Si se quiere que copie igual, hay que ampliar la policy del bucket (rama `mp/` también con `gastos:ver_exportar`), que es una decisión de permisos.
- **Pendiente con aprobación de Facu:** pasar a ruta las 12 filas viejas (es tocar datos). Hasta entonces esos 12 links siguen abriendo sin sesión.
- Suites: test-materia-prima-fotos 57/57 (nueva) · xss 167/167 · circuito 115/115 · números 87/87. Mutaciones: fotos 14/14 · xss 124/124 · circuito 33/33 · números 32/32.
