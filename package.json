{
  "name": "seis-destinos",
  "private": true,
  "description": "Sistema de gestión de fábrica de Grupo Nuss. Sin build: HTML, CSS y JS vanilla. Este package.json existe solo para las herramientas de prueba.",
  "scripts": {
    "pruebas": "node pruebas/check-bytes.js && node pruebas/correr-todo.js",
    "mutaciones": "node pruebas/correr-todo.js mut",
    "e2e": "playwright test --config e2e/playwright.config.js",
    "maqueta": "node e2e/maqueta/servir.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
